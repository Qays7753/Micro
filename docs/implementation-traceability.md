# Micro — Implementation Traceability

**الحالة:** `CURRENT / UPDATE WITH EVERY MERGED GROUP`
**آخر تحديث:** 2026-09-02 — المجموعة ٤ (القبول والتكامل النهائي) — الحالة النهائية للمرحلة المحلية

> سجل قابل للتتبع: ما نُفّذ فعلًا، وما أُجّل ولماذا، وما الاختبارات التي تثبته.
> لا يعلن بند مكتملاً إلا إذا كان منفذًا ومتحققًا منه. مرجع الحالة الحية التشغيلية
> يبقى `docs/operations/current-state.md`.

## المجموعة ١ — أساس التجربة (2026-09-02)

| الحقل | القيمة |
|---|---|
| نقطة الانطلاق | `main` @ `1b37c77c72f719a4b51be3aa5e243063c997df2e` |
| الفرع | `agent/group1-experience-foundation` |
| commit التنفيذ | `aa78757e37d0c86b942b2d4f363e4b9908019e9b` (PR #142) |
| رأس `main` بعد الدمج | `ead3545dc5cdd5d92658e559a1b89d5e083f4c5c` — CI ناجح |
| المخطط المحلي | 30 (كان 29) — مخزن `owner-profile` جديد |
| نسخة التصدير | 22 (كان 21) — تُقبل 21/29 وأقدم وتُهاجر بـ ownerProfile=null |
| عقد التنقل الجديد | `docs/contracts/26-navigation-referrer-and-deep-link-contract.md` |

### أ) المنفّذ والمتحقق منه

| البند | الحالة | الدليل |
|---|---|---|
| عقد تنقل موحّد: مصدر/رجوع/وصلات عميقة دفاعية | منفّذ | `app/navigationContract.ts` + 14 اختبارًا + عقد 26 |
| `?from` على كل مسار متعدد السياقات (رئيسية/عمل/مالي/أدواتي/محافظ/موردين/أطراف/مواد/جدول/أساس) | منفّذ | ~35 رابطًا معدّلًا؛ اختبارات Orders/U004/Foundation محدّثة |
| رجوع المحررات للمصدر مع بدائل قانونية | منفّذ | 20 محررًا عبر `useReturnPath()` |
| حماية مدخل ورقة الإضافة (سؤال خيارين هادئ) | منفّذ | `QuickActionSheet.guard.test.tsx` (٥ اختبارات) |
| الرئيسية: كتلة «الأهم الآن» + أفعال محددة + مؤهل الأمانات + بطاقة غير الموزع + «غير محدد بعد» + سطر المكان + حالة فراغ صادقة + مدخل ملف المالك | منفّذ | اختبارات الخدمة (٥ جديدة) + عرض `pages/Home.tsx` |
| العمل: «الأولوية الآن» دائمًا + صفوف بيع مختصرة + CTA بيع مباشر + «سجّل أول بيع» | منفّذ | `Orders.ui.test.tsx` (٨ اختبارات) |
| كشف الوقت/المادة الفعليين عند التنفيذ مع وصلة تحفظ سياق الطلب | منفّذ | `pages/OrderDetail.tsx` execution layer + `?order=` في محرر الاستهلاك |
| السعة والتكرار حيث يُقرأ الضغط (`?focus=capacity` + «حدّد سعة اليوم») | منفّذ | `pages/Schedule.tsx` |
| إدخال الموقف الافتتاحي الموجه من صفحة الأساس (`?focus=guided-import`) | منفّذ | `pages/Settings.tsx` + `pages/Foundation.tsx` |
| الإعداد المتدرج: مسودة أثناء الكتابة + استعادة بإشعار + ٣ حالات افتتاحية صادقة | منفّذ (تحقق اختباري جديد) | `Setup.ui.test.tsx` (٤ اختبارات جديدة) |
| ملف المالك: مخزن محلي مستقل + خدمة + شاشة `/profile` + ضمان الهوية عند الإقلاع | منفّذ | `ownerProfileService.test.ts` (٦) + `Profile.ui.test.tsx` (٩) + دورة تصدير/استيراد (٣) |
| التخزين/التصدير: مخطط ٣٠ / نسخة ٢٢ مع هجرة آمنة لكل الموجات السابقة | منفّذ | اختبارات transfers (٢٧) خضراء |
| حدود كثافة النص محدّثة بقرار موثّق (ملصقات مُكَلَّفة من نطاق معتمد) | منفّذ | `scripts/text-density-count.py` CAPS |

### ب) المؤجل صراحة (مسؤولية المجموعات اللاحقة)

| البند | المجموعة المسؤولة | السبب |
|---|---|---|
| CollectionSheet/مستلم الديون، WalletLedger، StatementView | المجموعة ٢ (المالية) | خارج نطاق المجموعة ١ (§13) |
| تصحيح المشتريات/سعر الطلب بعد الاتفاق، CorrectionPreview الموحد | المجموعة ٢ | نفسه |
| إعادة تصميم الحاسبة الكاملة وشاشة تعديل التقدير | المجموعة ٣ (المنتجات/الطلبات) | نفسه |
| مسار الكتالوج→البيع الكامل | المجموعة ٣ | نفسه؛ العقد (§4) يكفي |
| ربط استهلاك المادة بسياق الطلب في الطبقة المالية (استهلاك موثق بمصدر) | المجموعة ٢ | المجموعة ١ سلّمت الوصلة والتعبئة فقط |
| Google Auth / مزامنة / تعدد أجهزة / استرداد حساب | خارج المجموعات (قرار مرحلة) | النموذج محلي أولًا؛ الحقول المحجوزة null |
| `?mode=cover` و`?to=` تفعيل فعلي | المجموعات اللاحقة | معرفة في العقد ومحجوزة |
| السوق/التوصيل | توسعة مستقبلية | قرار E-00 مستقل |

### ج) بطاقات التتبع الخارجية (MC/D)

معرّفات MC-10…MC-24 وD1…D14 واردة من مرجع إعادة التصميم الخارجي وليست في المستودع؛
لا يمكن التحقق من نصوصها الحرفية من الكود. خُطّطت بالتطابق مع نطاق المجموعة ١ المعتمد:

| المعرف (كما ورد في أمر التنفيذ) | أقرب بند منفّذ في هذه المجموعة | الحالة |
|---|---|---|
| MC-10 (منطق المصدر/الرجوع) | عقد ٢٦ §2 + `?from` عبر التطبيق | منفّذ |
| MC-12 (وصلات عميقة دفاعية) | `parseDeepLink` + اختباراته | منفّذ |
| MC-14 (حماية المدخل العابر) | سؤال الخيارين في ورقة الإضافة | منفّذ |
| MC-15 (تغذية راجعة موحدة) | عقد ٢٦ §6 + الأنماط القائمة | منفّذ كعقد + أنماط |
| MC-16 (هرمية الرئيسية) | §7.1 كاملة | منفّذ |
| MC-18 (هرمية العمل) | §8.1/§8.2 | منفّذ |
| MC-19 (كشف القدرات المخفية) | Scope E (تنفيذ/سعة/استيراد موجه) | منفّذ |
| MC-20 (التدرج حتى أول فعل) | Scope F + اختباراته | منفّذ |
| MC-22 (ملف المالك) | Scope G كاملًا | منفّذ |
| MC-24 (معايير RTL/سلوك) | مطبّقة عبر الشاشات المتغيرة + فحص حي | منفّذ |
| D1/D6/D12/D13/D14 | تفسّر ضمن البنود أعلاه حسب سياقها (تنقل/نماذج/حالات) | مغطاة بالتطابق لا بالنص الحرفي |
| إضافة ملف المالك المعتمدة | Scope G | منفّذ |

### د) التحقق

| الفحص | النتيجة (2026-09-02) |
|---|---|
| `pnpm typecheck` (جذر + نموذج أولي) | نظيف |
| `pnpm lint` | 0 أخطاء / 37 تحذيرًا (السقف المعتمد) |
| `pnpm text-density` | كل الأسطح ضمن الحدود (CAPS موثّقة) |
| `pnpm design-guards` | نظيف (tokens + stylelint) |
| `pnpm test` (مجال) | 15 ملفًا / 182 اختبارًا خضراء |
| `pnpm prototype:test` | 79 ملفًا / 489 اختبارًا خضراء (منها ٧٧ جديدة للمجموعة ١) |
| `pnpm prototype:build` | بناء إنتاجي ناجح |
| فحص حي (متصفح، ٣٩٠×٨٤٤، RTL) | الرئيسية/العمل/الملف/الوصلات العميقة/النماذج الوسخة/إعادة تحميل دون اتصال — بلا أخطاء كونسول |
| CI (GitHub Actions) على رأس الدمج | ناجح (`ead3545`) |

---

## المجموعة ٢ — الحقيقة المالية وطبقة التصحيح (2026-09-02)

> استئناف بعد انقطاع: عُثر على العمل الجزئي غير المُلزَم في شجرة العمل المحلية (45 ملفًا:
> المجال + الخدمات + الواجهات)، صُوِّر أمانًا على `agent/group2-financial-truth` قبل المتابعة،
> وأُكمل من أول بند ناقص حتى الدمج. أُصلح في الاستئناف: 3 اختبارات متقطعة (API الكشف/دفتر
> المحفظة)، سقف lint أُعيد إلى 37 تحذيرًا بتحسينات هندسية صادقة (لا رفع سقف)، وأصلحَ عيبان
> حقيقيان كشفهما الفحص الحي: طمس شاشة نجاح التحصيل عند تجدد البيانات، ومضاعفة رقم الأثر
> في معاينة التصحيح.

| الحقل | القيمة |
|---|---|
| نقطة الانطلاق | `main` @ `b529eb459e7c83ce763d935fa9b1a7a6362071ea` (نهاية المجموعة ١ — تم التحقق من الرأس الفعلي) |
| الفرع | `agent/group2-financial-truth` |
| المخطط المحلي / التصدير | 30 / 22 — لم يتغيرا: المجموعة ٢ قراءة وتصحيح فوق السجلات القائمة، لا مخازن جديدة |
| العقد | عقد ٢٦ مُحدَّث (مسارات المجموعة ٢: `/collect`، `/finance/statement`، `/cash/wallet/:id`، ومعجم `?source/?view/?entry/?mode=cover`) |
| حالة «سجّل» | الاستئناف وجد 45 ملفًا غير مُلزَمة (M/جديد) على `main` المحلي؛ لا فروع بعيدة ولا PR ولا stash |

### أ) المنفّذ والمتحقق منه

| البند | الحالة | الدليل |
|---|---|---|
| بيع سريع: الدرج وجهة افتراضية معلنة + غير الموزع خيار صريح + معاينة الأثر + وصل «افتح السجل» + دين آجل موثق | منفّذ | `QuickActionSheet.tsx` + فحص حي (وصل صادق بالمبلغ والدين) |
| مصروف سريع: وجهة صرف صريحة (غير الموزع افتراضيًا بلا اختيار نيابة عن الصرف، المحفظة تغطي من رصيدها) + وصل المصدر | منفّذ | `QuickActionSheet.tsx` + `attributeToWallet(..., sourceRef)` |
| ورقة التحصيل: منتقي ذمم آمن + تعبئة المتبقي قابلة للتعديل + منع التجاوز برسالة تعرض المتبقي والمطلوب + جزئي/كامل + وجهة صريحة | منفّذ | `pages/Collect.tsx` + `collectionService.test.ts` (8) + `G2.dom.test.tsx` (3) |
| تحصيل واحد موثق عبر المسار المملوك (لا مصدر كتابة ثانٍ): Fulfillment للطلبات وDirectSaleService للبيع + تخصيص الكاش بـ`distributeUnallocated` بمعرّف عملية | منفّذ | `collectionService.ts` (§6) |
| مداخل التحصيل الثلاثة: بند الرئيسية (طلبات + ديون بيع آجل لا تختفي)، صف الطرف في دفتر الناس، وورقة الإضافة — كلٌّ يحفظ مصدره ويعود إليه | منفّذ | `homeControlCenterService.ts` + `Parties.tsx` + `MicroAppShell.tsx` + فحص حي |
| مالك عمليات الكاش الفيزيائي `/cash`: المجموع، محافظ، غير الموزع، الأمانات مؤهلًا، عدّ الصندوق، التوزيع، حالة التسوية، وصل الأثر | منفّذ | `CashWallets.tsx` (موجودة + مدخل دفتر المحفظة) |
| عدّ الدرج: المعدود يُقارن بالمسجل محايدًا، الفرق تسوية موثقة بتاريخ اليوم، لا رقم قديم يتغير | منفّذ | فحص حي (فرق −2.00 سُجل تسوية) |
| توزيع الكاش و`?mode=cover`: الغطاء من محفظة يفتح من بطاقة «في دفعة تحتاج تغطية» السالبة بشرح مسطح | منفّذ | `Finance.tsx` (CashDecisionSurface) + `CashDistribution.tsx` |
| مالي منقسم: «الوضع الآن» / «شو صار خلال الفترة» (`?view=`) بقرار أول واضح وكثافة متدرجة | منفّذ | `Finance.tsx` + U05/U07 محدّثة |
| كشف الفترة: نطاق معلن، كاش داخل/خارج مفصولان، النتيجة، الأمانات، الذمم، مال المالك، أسطر حقيقة، التصحيحات مرة واحدة في كتلتها | منفّذ | `statementService.ts` + `statementService.test.ts` (5) + `G2.dom.test.tsx` (2) |
| دفتر المحفظة: الرصيد + الحركات بالتسلسل + تمييز الأنواع + وصل كل تخصيص بمصدره + رجوع محفوظ + «رصيد المحفظة لا ربحها» | منفّذ | `walletLedgerService.ts` + `pages/WalletLedger.tsx` + اختبارات (3+2) |
| التاريخ الموحد: عائلات المشتريات/الدفعات وتعديل سعر الطلب والتراجع عن قبضة تنضم لأحداث/مبيعات/كاش بمصادرها وأسبابها | منفّذ | `correctionHistoryService.ts` (مجموعات purchases/orders) |
| CorrectionPreview الموحد: الأصل، الفعل، القيم قبل/بعد لكل بُعد (كاش/التزام/أمانات/مصروف/مال المالك)، ما لا يتغير، الرصيد الناتج، السبب الإلزامي، قابلية التراجع | منفّذ | `components/finance/CorrectionPreview.tsx` + تكامله في EventsLayer/OrderDetail/SupplierPurchaseEditor |
| تصحيح أحداث المالية: تعديل/حذف موثق/تراجع/استرجاع — الأصل باقٍ والتراجع لا يتكرر | منفّذ | `projectFinancialService.ts` (reverse/edit) + D005 محدّث |
| تراجع موثق عن قبضة طلب: الكاش يعود والمتبقي يُفتح والإيراد لا يمس، والتراكمي لا يتجاوز القبضة | منفّذ | `craft-order/policies.ts` `reverseOrderCollection` + `fulfillmentService.reverseCollection` + `OrderDetail.tsx` + اختبارات (5) |
| تعديل سعر الطلب بعد الاتفاق: لا إلغاء ولا إعادة إنشاء، الاتفاق القديم في الأحداث، العربون والقبضات كما هي، سبب إلزامي | منفّذ | `craft-order/policies.ts` `reviseAgreedPrice` + `fulfillmentService.revisePrice` + `OrderDetail.tsx` + اختبارات (7) |
| تصحيح شراء المورد: التكلفة والدفع الأولي بمراجعة تحفظ ما قبل التصحيح، والدفعات اللاحقة وتراجعاتها لا تُمس | منفّذ | `supplier-purchase/policies.ts` `updateSupplierPurchase` + `SupplierPurchaseEditor.tsx` + اختبارات (4) |
| تراجع موثق عن دفعة مورد: الدفعة الأصلية باقية والعلاقة صريحة، دفعة واحدة لا تُتراجع مرتين | منفّذ | `reverseSupplierPurchasePayment` + `SupplierPurchaseEditor.tsx` + اختبارات (3) |
| وصل مصدر التخصيص في سجل المحفظة (sale/expense/collection/order) | منفّذ | `cash-continuity/policies.ts` `normalizeSourceRef` + `cash-continuity-source-ref.test.ts` (4) |
| أمانات: ليست ربحًا ولا مالًا للمالك في الكاش/المالي/الكشف/دفتر المحفظة/الرئيسية؛ تسليم لا يتجاوز المحتجز؛ التصحيح بمعكوس الأثر | منفّذ | موجودة من الدورات السابقة (`amanah-loss.test.ts`) وثبتت في أسطح المجموعة ٢ (trustLine/qualifier) |
| مجهول الافتتاحي: يبقى «غير محدد بعد» لا 0.00 | منفّذ | `walletLedgerService` openingUnknown + فحص حي |
| عدم التكرار: مفاتيح idempotency في كل مسارات الكتابة الجديدة (تحصيل/تعديل/تراجع) + نجاح محلي مكتمل فقط | منفّذ | اختبارات التكرار (تحصيل/شراء/تراجع قبضة/دفعة) |

### ب) المؤجل صراحة

| البند | المجموعة المسؤولة | السبب |
|---|---|---|
| حاسبة التكلفة الكاملة، تعديل التقدير، جسر التقدير→مسودة | المجموعة ٣ | خارج نطاق المجموعة ٢ (§15) |
| اختيار منتج من الكتالوج أثناء البيع، Product-to-Sale الكامل | المجموعة ٣ | نفسه — عُرّف العقد الأدنى فقط |
| تنفيذ الطلب (غير تصحيح السعر) | المجموعة ٣ | نفسه |
| مزامنة/سحاب/OAuth/استرداد | خارج المجموعات | قرار مرحلة — النموذج محلي أولًا |
| أسعار/مقعد سادس | ممنوع | قشرة خمسية ثابتة |

### ج) بطاقات التتبع الخارجية

معرّفات MC/D المذكورة في أمر تنفيذ المجموعة ٢ غير موجودة كنصوص في المستودع (كما وثّقت
المجموعة ١)؛ خُطّطت بالتطابق مع بنود الأمر: MC-25/MC-26 (ورقة التحصيل/المداخل) → Scope B
أعلاه؛ MC-27/MC-28 (الوضع/الفترة) → §8؛ MC-29/MC-30 (دفتر المحفظة/الكشف) → §9؛ MC-31
(التاريخ الموحد)؛ MC-32/MC-33/MC-34/MC-35 (CorrectionPreview والتصحيحات وعائلاتها) →
§10-§12؛ MC-36 (أمانات)؛ D15-D19 (حمايات التحصيل/العدّ/التغطية) → ضمن اختبارات الخدمات
والفحص الحي.

### د) التحقق

| الفحص | النتيجة (2026-09-02) |
|---|---|
| `pnpm typecheck` + `pnpm prototype:check` | نظيفان |
| `pnpm lint` | 0 أخطاء / 37 تحذيرًا (السقف) — أُعيد إليه بتحسينات صادقة (استخراج مساعدين ومسارات اختبار أقصر) |
| `pnpm text-density` | ضمن الحدود (CAPS: Home 36، Finance 169، OrderDetail 157، CashWallets 70 بملاحظات قرار موثقة) |
| `pnpm design-guards` | نظيف |
| `pnpm test` (مجال) | 18 ملفًا / 205 اختبارًا (منها 23 جديدة للمجموعة ٢ + إصلاحات) |
| `pnpm prototype:test` | 83 ملفًا / 516 اختبارًا (منها 7 جديدة لأسطح المجموعة ٢ + تحديثات U05/U07/D005) |
| `pnpm prototype:build` + PWA | ناجح (62 مدخلًا مُسبق التخزين) |
| فحص حي (390×844، RTL) | الإعداد→بيع آجل→دين في الرئيسية→تحصيل جزئي→دفتر الناس→مالي وضع/فترة→كشف→محافظ→دفتر محفظة→عدّ صندوق→مصروف→تراجع موثق بمعاينة صادقة→سجل التصحيحات→رجوع محفوظ→إعادة تحميل — بلا أخطاء كونسول |
| عيوب وجدها الفحص وأُصلحت | طمس شاشة نجاح التحصيل (doneRef)؛ مضاعفة رقم الأثر في المعاينة (قبل/بعد صادقة) |

---

## المجموعة ٣ — الأدوات والمنتجات والطلبات وProduct-to-Sale (2026-09-02)

> جرد أولي كُتب قبل التنفيذ (كل بند بحالته على `main` @ `c7dbd5e`) ثم حُدِّث عند الاكتمال.
> الفحص الحي كشف وأصلح ٥ عيوب سابقة على المسار الحرج: (١) المسار الحرفي
> `/orders/draft/new` كان يطابق أولًا بلا معاملات فَيُفتح المحرر بخطأ «تعذر فتح المسودة»؛
> (٢) و٥-ب: wouter يعيد المسار بلا استعلام — `?intent/?estimate/?product` كانت تُقرأ من
> `location.split("?")` فلا تصل في المتصفح الحقيقي (المحاكاة كانت تخفيها)؛ (٣) حقل
> «سعر بيع افتراضي» في الكتالوج لم يكن يخرج من حالة الفراغ فيُحفظ null بصمت؛ (٤) تعبئة
> `?product` كانت تعلّم النموذج وسخًا فيعترض الحوار خروج مستخدم لم يكتب شيئًا؛ (٥) شاشة
> نتيجة التحصيل كانت تسجل وسخًا (سقوط wallets في طور done) فيفتح حوار «تعديلات غير
> محفوظة» عند «تم» — كلها أُصلحت داخل نطاق المجموعة ٣ وموثقة أدناه.

| الحقل | القيمة |
|---|---|
| نقطة الانطلاق | `main` @ `c7dbd5ec5770f1bbbe98f2a8aaf1c8747047f04b` (نهاية المجموعة ٢ — تم التحقق من الرأس الفعلي والفرع نظيف) |
| الفرع | `agent/group3-tools-products-orders` |
| المخطط المحلي / التصدير | 30 / 22 — لم يتغيرا: المجموعة ٣ حلقات وتدفقات فوق السجلات القائمة، لا مخازن ولا حقول جديدة |
| العقد | عقد ٢٦ مُحدَّث (مسارات `/tools/calculator` و`/tools/estimate/:id`، معجم `?product` و`?estimate`، بديل الكتالوج، وقاعدة و٥-ب لقراءة الاستعلام) |
| شاشات جديدة | `pages/CostCalculator.tsx`، `pages/EstimateDetail.tsx` |
| خط الأساس | `pnpm check` أخضر بالكامل قبل أي تغيير (typecheck، lint 0/37، كثافة، design-guards، 205+516 اختبارًا، بناء + PWA) |

### أ) المنفّذ والمتحقق منه

| البند | الحالة | الدليل |
|---|---|---|
| نتيجة حية للحاسبة أثناء الكتابة (مسار عميق كامل `/tools/calculator`) | منفّذ | `CostCalculator.tsx` + G3 (اختبار الحاسبة) + فحص حي (4.50 حيًا) |
| حالات المدخلات الناقصة: مادة بلا سعر + وقت غير مُدخل = «غير محدد بعد» لا صفر | منفّذ | سطرا الإرشاد في الحاسبة + G3 + فحص حي |
| ضمان لا-أثر-مالي للحاسبة/التقدير (حفظ/تعديل/حذف) | منفّذ ومتحقق | G3: 0 أحداث مالية/كاش/مخزون بعد الحفظ والتعديل؛ `costEstimateService.test.ts` (تحديث في مكانه) |
| حماية مدخلات الحاسبة (UnsavedChangesGuard + إعادة لقطة بعد التحميل/الحفظ) | منفّذ | الحاسبة مسجلة في routeClassifier deep + الحارس مربوط |
| قائمة التقديرات المحفوظة (تفتح صفحة التقدير، تحفظ الجسر U-004) | منفّذ | `Tools.tsx` + U004 + G3 (صف يفتح `/tools/estimate/:id?from=%2Ftools`) |
| تفصيل التقدير: المدخلات والنتيجة والمجهول والتاريخ والمؤهل + أفعال | منفّذ | `EstimateDetail.tsx` + G3 + فحص حي (حالة «ناقصة» تعرض «غير متوفر بعد») |
| تعديل التقدير (خدمة `update` كانت بلا واجهة) | منفّذ ومتحقق | `/tools/calculator?estimate=<id>` + `costEstimateService.test.ts` (تحدّث في مكانه بلا تكرار) |
| حذف التقدير من صفحة التفصيل (تأكيد من خطوتين) | منفّذ | `EstimateDetail.tsx` (حذف حر بلا أثر مالي) |
| بدء مسودة من التقدير — الرجوع يعود للتقدير نفسه | منفّذ ومتحقق | G3 + فحص حي (جسر + إشعار + رجوع لصفحة التقدير) |
| مرجع منتج/خدمة: الاسم/النوع/الوحدة/الحالة/سعر وتكلفة مقترحان (SKU/ملاحظات غير مطلوبين للحلقة) | منفّذ (قائم P-002) | `Catalog.tsx` + اختبارات الكتالوج القائمة |
| إصلاح حفظ «سعر بيع افتراضي» من الكتالوج (كان يسقط null بصمت) | منفّذ | فحص حي: البند يُحفظ ويظهر «سعر مقترح: 1.25 د.أ» في الصف |
| زر «سجّل بيع هذا المنتج/هذه الخدمة» من صف الكتالوج الفعّال | منفّذ ومتحقق | G3 (تنقّل بالمعامل والمصدر) + فحص حي |
| Product-to-Sale: `?product=` يعبّئ المرجع والاسم والسعر والتكلفة المقترحة مرة واحدة | منفّذ ومتحقق | G3 (prefill + تجاوز السعر + سجل واحد) + فحص حي |
| الموقوف يُخفض بإشعار صادق والمحذوف يُهمل بهدوء | منفّذ ومتحقق | G3 (اختبار الموقوف) |
| بيع بلا منتج من `سجّل` (ورقة الإضافة) — بلا مسار كتالوج إلزامي | منفّذ (قائم) | فحص حي (رحلة ٤: بيع آجل بلا منتج) |
| وجهة قبض صريحة في محرر البيع (الدرج افتراضيًا، غير الموزع خيار معلن) | منفّذ | `DirectSaleEditor.tsx` + G3 (تخصيص واحد بمصدر sale) |
| إغلاق واقعي بعد البيع: ما بيع، السعر الفعلي، أثر الكاش/الذمة، المرجع، «افتح السجل»، «تم» | منفّذ ومتحقق | G3 + فحص حي (شاشة النتيجة كاملة) |
| تخصيص الكاش بمعرّف عملية مشتق (idempotent عند التكرار) | منفّذ | `${idempotencyKey}:attribute` — نفس مسار المجموعة ٢ |
| تمايز مفاهيم تقدير/مسودة/طلب/عربون/تسليم/تحصيل/ذمة | منفّذ (قائم) | عقد ٠٢ + presentation — الحسابات والفحص الحي (رحلة ٣) |
| وصلة «المصدر: تقدير» في تفاصيل الطلب (إذا بقي التقدير) | منفّذ ومتحقق | G3 (اختبار تفاصيل الطلب) |
| تسميات سجل الطلب لـ `price_revised` و`collection_reversed` (كانت تسقط لتسمية عامة) | منفّذ | `OrderEventLog.tsx` + G3 + فحص حي |
| تصحيح المسار الحرفي `/orders/draft/new` (محلل wouter: يفتح الآن محرر النية) | منفّذ | فحص حي (المحرر يفتح والقيم تتعبأ) + حذف المسار المتكرر من الموجّه |
| و٥-ب: كل معاملات الاستعلام تُقرأ عبر `useSearch()` (DraftEditor/NewDraft/الحاسبة/محرر البيع) | منفّذ ومتحقق | G3/U004/U06 محدّثة بمحاكاة أمينة + فحص حي (النية والجسر يعملان) |
| إصلاح وسخ التعبئة (لا حوار على مستخدم لم يكتب) + وسخ شاشة نتيجة التحصيل | منفّذ | فحص حي (خروج صامت بعد التعبئة؛ «تم» يخرج بلا حوار) |
| رجوع آمن ووصلات عميقة لكل المسارات الجديدة (contract 26) | منفّذ ومتحقق | routeClassifier.test (مساران عميقان) + G3 (روابط withFrom) + فحص حي |
| توافق الترحيل/التصدير/الاستيراد | غير متأثر | لا مخازن/حقول جديدة — المخطط 30/22 كما هو؛ اختبارات transfers خضراء |
| الاختبارات الكاملة وtypecheck/lint/كثافة/بناء/PWA | منفّذ ومتحقق | `pnpm check` أخضر: 205 مجال + 526 نموذج أولي (منها 10 جديدة G3 + 2 خدمة + 3 مصنّف) |

### ب) المؤجل صراحة

| البند | المجموعة المسؤولة | السبب |
|---|---|---|
| حقل SKU/مرجع رقمي وملاحظات على عنصر الكتالوج | غير محدد (غير مطلوب) | حلقة Product-to-Sale تكتمل بالاسم/النوع/السعر/التكلفة/الوحدة/الحالة؛ إضافة حقول تعني مخططًا وهجرات بلا حاجة معلنة |
| سعر مستهدف معكوس (احسب الهامش من سعر تريده) | غير محدد | نموذج المجال (عقد ٠٣) يحسب هامش الحماية بالقيمة لا بالعكس؛ لا يُخترع حساب جديد |
| استيراد مخزون تاريخي/افتتاح مخزون مفروض | ممنوع في هذه المجموعة | أمر التنفيذ §15 — لا مخزون إلزامي أبدًا |
| Google/OAuth/سحاب/تعدد أجهزة | خارج المجموعات | قرار مرحلة — النموذج محلي أولًا |
| مقعد سادس / تعديل قشرة خمسية | ممنوع | القشرة الخمسية ثابتة |

### ج) بطاقات التتبع الخارجية

أمر تنفيذ المجموعة ٣ لم يُرفق بمعرّفات MC/D (لا توجد بطاقات خارجية في المستودع كما
وثّقت المجموعتان ١ و٢)؛ البنود أعلاه تُطابق نصوص أمثلة الأربع رحلات الإلزامية في §12
وأقسام النطاق A–G بندًا ببند.

### د) التحقق

| الفحص | النتيجة (2026-09-02) |
|---|---|
| `pnpm typecheck` + `pnpm prototype:check` | نظيفان |
| `pnpm lint` | 0 أخطاء / 37 تحذيرًا (السقف) — لا تحذيرات جديدة (قواعد التعقيد على src/tests فقط ولم تُمس) |
| `pnpm text-density` | ضمن الحدود — شاشات جديدة مقيسة من يومها الأول (CostCalculator 49/56، EstimateDetail 33/36، Tools 27/34) ورفع موثق فقط لملصقات مُكَلَّفة (DirectSaleEditor 66، OrderDetail 158، Catalog 94) |
| `pnpm design-guards` | نظيف |
| `pnpm test` (مجال) | 18 ملفًا / 205 اختبارًا (لم تُمس؛ المجموعة ٣ لم تغير المجال) |
| `pnpm prototype:test` | 84 ملفًا / 526 اختبارًا (G3.dom جديدة بـ8 + تحديث 2 خدمة + 3 مصنّف + تحديثات محاكاة أمينة U004/U06) |
| `pnpm prototype:build` + PWA | ناجح (64 مدخلًا مُسبق التخزين — الشاشتان الجديدتان) |
| فحص حي (390×844، RTL) | الرحلات الأربع الإلزامية §12 كاملة: (١) أدواتي→حاسبة→حفظ→مسودة→العودة للتقدير (٢) أدواتي→كتالوج→سجّل بيع→تعديل السعر→نقدي→السجل→العودة (٣) عمل→طلب→تعديل السعر 15→18→تنفيذ→جاهز→تسليم→تحصيل→إغلاق (٤) سجّل→بيع آجل بلا منتج→زبونة→الرئيسية→ورقة تحصيل→سداد — بلا أي خطأ كونسول أو صفحة، والبيانات تبقى بعد إعادة التحميل |
| عيوب وجدها الفحص الحي وأُصلحت | 5 عيوب سابقة (موثقة في المقدمة أعلاه) على المسار الحرج للمجموعة ٣ |

---

## المجموعة ٤ — القبول والتكامل النهائي (2026-09-02)

> جولة قبول كاملة فوق حالة المجموعات ١–٣ المدمجة: رحلات حية على متصفح حقيقي
> ٣٩٠×٨٤٤ RTL ببيانات مزروعة ومن ثم فارغة، مراجعة ملكية الشاشات والتنقل،
> تدقيق الحقيقة المالية رقميًا لا بصريًا، وفحص الكونسول. كشفت الجولة ٨ عيوب
> تكامل حقيقية على المسارات الحرجة أُصلحت كلها داخل النطاق المعتمد (الجدول
> أدناه) — سبعة في الجولة الأولى وواحد (G4-9) في جولة الاستئناف فوق كود نظيف.

| الحقل | القيمة |
|---|---|
| نقطة الانطلاق | `main` @ `f122094ebbadff9965e3ca01602de5d401a2bf6f` (نهاية المجموعة ٣ — تم التحقق من الرأس الفعلي البعيد قبل أي تعديل) |
| الفرع | `agent/group4-final-acceptance` |
| المخطط المحلي / التصدير | 30 / 22 — لم يتغيرا: المجموعة ٤ إصلاحات فوق السجلات القائمة لا مخازن ولا حقول جديدة |
| خط الأساس | `pnpm typecheck` ×2 نظيف؛ lint 0/37؛ text-density وdesign-guards نظيفان؛ 205 مجال + 526 نموذج أولي؛ بناء إنتاجي + PWA (64 مدخلًا) — كله أخضر قبل أي تغيير |

### أ) عيوب التكامل التي وجدها الفحص الحي وأُصلحت

| # | العيب (كما وُجد) | الأثر قبل الإصلاح | الإصلاح | التحقق |
|---|---|---|---|---|
| G4-1 | معاينة أثر التحصيل تعرض تجاوز المتبقي كنتيجة صالحة (+25.00 / متبقٍ 0.00) بينما الحارس يمنع الحفظ | رقم مضلل يقول إن الذمة ستُسدّ وهي لن تُسدّ — «نتيجة مرئية غير صادقة» | عند التجاوز يبقى المتبقي كما هو (بلا قصّ Math.max) ويظهر تحذير داخل المعاينة نفسها | فحص حي: 15.00 فوق 10.00 → متبقٍ 10.00 + تحذير داخلي |
| G4-2 | رسالة خطأ التحصيل تبقى عالقة بعد تصحيح المبلغ | المستخدم يقرأ «يتجاوز المتبقي» وهو صحّح فعلًا | مسح الرسالة عند تغيير المبلغ | فحص حي: تصحيح 15→10 يزيل التحذير فورًا |
| G4-3 | توزيع الكاش: المحفظة لا تُختار أبدًا (`??` لا يمرّر السلسلة الفارغة) بينما `<select>` يعرض خياره الأول | مسار التغطية (?mode=cover) — رحلة إلزامية — يتوقف بـ«اختر محفظة» بلا سبب ظاهر | `||` بدل `??` مع تعليق توثيقي | فحص حي: التغطية 7.25 من الدرج تعمل وغير الموزع يعود 0.00 |
| G4-4 | التراجع عن تخصيصٍ يُنقص المحفظة ولا يُرجع القيمة إلى «غير الموزع» (المجموع يسقط بصمت) | الكاش المسجل الكلي نقص 10.00 من 39.75 إلى 29.75 — فقدان كاش صامت يكسر عقد «التخصيص لا يغيّر الإجمالي» | استبعاد التخصيصات المُتراجَعة من مجموع التخصيص في `readPosition` | اختبار انحدار جديد (غير الموزع يستعيد 50000 والإجمالي لا يسقط) + فحص حي (26.75/13.00/39.75) |
| G4-6 | مسودة «تصميم مخطط» (جسر التقدير) بلا حقل اسم عميل تواجه شرط «سجل اسم العميل قبل الاتفاق» بلا أي حقل | رحلة إلزامية كاملة (تقدير→مسودة→اتفاق) طريق مسدود: جسر أدواتي→طلب لا يكتمل أبدًا | حقل اسم العميل المطلوب في محرر الاتفاق نفسه (يُحفظ في المسودة أولًا — مصدر كتابة واحد) | فحص حي: الاتفاق اكتمل والطلب وُجد وحُصّل وأُغلق |
| G4-7 | تصحيح السعر على طلب مغلق يفتح متبقيًا بحالة «partially_paid» عمياء: لا الرئيسية ولا دفتر الناس ولا ورقة التحصيل تعرضها | 2.00 دين على ليلى موجود في المجال بلا أي مدخل تحصيل — مسار التراجع عن قبضة يصف الحالة نفسها بـ«debt» (تناقض بين مساري تصحيح) | `settlementAfterPriceRevision` تتبع معيّن مسار التراجع: مغلق + متبقٍ = دين مسجل مرئي قابل للتحصيل من كل المداخل | اختبارا انحدار (رفع يفتح دينًا قابلًا للتحصيل؛ خفض يسدد) + فحص حي: الرئيسية/الناس/الورقة كلها تعرض 4.00 ثم تُسدّ |
| G4-8 | `<bdi>` داخل `<option>` في توزيع الكاش والتحويل والعدّ — المتصفح يرفض التعشيش ويسجل أخطاء كونسول | أخطاء كونسول في صفحات مالية أساسية (Section 11 يفرض مراجعة الكونسول) | نص خالص بـ`formatMoneyMinor` داخل الـoption | فحص حي: صفر أخطاء كونسول على التوزيع |
| G4-9 | فشل نسبة التحصيل إلى محفظة (حين يوجد فرق سالب في «غير الموزع») كان يُعلن فشل التحصيل كله بعد أن نُفّذت كتابته فعلًا: الدين نقص والكاش هبط في غير الموزع والرسالة تقول إن القبض لن يُسجّل | كتابة جزئية بكاذبة: إعادة المحاولة يمنعها حارس التجاوز بينما العملية تمّت؛ وجهة المحفظة المختارة تُستبدل بصمت (وُجد في جولة الاستئناف: مصروف من غير الموزع يفتح فرقًا سالبًا ثم تحصيل أكبر من المتاح بعده) | مسار الفشل اللين بمعيار محرر البيع نفسه: التحصيل ينجح والمال يبقى غير الموزع بلا فقدان وسبب النسبة يظهر في نتيجة التحصيل؛ وورقة الإضافة (بيع/مصروف) تعرض سبب فشل النسبة في وصلها بدل تجاهله الصامت | اختبار انحدار جديد (الدين يسدد، الكاش يرتفع بالمقبوض كاملًا، المحفظة لا تتغير، سبب النسبة ظاهر) + فحص حي: تحصيل 12.00 مع فرق سالب اكتمل بصدق بلا أي فقدان |

ملاحظة تحقق سالبة (G4-5): زر «سجّل بيع هذا المنتج» في الكتالوج ظهر مغطى بالشريط السفلي عند حافة التمرير — الفحص الهندسي أثبت أنه قابل للتمرير واللمس الطبيعي (scrollIntoView يكشفه)؛ عامل Sticky Nav طبيعي لا عيب.

### ب) مصفوفة القبول النهائية

معرّفات MC/D/المخرجات ليست نصوص في المستودع (كما وثّقت المجموعات ١–٣ في أقسام «ج»
لديها)؛ تُخطّط هنا بالتطابق مع أقرب متطلب مرقّم في المستودع: MC-01…MC-24 ↔ جدول
المتطلبات M1–M24 في `docs/reference/independent-flow-redesign.md` §6 (المرجع الخارجي
المعتمد المقروء)، وD1–D14 ↔ `docs/02-decision-log.md` D-001…D-014، والمخرجات أ–هـ ↔
الأسطح الخمسة المعتمدة للقشرة. لا يُحذف بند بصمت.

#### ب-١) MC-01…MC-24 (عبر M1–M24)

| المعرف | المتطلب (مختصر) | الحالة | الدليل |
|---|---|---|---|
| MC-01 (M1) | عدّ الصندوق ينتج تسوية موثقة | مُتحقق | `/cash/count` — فحص حي: 23.00 معدود، فرق −2.25 تسوية بتاريخ اليوم، لا رقم قديم يتغير |
| MC-02 (M2) | «لا أعرف» حالة غير محدد لا صفر | مُتحقق | Setup اختيار «ما بعرف الآن»؛ الدرج «رصيد الافتتاح غير معروف» في المحافظ ودفتر المحفظة |
| MC-03 (M3) | الحركات غير المصنفة بمخرج | مُتحقق | الكاش غير الموزع بطاقة/قرار صريح + حالة partial_needs_review للبيع + needs_review للطلب |
| MC-04 (M4) | تصحيح موثق منفصل عن الإلغاء | مُتحقق | CorrectionPreview قبل كل تصحيح؛ سجل التصحيحات الموحد بعائلاته؛ فحص حي (بيع/مصروف/سعر/تخصيص) |
| MC-05 (M5) | حاسبة مستقلة بلا طلب/مخزون | مُتحقق | `/tools/calculator` — فحص حي 7.00+3.00=10.00 حيًا + «ما انحفظت أي حركة» |
| MC-06 (M6) | مال المالك من الدرجة الأولى | مُتحقق | `/finance/withdraw` المدخل الموحد + دفتر المالك في مالي |
| MC-07 (M7) | الأمانات لا تمس الإيراد | مُتحقق | فحص حي: أمانة 15.00 رفعت الكاش ولم تلمس الربح/المالك؛ مؤهل في الرئيسية/المالي/الكشف |
| MC-08 (M8) | بيع آجل سريع باسم الزبون | مُتحقق | ورقة الإضافة «آجل» + اسم الزبون → دفتر الناس (سعاد) |
| MC-09 (M9) | تجربة العودة بعد الغياب | مُتحقق | بطاقة الغياب بعد ٧ أيام (homeControlCenterService.awaySection) — تعتمد على مرور الوقت لا على بيانات زائفة |
| MC-10 (M10) | نسخ احتياطي بتذكير ومعاينة استعادة | مُتحقق | تصدير مُتحقق (فحص حي: «آخر نسخة مُتحقق منها 2026-09-02») + استيراد بمعاينة + تذكير ٧ أيام |
| MC-11 (M11) | حالات صريحة Empty/Loading/Saving/Error/Success | مُتحقق | أنماط عقد ٢٦ §6 عبر الشاشات؛ فحص حي شمل الفارغ (رئيسية نظيفة) والخطأ (حارس التجاوز) والنجاح (وصل/نتيجة) |
| MC-12 (M12) | حفظ المسودة عند المقاطعة | مُتحقق | فحص حي: إعادة تحميل أثناء الإعداد تعيد المسودة بإشعار؛ حوار الوسخ في المحررات العميقة |
| MC-13 (M13) | بحث وفلترة ونطاق زمني | مُتحقق | بحث دفتر الناس بالاسم + نطاقات الكشف (أسبوع/شهر/مخصص) + تصفية سجل التصحيحات |
| MC-14 (M14) | تاريخ قابل للتعديل مع إعلان الفترة | مُتحقق | occurredOn في المحررات؛ الكشف يعلن نطاقه ويحسب داخله فقط |
| MC-15 (M15) | إخفاء المبالغ بلمسة | مؤجل بنطاق معتمد | قرار مؤجل موثق في مصدر الحقيقة §2 (الخصوصية على الجهاز المشترك) — ليس في نطاق المجموعات ١–٣ |
| MC-16 (M16) | أعمار الديون 0–30/30–60/60+ | مؤجل بنطاق معتمد | دفتر الناس المعتمد قراءة مجمّعة بالاسم (§11 مصدر الحقيقة) — التقسيم العمري غير مكلَّف |
| MC-17 (M17) | تفعيل مؤرخ لكل وحدة | مؤجل جزئيًا بنطاق معتمد | التفعيل المؤرخ للمخزون فقط (§13)؛ بقية الوحدات تعرض حالتها في أدواتي بلا مفاتيح تفعيل |
| MC-18 (M18) | حماية الإرسال المزدوج | مُتحقق | فحص حي: ثلاث نقرات متتالية على «سجّل البيع» كتبت بيعًا واحدًا (79.75 = 76.75+3.00 مرة واحدة) + مفاتيح idempotency في كل مسارات الكتابة |
| MC-19 (M19) | مشاركة ملخص عبر واتساب | محظور بنطاق هذه المرحلة | أمر التنفيذ §12 يمنع تكامل WhatsApp API — خارج المجموعات |
| MC-20 (M20) | تحويل بين محافظ كنقل لا إيراد | مُتحقق | `/cash/transfer` — أُصلح تعشيش bdi داخله (G4-8)؛ النقل لا يغيّر الإجمالي |
| MC-21 (M21) | قفل PIN/بصمة | مؤجل بنطاق معتمد | قرار مؤجل موثق (§2) — قيمة عالية قبل السحابة مؤجلة |
| MC-22 (M22) | نسخ مشفر بكلمة مرور | مؤجل بنطاق معتمد | مؤجل في المرجع §6.3 — خطر النسيان قبل السحابة |
| MC-23 (M23) | تصنيف تلقائي مقترح | مؤجل بنطاق معتمد | يحتاج بيانات سلوك — المرجع §6.3 |
| MC-24 (M24) | نقطة التعادل والتحليلات | مُتحقق جزئيًا | طبقة G5 (التغطية والتعادل) موجودة في قراءة الفترة ضمن النطاق المعتمد؛ التحليلات المتقدمة مؤجلة |

#### ب-٢) D1–D14 (عبر D-001…D-014 في سجل القرارات)

| المعرف | القرار (مختصر) | الحالة | الدليل |
|---|---|---|---|
| D1 (D-001) | SaaS للمالك لا Billing داخل تطبيق المستخدم | مُتحقق | لا أي Billing/دفع في النموذج الأولي — فحص الواجهات |
| D2 (D-002) | الأردن والدينار الأردني حصرًا | مُتحقق | كل المبالغ JOD بمنزلتين؛ «د.أ» بعد الرقم |
| D3 (D-003) | المركزية: تكلفة/ربح/كاش/طلبات/الفعل التالي | مُتحقق | القشرة الخمسة والإجراء التالي على كل سطح |
| D4 (D-004) | نواة مستقلة عن Accounting/zman | مُتحقق | `src/domain/` قائم بذاته يربطه Application layer |
| D5 (D-005) | Vertical Slice وModular Monolith لا Rewrite/ERP | مُتحقق | بنية المستودع وشرائح العقد |
| D6 (D-006) | التخزين هجين لكن الاختيار بعد prototype | مُتحقق | محلي أولًا بلا مزامنة؛ لا ادعاء سحابي في أي نص |
| D7 (D-007) | الطلب رحلة لا فاتورة | مُتحقق | رحلة ١٠ كاملة حية: مسودة→تكلفة→اتفاق→تنفيذ→تسليم→تحصيل→إغلاق |
| D8 (D-008) | التدرج مهام قصيرة لا LMS | مُتحقق | صفحة الأساس + onboarding متدرج + إرشادات سياقية |
| D9 (D-009) | فرضية ودليل وأقل نطاق لكل ميزة | مُتحقق | سجل الفرضيات ووثائق العقود |
| D10 (D-010) | لا كود خارجي دون ترخيص وفحص | مُتحقق | THIRD_PARTY_NOTICES + حارس الكود الخارجي |
| D11 (D-011) | أول شريحة طلب حرفة مخصصة | مُتحقق | دورة الطلب هي العمود الفقري (رحلة ١٠) |
| D12 (D-012) | خبرة المالك دليل مؤسس لا إحصائي | مُتحقق | خريطة الأدلة وحدود الادعاء موثقة |
| D13 (D-013) | النتيجة النهائية فقط عند known | مُتحقق | فحص حي: نتيجة الفترة «غير متاح» لتكلفة مجهولة — لا ربح متوهَّم |
| D14 (D-014) | إلغاء ذي عربون needs_review لا تسوية تلقائية | مُتحقق | عقد ٠٢ وسياسة الإلغاء بالتسويات الثلاثية |

#### ب-٣) المخرجات أ–هـ (الأسطح الخمسة المعتمدة)

| المخرج | السطح | الحالة | الدليل |
|---|---|---|---|
| أ | مشروعي الآن `/` — كتلة «الأهم الآن» + حقائق + أفعال | مُتحقق | فحص حي: أولوية الدين بحصّل، الحقائق 79.75/0/0/0، مؤهل الأمانات، خط الحقيقة |
| ب | العمل `/orders` — أولوية دائمة + مبيعات مختصرة + CTA | مُتحقق | فحص حي: الطلب المغلق وصفوف المبيعات وزر بيع مباشر |
| ج | سجّل (ورقة الإضافة) — بيع/مصروف/طلب/مسودة/تحصيل | مُتحقق | رحلات ٣ و٤ حية؛ وصل المصدر في كل مرة |
| د | مالي `/finance` — وضع/فترة + كشف + محافظ + عدّ + توزيع | مُتحقق | رحلتا ٥ و٦ حيتان + التغطية بعد إصلاح G4-3 |
| هـ | أدواتي `/tools` — حاسبة + تقديرات + حالة الوحدات + كتالوج | مُتحقق | رحلتا ٨ و٩ حيتان + جسر التقدير→المسودة |

#### ب-٤) نقاط الدخول والرحلات والثوابت

| البند | الحالة | الدليل |
|---|---|---|
| مداخل التحصيل الثلاثة (رئيسية/دفتر الناس/ورقة الإضافة) تحفظ مصدرها وتعود إليه | مُتحقق | فحص حي: `?from=/` و`?source=sale:<id>` والرجوع للمصدر |
| كل تنبيه الرئيسية يفتح مصدره الدقيق | مُتحقق | بند الدين → `/collect?source=…` لا قائمة عامة |
| Product-to-Sale يحفظ المصدر ويعود للكتالوج | مُتحقق | فحص حي: `?product=&from=/catalog` والإغلاق يعود للمصدر |
| جسر التقدير→مسودة يعيد للتقدير | مُتحقق | فحص حي: `?estimate=&from=/tools/estimate/:id` |
| رحلات ١–١٠ (أمر التنفيذ §7) عشرة كاملة | مُتحقق | كلها منفذة حية ٣٩٠×٨٤٤ RTL ببيانات مزروعة؛ التفاصيل في current-state §20 |
| JOD منزلتان حصرًا | مُتحقق | كل الأسطح؛ formatMoneyMinor/MoneyValue |
| الأمانة ليست ربحًا/مالكًا/نتيجة | مُتحقق | فحص حي + اختبارات amanah-loss القائمة |
| المجهول لا يصير صفرًا | مُتحقق | «غير محدد بعد»/«غير مسجل — سجّله» في كل الأسطح |
| بيع مباشر يعترف مرة عند تاريخ البيع | مُتحقق | إيراد معترف 45.00 (رحلة ٣) ثم 53.00 — التحصيل لم يزد الإيراد |
| التحصيل لا يعيد الاعتراف | مُتحقق | كاش +22.50 مقابل إيراد ثابت؛ «لا تتغير — القبض ليس إيرادًا» في المعاينة |
| التقديرات بلا أثر مالي/مخزون | مُتحقق | اختبارات G3 + نص الحصر الظاهر |
| منع التجاوز (تسليم/تحصيل/تخصيص) | مُتحقق | حارس التجاوز في ورقة التحصيل (فحص حي 25>20) وحارس التخصيص (اختبار) |
| معاينة التصحيح صادقة قبل/بعد لكل بُعد | مُتحقق | فحص حي (سعر الطلب 18→20) + إصلاح G4-1 للتحصيل |
| idempotency | مُتحقق | نقرة ثلاثية كتبت مرة واحدة + اختبارات التكرار في كل مسار |
| بقاء البيانات بعد إعادة التحميل ودون اتصال | مُتحقق | PWA إنتاجي على معاينة 5200: إعادة تحميل دون اتصال تخدم التطبيق والبيانات والوصلة العميقة |
| الترحيل/التصدير/الاستيراد | غير متأثر | المخطط 30/22 كما هو؛ لا حقول جديدة في المجموعة ٤؛ اختبارات transfers خضراء |

### ج) المؤجل صراحة (خارج نطاق المجموعات ١–٣)

| البند | السبب | المالك المستقبلي |
|---|---|---|
| Google/OAuth/سحاب/تعدد أجهزة/استرداد حساب | قرار مرحلة — النموذج محلي أولًا (§2/§14 مصدر الحقيقة) | مرحلة ما بعد إثبات القيمة |
| إخفاء المبالغ/PIN | قرار مؤجل موثق (خصوصية الجهاز المشترك) | قرار مالك مستقبلي |
| أعمار الديون | غير مكلَّف في الحالة المستهدفة المعتمدة | قرار مالك مستقبلي بعد Pilot |
| مشاركة واتساب | ممنوع صراحة في أمر تنفيذ هذه المرحلة | مرحة توسع مستقبلية |
| SKU/سعر مستهدف معكوس/استيراد مخزون تاريخي | مؤجل/ممنوع بنطاق المجموعة ٣ (ب) | غير محدد |
| QA جهاز فعلي وPilot بشري | خارج قدرات بيئة الفحص الحالية | خطة Pilot قائمة في todo.md |

### د) التحقق النهائي (بعد جولة الاستئناف وإصلاح G4-9)

| الفحص | النتيجة (2026-09-02) |
|---|---|
| `pnpm typecheck` + `pnpm prototype:check` | نظيفان |
| `pnpm lint` | 0 أخطاء / 37 تحذيرًا (السقف) — لا تحذيرات جديدة |
| `pnpm text-density` | ضمن الحدود — رفع موثق واحد: AgreementEditor 55→59 (إصلاح G4-6، ملصقات حقل اسم العميل المطلوب) |
| `pnpm design-guards` | نظيف |
| `pnpm test` (مجال) | 18 ملفًا / 207 اختبارات (+2 انحدار G4-7) |
| `pnpm prototype:test` | 84 ملفًا / 528 اختبارًا (+1 انحدار G4-4 +1 انحدار G4-9) |
| `pnpm prototype:build` + PWA | ناجح (64 مدخلًا مُسبق التخزين) |
| `pnpm format:check` | 9 تحذيرات تنسيق في ملفات مجال لم تمسها المجموعة ٤ (حالة سابقة عند نقطة الانطلاق f122094 — بوابة `pnpm check` القانونية لا تشملها) |
| فحص حي (٣٩٠×٨٤٤، RTL) | جولتان: الجولة الأولى (بيانات مزروعة) ثم جولة الاستئناف فوق خادم تطوير نظيف: رحلات ١–١٠ كاملة، إقلاع نظيف فارغ، إعادة تحميل دون اتصال عبر PWA الإنتاجي (5200) مع وصلة عميقة، صفر أخطاء كونسول/صفحة بعد الإصلاحات |
| مطابقة مالية رقمية (جولة الاستئناف) | كاش 110.00 = تصحيح بيع 42.00 + بيع 20.00 + بيع 5.00 + بيع 12.00 + بيع 15.00 + تحصيلات طلب 24.00 + أمانة 18.00 − مصاريف 24.00 − تسوية عدّ 2.00 (مطابقة تامة)؛ ذمم 0؛ مال مالك 0؛ الحارس المالي لـ G4-4 أثبت رقميًا أن فك التخصيص لا يُفقد الكاش (58.00 قبل وبعد) |

ملاحظة استئناف (جولة التسليم): الانقطاع الأولي أنهى عمله قبل الدفع/الـPR. أُعيد تشغيل جولة القبول فوق الخادم النظيف فكشفت G4-9 (كتابة جزئية كاذبة في نسبة التحصيل) وتحققت من كل إصلاحات G4-1…G4-8 حيةً — منها إعادة إثبات G4-7 حيةً بعد إعادة تشغيل خادم التطوير (ذاكرة تحويل قديمة في خادم الجولة الأولى كانت تخدم نسخة ما قبل الإصلاح من ملف المجال خارج جذر Vite؛ البناء الإنتاجي كان يحمل الإصلاح دائمًا).

## المجموعة ٥ — التدقيق الشامل والإصلاح والتنظيف (2026-09-02)

آخر تحديث للمستند. بدأت المجموعة ٥ من `origin/main` عند `cce75d7e7b12515b2a45dc3287d894b01a39073c`
(نهاية المجموعة ٤ — تحقق من الرأس البعيد الفعلي والشجرة نظيفة وخط الأساس أخضر قبل أي تعديل).
المنهجية: خمسة مدققين متخصصين متوازيين (تدفق/مال/موبايل-RTL/تنظيف-بنية/إصدار-أمن) بإخراج موحد
(`finding_id / severity / area / impact / evidence / recommended_action / scope / verification`)،
ثم توحيد النتائج عند الوكيل الأب وإصلاح كل عيب آمن داخل النطاق مع اختبار انحدار حيث يلزم،
ثم تشغيل جناح التحقق الكامل ومطابقة مالية رقمية حية قبل الدمج.

### أ) العيوب المُصلحة (fixed and verified)

| المعرف | الخطورة | الملخص | التحقق |
|---|---|---|---|
| S2-01 | حرجة | دفعة مورد تُسجَّل متجاهلة تراجعات الدفعات → مدفوع متضخم/ذمة سالبة/كاش يختفي من الإجماليات | `recordSupplierPurchasePayment` صار واعيًا للتراجعات (effectivePaid) + اختبارا انحدار مجال + مطابقة حية (ذمة 30→20→30 عند دفع/تراجع 10.00) |
| S2-02 | عالية | ورقة التحصيل على دين مسجل تسقط مفتاح idempotency → تحصيل مزدوج عند إعادة المحاولة | `collectDebt` يقبل operationKey وورقة التحصيل تمرره + انحدار ساعة متقدمة |
| S2-03 | عالية | تصدير/استيراد يرفض مخزنًا استعمل تراجع دفعة موثقًا (رفض نسخته الاحتياطية نفسها) | `validSupplierPurchase` واعٍ للتراجعات (دفعة واحدة لكل تراجع + تطابق مبلغ) + انحدار تصدير مُتحقق |
| S5-05 | متوسطة | مصفوفة قبول الاستيراد ترفض ملفات v18 حقيقية | قبول v18/27 صريح بالثابت الحرفي + انحدار |
| S2-06 | متوسطة | صفحة التوزيع اليدوي بلا مفتاح idempotency | operationKey لكل تركيب + إعادة النقر لا تكرر الكتابة |
| S2-08 | منخفضة | دفتر الناس يفترض payments[0] هو الدفع الأولي ويقرأ المدفوع إجماليًا | التماس الدفع الأولي بمعرفه + paidMinor الصافي + انحدار «دفعات سُددت» بعد تراجع |
| S2-10 | متوسطة | أسطح التسليم تعلن فشلًا بعد نجاح الكتابة (كتابة جزئية كاذبة) | الفشل اللين: إشعار غير حاجب لخطأ التخزين فقط |
| S2-04(ب) | متوسطة | «غير الموزع» السالب يظهر رقمًا مجردًا بلا تفسير | بطاقة الكاش تُظهر «تخصيص زائد — راجع تراجع التحصيل/تصحيح البيع…» مع role=alert |
| S2-05 | متوسطة | أسطب السيولة تُخفي الأمانة المحتجزة عن إجمالي الكاش | تعريض amanahHeldMinor بإشعار «مش ربحك ولا مالك» — تحقق حي (18.00) |
| G5-S6 | حرجة | **عيب مستتر من المجموعة ٤ كشفه الفحص الحي للمجموعة ٥**: هوكات بعد عوائد مبكرة في `SupplierPurchaseEditor` (useMemo) و`FinancialEventEditor` (useFormDirty/useUnsavedChangesGuard) → خطأ React رقم 310 يسقط مسار دفعة/تعديل المورد بعد التحميل غير المتزامن | نقل الهوكات فوق العوائد المبكرة + ماسح AST يؤكد صفر حالات متبقية في كل الصفحات + انحدار UI يفشل على النسخة المعطوبة وينجح بعد الإصلاح + تحقق حي (الصفحة تفتح بلا أخطاء كونسول) |
| G5-S7 | عالية | **كشفه الفحص الحي**: كشف الفترة يتجاهل تراجعات دفعات الموردين → صافي الكاش +100.50 بينما الكاش المسجل +110.50 (تضخيم «دفع للموردين» بمقدار المردود) | خصم التراجعات بتاريخها بسطر مصدر «تراجع عن دفعة» + انحداران (داخل/خارج الفترة) + تحقق حي (الصافي 110.50 والمدفوع 20.00) |
| S1-01…S1-14 | عالية/متوسطة | تنقل عقد ٢٦: فقدان `?from` من دفتر الناس/تفاصيل الطلب/مالي؛ حارس الوسخ في عدّ/توزيع/كتالوج/مرجع؛ وصل استهلاك بلا `?order=`؛ ١٠ محررات تتجاهل عودة المصدر بعد الحفظ؛ تركيز `?entry=` في دفتر المحفظة؛ قيم ربط عميق معلنة بلا مستهلك؛ رجوع أسطح المستوى الثاني؛ حذف تقدير بنقرة؛ زر رجوع الإعدادات | اختبارات + تحقق حي (روابط تحمل from=/parties وfrom=/finance وfrom=/cash؛ بعد الحفظ يعود للمصدر) |
| S3-01…S3-18 | متوسطة/منخفضة | جودة موبايل/RTL: تذييل حفظ لاصق في ١٤ محررًا (عقد 3.11)؛ أرضية لمس 44px لأفعال المال؛ رمز لون غير معرف؛ تباين AA لرقائق النجاح/التحذير؛ «غير متاح» نصًا عربيًا؛ تصحيح «سيوفه→صفوفه»؛ توحيد formatMoneyWithUnit؛ مصطلحات القاموس (دينار→د.أ، كلفة→تكلفة، ذمم→دين)؛ role=alert لأوعية الخطأ (١٢)؛ د.أ خارج bdi؛ إشعارات الإعدادات داخل أقسامها | design-guards + اختبارات + فحص بصري حي 390×844 |
| S4-01…S4-14 | متوسطة/منخفضة | تنظيف وبنية: إزالة 400 تكرار CSS (6316→4555 سطرًا، كل كتلة فريدة تبقى مرة)؛ حذف scheduleLayout الميت واختباره؛ InfoCard المشترك بدل التكرار؛ حذف listPolicies/readRecordedMargins غير المستدعيين؛ توحيد isValidLocalDate بالنسخة المجالية؛ summarizeCashContinuity المجالي لدفتر المحفظة؛ توسيع eslint ليشمل application/storage/app/contexts/presentation/pwa (مع منع any) وإضافة format:check للبوابة؛ نقل ملفات QA الجذرية إلى docs/quality/؛ تبسيط شرطيات الترحيل؛ تصحيح إصدارات المستندات 29/21→30/22 | البناء والاختبارات وstylelint خضراء؛ كل حذف أثبت بعدم وجود مستورد/مسار/استدعاء |
| S5-01…S5-14 | متوسطة/منخفضة | إصدار وأمن: استضافة خطوط IBM Plex محليًا (10 woff2 + fonts.css) وإسقاط CDN من البناء؛ ترويسة CSP في `_headers`؛ مسح مسودة التأسيس عند البدء من جديد؛ THIRD_PARTY_NOTICES لكل تبعيات التشغيل؛ تأجيل إبطال URL التصدير 30 ثانية (سلامة WebKit)؛ تثبيت Catalog على Asia/Amman؛ StartupGate يقرأ الملف مرة كل جلسة؛ BroadcastChannel لتحديث التبويبات المتقاطعة | فحص dist بلا مراجع CDN؛ precache 75 مدخلًا؛ تحقق حي عبر التبويبات |

### ب) المؤجل/الموثق صراحة (deferred with reason)

| البند | السبب |
|---|---|
| S2-04(أ) — فعل بنقرة واحدة «تراجع التخصيص المطابق» عند تراجع التحصيل المصدر | قرار تصميم يضيف اقتران كتابة جديدًا بين مجالي التحصيل والتخصيص؛ المسار القائم (تراجع التخصيص الموثق من دفتر المحفظة) يعمل ومُختبر (رحلة ٦)؛ الجزء (ب) من العرض الصادق نُفذ |
| S2-07 — سطحان متعمدان لرأس مال المالك | تصميم مقصود موثق (قرار المجموعة ٢)؛ يُراقب لا يُدمج بلا قرار مالك |
| S2-09 — إعادة عرض تصريحية عند تعديل شراء | ملاحظة صدق عرض؛ الشرح القائم كافٍ للمرحلة المحلية |
| S3-12 — سقوف كثافة النص لثلاث شاشات | رفع موثق بقرار مالك من المجموعة ٤؛ لا تغيير إضافي بلا قرار |
| S5-15 — تطبيع الأرقام العربية-الهندية للإدخال | قرار منتج موثق: إدخال أرقام إنجليزية 0–9 مقصود ومُختبر؛ نُفذ ثم أُرجع وحُفظ كقرار مؤجل |
| S5-07/S5-08/S5-16 — اتصال IndexedDB لكل عملية، إبطال dataVersion خشن، حماية التبويبات | بنية أداء تتطلب إعادة هيكلة أوسع بتغطية انحدار أوسع؛ تحسين جزئي نُفذ (S5-09/S5-12)؛ يُؤجل بقرار أدلة |
| S5-10 — كتلة index متحمسة 478KB | نتيجة تقييم؛ تفتيت أعمق يُؤجل لقرار أداء بعد Pilot |
| S4-11 — سطح API متصدر عريض | ملاحظة بنية لا عيب؛ الضغط يتطلب قرار مالك |
| QA جهاز فعلي/Pilot بشري/إنتاج Cloudflare | خارج قدرات بيئة الفحص؛ خطة Pilot قائمة في todo.md |

### ج) ما تحقق أنه صحيح أصلًا (already correct and verified)

القشرة الخمسة وسلامة المسارات؛ ملكية شاشة واحدة لكل مفهوم؛ لا مسارات يتيمة أو حلقات ذاتية؛
مصدر كتابة مالي واحد لكل مفهوم (عدا الاستثناءين الموثقين S2-07/S2-06 قبل الإصلاح)؛
الإيراد يُعترف مرة عند تاريخ البيع (65.00 حية لم تزد بالتحصيل)؛ التحصيل يحرك الكاش والذمة لا الإيراد؛
الأمانة كاش محتجز لا ربح ولا ملك (18.00 حية بإشعار صريح)؛ التقديرات بلا أثر مالي/ذمة/مخزون؛
سعر الكتالوج اقتراح والفعلي حاكم؛ اختيار المنتج اختياري؛ منع التحصيل/التسليم الزائد؛
فك التخصيص لا يُفقد الكاش (70.50 قبل وبعد — حي)؛ التصحيحات تحفظ الأصل وتطبق الدلتا العكسية الدقيقة
(+2.00 حي)؛ idempotency منتظم بعد S2-02/S2-06؛ JOD منزلتان؛ المخطط 30/التصدير 22 لم يتغيرا في المجموعة ٥؛
صفر أسرار في تاريخ git وفي الفرق؛ PWA يعمل دون اتصال ببياناته ووصلاته العميقة.

### د) التحقق النهائي (2026-09-02 — بعد كل إصلاحات المجموعة ٥)

| الفحص | النتيجة |
|---|---|
| `pnpm typecheck` + `pnpm prototype:check` | نظيفان |
| `pnpm lint` | 0 أخطاء / 37 تحذيرًا (السقف المعتمد، لا جديد) |
| `pnpm format:check` | نظيف (أصلحت انحرافات سابقة وأضيف للبوابة) |
| `pnpm text-density` | كل الأسطح ضمن §10 |
| `pnpm design-guards` | نظيف |
| `pnpm test` (مجال) | 18 ملفًا / 209 اختبارات |
| `pnpm prototype:test` | 84 ملفًا / 533 اختبارًا (528 + 5 انحدار جديدة للمجموعة ٥) |
| `pnpm prototype:build` + PWA | ناجح؛ 75 مدخلًا مُسبق التخزين (خطوط محلية) |
| فحص حي 390×844 RTL (بناء إنتاجي) | الرحلات العشر كاملة ببيانات عربية اصطناعية عبر الواجهة: بيع نقدي/آجل+تحصيل/مصروف/أمانة/عدّ+توزيع/تراجعان/تصحيحات/طلب كامل من تقدير حتى الإغلاق/مورد دفع+تراجع/كتالوج؛ إقلاع فارغ→تأسيس→«ما بعرف» محفوظ بطابع المحفظة؛ إقلاع ببيانات قائمة؛ إعادة تحميل دون اتصال بالبيانات والوصلة العميقة؛ **صفر أخطاء كونسول/صفحة** |
| مطابقة مالية رقمية حية | كاش 110.50 = أمانة 18 + كاش بيع مباشر 65 + قبض طلب 60 − مصاريف 12.50 − دفع مورد صافي 20 (+0 تسوية معدولة +0 تخصيص مفكوك)؛ كشف الفترة يطابق المركز بعد G5-S7؛ إيراد الطلب 60 مرة واحدة؛ ذمة مورد 30؛ «غير محدد بعد» لم يتحول صفرًا |
| ماسح ترتيب الهوكات (AST) | صفر ملفات فيها هوكات بعد عوائد مبكرة (بعد G5-S6) |
| فرع الاسترداد | `recovery/group5-pre-validation` يحفظ كل العمل غير الملتزم قبل التحقق (7e2234f) |

---

## المجموعة ٦ — الاسترداد والإغلاق النهائي (2026-09-02)

**نقطة البداية الفعلية:** `8ed175fc69487465d1ee6834fbea00883b65eebc` (فرع `main` البعيد — مطابقة للأساس المتوقع). تنفيذ سابق مُتوقف للمجموعة ٦ وُجد غير ملتزم في شجرة العمل على فرع `agent/group6-final-closure` (المبنودان ١–٣ منجزة، ٤–٦ جزئية) — حُفظ كاملًا في لقطة استرداد `3c6911d`/فرع `recovery/group6-interrupted` قبل أي تعديل. لا force-push ولا إعادة كتابة تاريخ ولا إبطال عمل سليم.

### أ) الانحدار البصري: السبب والإصلاح

| البند | التفصيل |
|---|---|
| السبب الجذري | **إتلاف أداة «إزالة تكرار CSS» في المجموعة ٥ (ارتكاب 146b18f)**: عند حذف قاعدة «مكررة» تركت المحدِّدات ملصوقة بمسافة أمام القاعدة التالية (بلا فاصلة) وأسربت أجسام `@media` إلى مستوى الجذر. أنتج ٥٩+ سلسلة ملصقة لا تطابق شيئًا أبدًا — منها `.micro-nav-item:active .micro-fab` (أفقد الزر البني الدائري)، `.micro-priority-truth strong .micro-button` (أسقط أساس `.micro-button`)، `.micro-icon-button:active .micro-icon-button svg .micro-main` (أسقط حشوات safe-area)، وشبكات البيت/مالي والأوراق النمطية |
| الدليل | فحص DOM حي 390×844 قبل الإصلاح: FAB شفاف `rgba(0,0,0,0)` نصف قطره 0 وقياسه 74.8×49.2؛ الأزرار الأولية شفافة؛ `.micro-home-facts` من `grid` إلى `block`؛ مقارنة نصية مع قاعدة Group 4 (cce75d7) أثبتت الالتصاق حرفيًا في فرق الارتكاب |
| الإصلاح | **إعادة بناء `index.css` من قاعدة Group 4 المعتمدة (cce75d7) + إعادة تطبيق ١٢ دلتا مشروعة مثبتة فقط**: S3-06 (رمزا :root)، S3-04 (موقعان bg-surface)، S3-03 (أرضية 44px)، S3-06 (علامتا مجهول secondary)، S3-02 (تذييل الحفظ اللاصق)، S1-08 (تركيز دفتر المحفظة ?entry=)، S3-01 (تأكيد حذف التقدير — مع رمز radius-control بدل radius-sm غير المعرف)، supplier-payable، وكتل المجموعة ٦ الثلاث (position-link وrestatement-note وowner-layer-body) |
| التحقق | كل محددات G4 الـ1115 حاضرة بعد البناء (فحص آلي)؛ stylelint وdesign-guards والبناء وكل الاختبارات خضراء؛ فحص حي: FAB بني rgb(150,78,51) دائري 56px، شبكة حقائق البيت عمودان (175×2)، شبكة مركز مالي 2×2، خيارات الإضافة الخمسة كاملة، لا تجاوز أفقي، مساحات آمنة سليمة |

### ب) بنود المجموعة ٦ السبعة — الحالة النهائية

| البند | الحالة | الأثر والتحقق |
|---|---|---|
| ١ — تراجع القبضة والتخصيص المطابق بنقطة واحدة | منجز ومُتحقق | `collectionReversalService` (معاينة + عكس دلتا دقيقة + قاعدة مطابقة operationKey→sourceRefLineId مع رفض غامض/معدل/مجزأ/مرتد) + `commitOrderCollectionReversal` بمعاملة IDB واحدة (طلب+كاش) مع فحص هوية داخل المعاملة؛ لوحة OrderDetail خلف إفصاح مسمّى؛ اختبارات 10 خدمة + 4 مجال + بيانان + تصدير/استيراد + DOM؛ **رحلة ١١ حية**: قبض طلب 60.00 → تراجع مزدوج → الكاش 112.50→52.50 بدقة، الدين عاد 60.00، سجل الأحداث يحفظ «تسجيل قبض … 60.00» و«التراجع عن قبضة … 60.00» معًا، الطبقة تختفي بعد التراجع (لا تراجع مزدوج) |
| ٢ — توحيد أسطح مال المالك | منجز ومُتحقق | صفحة `مال المالك` (المسار القديم محفوظ بإعادة توجيه): بطاقة رصيد + رأس المالك/الحق المتبقي + الفعلان + طبقة «حركات مالك» موحدة (قراءة تدمج ledger وpolicies) + حدود ⓘ؛ بطاقة مالي الواحدة تفتح الدفتر الموحد؛ إصلاح G6-U2-1/2/4؛ اختبارات خدمة 2 + بيانان + DOM 2 |
| ٣ — الشرح الصادق للتصحيح | منجز ومُتحقق | `RestatementNote` («الأصل محفوظ كما هو؛ التصحيح سِجِل جديد، والرقم الظاهر صافي أثرِهما») على مركز مالي (كل الفترة) والمدى والكشف ودفتر المحفظة وطبقة التصحيحات؛ حيًا: «٣ تصحيحات موثقة…» على المركز والطبقة |
| ٤ — الإفصاح التدريجي للشاشات الكثيفة | منجز ومُتحقق | OrderDetail 160→141 (تصحيحات الطلب خلف «تصحيحات موثقة على الطلب»)؛ Catalog 94→86 (٣ إفصاحات: الوحدة/الاقتراحات/تعديل الافتراضيات)؛ DirectSaleEditor 66→63 (المرجع + الشروح خلف إفصاحات)؛ كل الحقول الأولية مرئية، لا حذف معلومات، 44px |
| ٥ — أرقام إنجليزية وDD/MM/YYYY نظاميًا | منجز ومُتحقق | مُعيّن مركزي واحد (`formatLocalDate` رقمي منزلتين، الطويل=الرقي، الشهر MM/YYYY، الوقت DD/MM/YYYY HH:MM)؛ كل القيم عبر `bdi dir=ltr`؛ حدود الإدخال يطبّع الهندية/الفارسية (`normalizeAsciiDigits`)؛ إصلاح Profile (ar-JO) وStatement (نطاق الكشف ISO خام→رقمي) وSupplierPurchaseEditor (dueOn خام→رقمي)؛ **فحص حي لـ١٧ مسارًا: صفر أرقام هندية، صفر أسماء شهور، صفر YYYY/MM/DD، ISO واحد في الكشف أُصلح**؛ اختبارات 15 (حالات الحد: بطن المنزلة، الكبيسة، المستحيل، الحدود، ASCII خالص، منازل السنة) |
| ٦ — تحسينات الأداء الآمنة | منجز ومُتحقق | اتصال IDB واحد مخزَّن (53 فتحًا لتحميل «مالي» → 1؛ اختبار open-count) + إقالة عند versionchange وإعلان قدم صادق (G6-P4-2)؛ خدمات singleton (لا إعادة بناء ~35 خدمة عند كل كتابة)؛ SWR بلا وميض تحميل (Home/Schedule)؛ إبطال dataVersion موجه في 42 ملفًا؛ QuickActionSheet كسول + radix-runtime منفصل + جلب عند الخمول؛ **القياس: index 114.8KB→102.3KB gz (−11.4%) + ورقة الإضافة عند الطلب؛ precache 80** |
| ٧ — تقليص سطح API المصدر بأمان | منجز ومُتحقق | الطبقة ١ المحافظة: ٣٢ كلمة `export` سُحبت (23 دومين + 5 تخزين + 2 تطبيق) كل رمز بدليل «صفر مستوردات خارجية» أُعيد التحقق منه قبل السحب؛ حذف `isSurfacePath` (الرمز الميت الوحيد) وسطر إعادة التصدير الزائد؛ إصلاح برميلي (craft-order/owner-entitlement)؛ **اختبار قفل سطح الدومين العام الجديد (13 اختبارًا) يثبّت كل عقد البراميل وقت التشغيل والترجمة ويمنع انجراف السطح مستقبلًا**؛ THIRD_PARTY_NOTICES +3 اعتماديات شاغلة؛ lint يشمل lib/؛ ARCHITECTURE.md أرقام مصححة |

### ج) التحقق النهائي للمجموعة ٦ (2026-09-02)

| الفحص | النتيجة |
|---|---|
| `pnpm typecheck` + `pnpm prototype:check` | نظيفان |
| `pnpm lint` | 0 أخطاء / 37 تحذيرًا (السقف المعتمد — لا تحذيرات جديدة غير مبررة) |
| `pnpm format:check` | نظيف |
| `pnpm text-density` | كل الأسطح ضمن §10 (OrderDetail 141، Catalog 86، DirectSaleEditor 63 — بأسباب مالك موثقة) |
| `pnpm design-guards` + stylelint | نظيفان |
| `pnpm test` (مجال) | 19 ملفًا / **226 اختبارًا** (+13 قفل السطح) |
| `pnpm prototype:test` | 88 ملفًا / **571 اختبارًا** (+6 حدود التاريخ) |
| `pnpm prototype:build` + PWA | ناجح؛ 80 مدخلًا مُسبق التخزين؛ index 102.3KB gz |
| فحص حي 390×844 RTL (بناء إنتاجي، بيانات عربية عبر الواجهة) | **الرحلات الإحدى عشرة كاملة** (العشر القائمة + رحلة التراجع المزدوج الجديدة): بيع نقدي 25؛ آجل 40 + قبض 10/30 والدين صفر؛ مصروف 12.50؛ عدّ (نقص صادق −14.50) + تغطية توزيع؛ تراجع تخصيص محافظ للإجمالي؛ تراجع تسوية عكس دقيق +14.50؛ طبقة تصحيحات بوصلة عميقة؛ طلب كامل (هند 60: مسودة→تكلفة→اتفاق 10/09/2026→تنفيذ→تسليم→قبض→مغلق ثم تراجع مزدوج)؛ مورد 50/20/10/تراجع 10 (ذمة 30→20→30، «مرتدة موثقًا»)؛ كتالوج + 404 صادق؛ إعادة تحميل دون اتصال؛ **صفر أخطاء كونسول/صفحة** |
| مطابقة رقمية حية | كاش 32.50 = 25+10+30−12.50−14.50+14.50+60−60−20؛ **الكشف +32.50 يطابق المركز حرفيًا**؛ مديونية 60.00 (عادت بالتراجع المزدوج)؛ ذمة مورد 30.00؛ ربح الطلب 60 مرة واحدة؛ مال المالك 0.00 |
| تدقيق نصي رقمي/تاريخ حي | 17 مسارًا: 0 أرقام هندية، 0 أسماء شهور، 0 YYYY/MM/DD؛ ISO الوحيد الظاهر (نطاق الكشف) أُصلح إلى 30/08/2026→05/09/2026 |
| فحص بصري (VLM + DOM) | FAB بني دائري 56px بالنقرة الخمسة؛ شبكة 2×2 للمركز؛ لا قصّ/تداخل؛ بيت/أدوات/نماذج بمساحات آمنة |
| أسرار/اعتماديات | صفر أسرار في الفرق النهائي (فحص آلي)؛ الرموز خارج المستودع |

### د) ما لم يُنفذ من أصل الطبقة ٢ الاختيارية (موثق لا مسكوت عنه)

| البند | السبب |
|---|---|
| الطبقة ٢ لتقليص السطح (+130–135 كلمة export) | قرار مدقق فرعي: الطبقة ١ المحافظة هي الموصى بها؛ الطبقة ٢ «ممكنة لكن دون قيمة تشغيلية مثبتة» وتضيف تعديلات واسعة بلا دفع تشغيلي؛ سُلمت بأدلة لكل رمز واختبار القفل يمنع الانجراف مستقبلًا. إعادة التصدير لاحقًا سطر واحد إن لزم |
| قياس أداء قبل/بعد على جهاز حقيقي | قياسات البنية (فتحات IDB، أحجام الحزم) موثقة أعلاه؛ قياس إطار متصفح/جهاز حقيقي خارج بيئة الفحص — خطة Pilot في todo.md |

### هـ) قرار «السقف البني» المحفوظ

القشرة المعتمدة الواحدة: ترويسة (علامة+إعدادات+مظهر — ليست تنقلًا) + شريط سفلي واحد بخمسة مقاعد (مشروعي الآن | العمل | سجّل-FAB | مالي | أدواتي) — الزر البني الدائري «سجّل» في المقعد الأوسط ضمن الشريط نفسه. تحقق حي: headerCount=1, bottomNavCount=1, fabCount=1، لا شريط منافس ثانٍ في أي مسار.

## المجموعة ٤ — التمويل العميق: الأصول والقروض والعربون المحتفظ وأتمتة المخزون (2026-09-04)

**العقد الحاكم:** `docs/contracts/29-group4-deep-finance-contract.md` ·
**البرنامج:** نقل قدرات Zman إلى عقود Micro (المجموعة ٤ من ٦).

### أ) المنفّذ والمتحقق منه

- دومينان جديدان: `src/domain/asset` (عقد الأصل + جدول الإهلاك خط المستقيم
  بتقريب أرضي وجمع الشهر الأخير + التخلص/الشطب/مراجعة العقد) و `src/domain/loan`
  (عقد القرض + الدفعات بتراجع موثق + تصحيح بحد «لا ينزل دون المسدَّد»).
- ٩ أنواع أحداث مالية جديدة و٣ أعمدة دلتا اختيارية (أصول/قروض/إيراد عربون)
  وسياقات مرتبطة إلزامية — قاعدة الدلتا الثمانية مع تراجع كامل.
- مخزنان جديدان (`assets`/`loans`) ومخطط ٣٤/نسخة ٢٦ مع قبول ٢٥/٣٣ وقوائم
  فارغة بلا اختراع؛ ٦ معاملات ذرّية جديدة في IndexedDB والذاكرة.
- القارئ الكنوني: ٤ بنود فترة مستقلة + ٣ طبقات مركز مستقلة.
- تصنيف العربون المحتفظ (مال مالك / إيراد / معلق) بقرار + تصحيح موثق، وعلم
  `autoConsumeOnDelivery` على قوالب المنتج يكمّل أتمتة الخصم عند التسليم ضمن
  معاملة D4 الذرّية نفسها (بلا أي خصم عند الفتح/الحفظ).
- فحوص MIC-10..13 (أصول/قروض/عربون/ربط استهلاك التسليم) — قراءة فقط.
- أسطح: `/assets` (+جديد/تفصيل)، `/loans` (+جديد/تفصيل)، ورقة سداد سفلية،
  طبقتا «مالي»، قرار العربون من صفحة الطلب.

### ب) المؤجل صراحة (موثق في العقد ٢٩ §٥)

- توزيع محفظة القرض: وسم معلوماتي فقط؛ التوزيع يبقى مسار توزيع الكاش.
- رد العربون بلا حدث كاش خارج (حد المجموعة ٣ D6 كما هو).
- لا إهلاك بأثر رجعي تلقائي — التسجيل بتاريخ يختاره المالك والتصحيح عكس موثق.

### ج) التحقق

- الدومين ٢٧٤/٢٧٤ (منها ٢٧ جديدًا) · النموذج ٧٠٦/٧٠٦ (منها ٤٦ جديدًا) ·
  البوابات: typecheck×2، lint 0/37، format، density (قياس يوم أول)،
  design-guards، build PWA.

## المجموعة ٣ — برنامج النقل: التكلفة والتسليم والبيع المباشر (PR #151، 2026-09-04)

- الفرع: `agent/group3-cost-product-order` (مدمج ومحفوظ في الأصل للتوثيق التاريخي).
- وسم البناء: `b6a100f` — مراجعة التسليم قبل الالتزام بمعاملة ذرّية واحدة، عكس
  التسليم تصحيحًا موثقًا (إيراد يُحيَّد، مرايا حركات، كاش لا يُمس)، هوية المادة
  في بنود التكلفة والقالب وربط الحركة بالبيع، مخطط ٣٣/تصدير ٢٥ بقبول إرث
  ٢٤/٣٢ و٢٣/٣١. أرقام لحظة الدمج: domain 246/246، prototype 660/660.

## المجموعة ٤ — برنامج النقل: التمويل العميق (PR #152، 2026-09-04)

- الفرع: `agent/group4-deep-finance` (مدمج ومحفوظ). وسم البناء: `f20f513`.
- دومينا الأصول والقروض، ٩ أنواع أحداث و٣ أعمدة دلتا بسياقات إلزامية، تصنيف
  العربون المحتفظ، أتمتة خصم التسليم بعلم قالب صريح، مخطط ٣٤/نسخة ٢٦،
  MIC-10..13، أسطح `/assets` و`/loans`. ٢٧+٧٤ اختبارًا جديدًا.
- الإرث المستمر: انقطاعات npm audit أثناء الدمج (ERR_SOCKET_TIMEOUT) عولجت
  بإعادة تشغيل شفافة — انظر سياسة المحاولة المحدودة في CI بعد تدقيق المجموعة ٦.

## المجموعة ٥ — برنامج النقل: الظهور والاستمرارية (PR #153، 2026-09-05)

- الفرع: `agent/group5-visibility-continuity` (مدمج ومحفوظ). وسم البناء: `4445da0`
  (٨٣ ملفًا / +5802 −151) بعد أربع جولات استرداد مذكورة بسلسلة الفرع.
- قارئ النشاط الموحد، كشف الفترة كتقرير محلي، MIC-14..16، سجل التصحيحات،
  مسودات النماذج، القفل المحلي (عقد ٣٧)، PWA dirty-safe، المشاركة اليدوية،
  مظروف النسخ v27 (عقد ٣٩).

## المجموعة ٦ (تدقيق أ) — التدقيق المتوازي لسلامة النظام (فرع معزول، 2026-09-05)

- الفرع: `agent/group-6-micro-system-audit` — **غير مدمج**؛ خط الأساس
  `main` @ `ad9fc13`؛ لقطة استرداد محلية غير دافعة
  `recovery/group-6-audit-baseline-2026-09-05` + حزمة bundle كاملة خارج المستودع.
- المدققون الخمسة (قراءة فقط): الحقيقة المالية، البيانات/الاستمرارية، UX/RTL،
  الأمن/الأداء، الإصدار/العدائي. صفر P0. أُصلحت على الفرع: FT-01/FT-02/FT-03،
  DP-01/DP-09، SP-01..06، AR-01/AR-03 (آليات CI)، AR-02 (هذا التسديد).
- الاختبارات الجديدة: قفل محلي ٩ + بوابة إعدادات ٤ + إعادة التسليم ٣ + عكس
  تخصيص الإلغاء ٣ + حارس عائلات الأحداث ٢ + مظروف ٢.
- التقرير الكامل: مستودع Documents — مجلد الوكيل ١ (انظر current-state §28).
