# Micro — Implementation Traceability

**الحالة:** `CURRENT / UPDATE WITH EVERY MERGED GROUP`
**آخر تحديث:** 2026-09-02 — المجموعة ٢ (الحقيقة المالية وطبقة التصحيح)

> سجل قابل للتتبع: ما نُفّذ فعلًا، وما أُجّل ولماذا، وما الاختبارات التي تثبته.
> لا يعلن بند مكتملاً إلا إذا كان منفذًا ومتحققًا منه. مرجع الحالة الحية التشغيلية
> يبقى `docs/operations/current-state.md`.

## المجموعة ١ — أساس التجربة (2026-09-02)

| الحقل | القيمة |
|---|---|
| نقطة الانطلاق | `main` @ `1b37c77c72f719a4b51be3aa5e243063c997df2e` |
| الفرع | `agent/group1-experience-foundation` |
| commit التنفيذ | `aa78757e37d0c86b942b2d4f363e4b9908019e9b` (PR #142) |
| رأس `main` بعد الدمج | `ead3545dc5cdd5d92658e559a1b89d5e083f4c5c` — CI ناجح |
| المخطط المحلي | 30 (كان 29) — مخزن `owner-profile` جديد |
| نسخة التصدير | 22 (كان 21) — تُقبل 21/29 وأقدم وتُهاجر بـ ownerProfile=null |
| عقد التنقل الجديد | `docs/contracts/26-navigation-referrer-and-deep-link-contract.md` |

### أ) المنفّذ والمتحقق منه

| البند | الحالة | الدليل |
|---|---|---|
| عقد تنقل موحّد: مصدر/رجوع/وصلات عميقة دفاعية | منفّذ | `app/navigationContract.ts` + 14 اختبارًا + عقد 26 |
| `?from` على كل مسار متعدد السياقات (رئيسية/عمل/مالي/أدواتي/محافظ/موردين/أطراف/مواد/جدول/أساس) | منفّذ | ~35 رابطًا معدّلًا؛ اختبارات Orders/U004/Foundation محدّثة |
| رجوع المحررات للمصدر مع بدائل قانونية | منفّذ | 20 محررًا عبر `useReturnPath()` |
| حماية مدخل ورقة الإضافة (سؤال خيارين هادئ) | منفّذ | `QuickActionSheet.guard.test.tsx` (٥ اختبارات) |
| الرئيسية: كتلة «الأهم الآن» + أفعال محددة + مؤهل الأمانات + بطاقة غير الموزع + «غير محدد بعد» + سطر المكان + حالة فراغ صادقة + مدخل ملف المالك | منفّذ | اختبارات الخدمة (٥ جديدة) + عرض `pages/Home.tsx` |
| العمل: «الأولوية الآن» دائمًا + صفوف بيع مختصرة + CTA بيع مباشر + «سجّل أول بيع» | منفّذ | `Orders.ui.test.tsx` (٨ اختبارات) |
| كشف الوقت/المادة الفعليين عند التنفيذ مع وصلة تحفظ سياق الطلب | منفّذ | `pages/OrderDetail.tsx` execution layer + `?order=` في محرر الاستهلاك |
| السعة والتكرار حيث يُقرأ الضغط (`?focus=capacity` + «حدّد سعة اليوم») | منفّذ | `pages/Schedule.tsx` |
| إدخال الموقف الافتتاحي الموجه من صفحة الأساس (`?focus=guided-import`) | منفّذ | `pages/Settings.tsx` + `pages/Foundation.tsx` |
| الإعداد المتدرج: مسودة أثناء الكتابة + استعادة بإشعار + ٣ حالات افتتاحية صادقة | منفّذ (تحقق اختباري جديد) | `Setup.ui.test.tsx` (٤ اختبارات جديدة) |
| ملف المالك: مخزن محلي مستقل + خدمة + شاشة `/profile` + ضمان الهوية عند الإقلاع | منفّذ | `ownerProfileService.test.ts` (٦) + `Profile.ui.test.tsx` (٩) + دورة تصدير/استيراد (٣) |
| التخزين/التصدير: مخطط ٣٠ / نسخة ٢٢ مع هجرة آمنة لكل الموجات السابقة | منفّذ | اختبارات transfers (٢٧) خضراء |
| حدود كثافة النص محدّثة بقرار موثّق (ملصقات مُكَلَّفة من نطاق معتمد) | منفّذ | `scripts/text-density-count.py` CAPS |

### ب) المؤجل صراحة (مسؤولية المجموعات اللاحقة)

| البند | المجموعة المسؤولة | السبب |
|---|---|---|
| CollectionSheet/مستلم الديون، WalletLedger، StatementView | المجموعة ٢ (المالية) | خارج نطاق المجموعة ١ (§13) |
| تصحيح المشتريات/سعر الطلب بعد الاتفاق، CorrectionPreview الموحد | المجموعة ٢ | نفسه |
| إعادة تصميم الحاسبة الكاملة وشاشة تعديل التقدير | المجموعة ٣ (المنتجات/الطلبات) | نفسه |
| مسار الكتالوج→البيع الكامل | المجموعة ٣ | نفسه؛ العقد (§4) يكفي |
| ربط استهلاك المادة بسياق الطلب في الطبقة المالية (استهلاك موثق بمصدر) | المجموعة ٢ | المجموعة ١ سلّمت الوصلة والتعبئة فقط |
| Google Auth / مزامنة / تعدد أجهزة / استرداد حساب | خارج المجموعات (قرار مرحلة) | النموذج محلي أولًا؛ الحقول المحجوزة null |
| `?mode=cover` و`?to=` تفعيل فعلي | المجموعات اللاحقة | معرفة في العقد ومحجوزة |
| السوق/التوصيل | توسعة مستقبلية | قرار E-00 مستقل |

### ج) بطاقات التتبع الخارجية (MC/D)

معرّفات MC-10…MC-24 وD1…D14 واردة من مرجع إعادة التصميم الخارجي وليست في المستودع؛
لا يمكن التحقق من نصوصها الحرفية من الكود. خُطّطت بالتطابق مع نطاق المجموعة ١ المعتمد:

| المعرف (كما ورد في أمر التنفيذ) | أقرب بند منفّذ في هذه المجموعة | الحالة |
|---|---|---|
| MC-10 (منطق المصدر/الرجوع) | عقد ٢٦ §2 + `?from` عبر التطبيق | منفّذ |
| MC-12 (وصلات عميقة دفاعية) | `parseDeepLink` + اختباراته | منفّذ |
| MC-14 (حماية المدخل العابر) | سؤال الخيارين في ورقة الإضافة | منفّذ |
| MC-15 (تغذية راجعة موحدة) | عقد ٢٦ §6 + الأنماط القائمة | منفّذ كعقد + أنماط |
| MC-16 (هرمية الرئيسية) | §7.1 كاملة | منفّذ |
| MC-18 (هرمية العمل) | §8.1/§8.2 | منفّذ |
| MC-19 (كشف القدرات المخفية) | Scope E (تنفيذ/سعة/استيراد موجه) | منفّذ |
| MC-20 (التدرج حتى أول فعل) | Scope F + اختباراته | منفّذ |
| MC-22 (ملف المالك) | Scope G كاملًا | منفّذ |
| MC-24 (معايير RTL/سلوك) | مطبّقة عبر الشاشات المتغيرة + فحص حي | منفّذ |
| D1/D6/D12/D13/D14 | تفسّر ضمن البنود أعلاه حسب سياقها (تنقل/نماذج/حالات) | مغطاة بالتطابق لا بالنص الحرفي |
| إضافة ملف المالك المعتمدة | Scope G | منفّذ |

### د) التحقق

| الفحص | النتيجة (2026-09-02) |
|---|---|
| `pnpm typecheck` (جذر + نموذج أولي) | نظيف |
| `pnpm lint` | 0 أخطاء / 37 تحذيرًا (السقف المعتمد) |
| `pnpm text-density` | كل الأسطح ضمن الحدود (CAPS موثّقة) |
| `pnpm design-guards` | نظيف (tokens + stylelint) |
| `pnpm test` (مجال) | 15 ملفًا / 182 اختبارًا خضراء |
| `pnpm prototype:test` | 79 ملفًا / 489 اختبارًا خضراء (منها ٧٧ جديدة للمجموعة ١) |
| `pnpm prototype:build` | بناء إنتاجي ناجح |
| فحص حي (متصفح، ٣٩٠×٨٤٤، RTL) | الرئيسية/العمل/الملف/الوصلات العميقة/النماذج الوسخة/إعادة تحميل دون اتصال — بلا أخطاء كونسول |
| CI (GitHub Actions) على رأس الدمج | ناجح (`ead3545`) |

---

## المجموعة ٢ — الحقيقة المالية وطبقة التصحيح (2026-09-02)

> استئناف بعد انقطاع: عُثر على العمل الجزئي غير المُلزَم في شجرة العمل المحلية (45 ملفًا:
> المجال + الخدمات + الواجهات)، صُوِّر أمانًا على `agent/group2-financial-truth` قبل المتابعة،
> وأُكمل من أول بند ناقص حتى الدمج. أُصلح في الاستئناف: 3 اختبارات متقطعة (API الكشف/دفتر
> المحفظة)، سقف lint أُعيد إلى 37 تحذيرًا بتحسينات هندسية صادقة (لا رفع سقف)، وأصلحَ عيبان
> حقيقيان كشفهما الفحص الحي: طمس شاشة نجاح التحصيل عند تجدد البيانات، ومضاعفة رقم الأثر
> في معاينة التصحيح.

| الحقل | القيمة |
|---|---|
| نقطة الانطلاق | `main` @ `b529eb459e7c83ce763d935fa9b1a7a6362071ea` (نهاية المجموعة ١ — تم التحقق من الرأس الفعلي) |
| الفرع | `agent/group2-financial-truth` |
| المخطط المحلي / التصدير | 30 / 22 — لم يتغيرا: المجموعة ٢ قراءة وتصحيح فوق السجلات القائمة، لا مخازن جديدة |
| العقد | عقد ٢٦ مُحدَّث (مسارات المجموعة ٢: `/collect`، `/finance/statement`، `/cash/wallet/:id`، ومعجم `?source/?view/?entry/?mode=cover`) |
| حالة «سجّل» | الاستئناف وجد 45 ملفًا غير مُلزَمة (M/جديد) على `main` المحلي؛ لا فروع بعيدة ولا PR ولا stash |

### أ) المنفّذ والمتحقق منه

| البند | الحالة | الدليل |
|---|---|---|
| بيع سريع: الدرج وجهة افتراضية معلنة + غير الموزع خيار صريح + معاينة الأثر + وصل «افتح السجل» + دين آجل موثق | منفّذ | `QuickActionSheet.tsx` + فحص حي (وصل صادق بالمبلغ والدين) |
| مصروف سريع: وجهة صرف صريحة (غير الموزع افتراضيًا بلا اختيار نيابة عن الصرف، المحفظة تغطي من رصيدها) + وصل المصدر | منفّذ | `QuickActionSheet.tsx` + `attributeToWallet(..., sourceRef)` |
| ورقة التحصيل: منتقي ذمم آمن + تعبئة المتبقي قابلة للتعديل + منع التجاوز برسالة تعرض المتبقي والمطلوب + جزئي/كامل + وجهة صريحة | منفّذ | `pages/Collect.tsx` + `collectionService.test.ts` (8) + `G2.dom.test.tsx` (3) |
| تحصيل واحد موثق عبر المسار المملوك (لا مصدر كتابة ثانٍ): Fulfillment للطلبات وDirectSaleService للبيع + تخصيص الكاش بـ`distributeUnallocated` بمعرّف عملية | منفّذ | `collectionService.ts` (§6) |
| مداخل التحصيل الثلاثة: بند الرئيسية (طلبات + ديون بيع آجل لا تختفي)، صف الطرف في دفتر الناس، وورقة الإضافة — كلٌّ يحفظ مصدره ويعود إليه | منفّذ | `homeControlCenterService.ts` + `Parties.tsx` + `MicroAppShell.tsx` + فحص حي |
| مالك عمليات الكاش الفيزيائي `/cash`: المجموع، محافظ، غير الموزع، الأمانات مؤهلًا، عدّ الصندوق، التوزيع، حالة التسوية، وصل الأثر | منفّذ | `CashWallets.tsx` (موجودة + مدخل دفتر المحفظة) |
| عدّ الدرج: المعدود يُقارن بالمسجل محايدًا، الفرق تسوية موثقة بتاريخ اليوم، لا رقم قديم يتغير | منفّذ | فحص حي (فرق −2.00 سُجل تسوية) |
| توزيع الكاش و`?mode=cover`: الغطاء من محفظة يفتح من بطاقة «في دفعة تحتاج تغطية» السالبة بشرح مسطح | منفّذ | `Finance.tsx` (CashDecisionSurface) + `CashDistribution.tsx` |
| مالي منقسم: «الوضع الآن» / «شو صار خلال الفترة» (`?view=`) بقرار أول واضح وكثافة متدرجة | منفّذ | `Finance.tsx` + U05/U07 محدّثة |
| كشف الفترة: نطاق معلن، كاش داخل/خارج مفصولان، النتيجة، الأمانات، الذمم، مال المالك، أسطر حقيقة، التصحيحات مرة واحدة في كتلتها | منفّذ | `statementService.ts` + `statementService.test.ts` (5) + `G2.dom.test.tsx` (2) |
| دفتر المحفظة: الرصيد + الحركات بالتسلسل + تمييز الأنواع + وصل كل تخصيص بمصدره + رجوع محفوظ + «رصيد المحفظة لا ربحها» | منفّذ | `walletLedgerService.ts` + `pages/WalletLedger.tsx` + اختبارات (3+2) |
| التاريخ الموحد: عائلات المشتريات/الدفعات وتعديل سعر الطلب والتراجع عن قبضة تنضم لأحداث/مبيعات/كاش بمصادرها وأسبابها | منفّذ | `correctionHistoryService.ts` (مجموعات purchases/orders) |
| CorrectionPreview الموحد: الأصل، الفعل، القيم قبل/بعد لكل بُعد (كاش/التزام/أمانات/مصروف/مال المالك)، ما لا يتغير، الرصيد الناتج، السبب الإلزامي، قابلية التراجع | منفّذ | `components/finance/CorrectionPreview.tsx` + تكامله في EventsLayer/OrderDetail/SupplierPurchaseEditor |
| تصحيح أحداث المالية: تعديل/حذف موثق/تراجع/استرجاع — الأصل باقٍ والتراجع لا يتكرر | منفّذ | `projectFinancialService.ts` (reverse/edit) + D005 محدّث |
| تراجع موثق عن قبضة طلب: الكاش يعود والمتبقي يُفتح والإيراد لا يمس، والتراكمي لا يتجاوز القبضة | منفّذ | `craft-order/policies.ts` `reverseOrderCollection` + `fulfillmentService.reverseCollection` + `OrderDetail.tsx` + اختبارات (5) |
| تعديل سعر الطلب بعد الاتفاق: لا إلغاء ولا إعادة إنشاء، الاتفاق القديم في الأحداث، العربون والقبضات كما هي، سبب إلزامي | منفّذ | `craft-order/policies.ts` `reviseAgreedPrice` + `fulfillmentService.revisePrice` + `OrderDetail.tsx` + اختبارات (7) |
| تصحيح شراء المورد: التكلفة والدفع الأولي بمراجعة تحفظ ما قبل التصحيح، والدفعات اللاحقة وتراجعاتها لا تُمس | منفّذ | `supplier-purchase/policies.ts` `updateSupplierPurchase` + `SupplierPurchaseEditor.tsx` + اختبارات (4) |
| تراجع موثق عن دفعة مورد: الدفعة الأصلية باقية والعلاقة صريحة، دفعة واحدة لا تُتراجع مرتين | منفّذ | `reverseSupplierPurchasePayment` + `SupplierPurchaseEditor.tsx` + اختبارات (3) |
| وصل مصدر التخصيص في سجل المحفظة (sale/expense/collection/order) | منفّذ | `cash-continuity/policies.ts` `normalizeSourceRef` + `cash-continuity-source-ref.test.ts` (4) |
| أمانات: ليست ربحًا ولا مالًا للمالك في الكاش/المالي/الكشف/دفتر المحفظة/الرئيسية؛ تسليم لا يتجاوز المحتجز؛ التصحيح بمعكوس الأثر | منفّذ | موجودة من الدورات السابقة (`amanah-loss.test.ts`) وثبتت في أسطح المجموعة ٢ (trustLine/qualifier) |
| مجهول الافتتاحي: يبقى «غير محدد بعد» لا 0.00 | منفّذ | `walletLedgerService` openingUnknown + فحص حي |
| عدم التكرار: مفاتيح idempotency في كل مسارات الكتابة الجديدة (تحصيل/تعديل/تراجع) + نجاح محلي مكتمل فقط | منفّذ | اختبارات التكرار (تحصيل/شراء/تراجع قبضة/دفعة) |

### ب) المؤجل صراحة

| البند | المجموعة المسؤولة | السبب |
|---|---|---|
| حاسبة التكلفة الكاملة، تعديل التقدير، جسر التقدير→مسودة | المجموعة ٣ | خارج نطاق المجموعة ٢ (§15) |
| اختيار منتج من الكتالوج أثناء البيع، Product-to-Sale الكامل | المجموعة ٣ | نفسه — عُرّف العقد الأدنى فقط |
| تنفيذ الطلب (غير تصحيح السعر) | المجموعة ٣ | نفسه |
| مزامنة/سحاب/OAuth/استرداد | خارج المجموعات | قرار مرحلة — النموذج محلي أولًا |
| أسعار/مقعد سادس | ممنوع | قشرة خمسية ثابتة |

### ج) بطاقات التتبع الخارجية

معرّفات MC/D المذكورة في أمر تنفيذ المجموعة ٢ غير موجودة كنصوص في المستودع (كما وثّقت
المجموعة ١)؛ خُطّطت بالتطابق مع بنود الأمر: MC-25/MC-26 (ورقة التحصيل/المداخل) → Scope B
أعلاه؛ MC-27/MC-28 (الوضع/الفترة) → §8؛ MC-29/MC-30 (دفتر المحفظة/الكشف) → §9؛ MC-31
(التاريخ الموحد)؛ MC-32/MC-33/MC-34/MC-35 (CorrectionPreview والتصحيحات وعائلاتها) →
§10-§12؛ MC-36 (أمانات)؛ D15-D19 (حمايات التحصيل/العدّ/التغطية) → ضمن اختبارات الخدمات
والفحص الحي.

### د) التحقق

| الفحص | النتيجة (2026-09-02) |
|---|---|
| `pnpm typecheck` + `pnpm prototype:check` | نظيفان |
| `pnpm lint` | 0 أخطاء / 37 تحذيرًا (السقف) — أُعيد إليه بتحسينات صادقة (استخراج مساعدين ومسارات اختبار أقصر) |
| `pnpm text-density` | ضمن الحدود (CAPS: Home 36، Finance 169، OrderDetail 157، CashWallets 70 بملاحظات قرار موثقة) |
| `pnpm design-guards` | نظيف |
| `pnpm test` (مجال) | 18 ملفًا / 205 اختبارًا (منها 23 جديدة للمجموعة ٢ + إصلاحات) |
| `pnpm prototype:test` | 83 ملفًا / 516 اختبارًا (منها 7 جديدة لأسطح المجموعة ٢ + تحديثات U05/U07/D005) |
| `pnpm prototype:build` + PWA | ناجح (62 مدخلًا مُسبق التخزين) |
| فحص حي (390×844، RTL) | الإعداد→بيع آجل→دين في الرئيسية→تحصيل جزئي→دفتر الناس→مالي وضع/فترة→كشف→محافظ→دفتر محفظة→عدّ صندوق→مصروف→تراجع موثق بمعاينة صادقة→سجل التصحيحات→رجوع محفوظ→إعادة تحميل — بلا أخطاء كونسول |
| عيوب وجدها الفحص وأُصلحت | طمس شاشة نجاح التحصيل (doneRef)؛ مضاعفة رقم الأثر في المعاينة (قبل/بعد صادقة) |
