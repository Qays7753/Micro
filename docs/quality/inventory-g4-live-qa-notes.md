# ملاحظات QA الحية — G4 مواد ومخزون

**الحالة:** `WORKING NOTES / لم تعتمد الشريحة بعد`
**التاريخ:** 23 أغسطس 2026

| الخطوة | النتيجة الحية | الدليل المعروض |
|---|---|---|
| فتح `/inventory` على قاعدة كانت schema 13 بلا مخازن مواد | فشلت القراءة أولًا؛ كشف الفحص أن القاعدة كانت version 13 بلا `materials` و`inventory-movements` | أضيف schema 14 واختبار ترحيل مخصص |
| إعادة فتح `/inventory` بعد schema 14 | ناجح | حالة فارغة واضحة وأفعال تعريف المادة/الاستلام/الاستهلاك/الهدر |
| مادة «خشب اختبار» بافتتاح `10.000` وقيمة `40.00` | ناجح | قائمة المواد: `10` و`40.00 د.أ` وحركة افتتاح واحدة |
| شراء G2 آجل من «مورد الاختبار» بقيمة `30.00` | ناجح | ذمة المورد `30.00 د.أ`، بلا ادعاء مصروف أو مخزون جاهز |
| استلام `5.000` بقيمة `30.00` مرتبط بشراء G2 | ناجح | قائمة المواد: `15` و`70.00 د.أ` وحركتا افتتاح/استلام |
| استهلاك `2.000` لطلب محلي مثبت | ناجح | المخزون: `13` و`60.67 د.أ`؛ سجل الاستهلاك `-2` و`-9.33 د.أ`، بلا تعديل Snapshot ظاهر |
| هدر `1.000` بسبب «كسر أثناء القص» ثم عكسه بسبب | ناجح | انخفض المخزون إلى `12` و`56.00` ثم عاد إلى `13` و`60.67`؛ الأصل والعكس ظاهران |
| الوضع المالي بعد السيناريو | ناجح | الكاش `0.00`، مال المالك `0.00`، نتيجة الفترة `0.00`، وذمة المورد `30.00`؛ شراء واحد مسجل |

لا تزال الحاجة قائمة لفحص Light/Dark والأحجام الثلاثة، وتدقيق تصدير/استيراد مرئي ثم تنظيف بيانات الاختبار قبل PR.
