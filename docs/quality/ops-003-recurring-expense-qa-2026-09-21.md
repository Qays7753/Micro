# سجل QA — OPS-003 (المصروفات المتكررة) · 2026-09-21 · رأس الفرع `4d72806`

## الحالة: PR_READY — QA الحي `NOT_EXECUTED`

لا متصفح حي ولا جهاز فعلي في بيئة التنفيذ. المسارات القابلة للأتمتة مغطاة باختبارات DOM (jsdom) في `client/src/RecurringExpenseSurfaces.dom.test.tsx` (١٧ اختبارًا) و`FinancialEventEditor.ui.test.tsx` (+١):

- **الفراغ الصادق** بلا اختراع سجلات + زر الإنشاء + ملاحظة القراءة فقط.
- **النص الطويل** (عنوان ٨٠ حرفًا) يُعرض كاملًا `dir="auto"`.
- **الخطأ المعزول** ببطاقة صادقة وزر إعادة محاولة حقيقي.
- **لوحة المفاتيح**: أزرار القرار `<button>` حقيقية قابلة للتركيز غير معطلة (`document.activeElement` مثبت).
- **الحالات الأربع الملزمة حرفيًا**: «تم تسجيل المصروف» / «المصروف مسجل مسبقًا لهذه الفترة» / «لم يُسجل المصروف» / «نتيجة التسجيل غير معروفة».
- **متأخر انتباه لا دين** + فتح التفصيل لا يكتب حدثًا (عدّاد الأحداث صفر).
- **تأكيد واحد** بمعاينة قبل الالتزام؛ **إعادة استخدام** بلا حدث ثانٍ.
- **مجهولة**: علامة حياد (`StatusChip state="unknown"`) وزر تحقق لا يزعم نجاحًا بلا نتيجة.
- **سؤال الشهر القصير** بجوابيه الموثقين؛ **إلغاء بسبب** يلغي المستقبلية فقط؛ **معكوس** عرض مشتق مع رابط سجل الأحداث.
- **المصروف اليدوي المتزامن يحذّر لا يحجب** (تحذير `role="status"` والحفظ يمر).

## المطلوب عند المراجعة أو قبل الـPilot (على جهاز/متصفح حي)

1. عرض ٣٢٠/٣٦٠/٣٩٠/٤٣٠ عمودي × الوضعين: صفر تمرير أفقي، وزر التأكيد الأساسي غير محجوب بلوحة مفاتيح أو تكبير نص.
2. رحلة كاملة يدوية: مسودة→تفعيل→انتظار استحقاق→مراجعة/معاينة/تأكيد→«تم تسجيل المصروف»→تراجع من سجل الأحداث→عرض «معكوس».
3. إيقاف مرور شهر→استئناف (لا استكمال بأثر رجعي)؛ تخطٍ بتوثيق؛ إلغاء بسبب.
