# عقد ٤٠ — خريطة الملكية التقنية وحدود الوحدات

**الحالة:** `CURRENT / BINDING` — اعتُمدت مع الموجة الثالثة (العقود والملكية التقنية، EXE-017 / TOOL-002 + CAT-001) بقرار المالك المسبق في تكليف الموجة.
**المرجع التنفيذي:** `apps/prototype-web/client/src/app/ownershipBoundaries.exe017.test.ts` (الحارس المعماري الدائم) + هذا الملف (الخريطة المعتمدة الواحدة).
**قاعدة النطاق:** هذه الخريطة تثبت الملكية التقنية للخدمات والكتابة والسياسات، ولا تنقل أي سطح مرئي ولا تغير أي اسم ظاهر — إعادة التموضع المرئي مؤجلة بالكامل للموجة الرابعة.

## ١. سؤال القرار

من يملك كل خدمة وكتابة وسياسة عندما تتقاطع الوحدات؟ الكتالوج يحتاج قراءة الهامش، وفحص السلامة يظهر في أدواتي، والنسخ الاحتياطي خلف الإعدادات — فمن «المالك التقني» الذي يقرر ويُسأل عند الخطأ، ومن «السطح المؤقت» الذي يعرض فقط؟

## ٢. جدول الملكية المعتمد (القرار الملزم)

| الوحدة | المالك التقني | الخدمات المالكة | يقرأ | يكتب | الأثر المالي | مشاركة التصدير/الاستعادة |
|---|---|---|---|---|---|---|
| فحص سلامة الحسابات | **المالية** | `application/finance/integrityCheckService.ts` (MIC-1..16، قراءة فقط) | السجل المالي كاملًا: الأحداث والطلبات والمحافظ والمخزون والمشتريات والأصول والقروض | لا شيء — وعد معماري «يقرأ أرقامك ولا يغيّر شيئًا» يحرسه اختبار لقطة قبل/بعد | تشخيصي فقط: يكشف الانحراف ولا يصححه | لا يخزن شيئًا بنفسه |
| النسخ الاحتياطي والاستعادة | **الإعدادات/البيانات** | `application/transfers/localTransferService.ts` (تصدير/استيراد/تصفير) و`application/transfers/guidedOpeningImportService.ts` (الإدخال الافتتاحي الموجه) | اللقطة كاملة (`readSnapshot`) | `replaceSnapshot` (أقوى كاتب في التطبيق — استبدال اللقطة كلها) | استبدال كامل للسجل بعد بوابة تحقق ونسخة احتياطية مُتحقّقة (EXE-014) | يملكها: هو منشئ المظروف ومستهلكه |
| سياسات الربح والتوزيع (التحميل) | **المالية** | `application/finance/recurringWorkService.ts` — `createPolicy` / `createPolicySuccessor` / `deactivatePolicy` تكتب `saveAllocationPolicy` / `commitAllocationPolicySuccessor` | الكتالوج والطلبات وحركات المخزون وسجل الوقت الفعلي لاشتقاق `readRecurringWork` (قراءة الهامش G4-B) | مخزن `allocationPolicies` (عائلة لقطة مصدَّرة) | سياسة توزيع مشترك على الطلبات — قرار مالي بلا حدث نقدي لحظة الحفظ | `allocationPolicies` داخل المظروف |
| سياسات استحقاق المالك | **المالية** | `application/finance/ownerEntitlementService.ts` | سياسات وسجلات وحركات المالك | `saveOwnerEntitlementPolicy` / `commitOwnerEntitlementPolicySuccessor` / `commitOwnerMovement` | مباشر: مال المالك وتوزيعاته | عائلات المالك داخل المظروف |
| الكتالوج | **مستهلك للنتائج/رابط** — لا يملك سياسة مالية | `application/catalog/catalogService.ts` (عناصر/وحدات/تحويلات/قوالب — بيانات مرجعية) | بيانات مرجعية + `recurringWork.readRecurringWork` (View Model من الخدمة المالية) | مخازن الكتالوج المرجعية فقط (`saveCatalogItem` وأخواتها) | لا حدث نقدي ولا سياسة — اقتراحات أسعار مرجعية (P-002) | عائلات الكتالوج داخل المظروف |
| السوق | وحدة مستقلة **مستقبلية** | لا شيء (إعلان «قريبًا» صادق) | لا شيء | لا شيء — ممنوع كتابة مالية في حالة «قريبًا» | صفر | لا يشارك |
| النقل والتوصيل | وحدة مستقلة **مستقبلية** | لا شيء (مدخل ترويسة «قريبًا») | لا شيء | لا شيء | صفر | لا يشارك |
| أدواتي — الحاسبة والتقديرات | **أدوات مستقلة** | `application/cost-estimates/` (تقديرات التكلفة) | مخزن التقديرات + مقترحات مواد المخزون (قراءة تشغيلية مرجعية) | `saveCostEstimate` / `deleteCostEstimate` (مخزن الأداة المستقل وحده) | صفر — «تقديري دومًا» ولا حدث مالي ولا حركة مخزون | `costEstimates` داخل المظروف |

## ٣. عقد أهلية «أدواتي»

الأداة المستقلة في «أدواتي» تلتزم بالحدود الثلاثة، ويحرسها الاختبار المعماري:

1. **لا تقرأ السجل المالي الرسمي** (الأحداث المالية، الذمم، المحافظ، مشتريات الموردين، مال المالك) إلا عبر مسار توافق موثق (§٤).
2. **لا تكتب فيه إطلاقًا** — كاتبها الوحيد المسموح مخزن التقديرات المستقل (`costEstimates`).
3. **لا تغير نتيجة أو رصيدًا أو ذمة أو سياسة** — الحاسبة والتقديرات والفحص كلها قراءة صرفة.

أي إضافة قارئ سجل مالي جديد من صفحات أدواتي قرار موثق يضاف لقائمة التوافق في الاختبار المعماري صراحةً — لا إضافة صامتة.

## ٤. أسطح التوافق المؤقتة (Compatibility Surfaces)

قرار المالك المعتمد: السطح القديم يبقى في مكانه المرئي حتى موجته، لكنه **لا يملك الخدمة ولا السياسة**، ولا تُنشأ له نسخة ثانية موازية. الطريق القانوني الوحيد لكل منها هو سياق الخدمات (`usePrototypeServices`) فوق الخدمة المالكة:

| السطح المؤقت | مكانه | المالك التقني الفعلي | الطريق القانوني | ما يحرسه الاختبار |
|---|---|---|---|---|
| فحص سلامة الحسابات | صفحة `/tools/integrity` (`pages/ToolsIntegrity.tsx` — مسار التوافق بقرار F05) | المالية (`application/finance/integrityCheckService.ts`) | سياق الخدمات؛ الخدمة مقيمة في بيت المالية | وجود الخدمة في `application/finance/`؛ أدواتي بلا كتابة |

**سطح أُغلق في Wave 4.2 (P-4.2-5 — قرار F02/T3):** قسم سياسات التوزيع في الكتالوج
(`components/catalog/CatalogPoliciesSection.tsx` كان يستدعي `recurringWork.createPolicy*`
/`deactivatePolicy`). **السطح المرئي انتقل إلى المالية** داخل «ملخص الفترة» بجوار
«التغطية والتعادل» (`components/finance/FinancePoliciesSection.tsx`) — نقلًا لا نسخًا.
الكتالوج صار **بلا أي كتابة سياسة**: يعرض القراءات ونتيجة السياسة ورابطًا سياقيًا
واحدًا إلى السطح المالي. ملكية المنتجات لهذه السياسات مغلقة نهائيًا — الكاتب
القانوني كما هو `recurringWorkService` في `application/finance/` عبر سياق الخدمات،
ولا نسخة ثانية موازية (§4 الأصل) ولا Writer جديد.

**أسطح أُغلقت في Wave 4.2 (P-4.2-2 — قرارات F03/T4/T5/T6):**

| السطح المغلق | ما صار بدله |
|---|---|
| شارات حالة الوحدات في `pages/Tools.tsx` (قراءات `partyLedger.read()`/`supplierPurchases.readSummary()`) | **أُزيلت بالكامل** — حالة كل وحدة تُقرأ من بيتها (الموردون `/suppliers`؛ دفتر الناس `/parties`)؛ أدواتي أدوات مستقلة فقط، والإعدادات بيت التهيئة والقدرات الوحيد (F03). قائمة التوافق في الحارس المعماري تقلصت بالإزالة لا بالاستبدال (بند §٦-٤ تحقق) |
| بطاقة «النسخ الاحتياطي والبيانات» في `pages/Tools.tsx` (رابط إلى `/settings`) | **أُزيلت** — البيت الحصري قسم «البيانات والنسخ الاحتياطي» في الإعدادات (P-4.2-3/T2)؛ لا مدخل ثانٍ في أدواتي |

## ٥. مصفوفة القراءة والكتابة للوحدات المفحوصة (قبل الموجة الثالثة ← بعدها)

| الوحدة | قبل | بعده |
|---|---|---|
| الكتالوج | يملك سطح الكتابة الوحيد لسياسات التوزيع (المنتج الوحيد للاستدعاء) والخدمة في `application/recurring-work/` بلا مالك معلن | الخدمة مقيمة في `application/finance/` بترويسة ملكية؛ الكتالوج مستهلك View Model عبر السياق؛ الاستيراد القيمي من كتالوج محظور معماريًا |
| أدواتي | يقرأ السجل (أطراف/مشتريات/فحص) بلا عقد أهلية ولا حارس | عقد أهلية §٣ + حارس معماري: لا كتابة إطلاقًا، والقراءة محصورة في قائمة تواصل موثقة |
| السوق/النقل | «قريبًا» بلا فحص دوري | حارس معماري يفشل عند أي خدمة أو كتابة تظهر في سطحيهما |
| النسخ والاستعادة | ملكية صحيحة واقعًا بلا حارس | حارس: خدمات النقل محصورة في وحدة الإعدادات وجذر التركيب |
| فحص السلامة | الخدمة في المالية والسطح في أدواتي بلا توثيق ملكية | الملكية موثقة هنا + حارس الإقامة في `application/finance/` |

## ٦. Backlog الموجة الرابعة (تنفذ في موجاتها)

1. **الأسطح التي ستنتقل:** قسم سياسات التوزيع من الكتالوج إلى سطح مالي (**نُفذ — P-4.2-5: `FinancePoliciesSection` في «ملخص الفترة»**)؛ صفحة فحص السلامة: مدخلها المرئي إلى المالية ← المزيد (**Wave 4.2 — P-4.2-4**) والمسار `/tools/integrity` يبقى للتوافق (F05)؛ شارات حالة الوحدات المالية من صفحة أدواتي (**نُفذ — P-4.2-2**).
2. **الأسماء التي ستتغير:** تُقرر في الموجة الرابعة — لا اسم ظاهر تغير في الموجة الثالثة.
3. **المداخل المرئية التي ستلغى أو تتحول لروابط سياقية:** بطاقة «النسخ الاحتياطي والبيانات» في أدواتي (**أُلغيت — P-4.2-2**)؛ بطاقات الوحدات المالية في أدواتي (**أُلغيت — P-4.2-2**).
4. **قارئ حالة وحدات بلا سجل:** استبدال قراءات `partyLedger`/`supplierPurchases` في أدواتي بقارئ حالة لا يمس السجل المالي الرسمي — **حُسم بقرار المالك F03 (الخيار أ): إزالة بلا قارئ بديل**؛ قائمة التوافق انكمشت بالإزالة (عقد 40 §٦-٤ يتحقق بالإزالة لا بالاستبدال).

## ٧. ما لم يتغير

- لا بطاقة ولا زر ولا صفحة انتقلت بين الأقسام؛ ولا اسم ظاهر تغير؛ ولا الشريط الخماسي أو ترتيبه مس.
- لا قاعدة مالية ولا معادلة ولا Writer مالي تغير — `localSchemaVersion`/`localExportVersion` (35/27) كما هما.
- لا خدمة موازية أُنشئت: النقل كان نقل ملكية للخدمة نفسها (`recurringWorkService`) بلا تغيير سلوك.
- طبقات الحراسة القائمة (ESLint نقاء المجال، حظر التخزين من الصفحات، touchpoints، الدورات) كما هي — هذا العقد يضيف حدود الملكية فوقها لا يستبدلها.
