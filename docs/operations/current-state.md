# حالة Micro الحية — مصدر الاستلام التنفيذي

**الحالة:** `CURRENT / UPDATE WITH EVERY MERGED SLICE`
**آخر تحديث:** 26 أغسطس 2026
**مرجع Git:** `main` الحالي هو `76bd42116dcaf454bafa594d3d77852d1ce48439` ولم يتغير. برنامج Bridge المالي هو `bridge/financial-decision-core-g3-g5`؛ SHA دمج تنفيذ G4-B الأصلي عبر PR #108 هو `21f15f79f71e6eaf230b152767eb6cc4c8f76a90`، وSHA دمج تصحيح قبول G4-B عبر PR #109 هو `c819247a3947fabfd583383c5b05b9e2e8793bb1`. كلاهما مرجع دمج تاريخي وليس بالضرورة رأس الـBridge الحالي إذا وُجدت commits لاحقة. يجب جلب رأس الـBridge البعيد قبل أي عمل؛ لا يعتمد سجل الحالة على SHA ذاتي بعد merge. تحقق دائمًا من المرجع البعيد بـ`git fetch origin --prune` قبل أي عمل لاحق؛ هذه الوثيقة لا تغني عن `git fetch`.

> هذا الملف يجيب عن سؤال واحد: **«إذا فتحت المستودع الآن، ما الذي أستطيع قوله أو تغييره بأمان؟»** لا يغير العقود ولا يحل محلها.
>
> **حالة الدمج الحالية:** القدرات السابقة وبرنامج إصلاح القرار البصري V1–V6 وتصحيح قبوله مدمجة على `main`. أما G3 الأساسية وتصحيح قبولها فمدمجان على Bridge عبر PR #98 وPR #99، وصُححت حالة التوثيق في PR #100 فقط، ثم دمجت O1 الأولية عبر PR #102 إلى Bridge فقط، ثم دمج تصحيح قبول O1 عبر PR #103 إلى Bridge فقط. لا يوجد Profile طعام أو خدمة أو زراعة أو سياحة منفذ؛ G4-A مدمجة الآن على Bridge عبر PR #105، وتنفيذ G4-B الأصلي مدمج عبر PR #108، وتصحيح قبول G4-B مدمج عبر PR #109؛ وكلها على Bridge فقط، ولا يُعلن قبول G5 النهائي قبل قرار قبول Manus Agent 1 والمالك.

## 1. قاعدة التشغيل الحالية

| الحقل | القيمة |
|---|---|
| الفرع المعتمد | `main` للقدرات المنشورة؛ Bridge فقط لشرائح مستقبلية تحتاج مراجعة قبل الدمج |
| لغة العمل | العربية؛ الإدخال الرقمي ASCII/LTR |
| نمط Prototype | Web-first، RTL، phone-first، local-first عبر IndexedDB |
| التدفق المعتمد | React → Application Service → Domain → `PrototypeLocalStore` → IndexedDB |
| قاعدة دمج | فرع مستقل → فحوص محلية → PR إلى Bridge عند برنامج متعدد المراحل → CI/Cloudflare ناجحان → PR تجميعية إلى `main` عند تفويض المالك |
| حساب المستخدم/السحابة | Auth والمزامنة والبيانات الحقيقية غير منفذة؛ PWA تشغيلية محلية/ثابتة فقط بلا حساب أو سحابة |

## 2. ما هو منفذ على `main`

| القدرة | الحالة والحد |
|---|---|
| الطلب والاتفاق والتنفيذ والتحصيل/الدين | منفذ محليًا؛ القبض ليس ربحًا والدين ليس كاشًا. |
| المصروف والمورد والشراء والالتزام والدفعات | منفذ؛ شراء المادة لا يصبح COGS تلقائيًا. |
| محافظ الكاش والافتتاح والتحويل والضبط والعكس | منفذ؛ كل تصحيح قابل للعكس وبسبب. |
| المادة والمخزون والاستهلاك والهدر | منفذ؛ فرق المادة للطلب تفسيري فقط. |
| نتيجة الفترة الأوسع G3 | غير مدمجة في `main`؛ التنفيذ الحالي موجود على Bridge بعد PR #98 وPR #99. لا تُقرأ كقدرة منشورة على `main` قبل PR تجميعية منفصلة. |
| الكتالوج الاختياري G4-A التاريخي | منفذ على `main` وفق سجله السابق؛ مرجع منتج/خدمة اختياري، ربط صريح للطلب الجديد، هامش مباشر مسجل وفرق مادة تفسيري. |
| وضع التشغيل والوقت الفعلي G4-B التاريخي | منفذ على `main` محليًا وبشكل اختياري: تفضيل وضع تشغيل، تفعيل تتبع وقت، تسجيل دقائق للطلب وعكسها بسبب، وفرق وقت مخطط/منفذ تفسيري. يستند إلى Domain/Application/LocalStore وschema 18/export 9؛ لا أجر أو COGS كاملة أو تكلفة فعلية أو تسعير أو تعديل Snapshot. |
| هامش المساهمة والسيولة القصيرة G5 التاريخية | منفذ على `main` كقراءة قرار محلية: هامش من طلبات نهائية وتكاليف مسجلة، نقطة تعادل مفككة من المزيج، وتوقع قصير من الكاش المسجل والتحصيلات/الالتزامات المؤرخة أو المعلنة. الإعلان لا يصبح قبضًا أو دفعًا، ولا يتجاوز الرصيد المرتبط، والعكس يحفظ سبب المالك. ليست صافي ربح نهائيًا أو COGS كاملة/قانونية أو تنبؤًا أو توصية. |
| الاستعادة المحلية | export/import ذري مع ترحيل الإصدارات السابقة؛ لا backup سحابي أو مزامنة. |
| PWA التشغيلية | منفذة كطبقة نشر/تشغيل فقط: Manifest RTL محلي، أيقونات محلية، App Shell precache، direct-route fallback وCloudflare headers، وتثبيت صادق Android/iOS وتحديث اختياري؛ لا تقرأ IndexedDB ولا تضيف backup أو sync. القبول الفعلي على جهاز Android/iOS وعنوان Pages الإنتاجي ما زال مطلوبًا. |
| G6-A التقويم الشهري التشغيلي | منفذ كقراءة `MonthOverview` مشتقة من المواعيد القائمة وشبكة شهر RTL داخل `/schedule`؛ يوضح الوقت المعروف والمجهول والتحذير المشتق من التعارض/السعة، ولا ينشئ تكرارًا أو موعدًا أو تخزينًا جديدًا أو أثرًا ماليًا. |
| G6-B التكرار المحلي المحدود | منفذ محليًا: قوالب أسبوعية/شهرية، 1–12 ظهورًا مستقلًا، idempotency، skips صريحة، وإلغاء الظهورات المستقبلية المشتقة بسبب مكتوب؛ لا reminders أو cron أو تقويم خارجي أو حجز أو موارد. |
| G7-A مصدر الاتفاق والمتابعة | منفذ محليًا: مصدر اتفاق اختياري، ملخص متابعة مكتوب، موعد محلي، قراءة مستحق/قادم، وتاريخ تغيير أو إزالة بسبب مكتوب؛ عرض A1 الآن يصنف اليوم المحلي في `Asia/Amman` إلى متأخر/مستحق اليوم/قادم ويعرض `DD/MM/YYYY` مع بقاء التخزين ISO؛ لا CRM أو رسائل أو تذكيرات تلقائية أو أثر مالي. |
| A2 عرض المال والتاريخ | منفذ كطبقة عرض مشتركة فقط: مبالغ minor بوحدات JOD مع أرقام ASCII واتجاه LTR، وتواريخ ظاهرة `DD/MM/YYYY` وأوقات/كميات معزولة؛ لا يغير قيمة التخزين أو الحساب أو الإدخال ISO ولا Domain أو CostSnapshot أو schema/export؛ مدمج عبر PR #71. |
| A3 المجهول والصفر المعروف | منفذ ومُدمج عبر PR #72: النتيجة الإجمالية للفترة غير الصالحة أو الناقصة تصبح `غير متاح`/`null`، بينما تبقى المجاميع الجزئية المعروفة ظاهرة، ويبقى الصفر الرقمي للفترة الخالية معروفًا فقط؛ لا تغيير لحساب Domain أو CostSnapshot أو schema/export. |
| B1 حماية المدخلات غير المحفوظة | منفذ ومُدمج عبر PR #73: حارس مشترك في `CostEditor` و`AgreementEditor` و`ScheduleEditor` لتمييز dirty/no-dirty، وعرض «احفظ واستمر»/«اخرج دون حفظ»/«إلغاء» قبل الخروج الداخلي؛ لا يدّعي اعتراض browser back أو tab close. |
| B2 الأفعال السريعة الصادقة | منفذ ومُدمج عبر PR #74: `طلب مخصص` يذهب إلى `/orders/new`، و`عربون أو تحصيل` يذهب إلى `/orders` مع ارتباطه بطلب موجود، و`تقدير تصميم — قريبًا` معطل صراحة لغياب مسار مستقل؛ لا تحصيل عام ولا POS. |
| B3 فصل سعر الحماية عن السعر المتفق عليه | منفذ ومُدمج عبر PR #75: سعر الحماية المشتق من Snapshot ظاهر منفصلًا، وحقل السعر المتفق عليه يبدأ فارغًا، ولا يُستخدم سعر الحماية كبداية إلا بفعل صريح قابل للمراجعة؛ لا تغيير لـ`CostSnapshot` أو Domain أو schema/export. |
| C1 تصحيح الأحداث المالية العامة | منفذ كمبدأ وعملية فعلية: الأصل immutable، وأي تصحيح يكون حدث عكس كامل جديدًا مرتبطًا بسبب ومفتاح idempotency، مع بقاء الأصل والسجل ظاهرين؛ لا تعديل جزئي أو حذف صامت أو restatement. |
| C2 قرار Review وFinance | منفذ ومُدمج عبر PR #77: بطاقة Review التفصيلية تمثل طلبًا واحدًا، والملخص العلوي قراءة محلية مجمعة؛ يبدأ سؤال Finance بالكاش المسجل والذمم والالتزامات، مع Pilot حقيقي قبل أي توسعة؛ لا Dashboard جديد. |
| QA/Pilot قبول نهائي | منفذ ومُدمج عبر PR #78 كتسليم توثيقي: فحوص sandbox والمتصفح والتصدير/الاستيراد والتنظيف موثقة، وبروتوكول القبول اليدوي يترك الجهاز الفعلي وoffline الحقيقي وCloudflare Pages production وPilot البشري للمالك؛ لا ادعاء قبول غير مختبر. |
| برنامج إصلاح القرار البصري V1–V6 وتصحيح القبول | مدمج على `main` عبر PR #96 بعد PR #95 التصحيح: ترتيب أفعال الطلب، صدق المدخلات وحماية البيانات، Finance cash-first، Schedule follow-up-first، والتسميات/الرجوع الآمن والإفصاحات، ثم QA وحالة التشغيل. أزيلت قدرة مسح البيانات المحلية غير المصرح بها؛ لا يغير البرنامج Domain أو Schema أو Export/Import أو معنى المال. |

## 2A. برنامج Bridge المالي

| العنصر | الحالة على Bridge | الحد |
|---|---|---|
| G3 — نتيجة الفترة الأوسع والمصاريف المشتركة | مدمج على `bridge/financial-decision-core-g3-g5` عبر PR #98 ثم تصحيح PR #99؛ حدّثت PR #100 الحالة التوثيقية فقط ولا تنسب إليها ميزة مالية | يدعم مبلغ الحصة الثابت، والنسبة المعلنة، وتقدير المالك، والتأجيل غير المحمل؛ ويعرض «صافي الربح التشغيلي المسجل للفترة» ضمن فترة صالحة مع الأسباب. |
| G3 — COGS الاختيارية | مدمجة ضمن PR #99 على Bridge | تظهر فقط من استهلاك مثبت القيمة مرتبط بطلب `final` وغير معكوس؛ Snapshot بديل معلن عند غياب الدليل. ليست COGS كاملة أو قانونية، ولا توزيعًا على منتج، ولا صافي ربح نهائيًا. |
| O1 — استحقاق المالك والحركات الفعلية | التنفيذ الأولي مدمج على Bridge عبر PR #102، وتصحيح القبول مدمج عبر PR #103؛ يشمل قفل نطاق الأهلية، successor مؤرخًا، مصادر افتتاح مرتبطة، وعكسًا غير حاذف | دفتر محلي RTL فقط؛ لا راتب موظف، ولا ضرائب، ولا تعدد ملاك قانوني، ولا Auth/Sync/Cloud/Banking. G4-A مدمجة عبر PR #105؛ تنفيذ G4-B الأصلي مدمج عبر PR #108 وتصحيح قبوله عبر PR #109 إلى Bridge فقط؛ لا يُعلن قبول G5 النهائي قبل قرار قبول Manus Agent 1 والمالك. |
| حدود الدمج والتسلسل | Bridge فقط؛ G4-A مدمجة عبر PR #105، وتنفيذ G4-B الأصلي عبر PR #108 وتصحيح قبوله عبر PR #109، و`main` عند `76bd42116dcaf454bafa594d3d77852d1ce48439` لم يتغير | التسلسل المعتمد على Bridge هو G3 → O1 → G4-A → G4-B → G5. G4-A وتنفيذ G4-B الأصلي مدمجان على Bridge فقط عبر PR #105 وPR #108، وتصحيح قبول G4-B عبر PR #109؛ لا يُعلن قبول G5 النهائي قبل قرار قبول Manus Agent 1 والمالك. PR إلى `main` لا تكون إلا بعد اكتمال الحزمة كلها ومراجعة قبول مستقلة وتفويض تجميعي صريح من المالك. كل شريحة لاحقة، بما فيها أي تصحيح توثيقي، تصل عبر PR إلى Bridge فقط؛ لا push مباشر إلى Bridge. |

## 3. الحدود غير القابلة للتفاوض

لا تصف أي سطح حالي بأنه COGS كاملة أو صافي ربح نهائي أو قيمة مشروع أو توقع نقدي. في G3، COGS اختيارية؛ تظهر فقط من استهلاك ذي قيمة تكلفة محفوظة وارتباط صالح بطلب مكتمل وغير معكوس. عند غياب الدليل تستخدم قراءة الفترة Snapshot كبديل معلن ولا تحول الغياب إلى صفر، ويظل صافي الربح التشغيلي المسجل ظاهرًا ضمن نطاقه مع حالة وأسباب المراجعة. لا يغير أي تعديل `CostSnapshot` أو طلبًا تاريخيًا بصمت. لا توزع مصروفًا مشتركًا أو استهلاكًا عامًا على طلب أو منتج بلا عقد وأساس معلن، ولا تخصم المصدر غير المحمل خفية من نتيجة الفترة. لا تحول الوقت الفعلي إلى أجر أو تكلفة فعلية أو سعر مقترح. لا تحذف حدثًا حساسًا؛ صححه بعكس أو تسوية موثقة.

## 4. الحالة المتوقفة عمدًا

**لا تبدأ Profile أو قدرة مالية/بنية جديدة تلقائيًا.** التسلسل المالي المعتمد على Bridge هو G3 → O1 → G4-A → G4-B → G5. G3 الأساسية وتصحيحها مدمجان على Bridge عبر PR #98 وPR #99، وPR #100 صححت التوثيق فقط، وO1 الأولية مدمجة عبر PR #102، وتصحيح قبول O1 مدمج عبر PR #103، وG4-A مدمجة عبر PR #105، وتنفيذ G4-B الأصلي مدمج عبر PR #108، وتصحيح قبول G4-B عبر PR #109؛ وكلها إلى Bridge فقط. لا يُعلن قبول G5 النهائي قبل قرار قبول مستقل من Manus Agent 1 والمالك. G15/G16 لقبول الجهاز وPilot ما زالا غير منفذين ولا يوقفان هذا التسلسل المالي على Bridge. لا PR إلى `main` قبل اكتمال الحزمة كلها ومراجعة قبول مستقلة وتفويض تجميعي صريح من المالك. القدرات التاريخية G4-B وG5 وG6-A وG6-B وG7-A وبرنامج إصلاح القرار البصري V1–V6 مدمجة على `main` وفق سجلاتها السابقة، ولا تُخلط مع برنامج Bridge الجديد. لا يصرح قرار النواة العامة وActivity Profiles بطعام/خدمات/زراعة/سياحة أو mixed schema. لا توسع G6-B إلى daily/RRULE/reminders أو التقويم الخارجي أو الحجز أو الموارد. لا توسع G7-A إلى CRM أو رسائل أو تذكيرات تلقائية. لا تنتقل إلى POS أو مزامنة أو Auth أو أي معنى سحابي قبل قرار ما بعد Pilot. لا توسّع الوقت الفعلي إلى أجر أو تكلفة أو توصية سعر، ولا توسّع G5 إلى صافي ربح نهائي أو COGS كاملة/قانونية أو تنبؤ/توصية من دون عقد مستقل.

## 5. أول قراءة إلزامية حسب المهمة

| إذا كان المطلوب | اقرأ بعد هذا الملف |
|---|---|
| مراجعة أو تعديل مالي | `docs/contracts/05-financial-p0-policies.md` ثم عقد G3 `docs/contracts/14-period-result-allocation-policy-prototype-contract.md` و`docs/implementation/02-domain-contract-coverage.md` و`src/domain/financial-event/` و`apps/prototype-web/client/src/application/finance/` |
| مراجعة أو تعديل G4-B عند تفويض جديد | `docs/contracts/16-optional-operating-mode-and-actual-time-contract.md` ثم `src/domain/actual-time/` و`apps/prototype-web/client/src/application/time/` و`apps/prototype-web/client/src/components/presentation/ActualTimePanel.tsx` |
| مراجعة أو تعديل G5 عند تفويض جديد | `docs/contracts/17-contribution-break-even-short-cash-g5-contract.md` ثم `src/domain/g5/` و`apps/prototype-web/client/src/application/g5/` و`apps/prototype-web/client/src/pages/Finance.tsx` |
| مراجعة أو تعديل G6-A عند تفويض جديد | `docs/contracts/18-derived-monthly-order-schedule-g6-a-contract.md` ثم `apps/prototype-web/client/src/application/scheduling/scheduleService.ts` و`apps/prototype-web/client/src/pages/Schedule.tsx` |
| مراجعة أو تعديل G6-B/G7-A عند تفويض جديد | `docs/contracts/19-bounded-local-schedule-recurrence-g6-b-contract.md` و`docs/contracts/20-agreement-source-follow-up-g7-a-contract.md` ثم خدمات recurrence/agreement context؛ لا توسع الحدود دون عقد جديد. |
| واجهة Prototype | `docs/product/mobile-ui-ux-reference-v1.md` و`docs/implementation/mobile-prototype-spec-v1.md` والمهارات المحلية المذكورة في `AGENTS.md` |
| LocalStore أو استعادة | `docs/contracts/16-optional-operating-mode-and-actual-time-contract.md` و`apps/prototype-web/client/src/storage/local/` و`apps/prototype-web/client/src/application/transfers/` |

قبل أي من هذه القراءات المتخصصة، اقرأ `docs/operations/micro-thinking-charter-v1.md` وأنشئ بطاقة فهم. لا يملك الوكيل حق البدء من نقطة تقنية فقط؛ يجب أن يربط الشريحة بموقف مستخدم وسؤال قرار وحد معرفة وبوابة قدرة.

إصلاح A1 مصمم كطبقة عرض/قراءة فوق G7-A: مصدر الحقيقة هو `followUpDate` المحلي المحفوظ، والمقارنة لا تعتمد على نص `Intl` الظاهر ولا تغير السجل أو موعد التسليم أو المال. دليل القبول في `docs/quality/follow-up-local-date-acceptance-v1.md` يثبت الاختبارات الآلية والمعاينة المحلية فقط؛ قبول الأجهزة الفعلية وPages production ما زال حدًا معلنًا.

إصلاح A2 مدمج عبر PR #71 كطبقة عرض مشتركة فوق الأسطح الحالية: `formatMoneyMinor` و`formatLocalDate` و`formatLocalDateTime` و`formatMonthLabel` و`formatQuantityMilli`، مع مكونات عرض تعزل القيم بصريًا في RTL. الإدخالات تبقى ASCII/ISO، والحسابات والخدمات والعقود المالية لم تتغير.

إصلاح A3 مدمج عبر PR #72 كطبقة حالة/عرض محدودة: `RecordedPeriodResult` يميز `recorded_only` المعروف عن `incomplete` و`invalid`، والنتيجة الإجمالية غير القابلة للاعتماد تحمل `null` وتظهر `غير متاح`، مع إبقاء البنود المسجلة المعروفة وأسباب النقص والفعل التالي. لا يغيّر Domain أو `CostSnapshot` أو schema/export.

إصلاح B1 مدمج عبر PR #73 كحارس مشترك محدود في `CostEditor` و`AgreementEditor` و`ScheduleEditor`: يقارن القيم الأولية بالحالية، ويعرض قرار الخروج الداخلي الصريح، ولا يحفظ تلقائيًا. نجاح الحفظ هو شرط الانتقال؛ فشل الحفظ يبقي المستخدم داخل النموذج. لا ندّعي browser back أو tab close لأنهما غير مضمونين في هذا النطاق.

إصلاح B2 مدمج عبر PR #74 كتصحيح لمسارات QuickActionSheet فقط: كل فعل يذهب إلى طريق موجود أو يبقى معطلًا بعلامة «قريبًا». لا ينشئ الخيار العام تحصيلًا أو أثرًا ماليًا ولا يوسع POS.

إصلاح B3 مدمج عبر PR #75 كفصل عرض/قرار محدود: قيمة الحماية المشتقة من التكلفة لا تُعرض كسعر اتفاق، ويبدأ سعر الاتفاق فارغًا، ويُنسخ فقط بعد فعل «استخدم سعر الحماية كبداية». لا يضيف توصية سوق ولا يغير Snapshot أو التاريخ المالي.

C1 مدمج عبر PR #76 ثم G14: تصحيح الحدث المالي العام يتم بعكس كامل جديد مرتبط بسبب ومفتاح idempotency، مع بقاء الأصل والسجل ظاهرين. لا يوجد تعديل جزئي أو حذف صامت أو restatement؛ يبقى أي UX جديد للعكس ضمن العقد وفحوصه.

C2 مدمج عبر PR #77 كمذكرة قرار فقط: لا يغير Review أو Finance ولا يبني Dashboard؛ يحدد معنى الملخص وبطاقة الطلب وأولوية سؤال الكاش/الذمم/الالتزامات وبوابة Pilot.

QA/Pilot مدمج عبر PR #78 كتقرير وبروتوكول قبول وفجوات فقط؛ لا يضيف وظيفة ولا يرفع نتيجة sandbox إلى قبول جهاز أو إنتاج أو Pilot بشري.

## 6. حالة مساحة العمل المقبولة

ملف `apps/prototype-web/client/public/__manus__/version.json` مولد محليًا وقد يظهر كتغيير؛ **لا يضاف إلى Git**. يجب أن يكون أي تغيير آخر مفسرًا في PR أو مُزالًا قبل التسليم.

## 7. ما يجب تحديثه عند الإغلاق القادم

عند إغلاق/دمج شريحة جديدة، يعدل الوكيل هذا الملف في نفس PR ليحدث: commit/PR، جدول «ما هو منفذ»، جدول «الحالة المتوقفة عمدًا»، إصدارات schema/export إن تغيرت، ومسار القراءة التالي. PWA مدمجة على `main` عبر PR #54 وG5 عبر PR #53 وG6-B/G7-A عبر PR #60، وA1 عبر PR #70، وA2 عبر PR #71، وA3 عبر PR #72، وB1 عبر PR #73، وB2 عبر PR #74، وB3 عبر PR #75، وC1 عبر PR #76، وC2 عبر PR #77، وQA/Pilot عبر PR #78. برنامج إصلاح القرار البصري V1–V6 مدمج على `main` عبر PR #96 بعد تصحيح PR #95؛ لا يوجد فعل مسح محلي في المنتج. **G3 الأساسية وتصحيح قبولها مدمجان على Bridge عبر PR #98 وPR #99، وO1 الأولية مدمجة عبر PR #102، وتصحيح قبول O1 مدمج عبر PR #103، وG4-A مدمجة عبر PR #105، وتنفيذ G4-B الأصلي مدمج عبر PR #108، وتصحيح قبول G4-B عبر PR #109؛ وكلها إلى Bridge فقط.** يدعم برنامج Bridge تسلسل G3 → O1 → G4-A → G4-B → G5. لا يُعلن قبول G5 النهائي قبل قرار قبول مستقل من Manus Agent 1 والمالك. كل شريحة لاحقة PR مستقلة إلى Bridge، بما فيها أي تصحيح توثيقي؛ لا يوجد push مباشر إلى Bridge ولا PR إلى `main` قبل اكتمال الحزمة كلها ومراجعة قبول مستقلة وتفويض تجميعي صريح من المالك. يبقى `main` عند `76bd42116dcaf454bafa594d3d77852d1ce48439` خارج التغيير. يظل اختبار iOS الفعلي وAndroid وstandalone وOffline وCloudflare Pages production وPilot البشري مطلوبًا قبل إعلان قبول PWA الإنتاجي متعدد المنصات. ثم يحدث `todo.md` فقط بما يطابق الواقع المندمج.

## 8. قرار G8.1 — الثقة والاستعادة المحلية

أثبتت مراجعة `main` أن طبقة PWA والاستعادة المحلية المطلوبة في G8.1 موجودة جزئيًا في البناء المحلي: التثبيت اختياري وصادق، والتحديث بقرار المستخدم، وservice worker وWorkbox precache تفعلا في built preview. لكن محاولة reload مع الخادم متوقف عادت بعلامات `ERR_` ولم تعرض App Shell؛ لذلك تسجل QA/Pilot الحالة `OFFLINE_SHELL_NOT_VERIFIED` ولا ترفع cache observation إلى PASS. يظل السجل المالي محليًا على الجهاز، ولا يَعِد Micro بنقل البيانات بين الأجهزة؛ يجب تصدير ملف محلي قبل حذف بيانات المتصفح أو تغيير الجهاز. لم يتغير Domain أو `CostSnapshot` أو schema/export أو LocalStore في هذه المرحلة.

القبول المتاح هو قبول مصدر/بناء/محاكاة محلية فقط. لا يُعلن قبول Android أو iOS أو Cloudflare Pages الإنتاجي قبل اختبار جهاز فعلي وعنوان الإنتاج. يبقى `iOS physical acceptance pending` و`Cloudflare Pages production verification pending` حدّين صريحين.

يوجد في التفويض مرجع إلى `docs/decisions/remaining-capabilities-review-v2.md`، لكنه غير موجود في `main`؛ المرجع الموجود هو v1 وأقدم من الحالة الحية. لذلك اتُّبعت الحالة الحية والعقود، وسُجل هذا الفرق بدل اختلاق ملف أو حالة جديدة.

## 9. قرار G8.3 — الاستيراد الافتتاحي الموجّه

أصبح في فرع G8.3 معالج Application مستقل يقرأ ملفًا افتتاحيًا محدودًا، يتحقق منه كاملًا قبل الكتابة، يبني سجلات Domain افتتاحية للكاش والمادة، ويستخدم `replaceSnapshot` الذري على Store فارغ فقط. المعالج يرفض الملف الناقص أو التالف أو غير المدعوم، يرفض Store غير الفارغ، ويحافظ على التكرار الآمن عبر مفاتيح deterministic مرتبطة بـ`importId`. الواجهة تعرض معاينة عربية قبل التأكيد وتوضح أن الموقف الافتتاحي ليس مبيعات أو ربحًا.

لم تتغير schema/export العامة، ولم تُضف مزامنة أو حسابات أو تاريخ مالي مختلق. قبول هذه القدرة في `main` مشروط بمرور CI/Cloudflare في PR؛ QA الجهاز الفعلي وCloudflare الإنتاجي يظلان خارج بيئة sandbox.

## 10. قرار G9.2 — قراءة قيد السعة

تضيف طبقة Application دالة قرار صريحة فوق `ScheduleDay` القائم. تعيد `unknown` عند عدم تفعيل السعة، و`needs_review` عند مدد ناقصة، و`within_limit` أو `over_limit` عند وجود بيانات كاملة. يبقى التعارض منفصلًا، ولا ينتج القرار كتابة أو تغييرًا ماليًا أو تعديلًا للموعد.

## 11. قرار G9.3 — سطح قرار التشغيل

يستهلك `/schedule` الآن `buildCapacityDecisionViewModel` من طبقة Application ويعرض عبر `DecisionPanel` الحالة والحقيقة المعروفة والفعل التالي لليوم المختار أو اليوم الأول في الأسبوع. يعرض التعارض كتحذير مستقل؛ لا تحسب React السعة ولا تكتب إلى التخزين. الحالات الفارغة والناقصة منفصلة، والقرار النهائي يبقى للمالك.

تم اختبار الواجهة الحية ببيانات اصطناعية فقط لحالات عدم وجود سعة، وضمن الحد، وفوق الحد، والمدة المجهولة، والتعارض، وإزالة السعة، ثم أُفرغت مخازن IndexedDB. أضيفت لقطات smoke للأبعاد 360/390/430، وتحقق RTL وLight/Dark؛ لا يُعلن قبول جهاز فعلي أو Pilot أو إنتاج متعدد الأجهزة.

## 12. تصحيح قبول G9 — ربط القرار بالواجهة

صُحح نطاق G9.2/G9.3: القرار لم يعد دالة معزولة؛ صار مستهلكًا فعليًا في `/schedule` عبر view model و`DecisionPanel`. لا يغير الربط أي مال أو طلب أو Snapshot أو موعد عند القراءة أو التوجيه، ولا يضيف قدرة تشغيلية جديدة.

## 13. قرار G9.4/G9.5 — لا توسعة متابعة جديدة الآن

راجعت الشريحة الحالية فوق G7-A وسيناريوهات المتابعة المحلية. لم تثبت فجوة واحدة محددة ومدعومة تمنع قرارًا متكررًا؛ G7-A يعبر عن المصدر والملخص والموعد والسبب وتاريخ التغيير/الإزالة. لذلك لا تضاف وظيفة جديدة ولا حقول CRM، وتبقى G9.5 محققة بقرار عدم التوسعة. إعادة الفتح تحتاج دليل Pilot حقيقيًا على فقد معلومة محلية لا يمكن تمثيلها حاليًا.

## 14. برنامج إصلاح القرار البصري V1–V6 على `main`

| المجموعة | الأثر في الواجهة | حدود القبول الحالية |
|---|---|---|
| V1 | وحّد مدخلات QuickAction ومسار `/orders/new` حول «مسودة تصميم»، وقدم CTA قبل تفاصيل OrderDetail الاختيارية. | فحوص PR #89 وCloudflare نجحت؛ لا أثر مالي أو quote مستقل. |
| V2 | جعل العربون الفارغ غير مسجل حتى حدود Application، وجعل عناصر التكلفة الاختيارية غير مضافة صراحة، ولا يعرض سعر حماية عندما تكون المعرفة ناقصة. أزيلت قدرة المسح المحلي غير المصرح بها في PR #95. | لا Cloud recovery ولا تغيير Schema أو Export/Import. |
| V3 | جعل Finance cash-first مع نطاق فترة واضح وقراءات G5 ثانوية. | لا صافي ربح أو COGS فعلي أو توصية. |
| V4 | قدم متابعة Schedule (متأخر/اليوم/قادم) وCTA قبل الشهر، ثم نقل التكرار والسعة الاختيارية إلى الأسفل؛ السعة تحذير فقط. | لا تقويم أو تذكير أو حجز أو أثر مالي. |
| V5 | استبدل labels التقنية الظاهرة بـ`أرقام 0–9`، وربط رجوع Draft بحارس التعديلات غير المحفوظة، ووضح Settings، واختصر الإفصاحات. | QA محلي فقط؛ الجهاز وOffline وProduction وPilot للمالك. |
| V6 | وثّق حالة التشغيل، خرائط القبول، وبوابة Pilot/قرار Finance وReview، ثم صُححت حدود V2 وأدمج البرنامج عبر PR #96. | لا ادعاء أن G15/G16 نفذا فعليًا. |

## 15. قرار QA بعد V1–V6

اجتازت الواجهة المعدلة فحوص Agent داخل Chromium/Vite مع Light/Dark وRTL وبيانات اصطناعية فقط، وسُجلت مسارات Settings وNew Draft وDraft وOrders وSchedule، بما في ذلك الحالة الفارغة والخطأ والرجوع غير المحفوظ. لم تُثبت هذه الجولة Android أو iOS أو standalone أو safe area أو Offline reload أو Files الأصلية أو أداء هاتف حقيقي أو Pilot G16؛ تبقى تلك البنود `ينتظر المالك` أو `خارج النطاق` وفق G15/G16. يجب تنفيذ تنظيف IndexedDB وCache Storage وlocalStorage وsessionStorage وService Workers وملفات QA قبل تسليم البيئة.

## 16. حالة G4-A على Bridge

| العنصر | الحالة الحالية | الحد |
|---|---|---|
| فرع التنفيذ | `agent3/g4a-core-catalog-foundation` دُمج إلى `bridge/financial-decision-core-g3-g5` عبر PR #105، وSHA الدمج `f2d90c874f9f9c5b65b7c9bda8678768678838f1` | `main` لم يتغير؛ قبول G4-A النهائي ما زال لـManus Agent 1 والمالك |
| Domain/Application | `CatalogItem.unitId` اختياري، ووحدات `count/mass/volume/time/distance/area`، وتحويل مباشر دقيق داخل البعد، وقالب components/yield اختياري مع `not_configured/ready/needs_conversion` ومراجعة مؤرخة | لا Profile قطاعي، ولا graph أو تحويل متعدد الخطوات أو كثافة أو BOM أو إنتاج |
| التخزين والاستعادة | Memory/IndexedDB والمنافذ وschema `24` وexport `15`، migration تضيف `unitId: null` ومصفوفات جديدة فارغة فقط، والتحقق قبل replacement الذري | لا Auth/Sync/Cloud/Backend أو استعادة بين الأجهزة |
| الواجهة | `/catalog` امتداد اختياري: حفظ المرجع أولًا، قياس/تحويلات في لوحة فرعية، والقالب عند الطلب؛ RTL وLight/Dark وأرقام ASCII/LTR وempty/needs-conversion | لم يُتحقق بعد على هاتف فعلي أو Pages production أو Pilot |
| الأدلة المحلية/CI | `pnpm install --frozen-lockfile` و`pnpm check` ناجحان، بما فيه Domain/Application/Storage/Transfer/UI build؛ اختبارات G4-A تشمل 16 حالة مستهدفة ضمن suite المشروع؛ Cloudflare Pages check في PR #105 ناجح | قبول G4-A النهائي ومراجعة Agent 1 ما زالا مطلوبين؛ لم يُختبر هاتف فعلي أو Production أو Pilot |

G4-A لا تغير طلبًا تاريخيًا أو اتفاقًا أو `CostSnapshot` أو `recognizedCostMinor` أو نتيجة الفترة، ولا تحول القالب إلى Purchase أو Inventory أو Consumption أو COGS أو كاش. دُمجت عبر PR #105 إلى `bridge/financial-decision-core-g3-g5` فقط؛ PR #108 دمجت تنفيذ G4-B الأصلي إلى Bridge فقط، وPR #109 دمجت تصحيح قبوله إلى Bridge فقط، ولا توجد PR إلى `main` ولا يُعلن قبول G5 النهائي قبل قرار Manus Agent 1 والمالك.

## 16A. حالة G4-B على Bridge

| العنصر | الحالة الحالية | الحد |
|---|---|---|
| فرع التنفيذ | تنفيذ G4-B الأصلي على `agent4/g4b-explainable-margin-allocation` دُمج إلى `bridge/financial-decision-core-g3-g5` عبر PR #108، ثم صُحح القبول/المقياس على `agent4/g4b-per-unit-scale-state-correction` عبر PR #109؛ SHA الدمج التاريخي لـPR #108 هو `21f15f79f71e6eaf230b152767eb6cc4c8f76a90` ولـPR #109 هو `c819247a3947fabfd583383c5b05b9e2e8793bb1`، ولا يُعد أي منهما رأس Bridge الحالي؛ يجب جلب الرأس البعيد قبل أي عمل | `main` بقي عند `76bd42116dcaf454bafa594d3d77852d1ce48439`؛ لا يُعلن قبول G5 النهائي قبل قرار Manus Agent 1 والمالك |
| Domain/Application | قراءة مشتقة حسب Catalog Item وفترة محلية معلنة: `directMarginMinor` من final/recognized وSnapshot، فرق مادة ووقت، هدر سياقي، وسياسات `manual_amount`/`per_output_unit`/`actual_time`/`completed_revenue_percentage` مع حالة معرفة وفعل تالٍ؛ تصحيح قبول PR #109 يثبت أن `per_output_unit` يستخدم `rateMinorPerWholeUnit` لكل `1.000`، ويجمع `outputQuantityMilli` ثم يطبق round-half-up مرة واحدة إلى أقرب minor، مع منع overflow وتقريب float | لا محرك تسعير، ولا COGS كاملة/قانونية، ولا صافي ربح نهائي، ولا أجر أو تكلفة فعلية للوقت |
| الهدر | توسعة `InventoryMovement` بسياق واحد من خمسة مع تحقق علاقات الطلب/Catalog/Template؛ الهدر العام وغير المحمل يظهر منفصلًا ولا يُخصم تلقائيًا | لا جدول هدر منافس، ولا توزيع خفي، ولا ربط مفقود بالتخمين |
| التخزين والاستعادة | Memory/IndexedDB وLocalTransfer مع schema `26` وexport `17`؛ migration للهدر القديم إلى `general_project` فقط، وترحيل `rateMinor` القديم إلى `rateMinorPerWholeUnit` لسياسات `per_output_unit` بأمان دون تغيير Snapshot أو الهامش | replacement ذري؛ لا Auth/Sync/Cloud أو backup سحابي؛ PR #109 تصحيح قبول مدمج عبر PR إلى Bridge فقط |
| الواجهة | `/catalog` يعرض الفترة، القراءة المباشرة، الأدلة الناقصة، سياسة مصدرها وسببها، ونسخة successor غير حاذفة؛ محرر الهدر يعرض سياقه الصريح | RTL/phone-first محلي؛ لم يُختبر هاتف فعلي أو Pages production أو Pilot |
| الأدلة | اختبارات Domain `75`، واختبارات Prototype `219`، و`typecheck` و`prototype:check` و`prototype:build` ناجحة محليًا؛ قواعد الحساب والترحيل مغطاة اختباريًا، ونجحت CI وCloudflare Pages قبل دمج PR #109 | مراجعة Manus Agent 1 والمالك؛ لم يُختبر هاتف فعلي أو Production أو Pilot، ولا إعلان قبول 100% |

هذه الشريحة لا تغيّر `CostSnapshot` أو `recognizedCostMinor` أو `directMarginMinor` أو نتيجة G3 أو الكاش أو الذمم عند تسجيل الوقت/الهدر/السياسة. PR #108 هي تنفيذ G4-B الأصلي، وPR #109 هي تصحيح قبول G4-B ومقياس `per_output_unit`; SHA دمج PR #108 هو `21f15f79f71e6eaf230b152767eb6cc4c8f76a90` وSHA دمج PR #109 هو `c819247a3947fabfd583383c5b05b9e2e8793bb1`، وكلاهما مرجع تاريخي لا يُوصف بأنه رأس Bridge الحالي. يجب جلب رأس الـBridge البعيد بـ`git fetch origin --prune` قبل أي عمل لاحق. كل شريحة لاحقة، بما فيها أي تصحيح توثيقي، تصل عبر PR إلى Bridge فقط؛ لا push مباشر إلى Bridge. لا يعلن هذا الدمج قبولًا نهائيًا قبل مراجعة Manus Agent 1 والمالك، ولا يُعلن قبول G5 النهائي.

## 17. حالة تنفيذ G5 على Bridge — PR #111

| العنصر | الحالة الحالية | الحد |
|---|---|---|
| فرع التنفيذ | `agent5/g5-recorded-break-even-short-cash` يتتبع رأس `bridge/financial-decision-core-g3-g5` عند بدء العمل، وPR #111 هي PR الوحيدة لهذه الشريحة؛ لا تغيّر `main` | بعد الدمج يجب التحقق من رأس Bridge الفعلي بـ`git fetch origin --prune`؛ لا يُستخدم SHA ذاتي في هذه الوثيقة كرأس لاحق |
| Domain/Application | هامش مساهمة من الطلبات `final` المسلّمة داخل الفترة، تكاليف Snapshot المباشرة، تكاليف ثابتة مصنفة، وحالات `available/needs_review/incomplete/invalid`؛ الكميات تطبع إلى `quantityMilli`، وتُجمع الوحدات بعد تطابقها أو تحويل G4-A صريح ودقيق؛ لا `×1000` ولا تقريب خفي | لا صافي ربح نهائي، ولا COGS فعلية، ولا Forecast ملزم، ولا توصية شراء/اقتراض/سحب/تسعير |
| السيولة القصيرة | قراءة مشتقة من الكاش المسجل، الأرصدة المؤرخة أو إعلانات G5 الفعالة داخل الأفق، مع فصل الذمم/الالتزامات غير المؤرخة؛ الإعلانات لا تصبح قبضًا أو دفعًا | غياب الإعلان لا يعني أفقًا آمنًا؛ التوقع `null` عند نقص تاريخ مؤثر أو رابط متجاوز |
| الإعلانات والتصحيح | محرر إعلان مع اتجاه ومبلغ وتاريخ ومصدر ودرجة معرفة وملاحظة وربط اختياري بطلب أو التزام؛ idempotency؛ عكس ذري كامل بسبب مكتوب مع بقاء الأصل | لا تعديل للسجل المالي أو الطلب أو Snapshot؛ schema/export لا يتغيران في هذه الشريحة |
| التخزين والاستعادة | أضيفت عملية Store ذرية لعكس إعلان G5 في Memory/IndexedDB، وتحقق نقل أشد للمبلغ والتاريخ والعلاقات؛ schema `26` وexport `17` كما هما | لا Auth/Sync/Cloud/backup سحابي |
| الأدلة المحلية | `pnpm install --frozen-lockfile` و`pnpm check` و`prototype:check` واختبارات G5 Domain/Application وLocalTransfer نجحت محليًا؛ التغطية الموثقة في مصفوفة السيناريوهات | نجاح CI وCloudflare Pages ومراجعة Manus Agent 1 والمالك وقبول الهاتف/Production/Pilot ما زالت مطلوبة |

نفذت الشريحة وفق المسار `React → Application Service → G5 Domain → PrototypeLocalStore → IndexedDB`. لم تُضف `apps/prototype-web/client/public/__manus__/version.json`، ولم تُغيّر `main` أو تاريخ المال. بعد نجاح CI وCloudflare Pages ودمج PR #111، يبدأ أي عمل لاحق من `git fetch origin --prune` والتحقق من رأس Bridge البعيد، ولا يُعامل هذا التنفيذ كقبول نهائي قبل مراجعة Manus Agent 1 والمالك.

تصحيح القبول الحالي في PR #112 يقتصر على عرض وحدة/مقياس `breakEvenUnits` في Finance: يستخدم `quantityUnitLabel` المنظم عند وجوده، ويعرض «من المزيج المسجل» عند غياب الوحدة المنظمة أو الموحدة، ويبقي `null` بصيغة «غير متاحة» مع سببها. لا يغيّر هذا التصحيح الحساب أو العتبات أو التخزين أو schema/export أو المال. لا تبدأ مراجعة الحزمة أو PR إلى `main` قبل قبول Manus Agent 1 والمالك؛ يصل كل شيء إلى Bridge عبر PR فقط ولا يوجد push مباشر.

## References

[1]: [قائمة التنفيذ](../../todo.md)
[2]: [مطابقة العقود بالتنفيذ](../implementation/02-domain-contract-coverage.md)
[3]: [عقد G4-B](../contracts/16-optional-operating-mode-and-actual-time-contract.md)
[4]: [بروتوكول التسليم](agent-handoff-protocol-v1.md)
