## بطاقة الشريحة

| الحقل | المحتوى |
|---|---|
| مرجع `main` عند البدء | <!-- commit --> |
| سؤال القرار | <!-- سؤال واحد --> |
| العقد الحاكم | <!-- path --> |
| نطاق هذه الشريحة | <!-- ما تغير --> |
| خارج النطاق | <!-- ما لم يتغير عمدًا (non-goals) --> |
| storage/migration | <!-- لا شيء أو schema/export/migration --> |
| الـinvariants المالية/الحسابية المعنية | <!-- القواعد التي لا يجوز كسرها، أو: لا ينطبق — السبب --> |
| حالات الفشل المتوقعة | <!-- أهم حالتين ثلاث على الأقل، أو: لا ينطبق — السبب --> |
| حد الاستعادة (rollback boundary) | <!-- آخر التزام آمن للعودة إليه وأثر الرجوع --> |

## بطاقة فهم Micro

| السؤال | الإجابة |
|---|---|
| المستخدم والموقف اليومي | <!-- من؟ متى؟ --> |
| سؤال القرار | <!-- سؤال واحد --> |
| حد المعرفة | <!-- ما الذي لن ندعيه؟ --> |
| لماذا الآن لا القدرة المجاورة؟ | <!-- بوابة/اعتمادية --> |
| العقد والسيناريو | <!-- paths --> |

## الملفات والتأثير

- **الملفات/الوحدات المتغيرة:** <!-- قائمة صريحة → -->
- **ما لم يتغير عمدًا:** <!-- المال والتاريخ والتخزين والقدرات المؤجلة → -->
- **أثر المخطط/التصدير/الاستيراد/الترحيل:** <!-- لا شيء، أو التغيير وreasons؛ `localSchemaVersion`/`localExportVersion` كما هما أو بقرار مالك → -->
- **أثر الثبات التاريخي والعكس (snapshots/reversals):** <!-- لا ينطبق — السبب، أو الوصف → -->
- **أثر التزامن/الحتمية/إعادة المحاولة (sensitive writes):** <!-- لا ينطبق — السبب، أو الوصف وidempotency key → -->
- **أثر الواجهة (RTL/الوصول/الوضعين):** <!-- لا ينطبق — السبب، أو الوصف → -->

## الاختبارات

- **اختبارات النجاح:** <!-- ملف/أسماء → -->
- **اختبارات الفشل (خطأ صادق):** <!-- ملف/أسماء، أو لا ينطبق — السبب → -->
- **اختبارات الترحيل/التوافق (released pairs/envelopes):** <!-- ملف/أسماء، أو لا ينطبق — السبب → -->
- **اختبارات التزامن/إعادة المحاولة (إذا لمس الكود كتابة حساسة):** <!-- ملف/أسماء، أو لا ينطبق — السبب → -->

## التحقق

- [ ] `pnpm check` ناجح.
- [ ] الأوامر المركزة النتائج ملخصة أدناه (typecheck/lint/format/text-density/design-guards/guards/test…).
- [ ] `git diff --check` ناجح.
- [ ] فحص الأسرار والملفات المولدة نظيف (`node scripts/check-secrets.mjs`)، ولا `.only`/`.skip` (`node scripts/check-test-focus.mjs`)، وبيان الكيانات محدث إن لمس التخزين (`node scripts/check-entity-touchpoints.mjs`).
- [ ] أُجري QA حي وLight/Dark عند تغيير واجهة، وحُذفت بيانات الاختبار المحلية.
- [ ] لا يحتوي الالتزام ملف `apps/prototype-web/client/public/__manus__/version.json`.
- [ ] حدّثت `docs/operations/current-state.md` و`todo.md` بما يطابق ما اندمج فعلًا.
- [ ] لا تبدأ هذه PR قدرة أو مجموعة خارج النطاق المعلن.
- [ ] بطاقة الفهم تثبت أن الشريحة تحل قرارًا حقيقيًا، ولا تحوّل Micro إلى ERP/POS/CRM أو واجهة عامة.

**نتائج الأوامر المركزة:** <!-- أمر: نتيجة لكل أمر ذي صلة → -->

## التسليم

- **المؤجل/القيود المعروفة:** <!-- بنود صريحة، أو «لا يوجد» → -->
- **قرار المالك مطلوب؟** <!-- لا، أو نعم + السؤال الدقيق → -->
- **البوابة التالية فقط (لا تبدأ تلقائيًا):** <!-- ما يفهمه Agent جديد بعد الدمج، والبوابة التالية بلا بدء تلقائي. بعد إغلاق برنامج التحصين الكامل: البوابة التالية هي المسح الهيكلي للقراءة فقط بموجب AGENTS.md §11، ولا إعادة هيكلة قبل قرار المالك. -->
