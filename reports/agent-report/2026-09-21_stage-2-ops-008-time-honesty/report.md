# تقرير تنفيذ — Stage 2 / الموجة 5: OPS-008 (الوقت الفعلي مقابل المخطط — السبب الصادق من مصدر الوقت)

**التاريخ:** 2026-09-21 · **PR:** #201 · **الفرع:** `stage-2/completion-remaining` · **خط الأساس:** `8986385fcab1e1ee38c376d938f2475b6ab1c86c` (رأس main المتحقق — دمج الموجة 4 عبر PR #200 بCI أخضر على SHA نفسه)
**Workstream:** WS-168 · **Item:** OPS-008 · **الالتزامات:** إغلاق الموجة 4 + Claim `ddaae4f` ← كود `8a67efa` ← توثيق (هذا الالتزام)

## 1. خط الأساس والهوية
رأس main المتحقق نفسه (`8986385`). إغلاق الموجة 4 (OPS-007/OPS-009/WS-167 → VERIFIED) تم في الالتزام الأول من الفرع. فريق التحليل: عاملان قراءة-فقط (مجال/تطبيق + فريق أحمر) بتغطية كاملة للعقد ١٦ وسطح الوقت. **الاكتشاف:** مقارنة عقد ١٦ منفذة أصلًا على main (summarizeActualTime + ActualTimeService + ActualTimePanel) ومختبرة في الدومين والتطبيق — لا إعادة تنفيذ؛ الموجة أغلقت ثلاث فجوات صدق.

## 2. الملفات المتغيرة (كاملة)
معدّل: `application/time/actualTimeService.ts` (اشتقاق معرفة المقارنة من مصدر الوقت نفسه)؛ `components/presentation/ActualTimePanel.tsx` (استبدال سطر السبب المصنوع — تبديل محايد الكثافة)؛ `application/time/actualTimeService.test.ts` (+4 اختبارات). جديد: `ActualTimePanel.dom.test.tsx` (جذر client/src — 6 اختبارات DOM). توثيق: التراكر + current-state §52 + هذا التقرير + سجل QA + فهرس التقارير + Views/Excel/meta.

## 3. السلوك قبل/بعد
- **قبل:** (١) سطر needs_review يعلن «الفرق كبير — راجعه» — **سبب مصنوع**: الدومين لا يحسب أي عتبة حجم، والفرق نفسه قد يكون «غير متاح» في هذه الحالة نفسها؛ (٢) معرفة المقارنة مشتقة من حالة اللقطة **الكلية** (تغلبها مدخلات المواد) — طلب بمواد تقديرية ووقت مخطط معروف يظهر «يحتاج مراجعة» خلافًا لعقد ١٦ §٤ («يوجد وقت فعلي، ووقت Snapshot معروف» → recorded)؛ (٣) دقائق مخططة صفرية (يصنفها craft-order «ناقصة») تُعرض «0 دقيقة» كخطة واثقة ويحسب الفرق ضدها.
- **بعد:** المعرفة من `input.time` نفسه (confidence + وجود دقائق > 0)؛ المواد التقديرية لا تلطخ مقارنة الوقت؛ المخطط الصفري/الغائب → `plannedMinutes: null` أي «غير متاح» والفرق «غير متاح»؛ وسطر السبب الصادق: «سبب المراجعة: الوقت المخطط في لقطة التكلفة تقديري أو غير معروف بالكامل.»

## 4. العقود والـinvariants
عقد ١٦ (§٤: حالات المقارنة وحرفية «وقت Snapshot معروف»؛ §٦: لا يضرب الدقائق بسعر جديد — لم يُمس؛ التغيير لا يمس recognizedCostMinor/Revenue/directMargin/النتيجة)؛ عقد ٠٣ (اللقطة مصدر المخطط المجمد). **المخطط/التصدير:** `NO_SCHEMA_OR_EXPORT_CHANGE` (35/27 — سجل الوقت مخزن أصلًا ومسجل). **المعنى المالي:** `NO_FINANCIAL_SEMANTIC_CHANGE` — حساب الفرق (actual−planned) كما هو في الدومين؛ لا Writer جديد.

## 5. قرار التنفيذ ولماذا هو أدنى
إغلاق فجوات صدق فوق قراءة قائمة: تبديل نصي واحد محايد الكثافة (لا رفع سقف — OrderDetail 182/183)؛ اشتقاق المعرفة في خدمة التطبيق لا في الدومين (الدومين كما هو — معامل knowledge يُمرر من المصدر)؛ لا معالجة R5 (قسر `?? 0` في recurringWorkService للدقائق المخططة الغائبة) لأنه سطح مجارج ذا مصالحة مالية مستقلة — يسجل كقرار مالك؛ ولا تغيير لعرض «0 دقيقة» عند تساوي طرفين معلومين (صفر موثق الطرفين).

## 6. الاختبارات والتحقق
| الأمر | النتيجة |
|---|---|
| `corepack pnpm exec vitest run client/src/application/time/actualTimeService.test.ts` | 8/8 PASS (4 قائمة + 4 جديدة) |
| `corepack pnpm exec vitest run client/src/ActualTimePanel.dom.test.tsx` | 6/6 PASS |
| `pnpm check` | **EXIT 0** — 36+240 ملفًا؛ 404+1680 اختبارًا؛ lint 37/37؛ bundle 615,915 داخل الميزانية |
| `python3 scripts/operations-control/validate.py` | valid |
| `check-secrets` / `check-test-focus` / `git diff --check` | PASS / PASS / نظيف |

**مسارات الفشل المغطاة:** مواد تقديرية + وقت معروف → recorded (إثبات المعرفة من مصدر الوقت)؛ فعلي أقل من مخطط → فرق سالب −30 صادق؛ مخطط صفري → «غير متاح» والفرق «غير متاح» لا صفر واثق؛ قراءة متكررة بلا كتابة (مطابقة لقطة المخزن الكاملة)؛ DOM: كل الحالات + سطر السبب الصادق (لا «الفرق كبير») + التراجع الموثق يبقي الأصل ظاهرًا مع سببه وتاريخه + لا «0 دقيقة» مستقلة.

## 7. QA الحي والجهاز
`NOT_EXECUTED` — لا متصفح حي في البيئة؛ تبديل نصي داخل بنية قائمة. سجل QA: `docs/quality/stage-2-ops-008-qa-2026-09-21.md`.

## 8. التراكر والعروض
OPS-008: CLAIMED → IN_PROGRESS → IN_REVIEW (pr:201). WS-168: CLAIMED → IN_PROGRESS → IN_REVIEW (pr:201). إغلاق الموجة 4 (→ VERIFIED عند 8986385) في الالتزام الأول من الفرع نفسه. Views/Excel/meta مُجددة وvalidator ناجح.

## 9. القيود والأدلة التاريخية
R5 (recurringWorkService يجمّع الدقائق المخططة الغائبة كصفر في مرجع العمل المتكرر) — قيد معروف يحتاج قرار مالك قبل أي تغيير (سطح مجارج بمصالحة معرفية)؛ تسمية العقد «minutes» مقابل «minutesDelta» انجراف تسمية فقط لا سلوك. QA الحي NOT_EXECUTED.

## 10. حدود الاستعادة
حد آمن أخير: `8986385`. الرجوع = عدم دمج PR #201 — لا هجرة ولا كتابة بيانات. آخر التزام آمن على الفرع = `8a67efa` حيث `pnpm check` أخضر EXIT 0.

## 11. الكتابات المنفذة وغير المنفذة
**نُفذت:** فرع + دفع؛ 3 التزامات؛ PR #201؛ تحديثات تراكر المصدر مع Views/Excel/meta. **لم تُنفذ:** لا دفع مباشر لmain؛ لا دمج قبل CI + manifest؛ لا حذف/تسمية فروع؛ لا وصول لDocuments؛ لا نشر/إعدادات؛ `NO_TOKEN_STORED`؛ لا تنظيف فروع.
