# تقرير الموجة الثالثة — العقود والملكية التقنية — Micro

**التاريخ والوقت (Asia/Amman):** 17 أيلول/سبتمبر 2026
**BASE_SHA:** `81d8d253984dee410c7fd6c3e461fcd0e76f327e` — رأس main بعد دمج الموجة الثانية (PR #163)
**HEAD_SHA (رأس فرع الإصلاح قبل التوثيق):** `8fa1e4045cbb275f6f16f055d24f392ded58fc03`
**الفرع:** `refactor/wave-3-technical-contracts-ownership` من `81d8d25`
**المرجع:** تكليف الموجة الثالثة المعتمد (قرارات المالك مسبقة الاعتماد داخل النطاق)

---

## 1. الخلاصة التنفيذية

نُفّذت الوحدات الثلاث (EXE-015/016/017) كاملة بخمسة التزامات منطقية بترتيب التكليف §10 (الملكية أولًا ثم المشاركة ثم التنقل). النتيجة: **الوحدات الثلاث كلها FIXED** — كل معايير القبول مثبتة باختبارات صريحة (**1849 اختبارًا ناجحًا = خط الأساس 1813 + 36 جديدًا، 0 فاشل، 0 متخطى**)، و`pnpm check` خروج **0** كاملًا (بوابات AGENTS.md كلها)، ومراجعة مستقلة للـDiff **PASS** بلا أي انتهاك نطاق مرئي أو مالي.

**لم يُنقل أي سطح مرئي ولم يتغير أي اسم ظاهر ولم يعاد تصميم شيء** — حارس النطاق §5 التُزم حرفيًا. المخطط/التصدير **35/27 بلا تغيير** (لا سطر في `storage/local/types.ts`، و`src/domain/` فارغ من الـDiff). التغييران المرئيان الوحيدان مقصودان وموثقان كاستثناء «منع وعد كاذب»: (أ) نص مشاركة الطلب الملغى لم يعد يقول «جاهز للمتابعة»، (ب) رابط توزيع بمعرّف محفظة غير صالح يُخلي الاختيار بإشعار صادق بدل اختيار أول محفظة بصمت.

## 2. بوابة البدء وخط الأساس

| البند | النتيجة |
|---|---|
| دمج الموجة الثانية في main | `81d8d253984dee410c7fd6c3e461fcd0e76f327e` مطابق حرفيًا |
| تقرير الموجة الثانية | `reports/agent-report/2026-09-17_wave-2-financial-data-foundations/report.md` موجود |
| الوحدات EXE-007..014 | الالتزامات الثمانية على main + اختباراتها خضراء |
| خط الأساس | 404 (جذر) + 1409 (prototype) = **1813 ناجحًا / 0 فاشل / 0 متخطى** — مطابق للمرجع |
| المخطط/التصدير | **35/27** في `apps/prototype-web/client/src/storage/local/types.ts:38,45` |
| الشجرة وCI | نظيفة؛ main أخضر (checks=success، Cloudflare Pages=success) |

## 3. الالتزامات المنطقية وحدود الاستعادة (Rollback)

| الالتزام | الوحدة | المحتوى المختصر | حد الاستعادة |
|---|---|---|---|
| `98b448f` | EXE-017 | نقل `recurringWorkService` إلى `application/finance/` بترويسة ملكية + حارس معماري (8 اختبارات) + عقد الملكية 40 + تسجيله في الفهرس | العودة إلى `81d8d25` تعيد الخدمة إلى `application/recurring-work/` بلا أثر بيانات (نقل ملكية فقط؛ لا كتابة مالية ولا مخطط) |
| `707c26a` | EXE-015 | عقد المشاركة الموحد: `customerShareDraft` من السجل المحفوظ وحده + حذف الشيفرة الميتة + تعديل عقد 33 | عكس الالتزام يعيد الاختيار القديم للحالات؛ لا بيانات مستخدم مسها الحذف (مسودات المشاركة لا تُخزَّن أصلًا) |
| `81952c6` | EXE-016 | عقود الروابط: `returnTo` قانونيًا + `from` قراءة توافق + تقاعد `to` + `destinationWalletId` + حذف `export/today/priority` + حارسا المعجم وDOM + تعديل عقد 26 | عكس الالتزام يعيد إنتاج `?from=` ولا حالة مخزنة تعتمد على اسم المعامل |
| `335066c` | بوابات | تمرير Prettier + حارس null صارم في اختبار exe015 | تجميلي فقط |
| `8fa1e40` | مراجعة | استعادة توكيدَي التاريخ والتسليم المسدد من ملاحظات المراجعة المستقلة | تجميلي فقط |

**الإجمالي:** 75 ملفًا، +1543/−421. لا ملف مستخدم حُذف؛ `MicroRouter.tsx` و`App.tsx` و`app/navigation.ts` و`src/domain/` و`storage/local/types.ts` **صفر تغيير**.

## 4. الوحدة EXE-017 — الملكية التقنية وحدود الخدمات — **FIXED**

### السبب الجذري
الملكية التقنية كانت واقعية في الخدمات لكن بلا تثبيت: خدمة سياسات التوزيع (`recurringWorkService`) مقيمة في `application/recurring-work/` بلا مالك معلن ومنتِجها الوحيد سطح الكتالوج (بيته أدواتي)؛ لا يوجد أي حارس معماري يمنع عودة كاتب مالي إلى الوحدة الخطأ — كل الحراس القائمة طبقات (ESLint) لا ملكية وحدات.

### الحل
1. **نقل ملكية بلا خدمة موازية:** `application/recurring-work/recurringWorkService.ts` → `application/finance/recurringWorkService.ts` (نقل git موثق 94% تعليقات) — المالك التقني لسياسات الربح والتوزيع هو المالية الآن حرفيًا في بنية الكود؛ الكتالوج مستهلك View Model عبر سياق الخدمات (استيراد الأنواع فقط يبقى مباحًا).
2. **الحارس المعماري الدائم:** `ownershipBoundaries.exe017.test.ts` — 8 فحوصات تقرأ مصادر الإنتاج حية: كاتب `allocationPolicies` الوحيد خارج التخزين هو الخدمة المالية؛ كاتب `ownerEntitlementPolicy` الوحيد هو `ownerEntitlementService`؛ الكتالوج بلا استيراد قيمي للخدمة؛ أدواتي بلا أي كاتب خارج مخزن التقديرات المستقل + خدمات سياقها محصورة في قائمة التواصل الموثقة؛ السوق والنقل (ترويسة) بلا خدمات أو كتابة؛ خدمات النقل/الاستعادة محصورة في وحدة الإعدادات وجذر التركيب؛ فحص السلامة مقيم في المالية. **مثبت أن الحارس يعض** (اختبار سلبي: حقن استخدام خدمة في السوق أفشل الفحص ثم أُزيل).
3. **خريطة الملكية المعتمدة:** `docs/contracts/40-technical-ownership-map-contract.md` — جدول الملكية الكامل (الفحص المالي، النسخ للإعدادات، السياسات للمالية، الكتالوج مستهلك، السوق/النقل مستقبليتان، أدواتي المستقلة)، عقد أهلية أدواتي (لا قراءة سجل إلا بتوافق موثق، لا كتابة إطلاقًا، لا تغيير نتيجة/رصيد/ذمة/سياسة)، أسطح التوافق الأربعة المؤقتة للموجة الرابعة، وBacklog الموجة الرابعة — مسجلة في فهرس الوثائق (12C-3).
4. **بيانات touchpoints** حُدّثت لمسار الخدمة الجديد.

### مصفوفة الملكية (قبل ← بعد)
| الوحدة | قبل | بعد |
|---|---|---|
| سياسات التوزيع | خدمة في `application/recurring-work/` بلا مالك معلن؛ الكتالوج منتجها الوحيد | الخدمة في `application/finance/` بترويسة ملكية؛ الكتالوج مستهلك عبر السياق؛ الحارس يمنع عودة الكاتب |
| أدواتي | قراءات سجل (أطراف/مشتريات/فحص) بلا عقد أهلية | عقد أهلية موثق + قائمة توافق محصورة بالضبط (شارات الحالة + سطح الفحص) يفشل الاختبار عند أي إضافة صامتة |
| السوق/النقل | «قريبًا» بلا فحص دوري | حارس: أي خدمة أو كتابة تظهر في سطحيهما تفشل البناء |
| النسخ/الاستعادة | ملكية صحيحة بلا حارس | محصورة في وحدة الإعدادات وجذر التركيب معماريًا |
| فحص السلامة | الخدمة في المالية والسطح في أدواتي بلا توثيق | الملكية موثقة (عقد 40) + حارس الإقامة |

## 5. الوحدة EXE-015 — عقد المشاركة الموحد — **FIXED**

### السبب الجذري
`collectionShareDraft` (إشعار القبض) كانت ميتة منذ ولادتها (`ced148f`): مستوردة في OrderDetail بلا استدعاء قط، وتوقيعها يقبل أرقامًا حرة (`amountMinor`/`occurredOn`) فلا تستطيع التعبير عن «مرتبطة بحدث محفوظ»؛ زر المشاركة بلا أي حارس حالة: الطلب **الملغى** (ذمته صفر بتعريف `cancelOrder`) يسقط إلى `orderShareDraft` فيُشارك كـ«جاهز للمتابعة» — وعد كاذب حرفي؛ وإشعار التسليم يقول «سُلّم اليوم» بمشاركة لاحقة مهما مضى من الوقت.

### الحل
نقطة قرار واحدة `customerShareDraft(stored, deliveredOn)` في الخدمة القانونية الوحيدة، من السجل المحفوظ حصرًا:
1. **الملغى ← نص إلغاء صادق:** «أُلغي» + تسوية العربون من `settlementStatus` المحفوظ (رُدّ/احتُفظ/بانتظار الحسم) — لا «جاهز للمتابعة» ولا وعد متابعة.
2. **المسلّم/المسدد ← إشعار تسليم بتاريخ حدث التسليم الفعلي** (`deliveredAtIso` من آخر حدث `toStatus=delivered`) — لا «اليوم» الزائف.
3. **النشط وفيه قبض قائم ← إشعار القبض من الحدث نفسه:** `standingCollectionEvent` يجد أحدث حدث قائم — القبضات بعلاقة `reversesEventId` الفردية، والعربون بالرصيد الحي (عكس العربون في الدومين على مستوى المجموع). المبلغ والتاريخ من `event.amountMinor`/`event.createdAt`، والمتبقي من السجل الحي، و`origin: {orderId, eventId}` محمول في المسودة ولا يظهر في نص الزبون.
4. **الذمة بلا قبض قائم ← تذكير**، وغير ذلك ← رسالة الطلب «جاهز للمتابعة» (صادقة للنشط).
5. **إعادة الإرسال حتمية:** البناء صرف فوق السجل — النقر المزدوج لا يكتب حرفًا (اختبار لقطة قبل/بعد متطابقة) وإعادة المشاركة تشير للحدث نفسه.
6. **الشيفرة الميتة حُذفت بإثبات:** `statementShareDraft` + `StatementShareSummary` (مصدر موازٍ لكشف الفترة لم يُستهلك أبدًا — greps على BASE تثبت صفر استدعاء)، `normalizeJordanianPhone` (بلا مدخل)، التوقيع الحر القديم، والاستيراد الميت. لا بيانات مستخدم مسّها الحذف — مسودات المشاركة لا تُخزَّن أصلًا (عقد ٣٣ §4)، والأنواع التاريخية بلا بيانات مرتبطة.
7. عقد ٣٣ عُدّل (§7) بالمصفوفة والأدلة.

### مصفوفة الحالات (المطلوبة حصرًا)
| الحالة | حدث تحصيل محفوظ؟ | يسمح بمشاركة؟ | المرجع القانوني | البيانات الظاهرة | سبب المنع |
|---|---|---|---|---|---|
| نجاح حفظ حقيقي (قبضة/عربون) | نعم، قائم | إشعار قبض | الحدث المحفوظ (id) | مبلغ الحدث + تاريخه + المتبقي الحي | — |
| reused لإرسال سابق | الحدث نفسه | الإشعار نفسه حرفيًا | نفس eventId | نفس النص | — |
| فشل/حفظ جزئي | لا كتابة | لا مشاركة (لا زر مشاركة على حالة نجاح بصري؛ الباني يقبل سجلًا محفوذًا فقط) | — | — | لا حدث محفوظ |
| طلب ملغى | (سُوّيت) | نص إلغاء صادق فقط | settlementStatus | الإلغاء + تسوية العربون | «جاهز للمتابعة» وعد كاذب |
| تحصيل معكوس كاملًا | لا قائم | يسقط لتذكير الذمة | السجل الحي | المتبقي | لا تحصيل قائم |
| حالة بلا تحصيل أصلًا | لا | تذكير أو رسالة طلب | السجل | حسب الحالة | — |

### ملاحظة إفصاح (من المراجعة المستقلة)
للطلب النشط الذي فيه قبض قائم، نص الزر صار «إشعار قبض» بدل «تذكير ذمم» السابق — هذا هو منتج EXE-015 المعتمد حرفيًا («إشعار القبض يصبح قابلًا للمشاركة فقط عند وجود تحصيل حقيقي محفوظ») وموثق في عقد ٣٣ §7.1، ومكان الأزرار وتصميمها كما هو.

## 6. الوحدة EXE-016 — عقود التنقل والروابط العميقة — **FIXED**

### السبب الجذري
المعامل العام `?to` كان محجوزًا في المعجم («للمجموعات اللاحقة») بلا منتج ولا مستهلك قط، بينما حمل دلالة ثانية خام في CashDistribution (walletId) بلا منتج أيضًا — غموض مزدوج؛ `export`/`today`/`priority` قيم معجمية ميتة؛ التحويل القديم `/orders/new` يسقط سياق الرجوع بصمت؛ ولا حارس يمنع انحراف المعجم (قراءات خام خارج التوثيق).

### الحل
1. **`returnTo` وجهة الرجوع القانونية:** المُسلسِل الوحيد `withFrom` صار `withReturnTo` يُنتج `?returnTo=` حصرًا (37 ملف إنتاج + كل الاختبارات)؛ `resolveReturnPath` تقرأ `returnTo` ثم `from` القديم **توافقًا خلفيًا للقراءة فقط** — روابط `?from=` الموجودة عند المستخدم تستمر، والإنتاج الجديد لا يديمها.
2. **تقاعد `?to`:** حُذف من المعجم ولا يُقرأ أبدًا — لم يكن له منتج قط (مُثبت بgreps على BASE) فلا رابط ينكسر؛ الرابط القديم الغامض يُهمل بأمان ولا يختار وجهة أو محفظة عشوائية (مُختبر).
3. **`destinationWalletId` الدلالية:** القيمة الصالحة تختار المحفظة من البداية الباردة وبعد التحديث؛ غير الصالحة لا تختر أول محفظة ولا تسقط بصمت — إشعار صادق + خيار «اختر محفظة…» + التحقق عند الحفظ. لا منتج داخلي اليوم — مدخل وصلة عميقة موثق (كما `focus=recurrence`).
4. **حذف الميت:** `export`/`today`/`priority` من النوع والمعجم والوثيقة — لم يكن لهن مستهلك قط.
5. **التحويلات تحفظ السياق:** `/orders/new` يمرر `returnTo` (ويرقّي `from` القديم) بدل إسقاطه؛ تحويلات `/review` و`/finance/new/owner_withdrawal_cash` كما هي (صفر تغيير في MicroRouter).
6. **حارسا الانحراف:** `routeParamRegistry.exe016.test.ts` (لا قراءة لمعامل خارج المعجم، لا معامل موثق بلا مستهلك، لا إنتاج للمعاملات المتقاعدة — المعجم الكامل 19 معاملًا) و`Nav003.exe016.dom.test.tsx` (8 اختبارات على التطبيق الحقيقي: NotFound، /review، destinationWalletId بارد/تحديث/غير صالح/مفقود، to مهمل، mode=cover، الافتراضي).
7. عقد ٢٦ عُدّل بالكامل (قواعد returnTo، المعجم الكامل، مبرر التقاعد، جدول focus نظيف).

### سجل المعاملات (23 معاملًا فُحصت)
| المعامل | القرار | المنتج | المستهلك | التوافق |
|---|---|---|---|---|
| `returnTo` | **جديد قانوني** | `withReturnTo` (كل الروابط) | `resolveReturnPath`/`useReturnPath` (47 سطحًا) | هو العقد |
| `from` | قراءة فقط | **لا** (توقف الإنتاج) | نفس القراءة | روابط قديمة تستمر |
| `to` | **تقاعد** | لم يوجد قط | حُذف | يُهمل بأمان (مختبر) |
| `export`/`today`/`priority` | **حُذفت** | لا | لم يوجد قط | لا رابط خارجها |
| `destinationWalletId` | **جديد دلالي** | لا (مدخل خارجي موثق) | CashDistribution | بدل `to` الغامض |
| `focus` (capacity/recurrence/guided-import) | باقية | capacity/guided-import داخلية؛ recurrence يدوي | Schedule/Settings | — |
| `layer`/`event`/`view`/`entry`/`source`/`product`/`estimate`/`intent`/`order`/`sale`/`purchase`/`material`/`setup`/`created`/`mode` | باقية موثقة | موثق لكل منها | موثق لكل منها | — |

## 7. الاختبارات والأوامر والنتائج

| الأمر | النتيجة |
|---|---|
| `pnpm test` (جذر) | **404 ناجحًا** (36 ملفًا) |
| `pnpm prototype:test` | **1445 ناجحًا** (204 ملفات) — **36 اختبارًا جديدًا صافيًا** فوق خط الأساس |
| **الإجمالي** | **1849 = 1813 + 36** — 0 فاشل، 0 متخطى |
| `pnpm typecheck` + `pnpm prototype:check` | خروج 0 |
| `pnpm lint` | 37 تحذيرًا (السقف الموثق — لا جديد) |
| `pnpm format:check` | نظيف |
| `pnpm text-density` + `pnpm design-guards` | داخل السقوف؛ 82 زوج تباين يجتاز |
| `pnpm guards` | أسرار (1265 ملفًا)/تركيز اختبار/touchpoints/دورات — كلها PASS |
| `pnpm prototype:build` + ميزانية الحزمة | **590,480/650,000 خام — 139,941/155,000 مضغوط — PASS** |
| `pnpm check` الكامل | **خروج 0** |
| المخطط/التصدير | **35/27 بلا تغيير** (صفر diff في types.ts وsrc/domain) |

## 8. المراجعة المستقلة (§12)

وكيل مستقل راجع الـDiff كاملًا (75 ملفًا) على أسئلة الحصر الثمانية: **PASS** — لا اسم ظاهر تغير (عدا الاستثناءين المسموحين الموثقين)، لا بطاقة انتقلت، لا ترتيب تنقل تغير (`navigation.ts` صفر diff)، لا قاعدة مالية تغيرت (`src/domain/` فارغ)، لا خدمة موازية (الدليل المزدوج محذوف)، لا رابط قديم بلا توافق، لا بيانات تاريخية مسها الحذف، لا عنصر موجة رابعة نفّذ. ملاحظتا المتابعة الاختياريتان نُفّذتا (استعادة التوكيدات) وأُفصح عن تفسير الإيصال الافتراضي (§5).

## 9. ما لم يُختبر وسببه

- **جهاز فعلي:** لم يُدّعَ — كل تحقق الواجهات محاكاة متصفح (jsdom + fake-indexeddb) بنمط R2 المعتمد في المستودع؛ لقطة الجهاز خارج قدرة هذه البيئة. عقود الملكية والمعجم تُثبت بالكود والاختبارات لا بالصور (كما نص التكليف).
- **منتج `destinationWalletId` الداخلي:** لا منتج عمدًا («لا تنشئ سلوكًا جديدًا بالتخمين») — المستهلك مختبر باردًا وتحديثًا وغير صالح؛ إضافة منتج قرار الموجة الرابعة.
- **تعدد التبويبات أثناء فتح معاينة المشاركة:** النص في المعاينة مجمد بعقد ٣٣ (محرر نص يدوي يعرض ما سيُرسل حرفيًا)؛ الاختيار نفسه يعاد اشتقاقه من السجل عند كل فتح — موثق كحد معلن.

## 10. Backlog الموجة الرابعة (مسجل دون تنفيذ)

1. نقل قسم سياسات التوزيع من الكتالوج إلى سطح مالي (الCompatibility Surface الحالي موثق في عقد 40 §4).
2. نقل صفحة فحص السلامة من أدواتي إلى المالية.
3. استبدال شارات الحالة المالية في أدواتي (المشتقة من `partyLedger`/`supplierPurchases`) بقارئ حالة بلا سجل.
4. بطاقة «النسخ الاحتياطي والبيانات» في أدواتي: رابط سياقي أو إلغاء.
5. أماكن وتصاميم أزرار المشاركة النهائية.
6. الأسماء التي ستتغير — تقرر عند تصميم الموجة الرابعة.

## 11. الخرائط الثلاث قبل وبعد

- **المشاركة:** قبل = بناة خمسة مبعثرون (واحد ميت بعلامة قبض) واختيار بلا حرس حالة؛ بعد = خدمة واحدة بنقطة قرار واحدة من السجل + origin للحدث + حذف الميت.
- **الروابط:** قبل = 19 معاملًا مقروءًا (4 ميتة/غامضة، 8 غير موثقة في المعجم) ومسلسلان خام؛ بعد = 19 موثقة (حارس مزامنة) + returnTo قانوني + تقاعد موثق + تحويلات تحفظ السياق.
- **الملكية:** قبل = ملكية ضمنية بلا تثبيت؛ بعد = عقد 40 + حارس معماري يفشل البناء عند أي انتهاك جديد.

## 12. تأكيدات الختام

- **المخطط/التصدير 35/27 بلا تغيير** ولا قاعدة مالية ولا معادلة ولا Writer مالي ولا سياسة محاسبية تغيرت (`src/domain/` و`storage/local/types.ts` صفر diff).
- **لم يُنقل أي Surface ولم يُعاد تسمية أي اسم ظاهر ولم يتغير تصميم** — الاستثناءان المسموحان (نص الملغى، إشعار الرابط غير الصالح) هما أصغر تغيير ممكن لمنع وعود كاذبة وموثقان هنا.
- لا سقف رُفع (lint 37، حزمة 650K/155K كما هي) ولا سر في الـDiff ولا ملف دخيل.
