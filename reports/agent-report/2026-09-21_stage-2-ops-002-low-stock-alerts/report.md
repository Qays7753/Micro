# تقرير تنفيذ — Stage 2 / الموجة 6: OPS-002 (تنبيهات انخفاض المخزون — حد اختياري صادق)

**التاريخ:** 2026-09-21 · **PR:** #202 · **الفرع:** `stage-2/completion-remaining` · **خط الأساس:** `771db4e75b90ab0806b72efd39742e6d2516c94e` (رأس main المتحقق — دمج الموجة 5 عبر PR #201 بCI أخضر على SHA نفسه)
**Workstream:** WS-169 · **Item:** OPS-002 · **الالتزامات:** إغلاق الموجة 5 (مع الClaim أدناه) ← كود `6b5abaa` ← توثيق (هذا الالتزام)

## 1. خط الأساس والهوية
رأس main المتحقق نفسه (`771db4e`). إغلاق الموجة 5 (OPS-008/WS-168 → VERIFIED) مع تسجيل Claim WS-169 في الالتزام التوثيقي نفسه (لا تداخل نشط: لا Claim آخر نشط أثناء التنفيذ على الفرع المخصص). فريق التحليل: عاملان قراءة-فقط (مجال/تطبيق/واجهة + فريق أحمر). **الاكتشاف الحاسم:** لا يوجد أي منطق تنبيه انخفاض قبل هذه الموجة؛ نظام النقص (عقد ٢٨ §٨) نظام كتابة مختلف لم يُمس.

## 2. الملفات المتغيرة (كاملة)
جديد: `InventoryLowStock.dom.test.tsx` (5 اختبارات). معدّل: `src/domain/inventory-material/policies.ts` (lowStockAlertState + النوع)؛ `storage/local/types.ts` (حقل lowStockThresholdsMilli الاختياري)؛ `application/preferences/{preferenceService,updateLocalPreferences}.ts` (قراءة/حفظ الحدود عبر البوابة الموحدة + ترسيخ)؛ `application/inventory/inventoryMaterialService.ts` (الاشتقاق في overview بتدهور صادق)؛ `pages/InventoryMaterials.tsx` (شريحة التنبيه + محرر الحد داخل details)؛ `scripts/text-density-count.py` (سقف 60→61 موثق)؛ الاختبارات (دومين/خدمة/حفظ حقول/عقد الخدمة العام). توثيق: التراكر + current-state §53 + هذا التقرير + سجل QA + فهرس التقارير + Views/Excel/meta.

## 3. السلوك قبل/بعد
- **قبل:** لا تنبيه انخفاص إطلاقًا؛ النقص فقط (محاولة كتابة فوق المتاح — نظام مختلف)؛ وقراءة الموضع تعرض الكمية ومعرفتها بلا أي حد.
- **بعد:** لكل مادة متتبَّعة بكمية معروفة وحد معلن في التفضيلات: شريحة «المخزون تحت الحد الذي حددته (8)» تظهر فقط عند الكمية أقل من الحد (المساواة حالة معلنة صامتة)؛ الكمية غير المؤكدة (بمعرفة أو بلا حركات) وغير المتتبَّعة وغياب الحد = لا تنبيه أبدًا؛ محرر الحد داخل `<details>` منهار (إفراغ الحد وحفظه = إزالة السياسة)؛ تعطيل قدرة المخزون يخفي المحرر ويُبقي القراءة (G-004).

## 4. العقود والـinvariants
عقد ٢٨ (§٣: متابعة لكل مادة — الحد سياسة لكل مادة بوحدتها؛ §٤ R8: غياب المعرفة لا ينبئ كذبًا؛ §٨: النقص كتابة موثقة منفصلة لم تُمس)؛ عقد ١١ (قراءة فقط). **المخطط/التصدير:** `NO_SCHEMA_OR_EXPORT_CHANGE` (35/27 — حقل اختياري في سجل تفضيلات حر؛ سوابق disabledCapabilities/backupReminderEnabled بلا رفع، مُتحقق من تاريخ git)؛ الحقل ينتقل مع التصدير/الاستيراد تلقائيًا. **المعنى المالي:** `NO_FINANCIAL_SEMANTIC_CHANGE` — لا حدث مالي/حركة/مصروف/COGS من التنبيه إطلاقًا؛ لا اقتراح شراء ولا تنبؤ طلب.

## 5. قرار التنفيذ ولماذا هو أدنى
حد اختياري لكل مادة في التفضيلات (لا على سجل المادة — يتطلب تعديل عقد ٢٨ ورفع سقف MaterialEditor 41/41 وقرار مالك مستقل؛ ولا حد عام — غير صادق عبر الوحدات)؛ اشتقاق دومين نقي قابل للاختبار؛ فشل قراءة التفضيلات = سياسة غير معروفة = لا تنبيه (تدهور صادق لا فشل صفحة)؛ سقف كثافة +1 موثق فقط (تسمية «احفظ الحد») — كل نصوص المحرر داخل جسم details والشريحة قالب مُدار بالبيانات.

## 6. الاختبارات والتحقق
| الأمر | النتيجة |
|---|---|
| `corepack pnpm exec vitest run tests/domain/inventory-material.test.ts` | 16/16 PASS (4 جديدة) |
| `corepack pnpm exec vitest run client/src/application/inventory/inventoryMaterialService.test.ts` | 39/39 PASS (6 جديدة) |
| `corepack pnpm exec vitest run client/src/application/preferences/preferencesFieldPreservation.test.ts` | 3/3 PASS (حدود تنجو من كل كاتب) |
| `corepack pnpm exec vitest run client/src/InventoryLowStock.dom.test.tsx` | 5/5 PASS |
| `pnpm check` | **EXIT 0** — 36+241 ملفًا؛ 404+1693 اختبارًا؛ lint 37/37؛ bundle 617,376 داخل الميزانية |
| `validate.py` / `check-secrets` / `check-test-focus` / `git diff --check` | صالح / PASS / PASS / نظيف |

**مسارات الفشل المغطاة:** المصفوفة الكاملة (دومين)؛ بلا حقل (سجل قديم) = لا سياسة؛ الحفظ/الإزالة/الرفض عبر البوابة الموحدة + إعادة القراءة؛ بلا كتابة عند القراءة (مطابقة لقطة المخزن الكاملة)؛ فشل قراءة التفضيلات = تدهور صادق؛ DOM: صمت بلا حد، تنبيه بقيمته، صمت المساواة، الحفظ يفعّل بلا إعادة تحميل، تعطيل القدرة يبقي القراءة ويخفي المحرر.

## 7. QA الحي والجهاز
`NOT_EXECUTED` — لا متصفح حي في البيئة؛ شريحة نصية ببنية قائمة + محرر داخل details ببنية قائمة. سجل QA: `docs/quality/stage-2-ops-002-qa-2026-09-21.md`.

## 8. التراكر والعروض
OPS-002: READY → CLAIMED → IN_PROGRESS → IN_REVIEW (pr:202). WS-169: CLAIMED → IN_PROGRESS → IN_REVIEW (pr:202) — سُجل مع توثيق الموجة (لا Claim آخر نشط). إغلاق الموجة 5 (→ VERIFIED عند 771db4e) في نفس الالتزام. Views/Excel/meta مُجددة وvalidator ناجح.

## 9. القيود والأدلة التاريخية
الحد على سجل المادة (أدق دلاليًا) قرار مالك مستقل؛ الحقل ينتقل مع التصدير بلا بند تحقق جديد (سلوك disabledCapabilities نفسه — نقطة نظافة اختيارية للمستقبل)؛ QA الحي NOT_EXECUTED.

## 10. حدود الاستعادة
حد آمن أخير: `771db4e`. الرجوع = عدم دمج PR #202 — الحدود المخزنة تفضيلات اختيارية لا يمسها الرجوع. آخر التزام آمن على الفرع = `6b5abaa` حيث `pnpm check` أخضر EXIT 0.

## 11. الكتابات المنفذة وغير المنفذة
**نُفذت:** فرع + دفع؛ الالتزامات؛ PR #202؛ تحديثات تراكر المصدر مع Views/Excel/meta. **لم تُنفذ:** لا دفع مباشر لmain؛ لا دمج قبل CI + manifest؛ لا حذف/تسمية فروع؛ لا وصول لDocuments؛ لا نشر/إعدادات؛ `NO_TOKEN_STORED`؛ لا تنظيف فروع.
