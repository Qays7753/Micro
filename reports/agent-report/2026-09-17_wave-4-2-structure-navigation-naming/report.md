# تقرير Wave 4.2 — الهيكل والتنقل والتسمية

**BASELINE_SHA (بداية الموجة):** `281a2878b61870792f76d651d0d822c7f0a5c0e1` (Merge PR #165 — Wave 4.1)
**التاريخ:** 2026-09-17 · **الحزم:** خمس (P-4.2-1 → P-4.2-5) · **الحالة:** قيد التنفيذ — يُحدَّث بعد كل حزمة.

---

## 1. الخلاصة

تنفذ هذه الموجة قرارات المالك المعتمدة `F01..F14` (انظر `owner-decisions.md`) على بنية التطبيق: تثبيت App Shell والتسمية الموحدة، تنظيف «أدواتي»، تجميع «البيانات والنسخ الاحتياطي» في الإعدادات، إنشاء سطح «المالية ← المزيد» مع نقل مدخل سلامة الحسابات وتنفيذ خطة REV-003، ونقل سياسات التوزيع إلى المالية مع تثبيت البديل الكنوني `/` للكتالوج والمنتج المرئي لـ`destinationWalletId`.

**خط الأساس للاختبارات عند البداية:** 1849 ناجحًا (404 root/domain + 1445 prototype/web)، 0 فاشل، 0 متخطى، Schema/Export: 35/27.

## 2. حالة الحزم

| الحزمة | الفرع | الحالة | PR | Head SHA | Merge SHA | CI |
|---|---|---|---|---|---|---|
| P-4.2-1 App Shell والتنقل والتسمية | `feat/wave-4-2-1-shell-navigation-naming` | منفذة — بانتظار PR/CI | — | — | — | — |
| P-4.2-2 تنظيف «أدواتي» | `feat/wave-4-2-2-clean-tools` | منفذة — بانتظار PR/CI | — | — | — | — |
| P-4.2-3 البيانات والنسخ الاحتياطي في الإعدادات | `feat/wave-4-2-3-settings-data-backup` | منفذة — بانتظار PR/CI | — | — | — | — |
| P-4.2-4 المالية ← المزيد وسلامة الحسابات | `feat/wave-4-2-4-finance-more-integrity` | منفذة — بانتظار PR/CI | — | — | — | — |
| P-4.2-5 السياسات والمنتجات وdestinationWalletId | `feat/wave-4-2-5-policies-catalog-wallet-destination` | منفذة — بانتظار PR/CI | — | — | — | — |

---

## 3. قرارات المالك النهائية

موثقة كاملة في **`owner-decisions.md`** (F01..F14: الحالة، الفروق عن توصيات Wave 4.1، موضع التنفيذ، أسباب التأجيل). هذه الوثيقة هي سجل الاعتماد المعتمد لهذه الموجة، وتقرير Wave 4.1 لم يُمس (Audit Trail محفوظ).

## 4. الفروق الجوهرية عن توصيات Wave 4.1

1. **F02:** اسم التبويب الرسمي «ملخص الفترة» بدل «شو صار خلال الفترة» (الموضع نفسه: بجوار «التغطية والتعادل»).
2. **F07:** رفض إضافة «نزّل النص كملف» نهائيًا لهذه المرحلة (كانت توصية بإضافة منخفضة الأولوية) — ولا زر معطل.
3. **F09:** اسم السطح الموحد «شو عليّ؟» بدل «مين عليّ؟».
4. **F11:** توقيت عرض الجهاز المحلي بدل توصية عمّان — الوثائق النصية تُصحح في 4.2 والتنفيذ في 4.4.

## 5. نتائج الحزم

_(تُملأ بعد كل حزمة — انظر §5.1 أدناه)_

### 5.5 P-4.2-5 — السياسات والمنتجات وdestinationWalletId

**منفذة (F02 + F04 + T7).**
- **السياسات (F02/T3):** انتقل السطح المرئي لسياسات الربح والتوزيع من الكتالوج إلى **`FinancePoliciesSection`** داخل «ملخص الفترة» بجوار «التغطية والتعادل» — **نقلًا لا نسخًا** (حُذف `CatalogPoliciesSection.tsx` ونُقل منطق الحفظ/الإيقاف/النسخ حرفيًا). الخدمة `recurringWorkService` لم تتحرك من `application/finance/` ولا Writer جديد — الكاتب القانوني نفسه عبر سياق الخدمات. إغلاق ملكية المنتجات موثق في عقد 40.
- **منتجاتي وخدماتي:** بقي لتعريف ما يُباع — يعرض القراءات ونتيجة السياسة **ورابطًا سياقيًا واحدًا** إلى `/finance?view=period` (بـ`returnTo=/catalog`) — **بلا أي كتابة سياسة** (يحرسه فحص مصادر). البيع السياقي بـ`product=` و`returnTo` كما هو.
- **البديل الكنوني (F04):** `/catalog` → `/` مع العقد (26 §2.2) والاختبار في الالتزام نفسه؛ المسارات الأعمق تعود لقائمة الكتالوج كما هي.
- **destinationWalletId (T7):** دفتر المحفظة يعرض **«وزّع على هذه المحفظة»** عند وجود كاش غير موزع قابل للتوزيع فقط (قراءة موقف المالية — نفس مصدر شريط المالية؛ مجهول = لا زر ميت)، ويفتح `/cash/distribute?destinationWalletId=<id>&returnTo=/cash/wallet/<id>` — المحفظة مختارة مسبقًا (سلوك المستهلك القائم المختبر: بداية باردة/تحديث/معرّف غير صالح بإشعار صادق) والرجوع يعيد إلى الدفتر. المنتج موثق في عقد 26 §3.
- إفصاح تدريجي: طبقة السياسات لا تقرأ شيئًا حتى تُفتح — المستوى الأول للمالية لا يدفع كلفة القراءة.
- سقف كثافة النص: Finance 263→294 موثقًا (انتقال سطح بنصوصه؛ الكتالوج هبط 122→91 بالنقل نفسه).
- الحماية: لا تغيير منطق توزيع الكاش أو Idempotency أو دفتر المحفظة ماليًا أو مصدر كتابة جديد أو تصميم 4.3.

### 5.4 P-4.2-4 — «المالية ← المزيد» وسلامة الحسابات وREV-003

**منفذة (F01 + F05 + REV-003).** أُنشئ السطح `/finance/more` (صفحة `FinanceMore.tsx` + مسار في `MicroRouter` + تصنيف surface يبقي الشريط) بمداخل منظمة: **سلامة الحسابات أولًا** (بطاقة وعد تفتح `/tools/integrity` بمسمى N-22)، ثم **ملخصات وتقارير** (كشف الفترة، القارئ الكامل، سجل الأحداث `?layer=events`، سجل التصحيحات `?layer=corrections`)، ثم **أدوات مالية متقدمة** (عدّ الصندوق)، ثم **المالك والسياسات في مواضعها المعتمدة** (الدفتر الموحد، السحب، الاستثمار؛ السياسات بموضعها القائم حتى تنقلها P-4.2-5 إلى «ملخص الفترة»). أُضيف مدخل «المزيد من المالية» من المستوى الأول وحُذف سطر الفحص المباشر منه (انتقال WF-006).

**REV-003 (الخيار أ — التوافق الكامل):** طبقتا الأحداث والتصحيحات بقيتا في `/finance` — **كل منتجي `?event=` (٨ منتجات عبر 8 ملفات) و`?layer=` (٤ منتجات) كما هم بلا تغيير واحد**؛ حُصروا ووثقوا في عقد ٢٦ §3.3، والسطح الجديد يفتحهما بالروابط القائمة نفسها ب`returnTo=/finance/more`. الحرسان القائمان (U001.dom، G5Activity.dom) بقيا أخضرين بلا تعديل — الدليل أن الروابط القديمة لم تنكسر.

**F05:** المسار التقني `/tools/integrity` بقي؛ h1 صار «سلامة الحسابات»؛ **زر الرجوع ديناميكي حسب المصدر** (returnTo) — «المزيد من المالية»/«المالية»/«الإعدادات»/«رجوع» — ولا يعرض «أدواتي» لمن دخل من المالية؛ البديل الكنوني انتقل إلى `/finance/more` (تحديث موثق واحد في عقد ٢٦ §2.2 + navigationContract + اختباراته). الحماية: لا تغيير منطق الفحص أو عدد فحوصه أو أي Writer؛ سقف كثافة النص للسطح الجديد قُفل عند 35 (قياس أول) وToolsIntegrity 57→61 موثقًا (تسميات الرجوع الديناميكي).

### 5.3 P-4.2-3 — «البيانات والنسخ الاحتياطي» قسمًا موحدًا في الإعدادات

**منفذة (T2).** دُمجت الطبقتان القديمتان «بيانات ونسخ احتياطي محلي» و«بيانات البداية والاستعادة» في قسم واحد مسمى **«البيانات والنسخ الاحتياطي»** داخل `/settings` بترتيب «الأقل تدميرًا أولًا» (WF-025): الحقيقة → القفل المحلي → التصدير → الاستعادة الآمنة (بمعاينتها ونسخة ما قبل الاستبدال ونتيجة الفحص) → الإدخال الافتتاحي الموجه (ببطاقته المركّزة) → تذكير النسخ → التشخيص المحلي → دقة المال → التصفير أخيرًا. حُفظ السلوكان القائمان: إشعار التخزين يفتح القسم ويُمرّر النظر إليه، والوصلة العميقة `?focus=guided-import` تفتحه وتُمرّر إلى بطاقة الإدخال الافتتاحي. لا خدمة ولا نص ولا مسار ولا حارس تغير (PIN/المعاينة/نسخة ما قبل الاستبدال/EXE-014 حرفيًا كما كانت)؛ Schema/Export 35/27 لم يُمس.

### 5.2 P-4.2-2 — تنظيف «أدواتي»

**منفذة (F03 + T4/T5/T6).** أُزيل من `/tools`: قسم «حالة الوحدات» كاملًا (صفوف الحاسبة/النسخ/الفحص/المخزون/الكتالوج/المواعيد/الموردين/دفتر الناس/السوق)، قراءتا `partyLedger.read()` و`supplierPurchases.readSummary()`، بطاقة النسخ الاحتياطي، وصف «السوق والتوصيل» الميت. بقي: الحاسبة + التقديرات المحفوظة + جسر التقدير→المسودة + جهاز الحالة (تحميل/خطأ/جاهز). أُضيف الشرح الصادق الحرفي: «أدوات مستقلة للحساب والتقدير، لا تغيّر سجلات مشروعك». حُدث عقد الملكية 40 (§4 أُغلق سطحا التوافق المنظفان؛ §6 Backlog مؤشر منفذ) وحارس EXE-017 (قائمة أدواتي تقلصت إلى costEstimates/dataVersion/notifyDataChanged) في نفس الالتزام، مع حارس DOM جديد `ToolsCleanup.w42.dom.test.tsx`. لا خدمة حُذفت ولا مسار — التنظيف سطحي قارئ فقط.

### 5.1 P-4.2-1 — App Shell والتنقل والتسمية

**منفذة.** خمسة التزامات: وثيقة اعتماد القرارات، توحيد اسم الكتالوج «منتجاتي وخدماتي» (navigation.ts + حارس)، «سجّل» بالشدة في كل أزرار التسجيل (الرئيسية/الورقة/المالية/الموردين/الأساس/الوقت/الهدر)، توحيد «المالية» في نصوص الرجوع والعناوين (REV-010 كاملًا: Parties/Suppliers/CashWallets/OwnerEntitlement/Statement/FinancialEventEditor/G5DeclarationEditor/OwnerWithdrawalEditor/Home/Finance)، وإعادة تسمية تبويب الفترة إلى «ملخص الفترة» (F02). الاختبارات الحارسة حدثت في نفس الالتزامات — 1849/1849 خضراء، lint 37/37، bundle داخل الميزانية. الحماية محفوظة: لا مقعد سادس، لا تغيير Routes أو مصادر كتابة أو Schema/Export.

---

## ما بقي لـWave 4.3

- F06: زر «شارك إشعار القبض» في نتيجة التحصيل (مع تحديث عقد 33 §3/§7.6 — REV-009).
- F08: تطبيق معجم «وزّع/غطِّ/سُجّل التوزيع ✓» على شاشات الكاش (P-4.3-5).
- F09: تصميم سطح «شو عليّ؟» وبطاقات الموقف القابلة للنقر (P-4.3-3).
- F13: الحفظ بزر Enter بضوابطه (P-4.3-2).
- إعادة التصميم البصري للشاشات وفق Wireframes (كل شاشة تُلمس مرة واحدة).

## ما بقي لـWave 4.4

- F10: صيغة التاريخ والوقت الموحدة (12 ساعة + الفاصلة العربية).
- F11: تنفيذ توقيت الجهاز في طبقة العرض مع الاختبارات الشاملة.
- F14: بنود قبول Zoom/تكبير النص/النص الطويل/CTA — وإن فشل الوصول يُسجل ترحيل rem متطلبًا.
