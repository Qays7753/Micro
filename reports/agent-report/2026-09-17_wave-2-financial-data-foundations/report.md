# تقرير الموجة الثانية — الأساسات المالية والبيانات — Micro

**التاريخ والوقت (Asia/Amman):** 16–17 أيلول/سبتمبر 2026 — البدء ~16:00 (16/09)، الاكتمال 17/09
**BASE_SHA:** `5c54d8ab17a56637773f1fdaf9ae75455fc69777` — ناتج دمج PR #162 في main (بوابة البدء §2)
**HEAD_SHA (رأس فرع الإصلاح قبل التوثيق):** `a6b543df29576be0a1beba73522cd76232f9b970`
**الفرع:** `fix/wave-2-financial-data-foundations` من `5c54d8a`
**المرجع:** تقرير التدقيق العميق `reports/agent-report/2026-09-16_13-00-deep-system-audit/report.md` (قُرئ من فرعه عبر PR #161، وعُلّمت الفروق عن خط الأساس بعد الموجة الأولى)

---

## 1. الخلاصة التنفيذية

نُفّذت الوحدات الثماني (EXE-007..EXE-014) كاملة من خط الأساس `5c54d8a` عبر ثمانية التزامات منطقية + التزام بوابات، بترتيب التكليف §14 (008 → 009 → 007 → 010 → 011 → 012 → 013 → 014). النتيجة: **الوحدات الثماني كلها FIXED** — كل معايير القبول مثبتة باختبارات انحدار صريحة (56 اختبارًا جديدًا فوق خط الأساس 1757 → **1813 ناجحًا / 0 فاشلًا / 0 متخطى**) + تحقق حي 390×844 بـ16 لقطة + اختبار تسوية الدورة الكاملة دائم داخل المستودع يشتق كل رقم من خدمات حية لا من قيم Literal.

بوابة البدء (§2): PR #162 كان أخضر وقابلًا للدمج برأسه الموثق `33693f4`، فدُمج إلى main بcommit دمج `5c54d8a` وتحقق أن إصلاحات الموجة الأولى (تدوير مفتاح التوزيع، مفاتيح رد العربون، ثابت أنواع المصادر) موجودة فيه، ثم فُتح فرع الموجة الثانية. **المخطط/التصدير 35/27 بلا تغيير** (لا سطر واحد في `storage/local/types.ts`).

## 2. خط الأساس والالتزامات

| الالتزام | الوحدة | المحتوى المختصر |
|---|---|---|
| `05ab405` | EXE-008 | حارس افتتاح ثانٍ على حدود المخزن (IndexedDB داخل المعاملة + Memory) + رابط «شراء مورّد» في دفتر المحفظة |
| `df0484e` | EXE-009 | رحلة واحدة لمال المالك، حارس تكرار تقاطعي بين النموذجين التاريخيين، `owner_event` نوع مصدر قانوني، إعادة توجيه المسار القديم |
| `6c16f89` | EXE-007 | مالك واحد لإدخال المصروف (`expenseFormModel.ts`) — الاختصار وضع مختصر للرحلة نفسها: بند إلزامي، تاريخ قابل للتحرير، القواعد نفسها |
| `e3d2848` | EXE-010 | عكس موثق لتحصيل البيع المباشر (مطابقة بالمرجع القانوني، معاملة ذرية) + عكس العربون النشط قبل التسليم بإعادة استخدام آلة فك التخصيص القائمة |
| `4766847` | EXE-011 | لوحة استمرار «استلام المخزون» بعد حفظ شراء مرتبط بمادة + حالة «بانتظار الاستلام» رحلة لا نصًا ساكنًا |
| `0985de2` | EXE-012 | مدخل «ضبط جرد — قرار مالك» منفصل عن الهدر خطوة واحدة، عقد العنوان والحد الحقيقي «الضبط ليس هدرًا» |
| `5cfffb5` | EXE-013 | اختبار الدورة الكاملة الدائم على المحولين + توسيع حرس العائلات (loanContext/assetContext) للمسار العام |
| `0e18f92` | EXE-014 | سجل العائلات المؤثرة التعاقدي (33 عائلة، اكتمال وقت الترجمة) + نسخة احتياطية مُتحقّقة قبل الاستبدال + فحص سلامة بعد الاستيراد الافتتاحي |
| `a6b543d` | بوابات | تحميل كسول لمكدس النقل/الاستيراد يستعيد هامش ميزانية الحزمة (590,397/650,000) + سقوف كثافة الموجة بأرقام موثقة + صيانة اختبارات العقود الجديدة |

59 ملفًا: +5341/−258. لا ملف حُذف؛ لا تغيير تنقل عام ولا أدواتي ولا تنسيقات تاريخ/عملات ولا RTL (قواعد النطاق §5).

## 3. مصفوفة مسارات الكتابة — قبل وبعد

| الوحدة | مدخل الواجهة | الكاتب القانوني (بعد) | مفتاح العملية | العكس/التصحيح | الفجوة قبل الموجة |
|---|---|---|---|---|---|
| EXE-007 | محرر «المالية» + اختصار الرئيسية (وضع مختصر للرحلة نفسها) | `expenseFormModel.ts` وحده يملك القواعد؛ الطريقان ينتجان الحدث عبر `projectFinance.record` بالقيم نفسها | لكل تأكيد (قائم من الموجة الأولى) | عكس الحدث القائم | نموذجان: الاختصار يصنّع بندًا افتراضيًا، تاريخ اليوم فقط، تحقق/رسالة مختلفان |
| EXE-008 | `/cash/wallet/new` + `/cash/wallet/:id/opening-later` + التحويل والضبط | `cashContinuityService` فوق حارس حدود المخزن `cashContinuityCommitGuard` | قائم + رفض ضمن المعاملة | `cash_adjustment` موثق بالسبب والاتجاه | حد الخدمة يرفع الافتتاح الثاني لكن حدود المخزن كانت تقبله بمفتاح جديد |
| EXE-009 | `/finance/new/:type` + `/finance/withdraw` (المسار القديم يعاد توجيهه) | محررا الاستثمار/السحب فوق `projectFinance.record` + حارس تكرار تقاطعي في `ownerEntitlementService` | لكل تأكيد + `:attribute` للمسند | عكس الحدث يزيل علم التكرار | نموذجان تاريخيان بلا حارس تقاطعي؛ مسار `/finance/new/owner_withdrawal_cash` موازٍ؛ طريق مسدود عند سياسة بلا محافظ |
| EXE-010 | `SaleCollectionReversalSection` في محرر البيع + لوحة «عكس عربون نشط» في تفاصيل الطلب | `saleCollectionReversalService` + `fulfillmentService.reverseDeposit` (إعادة استخدام `buildDepositAllocationReversals` + `commitDepositRefundSettlement`) | جذر المفتاح + مرجع السطر الأصلي | هو نفسه العكس الموثق (`reversesEntryId` + سبب إلزامي) | لا عكس لتحصيل البيع المباشر (تعديل ينقص المقبوض بلا قيد كاش)؛ العربون النشط لا يُعكس قبل التسليم |
| EXE-011 | لوحة الاستمرار بعد حفظ الشراء + رابط حالة «بانتظار الاستلام» | آلة الاستلام القائمة (`receipt`) بمفاتيحها وحدودها | لكل استلام (قائم) | عكس الاستلام القائم | خروج صامت لقائمة المشتريات؛ الحالة نص ساكن |
| EXE-012 | زر «ضبط جرد — قرار مالك» من صفحة المخزون (خطوة واحدة) | محرر الضبط القائم `/inventory/movement/adjust` | لكل ضبط (قائم) | «تراجع» موثق لا حذف | المحرر كامل لكن يتيم — الزر الوحيد يفتح الهدر |
| EXE-013 | اختبار دائم داخل CI (لا مدخل مستخدم) | الخدمات الحية كلها على المحولين (Memory + fake-indexeddb) | — | حرس العائلات يوجه للسطح القانوني | التشخيص خارج المستودع؛ الحرس العام يستثني القروض/الأصول بلا توجيه |
| EXE-014 | الإعدادات: الاستيراد الافتتاحي + الاستعادة الكاملة | بوابة `INFLUENTIAL_SNAPSHOT_FAMILIES` (33 عائلة) + `confirmImport` ينشئ نسخة ما قبل الاستبدال ويتحقق منها | — | النسخة الاحتياطية نفسها قابلة للاستعادة | بوابة الافتتاح تفحص 14/30 حقلا فقط؛ لا نسخة قبل الاستبدال للاستيراد؛ لا فحص بعد الاستيراد الافتتاحي |

## 4. الوحدات — السبب الجذري والحل والدليل

### EXE-008 — حوكمة افتتاح وتصحيح الكاش (CASH-001) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | الخدمة ترفض الافتتاح الثاني (`cashContinuityService.ts:175-180`) لكن `commitCashContinuity` في المحولين كان يقبل أي `opening_balance` بمفتاح جديد — الحماية طبقة واحدة تنكسر بأي مستدعي يتجاوز الخدمة |
| الحل | حارس خالص مشترك `cashContinuityCommitGuard.ts` (نمط `supplierScheduleCommitGuard`): IndexedDB يمسح الحاوية داخل المعاملة ويُجهض `onabort` برفض `storage_stale` (لا كتابة جزئية)؛ Memory يعكسه؛ تعليقا العقد فوق `openWallet`/`recordOpeningBalanceLater` يثبتان قاعدة الطبقتين. رابط دفتر المحفظة لـ`supplier_purchase` أُضيف (كان نوعًا قانونيًا بلا عرض) |
| الاختبارات | `cashContinuityOpeningGuard.test.ts`: 6 سيناريوهات × المحولين — الأول يمر، الثاني مرفوض بلا كتابة، إعادة الإرسال reused، محفظة أخرى تمر، الضبط يمر، افتتاحان في دفعة واحدة مرفوضان داخل المعاملة |
| الحي 390×844 | ضمن رحلة الجلسة: افتتاح واحد فقط، والمعادلة 212.50 = 212.50 + 0.00 بعد كل سيناريوهات الموجة (التحقق النهائي §5) |

### EXE-009 — مال المالك والسحب الشخصي (OWN-001/002/003) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | نموذجان تاريخيان (الأحداث المالية مقابل حركات دفتر المالك) بلا حارس تكرار تقاطعي — الاستثمار نفسه يُسجل حدثًا وحركة فيُحسب مرتين؛ مسار `/finance/new/owner_withdrawal_cash` رحلة ثالثة موازية؛ عند سياسة «من محفظة» بلا محافظ يقف المحرر بطريق مسدود؛ `owner_draw` بلا كاتب واجهة و`ownerDrawMinor` لا يُعرض |
| الحل | `findCrossModelOwnerDuplicate` (اتجاه+مبلغ+تاريخ، أي النموذجين) + بطاقة `CrossModelDuplicateNotice` توقف الحفظ بإفصاح وخيار «عملية جديدة مختلفة» صريح؛ سؤال وجهة الكاش للاستثمار ومصدره للسحب مع إسناد تخصيص `owner_event`؛ المسار القديم يعاد توجيهه إلى `/finance/withdraw` محافظًا `?from`؛ الطريق المسدود صار «أنشئ محفظة» كخطوة تالية؛ `ownerDrawMinor` يُعرض مشروطًا عند وجوده؛ اتحادا `sourceRefKind` المنسوخان يدويًا صارا مشتقين من ثابت الدومين (يغلق W1-OUT-1) |
| الاختبارات | 6 خدمة (كشف تقاطعي/لا موجب كاذب/العكس يزيل العلم/دفتر يعلّم) + 7 DOM (وجهة افتراضية، استدعاء الإسناد، الحارس يوقف ويُتجاوز بتأكيد ×2، سؤال المصدر، لا طريق مسدود، إعادة التوجيه) + 209 انحدار |
| الحي 390×844 | `exe009-01` الشاشة بالإجرائين، `exe009-02` السحب — والدليل الرقمي في المعادلة 4 أدناه (8500 عبر النموذجين معًا) |

### EXE-007 — توحيد ملكية إدخال المصروف (FIN-006) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | الاختصار يملك قواعده الخاصة: بند مصنّع («مصروف نقدي»)، تاريخ اليوم فقط، تصنيف مثبت — فينتج حدثًا مختلف الدلالة عن المحرر الأساسي لنفس المصروف |
| الحل | `expenseFormModel.ts` مالك واحد: `EXPENSE_NOTE_REQUIRED_MESSAGE`، `expenseSourceHint`، `expenseSourceRuleViolation`، `defaultExpenseSource`، `coverExpenseFromWallet` (الإسناد بعد الحفظ) — الاختصار والمحرر يستهلكانه حرفيًا؛ `compactExpenseClassification` قيد مقصود موثق (التصنيف في الاختصار رقائق سريعة فقط) |
| الاختبارات | 5 اختبارات إثبات جديدة أبرزها: تكافؤ المدخلين (نفس سياق الحدث والدلتات حرفيًا) + نقر مزدوج = كتابة واحدة + البند المطلوب لا يُتجاوز من أي مدخل؛ تحديث مضاعفي QuickExpenseSource/QuickActionSheet للعقد الجديد |
| الحي 390×844 | `exe007-01` الاختصار بالبند الإلزامي والتاريخ القابل للتحرير، `exe007-02` المحرر الأساسي — نفس الأسئلة |

### EXE-010 — عكس تحصيل البيع والعربون (COL-001 + AUD-NEW-04/05) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | تحصيل البيع المباشر لا عكس له: التعديل كان ينقص `collectedMinor` بلا قيد كاش فيصبح غير الموزع سالبًا؛ العربون النشط محصور بين «ملغى» (سياسة `policies.ts:1112-1125` قبل الموجة) والدفع، فخطأ التسجيل قبل التسليم بلا مخرج موثق |
| الحل | (أ) `saleCollectionReversalService`: معاينة بأرقام حقيقية، مطابقة قيد التخصيص بالمرجع القانوني نفسه (لا بالمبلغ)، و`commitDirectSaleCollectionReversal` معاملة ذرية في المحولين (فحص الهوية + reused صادق + حارس سقف) برابط `reversesEntryId`. (ب) حدث دومين `deposit_reversed` + `reverseActiveDeposit`: حراسة ما قبل التسليم، عكس جزئي بسقف تراكمي طبيعي، وإصلاح احتساب مزدوج اكتشفه الاختبار (القائم = المقبوض − المحتفظ فقط)؛ `fulfillmentService.reverseDeposit` يعيد استخدام `buildDepositAllocationReversals` و`commitDepositRefundSettlement(eventType)` المعممة — لا منطق موازٍ |
| الاختبارات | 4 fulfillment + 6 خدمة + 2 DOM + انحدار 75 (finance/collections/direct-sales/G4/G6/adapters) + 14 دومين + 105 transfers |
| الحي 390×844 | `exe010-01/02` عكس التحصيل من التفاصيل وظهور القيد؛ `exe010-03` لوحة عكس العربون النشط (المبلغ المعبأ + السبب الإلزامي)؛ `exe010-04` بعد العكس: لا عربون قائم، المتبقي عاد 25.00، وسجل الطلب يعرض «تسجيل العربون» و«عكس عربون نشط» معًا بلا حذف |

### EXE-011 — من الشراء إلى استلام المخزون (PUR-001 + AUD-NEW-08) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | آلة الاستلام كاملة (حتمية، جزئي، حدود، عكس) لكن اكتشافها معدوم: حفظ الشراء يخرج صامتًا إلى القائمة (S1-07)، وحالة «بانتظار الاستلام» نص ساكن في بطاقة المادة |
| الحل | بعد حفظ شراء مرتبط بمادة متتبعة: لوحة استمرار «استلام المخزون» + خروج صريح؛ الشراء غير المرتبط/المادة غير المتتبعة يحتفظان بالخروج الكلاسيكي (لا CTA مضلل)؛ سطر الحالة صار رحلة إلى `/inventory/movement/receipt?material=<id>` مع بقاء اختيار الشراء صريحًا (OPS-001) |
| الاختبارات | `PurchasingBridgeExe011.dom.test.tsx`: 5 اختبارات (الاستمرار، الرابط العميق، الخروج، لا CTA لغير المرتبط/غير المتتبع، رابط بطاقة المادة) + انحدار group2 F6 لآلة الاستلام |
| الحي 390×844 | `exe011-01` اللوحة بعد الحفظ؛ `exe011-02` الرحلة تعبئ الشراء الصحيح لا الأول (`?purchase=<id>`)؛ `exe011-03` رابط الحالة؛ استلام جزئي 12/20 وحدة بقيمة كاملة 12/12 — الرصيد 22 بلا احتساب مزدوج والكاش وذمة المورد مستقلان عن الاستلام |

### EXE-012 — ضبط المخزون الموثق (AUD-NEW-06) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | محرر الضبط كامل لكن يتيم: زر واحد «هدر أو ضبط» يفتح الهدر حصرًا — الإجراء المالك مدفون بلا مدخل |
| الحل | زران صريحان من صفحة المخزون: «هدر مادة» و«ضبط جرد — قرار مالك» (خطوة واحدة، Deep Link صالح بلا اختيار صامت)؛ عقد عنوان المحرر: سبب إلزامي، لا حذف حركة، مفصول عن الهدر اسمًا وسببًا وأثرًا؛ بطاقة الحقيقة «الضبط ليس هدرًا — تصحيح جرد بمعنى محايد لا يُسجَّل خسارة»؛ حد مرحلي موثق: لا نظام أدوار في النموذج المحلي، فالتسمية والتحذير يحملان صفة قرار المالك |
| الاختبارات | `InventoryAdjustExe012.dom.test.tsx`: 4 اختبارات (مدخلان منفصلان + رابط عميق، زيادة بتكلفة مجهولة موسومة لا صفر واثق ولا هدر، نقص + عكس موثق يعيد الكمية بلا حذف التاريخ + reused للمفتاح، تموضع قرار المالك) |
| الحي 390×844 | `exe012-01` المدخل بوصف قرار المالك؛ `exe012-02` الحركة في السجل: «ضبط مادة · السبب: جرد فعلي بعد العد — 3 قطع غير مسجلة · قيمة غير محددة بعد» مع زر تراجع — الرصيد 22→25 |

### EXE-013 — التسوية الشاملة وحرس العائلات (ACC-001 + AUD-NEW-15) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | تشخيص الدورة الكاملة كان خارج المستودع (سيناريو تشغيل يدوي)؛ حرس التصحيح العام `familyCorrectionGuard` يستثني القروض والأصول بلا توجيه للمستدعي |
| الحل | `fullCycleReconciliation.exe013.test.ts` (447 سطرًا): الحلقات العشر عبر الخدمات الحية على **المحولين معًا** — افتتاح، بيع بدين وقبض جزئي، مصروف، شراء بدين ودفعتان، استثمار وسحب مالك، قرض وسداد، مادة واستهلاك، أصل وإهلاكان، عربون محتفظ مصنف إيرادًا، تخلص من أصل — ولا Fixtures تضع القيم النهائية؛ `familyCorrectionGuard` صار يرفض عكس/تصحيح/حذف أي حدث يحمل `loanContext` أو `assetContext` من المسار العام ويوجه لسطح العائلة، مع بقاء `restoreEvent` مفتوحًا للبيانات التاريخية (إعادة تسجيل حرفية) ومسارات العائلات القانونية تعمل (مثبتة بالاختبار) |
| المعادلات المثبتة (كلها مشتقة من مشي الخدمات) | **الكاش المسجل 24,700 = المحافظ 18,000 + غير الموزع 6,700** (مسار المصادر: 20,000 افتتاح + 3,000 قبض طلب + 1,500 قبض عربون محتفظ + 10,000 استثمار − 300 مصروف − 900 دفعتا شراء − 1,500 سحب − 2,000 قرض + 800 سداد − 6,000 أصل أول − 2,400 أصل ثانٍ + 2,500 مقابل التخلص)؛ **الذمم المدينة 2,000** (5,000 − 3,000)؛ **الدائنة 300** (1,200 − 900)؛ **مال المالك 8,500 مرة واحدة عبر النموذجين** (10,000 − 1,500)؛ **القيمة الدفترية 5,500**؛ **القروض القائمة 1,200**؛ **نتيجة الفترة 4,300 incomplete بأسبابها** (إيراد 5,000 − تكلفة فعالة 1,500 − مصروف 300 − إهلاك 700 + تخلص 300 + عربون محتفظ 1,500)؛ **لا حدث يُحسب مرتين** (التصنيف لا يضيف كاشًا، والاستهلاك يدخل التكلفة مرة)؛ جناح النزاهة يعيد **PASS الكلي** على السيناريو كله |
| المحولان | Memory + fake-indexeddb — نفس الأرقام حرفيًا |

### EXE-014 — الاستيراد والاستعادة دون فقد (DATA-001 + AUD-NEW-09) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | بوابة الاستيراد الافتتاحي تفحص 14 حقلا فقط من 30 (التفضيلات وملف المالك والمبيعات المباشرة والأصول والقروض ودفتر المالك وغيرها خارجها) — جهاز ممتلئ يُستورد فوقه افتتاحًا فيُمسح جزئيًا بصمت؛ الاستبدال ذري لكن بلا نسخة قابلة للاسترجاع قبل الاستيراد الافتتاحي، وبلا فحص سلامة بعده |
| الحل | `influentialSnapshotFamilies.ts`: سجل تعاقدي واحد لكل العائلات المؤثرة (33 عائلة) **مكتمل وقت الترجمة فوق `keyof LocalStoreSnapshot`** — حقل مخطط جديد يكسر البناء حتى يُسجل؛ استثناءات مقصودة موثقة (الأمان المحلي خارج المظروف؛ مسودات النماذج غير المسلّمة تبقى)؛ بوابة الافتتاح تستهلك السجل (لا قائمة مكررة ولا رقم ثابت)؛ `confirmImport` ينشئ نسخة ما قبل الاستبدال ويتحقق منها دورة كاملة قبل `replaceSnapshot` ويعيدها — تعذر النسخة = منع التأكيد لا وهم نجاح آمن؛ فحص سلامة بعد الاستيراد الافتتاحي أيضًا |
| الاختبارات | `dataRoundTrip.exe014.test.ts` (565 سطرًا): 10 اختبارات — كل عائلة مطلوبة ترفض البوابة بصفر كتابات، المتجر الفارغ يستورد، تصدير→تغيير→استعادة يعيد كل عائلة (عدادات + تساوي عميق ضد الملف المهاجر)، نسخة ما قبل الاستبدال نفسها قابلة للاستعادة للحالة السابقة، ملف غير صالح/إصدار غير مدعوم لا يغيّران شيئًا، وفشل الاستبدال يترك الحالة كاملة |
| الحي 390×844 | `exe014-01` رفض الافتتاح بأسماء العائلات وأمر التصدير أولًا؛ `exe014-02` معاينة الاستعادة: ما سيُستبدل (5 أحداث مع تغيير ما بعد التصدير) + الاستثناءات المقصودة صريحة + وعد النسخة؛ `exe014-03` بعد الاستعادة: النسخة جاهزة للتنزيل + «فحص السلامة بعد الاستعادة: سليم» — والكاش عاد 212.50 والمصروف اللاحق للاستيراد اختفى، وفحص الأدوات 13/13 «سليم — الأرقام متسقة» |

## 5. الاختبارات والبوابات

| البوابة | النتيجة |
|---|---|
| `pnpm check` (typecheck + lint + prettier + density + design-guards + guards + domain tests + prototype:check + prototype:test + build) | خروج 0 |
| الاختبارات | **1813 ناجحًا / 0 فاشلًا / 0 متخطى** (دومين 404 + ويب 1409) — خط الأساس 1757 + 56 جديدًا، واختبارات EXE-001..006 خضراء |
| lint | 0 خطأ / 37 تحذيرًا ≤ السقف 37 |
| الميزانية | خام 590,397/650,000؛ مضغوط 139,931/155,000 — PASS (بعد تحميل `LocalTransferService`/`GuidedOpeningImportService` كسولًا بعد الإقلاع؛ الإعدادات تحرس كل فعل تخزين على الجهوزية برسالة إعادة محاولة صادقة) |
| المخطط/التصدير | **35/27 بلا تغيير** — صفر أسطر في `storage/local/types.ts` |
| الكثافة | سقوف الموجة الثانية بأرقامها الموثقة (12 سطحًا + أول سقف صريح لـOwnerWithdrawalEditor) مع ملاحظات قرار مالك في عدّاد الكثافة |
| الأسرار/المسافات البيضاء | `check-secrets` PASS؛ `git diff --check` نظيف (بوابة CI للـPR) |

## 6. التحقق الحي (390×844، جلسة معزولة، بيانات تجريبية)

| السيناريو (التكليف §15) | الدليل |
|---|---|
| المصروف السريع والأساسي | `exe007-01/02` — نموذج واحد بقواعد واحدة |
| استثمار وسحب المالك | `exe009-01/02` — رحلة واحدة بإجرائين |
| عكس تحصيل بيع | `exe010-01/02` — القيد والعكس المرتبط |
| عكس عربون نشط | `exe010-03/04` + سجل الطلب (الحدثان معًا) |
| شراء مادة ثم استلامها | `exe011-01..03` — جزئي 12/20 ثم الحالة رحلة |
| ضبط مخزون | `exe012-01/02` — زيادة موثقة السبب + السجل |
| استيراد افتتاحي مرفوض عند وجود بيانات | `exe014-01` |
| استعادة كاملة ناجحة مع فحص ما بعدها | `exe014-02/03` + فحص الأدوات 13/13 سليم |

**ملاحظة صادقة:** التحقق الحي على خادم تطوير محلي بمحاكاة متصفح (لا جهاز فعلي ولا PWA مثبتة) — كالتكليف «لا تدّع اختبار جهاز فعلي إذا استخدمت محاكاة». الدخول التلقائي للحقول كان عبر أتمتة واجهة قياسية؛ إدخال تاريخ التسليم احتاج تجاوز متتبع قيمة React (تفصيل تنفيذي لا يؤثر في السلوك المثبت).

## 7. اكتشافات خارج النطاق (تسجيل فقط — لم تُصلح، التكليف §17)

| المعرف | الاكتشاف | الأولوية |
|---|---|---|
| W2-OUT-1 | رسائل reused في السطحين الموجودين من الموجة الأولى ما زالت بنبرة خطأ (`CashDistribution.tsx:224`، `OrderDetail.tsx:1440`) — محتوى صادق وتنسيق فقط | P3 |
| W2-OUT-2 | نص reused مُصلَّب داخل `run` المشترك في OrderDetail يحمل صياغة رد العربون تحديدًا — صحيح اليوم، خطر نسخ مستقبلي | P3 |
| W2-OUT-3 | تدوير مفتاح العملية بعد reused يختلف دلاليًا بين لوحتي EXE-001 (بعد نجاح غير مُعاد فقط) وEXE-010 (بعد أي ok) — كلاهما آمن ومختبر | P3 |
| W2-OUT-4 | «إعادة التحميل» الحقيقية للصفحة أثبتت عبر مثيلات خدمات جديدة فوق المخزن نفسه، لا عبر reload متصفح فعلي في كل اختبار | P3 (تقرير فقط) |

## 8. خطة التراجع

- **حد التراجع الكلي:** `5c54d8a` على main (إغلاق PR الموجة قبل الدمج، أو revert دمج بعده).
- **تراجع انتقائي:** كل وحدة التزام واحد قابل لـ`git revert` مستقل: EXE-008=‏`05ab405`، EXE-009=‏`df0484e`، EXE-007=‏`6c16f89`، EXE-010=‏`e3d2848`، EXE-011=‏`4766847`، EXE-012=‏`0985de2`، EXE-013=‏`5cfffb5`، EXE-014=‏`0e18f92`؛ التزام البوابات `a6b543d` تجميلي/أداء ويرجع منفردًا.
- **أثر الرجوع:** رجوع EXE-008 يعيد ثقب الافتتاح الثاني على حدود المخزن؛ رجوع EXE-009 يعيد التكرار التقاطعي؛ رجوع EXE-010 يعيد فقدان مسار العكس الموثق؛ رجوع EXE-014 يعيد البوابة الناقصة والمسح الصامت المحتمل. لا ترحيل بيانات في أي اتجاه (المخطط/التصدير لم يتغيرا).

## 9. الملفات المتغيرة لكل وحدة (مطابقة النطاق)

| الوحدة | ملفات التنفيذ | ملفات الاختبار |
|---|---|---|
| EXE-008 | `storage/local/cashContinuityCommitGuard.ts` (جديد)، `IndexedDbLocalStore.ts`، `MemoryLocalStore.ts`، `walletLedgerService.ts` | `cashContinuityOpeningGuard.test.ts` (جديد)، `walletLedgerService.test.ts` |
| EXE-009 | `ownerEntitlementService.ts`، `components/finance/CrossModelDuplicateNotice.tsx` (جديد)، `pages/FinancialEventEditor.tsx`، `pages/OwnerWithdrawalEditor.tsx`، `pages/OwnerEntitlement.tsx`، `app/MicroRouter.tsx`، `app/routeKnowledgeSync`، `domain/cash-continuity/types.ts`، `application/finance/projectFinancialService.ts`، `quickFormHelpers`، `walletLedgerService.ts` | `ownerEntitlementService` اختبارات، `OwnerJourneysExe009.dom.test.tsx` (جديد)، `public-surface.test.ts` |
| EXE-007 | `components/finance/expenseFormModel.ts` (جديد)، `components/finance/QuickExpenseForm.tsx`، `pages/FinancialEventEditor.tsx` | `QuickExpenseSource.dom.test.tsx`، `QuickActionSheet.category`، `ExpenseOwnershipExe007.dom.test.tsx` (جديد) |
| EXE-010 | `domain/craft-order/{types,policies,index}.ts`، `application/fulfillment/fulfillmentService.ts`، `saleCollectionReversalService.ts` (جديد)، `storage/local/{IndexedDb,Memory}LocalStore.ts`، `components/orders/OrderDepositPanels.tsx`، `components/direct-sales/SaleCollectionReversalSection.tsx` (جديد)، `pages/DirectSaleEditor.tsx`، `statementService.ts`، عرض الأحداث | `saleCollectionReversalService.test.ts`، `ReversalSurfacesExe010.dom.test.tsx` (جديد)، `fulfillmentService.test.ts`، انحدارات G4/G6/collections/transfers |
| EXE-011 | `pages/SupplierPurchaseEditor.tsx`، `pages/InventoryMaterials.tsx` | `PurchasingBridgeExe011.dom.test.tsx` (جديد) |
| EXE-012 | `pages/InventoryMaterials.tsx`، `pages/InventoryMovementEditor.tsx` | `InventoryAdjustExe012.dom.test.tsx` (جديد) |
| EXE-013 | `application/finance/projectFinancialService.ts` (الحرس) | `fullCycleReconciliation.exe013.test.ts` (جديد)، `integrityCheckService.test.ts`، `projectFinancialService.familyContexts.test.ts`، `statementService.test.ts` |
| EXE-014 | `storage/local/influentialSnapshotFamilies.ts` (جديد)، `application/transfers/guidedOpeningImportService.ts`، `localTransferService.ts`، `components/settings/SettingsGuidedOpeningSection.tsx`، `pages/Settings.tsx` | `dataRoundTrip.exe014.test.ts` (جديد) |
| بوابات | `app/PrototypeServicesContext.tsx` (تحميل كسول)، `scripts/text-density-count.py` (سقوف موثقة) | صيانة مضاعفات DirectSaleEditor/QuickFormsNotify |
| توثيق | `reports/agent-report/2026-09-17_wave-2-financial-data-foundations/**`، `reports/agent-report/README.md`، `CHANGELOG.md`، `docs/operations/current-state.md` | — |

## 10. الدفع والدمج

- الفرع `fix/wave-2-financial-data-foundations` يُدفع برأسه `a6b543d` + هذا الالتزام التوثيقي، ويُفتح PR إلى main.
- بعد CI أخضر وكل بوابات §15: **يُدمج فورًا ضمن المهمة نفسها** (قرارات المالك معتمدة مسبقًا في هذا التكليف §1)، ثم يُجلب main ويُسجل MERGE_SHA ويُتحقق أن EXE-007..014 فيه فعلًا وأن CI عليه أخضر.
- لا دفع مباشر إلى main في أي مرحلة (AGENTS.md).

## 11. ما لم يُختبر (صراحةً)

1. **جهاز فعلي/PWA مثبتة/Offline إنتاجي** — محاكاة متصفح 390×844 على خادم تطوير محلي بجلسة معزولة وبيانات تجريبية.
2. **نقر مزدوج فيزيائي على مستوى DOM للمصروف** — مثبت بنيويًا وخدميًا واختبار DOM مزدوج النقر مخصص (EXE-007) داخل jsdom.
3. **iOS/Android فعليان ولوحة مفاتيح النظام** — خارج البيئة.
4. **مقاسات غير 390×844** — لم تُختبر حيًا في هذه الموجة ولا يُدَّعى.
