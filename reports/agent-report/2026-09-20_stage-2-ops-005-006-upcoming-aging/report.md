# تقرير تنفيذ — Stage 2 / الموجة 2: OPS-005 + OPS-006 (سطح القادم/المتأخر الموحد والتقادم المبسط)

**التاريخ:** 2026-09-20 · **PR:** #197 · **الفرع:** `stage-2/ops-005-006-upcoming-aging` · **خط الأساس:** `bf24bdbfe653e9f7cf8f14f93af9fe6019bd1cb6` (رأس main المتحقق — دمج الموجة 1 بCI أخضر)
**Workstream:** WS-165 · **Items:** OPS-005 + OPS-006 · **الالتزامات:** إغلاق الموجة 1 + Claim `6933902` ← كود `3551765`

## 1. خط الأساس والهوية

- خط الأساس هو رأس main المتحقق نفسه (`bf24bdb` — MERGE SHA للموجة 1 وCI أخضر على SHA نفسه بعد الدمج)؛ لا PRs مفتوحة غير PR الموجة 1 المدموج؛ الاعتمادية OPS-001 = VERIFIED عند `bf24bdb` (أُغلق في الالتزام الأول من هذا الفرع مع فتح Claim WS-165 — التزام واحد يتجنب خطأ التداخل النشط، بنفس آلية المالك #192/#193).
- بوابة العمال الخمسة سارية من مرحلة ما قبل الكود (§9 في تقرير الموجة 1) — عامل 2-a هو مصدر تحليل هذا النطاق: لا تقادم قائم؛ classifyFollowUpDate/ScheduleService.overview/Home overdueDebtCount سوابق؛ كل قراءة بساعة قابلة للحقن.

## 2. الملفات المتغيرة (كاملة)

| الملف | النوع | الغرض |
|---|---|---|
| `application/finance/upcomingService.ts` | جديد | القارئ الموحد (قراءة فقط): أربع كتل مستقلة من مصادرها الرسمية + التقادم المبسط من إجماليات OPS-001 نفسها |
| `pages/FinanceUpcoming.tsx` | جديد | سطح `/finance/upcoming`: بطاقة تقادم مبسط + أربع كتل بفشل معزول (نمط G-005) + حالات فراغ صادقة + سطر الحقيقة (قراءة فقط) |
| `app/PrototypeServicesContext.tsx` | معدّل | توصيل `upcoming` فوق الخدمات القائمة نفسها |
| `app/MicroRouter.tsx` | معدّل | مسار `/finance/upcoming` (سطح يبقي الشريط — قاعدة U-005) |
| `app/routeKnowledgeSync.test.ts` | معدّل | عقد قصد المسار الجديد: سطح قراءة (حارس STR-010) |
| `pages/FinanceMore.tsx` | معدّل | مدخل دليل واحد «القادم والاستحقاقات» في قسم «ملخصات وتقارير» |
| `scripts/text-density-count.py` | معدّل | PAGES + قياس أولي موثق FinanceUpcoming=49 + رفع موثق FinanceMore 35→37 (سابقة ToolsIntegrity 85→86) |
| `application/finance/upcomingService.test.ts` | جديد | 3 اختبارات خدمة مركزة |
| `FinanceUpcoming.dom.test.tsx` | جديد | 3 اختبارات DOM |

## 3. السلوك قبل/بعد

- **قبل:** لا يوجد أي سطح أو قراءة موحدة للقادم/المتأخر — المواعيد مبعثرة: جدول الطلبات في مساره، استحقاق المورد في صفحة الموردين (بلا تصنيف قبل الموجة 1)، الذمم في ورقة التحصيل (بلا أي تاريخ)، الالتزامات داخل «شو عليّ؟» المالية (مجاميع فقط). لا تقادم في أي مكان (تأكيد عامل 2-a).
- **بعد:** مسار واحد `/finance/upcoming` (من «المالية ← المزيد») يجمع: طلبات قادمة ومتأخرة بموعدها المجدول (بلا مبلغ نقدي على موعد تنفيذ)، مدفوعات الموردين بتاريخ استحقاقها المخزن وتصنيفه (من OPS-001)، ذمم التحصيل بحالتها الصادقة «بلا تاريخ استحقاق مخزّن» والمبالغ ظاهرة، والتزامات المصاريف المستحقة بتسميتها الكنونية (`مصروف مستحق` من financialEventLabels — بيته الوحيد) ومتبقيها؛ بطاقة «تقادم مبسط» تجمع متأخر/لم يحن/بلا تاريخ للموردين من إجماليات OPS-001 نفسها، ومجاميع «ما لك» و«الالتزامات» مع التصريح بلا تاريخ مخزّن، وتعلن «غير متاح» لجزء فشل قراءته — بلا شرائح 30/60/90 (BE-005 مؤجل بدليله).

## 4. العقود والـinvariants المقروءة والمحترمة

عقد ٥ (P0 — كاملًا في الموجة 1 وأعيد استخدامه هنا: المجهول ليس صفرًا ولا اليوم)، عقد ٩ (dueOn)، عقد ١٨ (الجدول المشتق — مواعيد الطلبات من `ScheduleService.overview` وحده)، عقد ٢ (دورة حياة الطلب — الطلب المسلّم يخرج من الجدول النشط)، G7-A (خوارزمية التاريخ)، وقاعدة U-005 (القارئ سطح يبقي الشريط). **المخطط/التصدير:** `NO_SCHEMA_OR_EXPORT_CHANGE` (35/27 كما هما — قراءة فقط). **المعنى المالي:** `NO_FINANCIAL_SEMANTIC_CHANGE` — لا Writer جديد؛ كل المجاميع من مصادرها الرسمية القائمة (إجماليات OPS-001، ورقة التحصيل، الأحداث المالية)؛ لا يُعرض مبلغ نقدي على موعد تنفيذ طلب. **ملاحظة تشغيلية صريحة:** يعاد استخدام `ScheduleService.overview` كما هو — وهو كما في صفحة الجدول نفسها يجلب المواعيد المشتقة الناقصة بكتابة تشغيلية حتمية idempotent (مفتاح `${order.id}:schedule-created`، حارس HIGH-001) — ليست كتابة مالية ولا Writer جديد، وموثقة في الاختبار (فتح لاحق بلا أي كتابة).

## 5. قرار التنفيذ ولماذا هو أدنى

(أ) قارئ واحد يعيد استخدام أربع مصادر رسمية بدل أي تجميع جديد أو Writer؛ (ب) التقادم من إجماليات OPS-001 نفسها لا حساب ثانٍ؛ (ج) سطح جديد واحد بمسار واحد ومدخل دليل واحد بدل تعديل أسطح معتمدة مكتظة (Finance 300/300، Schedule 101/101، Home 77/78، Collect 58/58)؛ (د) مدخل «المزيد من المالية» رفع سقفه 35→37 فقط (تسميتان مفروضتان) موثقًا في السكربت هنا والتقرير — سابقة ToolsIntegrity 85→86 المقبولة؛ (هـ) قياس أولي FinanceUpcoming=49 موثق (سابقة FinanceMore 35/DeliveryReview 53)؛ (و) رسائل فشل الكتل بنمط `message:` الكنوني (لا تُحسب في الكثافة) بدل بطاقات دائمة.

## 6. الاختبارات والتحقق (الأوامر والخروج)

| الأمر | النتيجة |
|---|---|
| `corepack pnpm exec vitest run client/src/application/finance/upcomingService.test.ts client/src/FinanceUpcoming.dom.test.tsx` | 6/6 PASS |
| `pnpm check` | **EXIT 0** — typecheck 0؛ lint 37/37؛ text-density: FinanceUpcoming 49/49 (قياس أولي) وFinanceMore 37/37 (رفع موثق)؛ guards؛ 234+ ملفات ناجحة؛ build + budget **612,661/145,490 ≤ 650,000/155,000** |
| `python3 scripts/operations-control/validate.py` | valid — 64 items, 7 workstreams, 1 active claim |
| `git diff --check` | نظيف |

**تغطية مسارات الفشل:** فشل `listSupplierPurchases` → كتلة المدفوعات وحدها تتعثر (بطاقة صادقة + إعادة محاولة) والطلبات/الذمم/الالتزامات حية؛ `aging.payables=null` يعرض «غير متاح» لا صفرًا؛ لا كتابة رغم الفشل (مطابقة اللقطة بعد جلب الجدول الأولي الحتمي القائم)؛ المجهول «بلا تاريخ استحقاق مخزّن» والمبالغ ظاهرة ولا يظهر تاريخ اليوم (16/09/2026) في صفوف بلا تاريخ؛ الطلب المسلّم لا يدخل جدول القادم.

## 7. QA الحي والجهاز

`NOT_EXECUTED` — لا متصفح حي في بيئة التنفيذ. الصفحة الجديدة مبنية بنمط `micro-finance-more-page` القائم وأصنافه، وبطاقات الفشل role=alert بزر قابل للتركيز (نمط G-005 نفسه)، والأسماء الحرة dir=auto. سجل QA: `docs/quality/stage-2-ops-005-006-qa-2026-09-20.md`.

## 8. التراكر والعروض المولدة

الالتزام الأول من الفرع (`6933902`): OPS-001 + WS-164 → VERIFIED (merge_sha=verified_on_main_sha=bf24bdb بأدلة قابلة للوصول) + OPS-005/006 → IN_PROGRESS + WS-165 CLAIMED. الالتزام الحالي: OPS-005/OPS-006 + WS-165 → IN_REVIEW (pr:197). Views/xlsx/meta مُجددة وunzip -t سليم وvalidator ناجح. `VERIFIED` بعد الدمج وتحقق SHA نفسه على origin/main فقط.

## 9. القيود والأدلة التاريخية غير المُعاد تنفيذها

- التقادم المتقدم 30/60/90 (BE-005) مؤجل بدليل Pilot — لا يُدّعى هنا.
- اشتقاق «دين فات موعد متابعته» في الرئيسية بقي كما هو عمدًا (سطح معتمد) — يُدرس لتوحيد المصدر في موجة لاحقة بقرار مالك.
- أدلة التقرير الخارجي (Documents) `HISTORICAL_EVIDENCE` لم تُفتح.
- QA الحي/الجهاز `NOT_EXECUTED`.

## 10. حدود الاستعادة

حد آمن أخير: `bf24bdb` (رأس main المتحقق). الرجوع = عدم دمج PR #197 (أو revert دمجها) — لا هجرة بيانات. آخر التزام آمن على الفرع = `3551765` حيث `pnpm check` أخضر.

## 11. الكتابات التي نُفذت والتي لم تُنفذ

**نُفذت:** فرع `stage-2/ops-005-006-upcoming-aging` + دفعه؛ التزامان (إغلاق الموجة 1 + Claim، ثم الكود)؛ فتح PR #197؛ تحديثات تراكر المصدر مع Views/Excel/meta. **لم تُنفذ:** لا دفع مباشر إلى main؛ لا دمج قبل CI + manifest؛ لا حذف/إعادة تسمية فروع؛ لا وصول لDocuments؛ لا نشر أو إعدادات؛ `NO_TOKEN_STORED` (ملف 0600 مؤقت خارج المستودع يُمحى نهاية المهمة)؛ لا تنظيف فروع.
