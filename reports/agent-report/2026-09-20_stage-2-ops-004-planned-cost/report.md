# تقرير تنفيذ — Stage 2 / الموجة 3: OPS-004 (التكلفة والكمية المخططتين للقوالب — قراءة وتقدير فقط)

**التاريخ:** 2026-09-20 · **PR:** #198 · **الفرع:** `stage-2/ops-004-planned-cost` · **خط الأساس:** `18368558b6df0dfbc1b1910e961d1504e9e460fb` (رأس main المتحقق — دمج الموجة 2 بCI أخضر)
**Workstream:** WS-166 · **Item:** OPS-004 · **الالتزامات:** إغلاق الموجة 2 + Claim `ced402c` ← كود `f096233`

## 1. خط الأساس والهوية
رأس main المتحقق نفسه (`1836855` — MERGE SHA للموجة 2 وCI أخضر على SHA نفسه). الالتزام الأول من الفرع أغلق OPS-005/OPS-006/WS-165 كـVERIFIED (بأدلة قابلة للوصول) وفتح Claim WS-166 في التزام واحد (لا تداخل نشط). بوابة العمال الخمسة سارية (عامل 2-b مصدر تحليل هذا النطاق: القوالب بلا أسعار؛ الدليل المشترك الواحد materialSuggestionsFrom؛ لا مسار تعبئة من قالب).

## 2. الملفات المتغيرة (كاملة)
جديد: `application/catalog/templatePlannedCostService.ts` + `templatePlannedCostService.test.ts` + `CatalogPlannedCost.dom.test.tsx`. معدّل: `components/catalog/CatalogTemplatesSection.tsx` (بطاقة قراءة التكلفة/الكمية داخل جسم `<details>` — خارج كثافة السكون: Catalog 93/93 بلا تغيير)، `pages/Catalog.tsx` (قراءة الخدمة داخل التحميل المشترك وتمرير الخريطة)، `app/PrototypeServicesContext.tsx` (توصيل الخدمة)، `G3.dom.test.tsx` (إصلاق تكاملي — الهيكل يوصل الخدمة بعد صيرورة الصفحة معتمدة عليها).

## 3. السلوك قبل/بعد
- **قبل:** القوالب تعرض مكوناتها وكمياتها وبنودها الاختيارية بلا أي قراءة نقديّة — لا يوجد أي مسار يقدّر تكلفة قالب (تأكيد عامل 2-b).
- **بعد:** لكل قالب محفوظ بطاقة «الكمية المخططة والتقدير»: مجموع كميات المكوّنات والناتج المسجّل كما هما؛ التقدير النقدي من الدليل الكنوني الواحد (آخر استلام غير معكوس بمعيّنه الدقيق) لل مكوّنات المرتبطة بمادة ووحدتها وحدة المادة نفسها؛ الوقت وبنود القالب من القالب نفسه؛ حالة معرفة صادقة (تقديري/ناقص المعرفة/غير متاحة) والمجموع الكلي يختفي عند أي نقص (لا مجموع مضلل)؛ الوحدة المختلفة تُعلن ولا يُخمّن تحويلها؛ المكوّن الحر والمادة بلا استلام تصريح صادق — لا صفر أبدًا.

## 4. العقود والـinvariants
عقد ١٥ (§7: القالب لا ينشئ Purchase/حركة/استهلاك/هدر؛ §6: مرجع تخطيط)؛ عقد ٥ (المجهول ليس صفرًا؛ التقريب بالمعيّنات الكنونية)؛ عقد ١١ (المخزون والاستهلاك — قراءة فقط)؛ D5 (الدليل المشترك للمقترحات — مصدر السعر الواحد)؛ AGENTS §10 (لا Math في domain/application — استُخدم roundHalfUp المشترك). **المخطط/التصدير:** `NO_SCHEMA_OR_EXPORT_CHANGE` (35/27 — قراءة فقط). **المعنى المالي:** `NO_FINANCIAL_SEMANTIC_CHANGE` — لا Writer ولا حركة ولا COGS ولا استهلاك؛ الرياضيات بمعيّنات الحدّ المشتركة نفسها (roundHalfUp(كميةملي×سعر،1000) للبند وroundHalfUp(دقائق×أجر،60) للوقت) — لا مسار حساب ثانٍ.

## 5. قرار التنفيذ ولماذا هو أدنى
قراءة+تقدير فقط فوق بيانات قائمة: لا تعبئة مسودة من قالب (خارج البند)، لا بيانات محفوظة جديدة، بطاقة العرض داخل `<details>` منهار فلا أثر كثافة ولا سقف مرفوع، والرياضيات بمعيّن مشترك لا بمسار ثانٍ. قاعدة الوحدة الصادقة (تطابق اسم وحدة المكوّن الكنوني مع وحدة المادة — وإلا إعلان) fail-closed بلا اختراع جسر وحدات.

## 6. الاختبارات والتحقق
| الأمر | النتيجة |
|---|---|
| `corepack pnpm exec vitest run client/src/application/catalog/templatePlannedCostService.test.ts client/src/CatalogPlannedCost.dom.test.tsx` | 7/7 PASS |
| `pnpm check` | **EXIT 0** — lint 37/37؛ Catalog 93/93؛ build + budget **615,809/146,542 ≤ 650,000/155,000** |
| `python3 scripts/operations-control/validate.py` | valid |
| `git diff --check` | نظيف |

**مسارات الفشل المغطاة:** بلا استلام → «بلا سعر معروف» (لا صفر)؛ وحدة مختلفة → إعلان لا تحويل؛ مكوّن حر؛ قالب فارغ بلا بنود → «غير متاحة»؛ المجموع الكلي null عند أي نقص؛ فشل قراءة القوالب → storage_error صادق؛ لا كتابة عند القراءة (مطابقة اللقطة — خدمة + صفحة الكتالوج كاملة).

## 7. QA الحي والجهاز
`NOT_EXECUTED` — لا متصفح حي؛ البطاقة داخل `<details>` بنمط وفئات قائمة. سجل QA: `docs/quality/stage-2-ops-004-qa-2026-09-20.md`.

## 8. التراكر والعروض
OPS-004: IN_PROGRESS → IN_REVIEW (pr:198). WS-166: CLAIMED → IN_REVIEW (pr:198). Views/Excel/meta مُجددة وunzip -t سليم وvalidator ناجح.

## 9. القيود والأدلة التاريخية
تعبئة مسودة التكلفة من القالب ليست ضمن البند؛ المقارنة المخطط/الفعلي = الموجات 4/5؛ أدلة Documents الخارجية HISTORICAL_EVIDENCE لم تُفتح؛ QA الحي NOT_EXECUTED.

## 10. حدود الاستعادة
حد آمن أخير: `1836855`. الرجوع = عدم دمج PR #198 — لا هجرة ولا كتابة. آخر التزام آمن على الفرع = `f096233` حيث `pnpm check` أخضر.

## 11. الكتابات المنفذة وغير المنفذة
**نُفذت:** فرع + دفع؛ التزامان؛ PR #198؛ تحديثات تراكر المصدر مع Views/Excel/meta. **لم تُنفذ:** لا دفع مباشر لmain؛ لا دمج قبل CI + manifest؛ لا حذف/إعادة تسمية فروع؛ لا وصول لDocuments؛ لا نشر/إعدادات؛ `NO_TOKEN_STORED`؛ لا تنظيف فروع.
