# تقرير الموجة الأولى — التصحيح والصدق والحالات الآمنة — Micro

**التاريخ والوقت (Asia/Amman):** 16 أيلول/سبتمبر 2026 — البدء ~11:55، الاكتمال ~15:40
**BASE_SHA:** `37319ca6439746e8ab1946e891c849decca04e5d` (رأس `main` المتحقق حيًا قبل أي تعديل — مطابق لتعليمات التكليف)
**HEAD_SHA (رأس فرع الإصلاح):** يُثبَّت في §11 عند الدفع
**الفرع:** `fix/wave-1-correctness-truth` من `37319ca` — **لم يُدمج في main** (التكليف §1/§13/§14: فتح PR دون دمج)
**المرجع:** تقرير التدقيق العميق `reports/agent-report/2026-09-16_13-00-deep-system-audit/report.md` على فرع `report/deep-system-audit-20260916` / PR #161 (غير مدموج — قُرئ من فرعه ولم يُنسخ شيء منه إلى هذا الفرع)

---

## 1. الخلاصة التنفيذية

نُفّذت الوحدات الست (EXE-001..EXE-006) كاملة من خط الأساس `37319ca` عبر ستة التزامات منطقية + التزاما تصحيح نوع/تنسيق، بقراءة فقط لفرع تقرير التدقيق. النتيجة: **الوحدات الست كلها FIXED** — كل معايير القبول مثبتة باختبارات انحدار صريحة تعيد إنتاج العيب قبل الإصلاح (يفشل الاختبار على الكود القديم وينجح على الجديد) + تحقق حي على النظام المحلي بمقاس 390×844 للسيناريوهات الخمسة القابلة للعرض + مراجعة انحدار مستقلة (وكيل رابع) حكمت **PASS_WITH_NOTS — الموجة مقبولة** بلا أي انحراف نطاق.

البوابة الكاملة `pnpm check`: خروج 0 — دومين 36 ملفًا/404 اختبارات، ويب 191 ملفًا/1353 اختبارًا (منها 10 اختبارات انحدار جديدة)، ‏lint ‏0 خطأ/36 تحذيرًا (≤37)، typecheck نظيف، الكثافة كلها داخل السقوف بلا رفع أي حارس (Finance ‏261/261 وOrderDetail ‏179/179 بعد تصنيف رسالة اللحظة تصنيفًا صحيحًا)، بناء 644,022/650,000 خام و154,187/155,000 مضغوطًا — PASS. **المخطط/التصدير 35/27 بلا تغيير** (`storage/local/types.ts` صفر أسطر في الـdiff) ولا قاعدة مالية ولا معادلة محاسبية ولا مخزن جديد.

## 2. خط الأساس والالتزامات

| الالتزام | الوحدة | المحتوى |
|---|---|---|
| `c770cf0` | EXE-001 | تجديد مفتاح التوزيع لكل تأكيد ناجح + رسالة صادقة لـreused |
| `40936e0` | EXE-002 | بوابة merge موحدة للكتّاب السبعة + اختبار شامل |
| `93c11b9` | EXE-003 | MIC-2 يستورد SOURCE_REF_KINDS من الدومين + حرس تقاطع |
| `71f2428` | EXE-004 | مفتاح لكل تأكيد رد + عودة reused بنيوية |
| `ae96cdb` | EXE-005+006 | رجوع السوق إلى returnPath + رسالة الإعداد المصححة |
| `4f4158c` | style | prettier للملسات الملمة فقط |
| `2175a5d` | تصحيح نوع | إعادة حقل notice القائم (S2-10) + إكمال freshRecord |

21 ملفًا: 13 تنفيذًا + 8 اختبارات — كلها داخل الوحدات (جدول المطابقة في §10).

## 3. الوحدات — السبب الجذري والحل والدليل

### EXE-001 — توزيع الكاش المتكرر (CASH-002 + AUD-NEW-01) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري (أُثبت من الكود الحي قبل التعديل) | `CashDistribution.tsx:50` ينشئ مفتاح العملية مرة لكل تركيب صفحة؛ مسار النجاح (`:113-134` قبل الإصلاح) يصفّر الحقول ولا يجدد المفتاح ويبقى في الصفحة؛ `:122-131` يفحص `result.ok` فقط — فالتوزيع الثاني في الزيارة نفسها يصطدم بمسار الإعادة الحتمية في الخدمة (`projectFinancialService.ts:1045-1051` يعيد `{ok:true, reused:true}` بلا كتابة) بينما تعرض الواجهة «انخصص الكاش ✓» |
| الحل | بعد نجاح غير مُعاد: تجديد `operationKeyRef` (سطر 143 بعد الإصلاح) — كل تأكيد مقصود يملك مفتاحًا جديدًا؛ فرع صريح لـ`reused` (س128-131): رسالة «سُجّل سابقًا — لم يُنشأ قيد جديد» بلا ادعاء نجاح جديد وبلا كتابة؛ الفشل يبقي المفتاح (إعادة المحاولة بعد فشل لم يكتب شيئًا تمر كتابةً صحيحة). معادلة الكاش لم تُمس |
| الاختبارات | DOM: توزيعان (20 ثم 30) في زيارة واحدة → قيدا `allocation` بمقداري 2000/3000، محفظة 150.00، غير موزع 10.00، والهوية `recordedCash = walletCash + unallocated = 160.00`؛ خدمة: إعادة المفتاح نفسه → `reused:true` وقيد واحد وأرصدة كما هي |
| التحقق الحي | 390×844: بعد التوزيع الأول 40/120 → الثاني في الزيارة نفسها سُجل فعليًا 10/150 (في التدقيق كانت الأرقام تبقى 40/120 مع نجاح كاذب)، ودفتر المحفظة يعرض القيدين +20.00 و+30.00 |
| اللقطات | `evidence/screenshots/exe001-01..03` |

### EXE-002 — سلامة سجل التفضيلات (SET-003 + AUD-NEW-02) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | `actualTimeService.saveOperatingMode` (س60-80 قبل الإصلاح) و`scheduleService.setDailyCapacity` (س285-310) يعيدان بناء السجل كاملًا حقلًا حقلًا **بلا** `disabledCapabilities`، والكتابة استبدال سجل كامل — فتغيير «طريقة العمل» أو «سعة اليوم» يعيد تفعيل كل القدرات بصمت. النمط اليدوي هو السبب (أنتج SET-003-B سابقًا في ثلاثة كتّاب آخرين) |
| الحل | بوابة واحدة `updateLocalPreferences(store, patch, now)`: قراءة السجل الحالي → نشره → تطبيق الحقول المقصودة فقط → تثبيت `id`/`updatedAt` → إرساخ الحقول الاختيارية الثلاثة المعروفة عند غيابها من سجل قديم (نفس سلوك SET-003-B الموثق: الغياب يُقرأ افتراضًا ثم يُرسَّخ بأول حفظ). الكتّاب السبعة كلهم يمرون منها (فحص شامل: لا مستدعي لـ`savePreferences` خارج البوابة في كود التطبيق) |
| الاختبارات | ملف جديد `preferencesFieldPreservation.test.ts`: يمشي على الكتّاب السبعة كلهم (لا الكاتبين المكتشفين فقط) ويثبت أن كل كاتب يغيّر حقه فقط وتبقى `disabledCapabilities` وكل الحقول الأخرى؛ جهاز جديد بلا سجل = افتراضات صادقة؛ «إعادة تحميل» عبر مثيلات خدمات جديدة فوق المخزن نفسه تقرأ ما كُتب. اختبارات SET-003-B القائمة بقيت خضراء |
| التحقق الحي | 390×844: إيقاف «الطلبات والتنفيذ» → حفظ «طريقة العمل» = «المادة أولًا» → **القدرة بقيت موقوفة** (زر «تفعيل») والرئيسية ما زالت تخفي زري «طلب من عميل/مسودة تصميم» (في التدقيق كانت تعود مفعلة بصمت) |
| اللقطات | `exe002-01..02` |

### EXE-003 — MIC-2 وأنواع المصادر (TOOL-001 + AUD-NEW-03) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | الدومين يقبل 5 أنواع (`cash-continuity/policies.ts:50` قبل الإصلاح، منها `supplier_purchase` من إصلاح FIN-003) بينما الفاحص يكرر قائمة من 4 (`integrityCheckService.ts:72`) → فشل بنيوي زائف عند `:324` على كل دفعة مورد من محفظة |
| الحل | الثابت القانوني `SOURCE_REF_KINDS` معرّف ومصدَّر مرة واحدة من وحدة أنواع الدومين (`types.ts:16`)، والاتحاد `CashAllocationSourceKind` مشتق منه ويبقى غير مصدَّر (السطح العام مغلق)، والسياسات والفاحص يستوردانه — لا قائمة مكررة في أي خدمة/فاحص، وأي نوع مستقبلي يظهر تلقائيًا في الفحص. الإضافة للسطح العام **واعية ومختبرة** (تأكيد إيجابي بمحتوى الخمسة أنواع في `public-surface.test.ts:172-181` — وثيقة العقد لا تسمح برموز مسحوبة) |
| الاختبارات | محفظة 100 + شراء 50 + دفعة محفظة 20 → MIC-2 **سليم** والمعادلة 80=80+0؛ حرس تقاطع: لكل نوع في ثابت الدومين تخصيص نظيف → MIC-2 سليم (أي انحراف مستقبلي يكسر الاختبار)؛ نوع مزور `bogus_source` يبقى **خللًا بنيويًا**؛ عدّ الفحوص الديناميكي مطابق (اختبار TOOL-001 قائم أخضر) |
| التحقق الحي | 390×844: شراء مورد 50 بدفعة أولية 20 من «الدرج» → «فحص سلامة مالي»: **«سليم — الأرقام متسقة» و13/13** (في التدقيق: «خلل» و«اختلال بنيوي في 1 حركة» و12/13)، وسطر «بنية الكاش والمحافظ: سليم» صراحةً |
| اللقطات | `exe003-mic2-pass…` و`exe003-integrity-all-green` |

### EXE-004 — حتمية رد العربون (AUD-NEW-07) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | مفتاح حدث الرد `${id}:refund-deposit:${amount}:${reason.length}:${hour}` (`fulfillmentService.ts:417` قبل الإصلاح — دقة ساعة UTC): ردّان جزئيان متساويان داخل الساعة نفسها بأسبابين متساويي الطول يتصادمان، والثاني يُبتلع (الدومين يعيد الطلب دون تغيير عند وجود الحدث، والمخزن يعيد `ok/reused`، والفاعل يتجاهله) |
| الحل | `refundDeposit(id, reason, amount?, operationKey?)`: لوحة التسوية تولّد مفتاحًا جديدًا لكل تأكيد مستقل (`OrderDepositPanels.tsx:74-77` مرجع لكل تركيب، `:221-231` التقاط عند النقر وتمرير وتجديد بعد ok) — النقر المزدوج على التأكيد الواحد يبقى محتميًا بالمفتاح نفسه حتى يكتمل؛ فحص إعادة تشغيل مبكر يعيد الحالة المخزنة بعلم `reused` بنيوي **بلا أي كتابة**؛ علم `reused` في المخزن يُرجع نفس العلم؛ الواجهة تعرض النص الصادق لحظة الفعل عبر `setMessage` في `run` (`OrderDetail.tsx:457-458`). الاستدعاء المباشر بلا مفتاح يبقى على الاشتقاق القائم (توافق الاختبارات القائمة) وموثق أنه ليس مسار الواجهة. حقل `notice` القائم (نمط S2-10) أُعيد بعد أن أسقطته مرحلة وسيطة — رصدته المراجعة المستقلة وصُحح |
| الاختبارات | ردّان جزئيان متساويان (5.00+5.00) بالسبب نفسه والساعة نفسها (ساعة ثابتة عمدًا) بمفتاحين → **حدثان مستقلان بمفتاحين مختلفين** والحالة تنتقل needs_review→refund_deposit وcollected=0 (خدمة + DOM عبر اللوحة: 3000→1000 قروشًا وحدثان)؛ إعادة التأكيد نفسه → `reused:true` وحدث واحد وبلا كتابة. تغطية فك تخصيص المحفظة لكل رد قائمة في جناح FC-06 |
| التحقق الحي | حسب التكليف §11: يثبت أساسًا بالاختبار والدفتر — لا لقطة مطلوبة |

### EXE-005 — رجوع السوق (AUD-NEW-13) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | `Market.tsx:17,44-51` قبل الإصلاح: الزران ينفذان `window.history.back()` بينما يَعِدان بوجهة معلنة (returnPath) — عند البدء البارد بوصلة عميقة يهبط الزر على صفحة عشوائية سابقة (أثبتَه التدقيق حيًا بالهبوط على صفحة الفحص)، وفي PWA بلا تاريخ لا يفعل شيئًا |
| الحل | الزران يستخدمان `navigate(returnPath)` عبر wouter — الآلية المعتمدة نفسها التي تستخدمها بقية الأسطح؛ عرض «قريبًا» الصادق لم يُمس |
| الاختبارات | بدء بارد على `/market` (location=/market بلا ?from) → الزر ينفذ `/`؛ `?from=/tools` → ينفذ `/tools`؛ الزر العلوي كذلك |
| التحقق الحي | 390×844: فتح `/market` مباشرة ثم «رجوع لمشروعي» → الهبوط على `http://localhost:3000/` بعنوان «مشروع موجة-1» (الرئيسية) — لا على الصفحة السابقة في الجلسة |
| اللقطات | `exe005-01..02` |

### EXE-006 — رسالة نجاح الإعداد (SET-002 + AUD-NEW-11) — **FIXED**

| البند | التفصيل |
|---|---|
| السبب الجذري | لافتة `/?setup=1` تقول «صفحة الأساس عمق اختياري تكمله لاحقًا من «المالية»» بينما مقعد المالية بلا أي مدخل للأساس؛ المدخل الوحيد زر «صفحة الأساس» في قسم «مالي» داخل الرئيسية نفسها |
| الحل | سطر واحد: «…من زر «صفحة الأساس» في قسم «مالي» أسفل هذه الصفحة.» — عربية مباشرة مختصرة، بلا نقل صفحة وبلا مدخل جديد (diff الرئيسية سطر واحد) |
| الاختبارات | اللافتة تتضمن «قسم «مالي»» ولا تتضمن «من «المالية»» |
| التحقق الحي | 390×844: الرسالة المصححة ظهرت حرفيًا بعد إتمام الإعداد، وزر «صفحة الأساس» ظاهر في قسم «مالي» بالصفحة نفسها |
| اللقطات | `exe006-setup-banner-corrected` |

## 4. الاختبارات والأوامر والنتائج

**البوابة الكاملة** (`pnpm check`، خروج 0 — السجل الكامل حرفيًا في `test-results/gates.txt`):
- الدومين: 36 ملفًا / **404 اختبارات** ناجحة
- الويب: 191 ملفًا / **1353 اختبارًا** ناجحة (منها 10 انحدار جديدة: 2 توزيع + 2 تفضيلات شاملة + 1 أجهزة جديدة + 3 فاحص + 2 رد عربون خدمة… مع DOM: توزيع متتابع 1 + ردّان عبر اللوحة 1 + سوق/لافتة 2 ضمن المجموع)
- lint: ‏0 أخطاء / 36 تحذيرًا (السقف 37 — لم يُرفع)
- typecheck جذر+تطبيق: صفر أخطاء
- الكثافة النصية: كل الأسطح داخل السقوف — Finance ‏261/261، OrderDetail ‏179/179، البقية كما هي (لم يُرفع أي سقف)
- ميزانية الحزمة: PASS — ‏644,022/650,000 خام و154,187/155,000 مضغوطًا
- حراس المشروع (أسرار/تركيز/كيانات/دورات/مسافات): خضراء ضمن الأبواب

**التشغيلات المركزة أثناء التطوير** (كلها خروج 0): تفضيلات+وقت+جدول 26؛ فاحص 32؛ سطح عام 14؛ دومين كاش 9؛ رد عربون خدمة 24؛ G4 ‏DOM ‏5؛ Nav001 ‏7؛ CashJourneys+توزيع 20؛ أسطح الإعدادات والطلبات 29؛ جدولة+G6 ‏30؛ fulfillment+OrdJourneys ‏38.

**ملاحظتا تغطية مسجلتان (من المراجعة المستقلة، P3):** لا اختبار DOM مباشر للنقر المزدوج (مثبت خدميًا بالمفتاح نفسه + بنيويًا بتعطيل الزر أثناء الحفظ)؛ «إعادة التحميل» مثبتة عبر مثيلات خدمات جديدة فوق المخزن لا إعادة تحميل صفحة حقيقية.

## 5. التحقق الحي (390×844، خادم تطوير محلي، جلسة معزولة ببيانات تجريبية)

| السيناريو | النتيجة |
|---|---|
| إعداد مشروع جديد كليًا «مشروع موجة-1» بمحفظة «الدرج» 100.00 | رسالة الإعداد المصححة تظهر حرفيًا (EXE-006) |
| إيقاف قدرة «الطلبات والتنفيذ» ثم حفظ «طريقة العمل» | القدرة بقيت موقوفة والرئيسية تخفي أزرار الطلب (EXE-002) |
| بيع 60.00 مقبوض في غير الموزع ثم توزيع 20 ثم 30 في الزيارة نفسها | قيدان: غير الموزع 10.00، المحفظة 150.00، الإجمالي 160.00، الدفتر يعرض +20 و+30 (EXE-001) |
| شراء مورد 50.00 بدفعة أولية 20.00 من المحفظة ثم «فحص سلامة مالي» | «سليم — الأرقام متسقة» 13/13 و«بنية الكاش والمحافظ: سليم» (EXE-003) |
| بدء بارد على `/market` ثم «رجوع لمشروعي» | الهبوط على الرئيسية `http://localhost:3000/` (EXE-005) |
| رد العربون (EXE-004) | بالاختبار والدفتر حسب التكليف — لا لقطة مطلوبة |

10 لقطات في `evidence/screenshots/` — كلها 2026-09-16 ~15:10-15:15 بتوقيت Amman، مقاس 390×844، جلسة معزولة ببيانات تجريبية على خادم محلي، ولا تعرض أي سر أو بيانات حقيقية.

## 6. الأثر على المخطط والتصدير والقواعد المالية

| البُعد | الأثر |
|---|---|
| المخطط/التصدير | **لا تغيير** — 35/27 كما هي؛ `storage/local/types.ts` صفر أسطر في الـdiff؛ لا كيان دائم جديد (البوابة الجديدة مساعد طبقة تطبيق فوق السجل القائم) |
| القواعد المالية | **لا تغيير** — لا سطر في معادلات الإيراد/التكلفة/الكاش/القبض/النتيجة تغيّر؛ التعديل الوحيد في الدومين نقل ثابت الأنواع إلى وحدة الأنواع بمحتوى مطابق حرفيًا + تعليقات (مقفل باختبار السطح العام) |
| الحتمية | **محسّنة صراحة**: كل تأكيد مقصود مفتاحه (توزيع/رد عربون)، وreused يُفصح عنه ولا يُبتلع |
| السقف الرقمية | لم يُرفع أي سقف (lint ‏36≤37؛ كثافة داخل السقوف؛ ميزانية داخل الحد) |

## 7. ملاحظات المراجعة المستقلة (الوكيل الرابع — PASS_WITH_NOTS)

قبل الإغلاق راجع وكيل مستقل الـdiff كاملًا (21 ملفًا) مقابل معايير القبول: كل وحدة PASS جوهريًا؛ **صفر انحراف نطاق** (كل ملف يعود لوحدة؛ لا لمسات عرضية؛ رصد انحدارًا وسيطًا في مرحلة `71f2428` — إسقاط حقل `notice` القائم — وأثبت تصحيحه في `2175a5d`)؛ وأثبت حصانة مسار الإعادة المبكر (لا يفوّت أي كتابة). ملاحظاته P3 مسجلة: نبرة رسالتي reused (تنسيق خطأ لإشعار حميد — محتوى صادق)، نص reused مشترك في `run` صحيح اليوم (لا مؤثر آخر يعيد العلم) مع احتمال خطأ نسخ مستقبلي، واختلاف دلالي طفيف بين تجديد المفتاح في EXE-001 (بعد نجاح غير مُعاد فقط) وEXE-004 (بعد أي ok) — كلاهما آمن والاختبارات تغطيه.

## 8. اكتشافات خارج النطاق (تسجيل فقط — لم تُصلح، التكليف §12)

| المعرف | الاكتشاف | الأولوية |
|---|---|---|
| W1-OUT-1 | اتحادا نوع `sourceRefKind` منسوخان يدويًا في طبقة التطبيق بلا `supplier_purchase` (`projectFinancialService.ts:201`، `quickFormHelpers.ts:23`) — نفس نمط الانحراف الذي أزاله EXE-003 للفاحص، لكنهما نوعا إدخال للمنتجين ولا مسار وظيفي يحتاج النوع هناك اليوم | P3 |
| W1-OUT-2 | إشعارا «reused» الصادقان يظهران بنبرة خطأ (`micro-field-error`/`role=alert`) في السطحين | P3 |
| W1-OUT-3 | نص reused مُصلَّب داخل `run` المشترك في OrderDetail — صحيح اليوم (وحده refundDeposit يعيد العلم) وخطر نسخ مستقبلي فقط | P3 |
| W1-OUT-4 | لقطات التصدير/الاستيراد تحمل `preferences: null` (التفضيلات خارج النسخة الاحتياطية) — يعمّق إقليم DATA-001 المعروف من التدقيق، قائم قبل هذه الموجة | P3 (تقرير فقط) |

## 9. خطة التراجع

- **حد التراجع الكلي:** `37319ca` (لم يُدمج شيء في main — إغلاق الـPR يكفي للتراجع الكامل قبل الدمج).
- **تراجع انتقائي بعد الدمج (إن دُمج مستقبلًا):** كل وحدة التزام واحد قابل لـ`git revert` مستقل: EXE-001=‏`c770cf0`، EXE-002=‏`40936e0`، EXE-003=‏`93c11b9`، EXE-004=‏`71f2428`(+`2175a5d` تصحيح النوع)، EXE-005/006=‏`ae96cdb`؛ التزاما التنسيق/النوع تجميليان.
- **أثر الرجوع:** رجوع EXE-001/EXE-004 يعيدان ابتلاع العمليات المتكررة بصمت (فقدان تخصيص/رد مقصود)؛ رجوع EXE-002 يعيد إسقاط حقل القدرات عند كاتبي الوقت/الجدول؛ رجوع EXE-003 يعيد الفشل الزائف لـMIC-2. لا ترحيل بيانات مطلوب في أي اتجاه (لا مخطط ولا تصدير تغيّرا).

## 10. الملفات المتغيرة لكل وحدة (مطابقة النطاق)

| الوحدة | ملفات التنفيذ | ملفات الاختبار |
|---|---|---|
| EXE-001 | `pages/CashDistribution.tsx` | `CashJourneys.dom.test.tsx`، `application/finance/unallocatedDistribution.test.ts` |
| EXE-002 | `application/preferences/updateLocalPreferences.ts` (جديد)، `preferenceService.ts`، `time/actualTimeService.ts`، `scheduling/scheduleService.ts` | `application/preferences/preferencesFieldPreservation.test.ts` (جديد) |
| EXE-003 | `src/domain/cash-continuity/types.ts`، `policies.ts`، `application/finance/integrityCheckService.ts` | `tests/domain/public-surface.test.ts`، `application/finance/integrityCheckService.test.ts` |
| EXE-004 | `application/fulfillment/fulfillmentService.ts`، `components/orders/OrderDepositPanels.tsx`، `pages/OrderDetail.tsx` | `application/fulfillment/fulfillmentService.test.ts`، `G4RetainedDeposit.dom.test.tsx` |
| EXE-005/006 | `pages/Market.tsx`، `pages/Home.tsx` | `Nav001.dom.test.tsx` |
| توثيق | `reports/agent-report/2026-09-16_15-30-wave-1-correctness-truth/**`، `reports/agent-report/README.md`، `CHANGELOG.md`، `docs/operations/current-state.md` | — |

الإجمالي: 21 ملف كود/اختبار (+770/−125) + ملفات التوثيق. لا ملف حُذف ولا أُعيد تسميته ولا نُقل.

## 11. الدفع وPR

- الفرع `fix/wave-1-correctness-truth` دُفع إلى `Qays7753/Micro` (رأس الفرع `2dc53a3` — آخر التزام كود `2175a5d` يليه التزاما التوثيق) وفُتح **PR #162**: https://github.com/Qays7753/Micro/pull/162
- **لا دمج** — التكليف صريح (§1: «لا تدمج إلى main»؛ §13؛ §14: `PR_OPENED_NOT_MERGED`)، وقاعدة المستودع (AGENTS.md §9: لا دمج دون مراجعة diff وCI وموافقة المالك).

## 12. ما لم يُختبر (صراحةً)

1. **جهاز فعلي/PWA مثبتة/Offline إنتاجي** — لا عتاد لمس في البيئة؛ الاختبار الحي محاكاة متصفح 390×844 على خادم محلي.
2. **نقر مزدوج فيزيائي على مستوى DOM** — مثبت خدميًا وبنيويًا (تعطيل الزر أثناء الحفظ + مفتاح واحد للتأكيد الواحد) لا باختبار DOM مزدوج النقر.
3. **إعادة تحميل صفحة حقيقية للتفضيلات** — مثبتة بمثيلات خدمات جديدة فوق المخزن نفسه.
4. **IndexedDB الفعلي في الجلسة الحية** — التحقق الحي على خادم التطوير يستخدم التخزين الحقيقي للمتصفح، وتكافؤ المحولات مغطى باختبارات المطابقة القائمة ضمن `pnpm check`.
5. **مقاسات غير 390×844** — لم تُختبر حيًا ولا يُدَّعى.
