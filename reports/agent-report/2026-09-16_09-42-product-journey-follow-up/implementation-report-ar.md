# تقرير متابعة وتصحيح وتحقق ودمج — حزمة رحلة المنتج — Micro

**التاريخ والوقت (Asia/Amman):** 16 أيلول/سبتمبر 2026 — بدء الجولة ~07:10، اكتمال التحقيق ~07:45، الدمج لاحقًا هذا الصباح
**الفرع:** `fix/product-journey-verification-20260916` من `origin/main` @ `db003f4fa0b1f237d4499a7a3633b290ec28f4fa`
**الغرض:** تصحيح العيوب المؤكدة داخل نطاق حزمة رحلة المنتج المعتمدة (13 issue) بعد ظهور دليل CI فاشل ينقض موثوقية التقرير السابق، ثم PR ودمج مشروط ببوابات قبول صريحة، فتحقق قراءة فقط بعد الدمج. هذه جولة تصحيح مضبوطة، لا إعادة تصميم.

---

## 1. الخلاصة التنفيذية

أثبت تشغيل CI لاحق على `main` @ `db003f4` (الـRun 35046087986) أن ادعاء «404 + 1334 اختبارًا ناجحًا» في التقرير السابق لم يكن موثوقًا على مستوى التشغيل: كل ملفات الاختبار نجحت ظاهريًا لكن التشغيل فشل بـ**16 رفضًا غير معالج** (15 من `Settings.lockGate.dom.test.tsx` و1 من `U11.dom.test.tsx`)، كلها `TypeError: preferences.readDisabledCapabilities is not a function`. أُعيد إنتاج العطل محليًا من فرع نظيف قبل أي تعديل (14 اختبارًا ناجحًا + 15 رفضًا غير معالج، خروج 1).

بيّن التحقق ثلاثة عيوب مؤكدة داخل النطاق وأُصلحت كلها على فرع واحد مخصص بمراحل مُرحّلة:

1. **SET-003 (مضاعفات الاختبار):** مضاعفات `preferences` اليدوية في ملفَي اختبار الإعدادات كانت تنقصها `readDisabledCapabilities`/`saveDisabledCapabilities` وخدمات أعداد السجلات الأربع — استُبدلت بالخدمات الحقيقية عبر مخزن اختبار (موافقة للعقد بنيويًا).
2. **SET-003 (بقاء حقل القدرات):** ثلاثة كتّاب تفضيلات كاملة السجل (`markVerifiedExport` و`saveBackupReminderEnabled` و`saveInstallBannerDismissal`) كانت تُسقط حقل `disabledCapabilities` فيعيد تفعيل كل القدرات بصمت — أُصلحوا جميعًا مع اختبارات انحدار لكل كاتب.
3. **ORD-003 (تحرير الشروط المشتركة من صفحة الطلب):** مسار التحرير كان يصفّر حصتي التكلفة المشتركة دائمًا، ويفتح اللوحة من فراغ فيمسح أجرة/كلفة مسجلة بحفظ غير مقصود، ويحتفظ بمفتاح حتمية واحد طوال عمر الصفحة فيصير التعديل الثاني صمتًا — أُصلحت الثلاثة بتعبئة مسبقة من القيم المسجلة وحقلي حصص مطابقين لمحرر الاتفاق ومفتاح عملية جديد لكل فتح لوحة.

**ORD-001 (الرحلة):** التكليف المستعاد لا يتضمن نصًا يفرض رحلة من شاشة واحدة؛ التفسير الثلاثي المسجل (مسودة→تكلفة→اتفاق) يلبي المتطلبات المستعادة (نموذج واحد بلا سريع/متقدم، الحد الأدنى وصف واحد، المتقدم مطوي). **التصنيف: PRESERVE** مع توثيق الدليل وتسجيل اللبس المتبقي قرارًا للمالك. لم تُلمس بوابة §11 (لا عمل هيكلي في هذه الجولة إطلاقًا).

البقية (OPS-001/ORD-002/SET-001/SET-002/NAV-001/NAV-002/CON-001/MOB-001/TOOL-001/DOC-001): **PRESERVE** — لا عيب مثبت؛ أدلة كل تصنيف في §5.

**النتيجة النهائية للأبواب على الفرع:** `pnpm check` خروج 0 كاملًا — جذر 36 ملفًا/404 اختبارات، ويب 190 ملفًا/1341 اختبارًا (منها 7 جديدة)، صفر رفض غير معالج، صفر `.only`/`.skip`، lint ‏0 خطأ/36 تحذيرًا (≤37)، الكثافة كلها ضمن السقوف (OrderDetail ‏179/179 دون رفع)، والبناء 646,888/650,000 خام و154,359/155,000 مضغوطًا. لا تغيير مخطط/تصدير (35/27)، ولا تغيير أي قاعدة مالية.

## 2. أصناف الأدلة

| الصنف | التعريف في هذا التقرير |
|---|---|
| `VERIFIED` | ثُبّت بمصدر حي (كود/اختبار/سجل Git/واجهة GitHub API) هذه الجولة، وأُعيد إنتاجه حيث أمكن |
| `INFERRED` | استنتاج مبني على أدلة موثقة لكن بلا إعادة إنتاج مباشرة |
| `UNVERIFIED` | بيان تاريخي لم يعد قابلًا للتحقق المستقل الآن |
| `NOT_EXECUTED` | عمل مصرح به ولم يُنفذ في هذه الجولة مع السبب |
| `DEFERRED` | يحتاج قرار مالك أو موجة لاحقة |
| `BLOCKER` | يعيق بوابة قبول |

## 3. خط الأساس وحالة الفروع/PRs (SHAs كاملة)

| البند | القيمة | الدليل |
|---|---|---|
| `origin/main` المتحقق حيًا قبل أي كتابة | `db003f4fa0b1f237d4499a7a3633b290ec28f4fa` («docs: finalize product-journey report SHA references») | `git fetch origin` + `git rev-parse origin/main` + GitHub API — مطابق للمتوقع في التكليف |
| نظافة شجرة العمل | نظيفة بعد تحييد أثر أنماط الملفات (1206 تغيير mode فقط 100644→100755، صفر إدراج/حذف — أثر نظام ملفات لا انجراف محتوى؛ عُولج محليًا بـ`core.fileMode false` دون أي تغيير في المستودع) | `git status` / `git diff --stat` |
| فرع التنفيذ | `fix/product-journey-verification-20260916` من `db003f4` | `git checkout -b` من origin/main المتحقق |
| التقرير التاريخي (لا يُمس) | `reports/agent-report/2026-09-16_05-10-product-journey/implementation-report-ar.md` | قراءة كاملة هذه الجولة |
| سجل أبواب التقرير التاريخي | `reports/agent-report/2026-09-16_05-10-product-journey/test-results/gates.txt` | قراءة كاملة هذه الجولة |
| فشل CI الموثد | Run **35046087986** (push، main @ `db003f4`، 2026-09-16T01:57:15Z): خطوة «Verify domain and prototype» فاشلة | https://github.com/Qays7753/Micro/actions/runs/35046087986 — السجل نُزّل وفُحص سطرًا سطرًا |
| فروع/PRs أخرى | لا PR مفتوح على main وقت البدء؛ لم يُلمس أي فرع أو PR آخر | GitHub API (`/pulls?state=all`) |
| طريقة الدمج المعتمدة للمستودع | merge commit (عرف المستودع القائم: `Merge pull request #NNN …` في تاريخ main) | `git log --merges` |

## 4. التكليف الأصلي المستعاد: المصدر والمتطلبات الأساسية

**المصدر:** تكليف المالك المعتمد «Micro — Approved Product Journey, Navigation, Setup, Mobile and Content Implementation» كما بقي محفوظًا في سياق المحادثة نفسها (ملخص الجلسة يثبت قائمة الـ13 issue بمُعرّفاتها ومتطلباتها الجوهرية)، متقاطعًا مع ثلاثة مصنّفات مستودعية حية تؤكد النطاق والمصطلحات: تقرير التنفيذ السابق (§«النطاق المنفذ» وجدول الـ13)، و`docs/operations/current-state.md` §1-ب (حزمة رحلة المنتج: ما تغير وما لم يتغير)، و`docs/08-glossary.md` (قسم مصطلحات الحزمة CON-001). **النص الحرفي الكامل للتكليف غير متاح**؛ المستعاد هو جوهره المثبت بهذه المصادر الثلاثة المستقلة. لهذا السبب تحديدًا صُنّف بند ORD-001 (الوحيد الذي يُعتمد فيه على تفسير النص) PRESERVE مع تسجيل اللبس، ولم يُبنَ أي تغيير دلالي على نص غير مستعاد.

**المتطلبات الأساسية المستعادة (ذات الصلة بهذه الجولة):**
- 13 issue: OPS-001، ORD-001..003، SET-001..003، NAV-001/002، CON-001، MOB-001، TOOL-001، DOC-001.
- ORD-003: أربع مسؤوليات نقل (`project_pays / customer_pays_project / customer_pays_courier / shared`)؛ أعلام احتواء تمنع الاحتساب المزدوج؛ `null` = غير مسجل (لا صفر مخترعًا) و`0` قيمة صريحة؛ `customer_pays_courier` معلومة سياقية لا أثر ماليًا؛ `orderValueMinor = agreedPriceMinor + billableFee`.
- SET-003: طبقة قدرات توقف مداخل الإدخال اليومية فقط؛ السجلات والديون القائمة تبقى ظاهرة؛ الأساس (بيع/مصروف/كاش/تحصيل) لا يُوقف؛ التفعيل لاحقًا يعيد كل شيء.
- قواعد المال الملزمة (AGENTS.md §6): القبض ليس الربح؛ التسليم لا يسجل قبضًا تلقائيًا؛ لا كتابة عمياء؛ حتمية لكل حدث حساس.
- ممنوعات: لا مزامنة سحابية، لا سوق/نقل/AI مشغل، لا تزييف؛ غير المشغل = «قريبًا».

## 5. جدول مطابقة المشكلات الـ13

| المعرّف | التصنيف | الدليل المختصر |
|---|---|---|
| OPS-001 | `PRESERVE` | `InventoryMovementEditor.tsx`‏97/127/145/161: لا اختيار أول سجل؛ معرّف وصلة غير صالح ⇒ رسالة «لم نجد المرجع المطلوب في الوصلة…»؛ خيارات «اختر…» صريحة؛ `Ops001MovementSelection.dom.test.tsx` خضراء في `pnpm check` |
| ORD-001 | `PRESERVE` (مع لبس مسجل — §9) | رحلة ثلاثية بنموذج واحد: مسودة بوصف واحد وزبون اختياري (رفض دقيق للفراغ)، تكلفة ناقصة قابلة للحفظ صراحة، اتفاق بالحياة اليومية أولًا والمتقدم مطوي؛ `?created=1` موثق؛ لا مسار سريع/متقدم منفصل — مطابق للمتطلبات المستعادة |
| ORD-002 | `PRESERVE` | إيصال أول تسليم مخصص (`delivery-success-receipt`) وملخص المسلّم عند إعادة الفتح، مغطيان في `OrdJourneys.dom.test.tsx` و`G3Delivery.dom.test.tsx`؛ حتمية التسليم مختبرة (اختبار الالتزام المزدوج) وخضراء |
| ORD-003 | `FIX_NOW` (نُفّذ — §6-ج) | مسار تحرير OrderDetail كان ينقض «تُحرر وتُحفظ وتُعرض» للحصص المشتركة |
| SET-001 | `PRESERVE` | `Setup.tsx`‏209-218: قيمة المنتج المحلية الصادقة («محفوذة على هذا الجهاز فقط… لا مزامنة سحابية») بلا وعد قدرة غير مشغلة |
| SET-002 | `PRESERVE` | الإتمام يفتح `/?setup=1` بلافتة نجاح؛ الأساس عمق اختياري قابل للاستئناف؛ اختبارات `Setup.ui` و`Foundation.ui` خضراء |
| SET-003 | `FIX_NOW` (نُفّذ — §6-أ و§6-ب) | مضاعفات اختبار ناقصة أسقطت CI؛ وثلاثة كتّاب تفضيلات يسقطون حقل القدرات |
| NAV-001 | `PRESERVE` | `BottomNav.tsx`: خمسة مقاعد متساوية بلا زر مركزي؛ `/market` إعلان قريبًا؛ `Nav001.dom.test.tsx` خضراء |
| NAV-002 | `PRESERVE` | «النتيجة المتاحة» بطاقة مستوى أول في المالية (سقف Finance ‏261 ضمن الكثافة)؛ مصطلحات FIN-001 محفوظة |
| CON-001 | `PRESERVE` | `docs/08-glossary.md` §«مصطلحات حزمة رحلة المنتج (CON-001)»: الربح التقديري/غير مسجل/النقل والتوصيل… نصوص هذه الجولة التزمت بها (تسميات حقلي الحصص مطابقة لمحرر الاتفاق) |
| MOB-001 | `PRESERVE` | إصلاحا المحاكاة قائمان (`index.css`‏724: `repeat(5, minmax(0, 1fr))` لعمود الشبكة؛ حارس إيصال التسليم)؛ لا جهاز فعلي في البيئة — قيد مسجل أصلًا في التقرير السابق |
| TOOL-001 | `PRESERVE` | `ToolsIntegrity.tsx`‏35/80/116-120: العدّ من `registeredCheckCount()` وحالة «غير متاح» مستقلة ومجاميع متفقة مع طول القائمة |
| DOC-001 | `PRESERVE` | `current-state.md` §1-ب + عقد 02 + القاموس + CHANGELOG متزامنة مع الحزمة؛ وهذه الجولة تضيف تحديث توثيق المتابعة نفسه |

## 6. السبب الجذري لكل عيب أُصلح

كل عيب موثق بالبطاقة الإلزامية:

### أ) SET-003-A — مضاعفات اختبار الإعدادات تنقصها عقد الخدمة

| البند | التفصيل |
|---|---|
| ID | `SET-003-A` |
| Classification | `VERIFIED` — أُصلح (`FIX_NOW` منفذ) |
| Evidence | سجل CI Run 35046087986: 16 رفضًا غير معالجًا `TypeError: preferences.readDisabledCapabilities is not a function` عند `SettingsCapabilitiesSection.tsx:56:44` (15 من lockGate + 1 من U11). إعادة إنتاج محلية قبل أي تعديل: `vitest run client/src/Settings.lockGate.dom.test.tsx` ⇒ 14 اختبارًا ناجحًا + 15 رفضًا غير معالج، خروج 1. قراءة المصدر: المضاعفات اليدوية الثلاثة (U11:43، lockGate:81، lockGate:253 قبل الإصلاح) لا تحوي `readDisabledCapabilities`/`saveDisabledCapabilities` ولا الخدمات الأربع (agreements/inventory/supplierPurchases/catalog) التي تمررها صفحة الإعدادات إلى قسم القدرات |
| Affected files/modules | `U11.dom.test.tsx`، `Settings.lockGate.dom.test.tsx` (مضاعفات فقط — لا كود إنتاج) |
| Impact | فشل CI على main رغم نجاح كل الاختبارات ظاهريًا؛ ادعاء الاختبار الأخضر في التقرير السابق غير موثوق |
| Dependencies | لا شيء (عزل كامل) |
| Minimum safe remediation | استبدال المضاعفات بالخدمات الحقيقية عبر مخزن ذاكرة مخصص للاختبار — موافقة للعقد بنيويًا فلا يمكن أن تنقص دالة. لم يُضعف أي سلوك إنتاج ولم تُلمس حراسة `Home`/`Orders` القائمة (`typeof … === "function"`) |
| Tests | تشغيل الملفين معًا: خروج 0، ‏15/15 اختبارًا، صفر رفض غير معالج. اختبار انحدار DOM جديد `Set003SettingsSection.dom.test.tsx` يصيّر صفحة الإعدادات كاملة عبر الخدمات الحقيقية ويبدّل قدرة عبر المخزن الحقيقي ويعيد قراءتها منه |
| Acceptance criteria | كل اختبارات الإعدادات المتأثرة تستخدم مضاعفًا موافقًا للعقد ✔؛ لا رفض غير معالج في أي مجموعة ✔ (تحقق مباشر + `pnpm check` كاملًا)؛ تغطية انحدار تمنع عودة الدالة المفقودة بصمت ✔ (اختبار DOM + تجميد عقد `PreferenceService.prototype`)؛ سلوك SET-003 محفوظ (الإيقاف يخفي مداخل الإدخال فقط ولا يمس السجلات — `Set003Capabilities.dom.test.tsx` قائم وأخضر) ✔ |
| Rollback boundary | `git revert` لمرحلة `ac8176a` وحدها تعيد المضاعفات اليدوية دون مساس ب/ج |

### ب) SET-003-B — كتّاب تفضيلات كاملة السجل يُسقطون حقل القدرات

| البند | التفصيل |
|---|---|
| ID | `SET-003-B` |
| Classification | `VERIFIED` — أُصلح (`FIX_NOW` منفذ) |
| Evidence | `preferenceService.ts`: `save(theme)` (س49) و`saveDisabledCapabilities` (س156) ينقلان الحقل، بينما `markVerifiedExport` (س67-77 قبل الإصلاح) و`saveBackupReminderEnabled` (س86-96) و`saveInstallBannerDismissal` (س119-129) يكتبون السجل كاملًا بدونه. عقد التخزين: `savePreferences` استبدال سجل كامل (`MemoryLocalStore` س119-122 استنساخ واستبدال؛ `IndexedDbLocalStore.writeOne`). النتيجة: أي تصدير متحقق منه أو إطفاء لتذكير النسخ أو إخفاء لبطاقة التثبيت كان يعيد تفعيل كل القدرات بصمت |
| Affected files/modules | `application/preferences/preferenceService.ts` (ثلاث إضافات سطر واحد لكل كاتب مع تعليق مؤرخ) |
| Impact | فقدان صامت لحالة القدرات عند أفعال غير مرتبطة بها — نقض مباشر لقبول SET-003 («يبقى بعد كل حفظ تفضيل آخر») |
| Dependencies | لا شيء |
| Minimum safe remediation | إضافة `disabledCapabilities: current.value?.disabledCapabilities ?? []` إلى الكاتب الثلاثة — أصغر تغيير آمن بلا أي تغيير دلالي آخر |
| Tests | `preferenceService.test.ts`: اختبار يمشي على الكتّاب الأربعة غير المرتبطين (المظهر/التصدير المتحقق/تذكير النسخ/بطاقة التثبيت) ويثبت بقاء `["orders","inventory"]` بعد كل واحد + بقاء بقية الحقول؛ اختبار سجل قديم بلا الحقل يُقرأ «الكل مفعّل» ويُرسَّخ بأول حفظ؛ اختبار تجميد عقد `PreferenceService.prototype` (أي دالة عامة جديدة تكسر الاختبار فتُلزم تحديث المضاعفات والكتّاب معًا) |
| Acceptance criteria | قدرة موقفة تبقى موقفة بعد كل كاتب غير مرتبط ✔ (اختبار لكل كاتب)؛ السجلات القديمة بلا الحقل متوافقة رجعيًا وتُقرأ كل القدرات مفعلة ✔؛ لا حقل آخر يضيع بالتصحيح ✔ (اختبار صريح)؛ تغطية كل الكتّاب لا المظهر وحده ✔ |
| Rollback boundary | `git revert ac8176a` يعيد الكاتب الثلاثة (يرجع العيب ب مع أ) |

### ج) ORD-003-C — تحرير شروط النقل المشتركة من صفحة الطلب

| البند | التفصيل |
|---|---|
| ID | `ORD-003-C` |
| Classification | `VERIFIED` — أُصلح (`FIX_NOW` منفذ) |
| Evidence | `OrderDetail.tsx` قبل الإصلاح: (1) `saveDeliveryTerms` يرسل حرفيًا `projectShareMinor: null, customerShareMinor: null` فيمحو حصتين مسجلتين عند أي تعديل شروط؛ (2) لا تعبئة مسبقة إطلاقًا — فتح «تعديل شروط النقل» يبدأ من `project_pays` ومبالغ فارغة فيمسح حفظٌ غير مقصود أجرة/كلفة مسجلتين؛ (3) `termsOperationKeyRef` يولد مرة عند تركيب الصفحة ولا يتجدد، و`recordDeliveryTerms` (policies.ts س464) يرجع الطلب دون تغيير عند وجود حدث بنفس المفتاح — فالتعديل الثاني بعد نجاح الأول **صمت تام**. محرر الاتفاق (السطح الأصلي) لديه حقلا الحصص (AgreementEditor.tsx س600-633) وتحقق مطابق (س239-248) — التناقض بين السطحين مؤكد من المصدر |
| Affected files/modules | `pages/OrderDetail.tsx` (حالة + تعبئة مسبقة + مدخلا حصص + مسار حفظ + سطرا عرض + تجديد مفتاح)، `OrdJourneys.dom.test.tsx` (3 اختبارات) |
| Impact | فقدان بيانات موثقة (الحصص) ومحو قيم مسجلة (أجرة/كلفة) وتجاهل صامت لتحرير صريح — نقض لقبول «تُدخل وتُحرر وتُحفظ وتُفتح ثانية وتُعرض» للشروط المشتركة |
| Dependencies | لا شيء على الدومين — `recordDeliveryTerms`/`buildDeliveryTerms` سليمان ويقبلان الحصص أصلًا (عقد قائم) |
| Minimum safe remediation | (1) `openTermsPanel()` يملأ كل الحقول من `order.deliveryTerms` المسجلة ويجدد مفتاح العملية لكل فتح (نمط `openReversalPanel` نفسه)؛ (2) حقلا حصص بنفس مكونات وتسميات محرر الاتفاق (`micro-shared-costs`) بشرط `shared`؛ (3) مسار الحفظ يحمل الحصص عند `shared` ويصفّرهما خارجها (عقد الدومين: الحصص لا تُسجل إلا للمشتركة)؛ (4) سطرا عرض للحصتين في لوحة الشروط |
| Tests | `OrdJourneys.dom.test.tsx`: تعبئة مسبقة كاملة من قيم مسجلة + تعديل حصة يُحفظ ولا يُصفَّر؛ تعديل ثانٍ بعد نجاح الأول = حدثان `delivery_terms_recorded` وقيمة قابلة للتحصيل مشتقة (50+3=53)؛ التحويل خارج المشتركة يخفي الحقلين ويصفّرهما عمدًا. 9/9 خضراء |
| Acceptance criteria | الخيارات الأربعة تبقى مميزة ✔؛ الشروط المشتركة تُدخل وتُحرر وتُحفظ وتُفتح ثانية وتُعرض وفق التكليف ✔؛ أعلام الاحتواء تمنع الاحتساب المزدوج كما هي (لم تُمس — اختبارات الدومين الـ13 خضراء) ✔؛ الطلبات القديمة بلا شروط متوافقة رجعيًا (قراءة `null` = لا شروط) ✔؛ اختبارات دومين وواجهة مركزة خضراء ✔ |
| Rollback boundary | `git revert d36fc5f` وحدها تعيد مسار التحرير القديم (الحصص تعود للتصفير) دون مساس أ/ب |

### د) ORD-001 — تصنيف الرحلة (لم يُصلح شيء عمدًا)

| البند | التفصيل |
|---|---|
| ID | `ORD-001-D` |
| Classification | `PRESERVE` — `VERIFIED` (ضد المتطلبات المستعادة) مع لبس متبقٍ `DEFERRED` لقرار المالك |
| Evidence | التكليف المستعاد (ملخص الجلسة + تقرير التنفيذ + current-state §1-ب) يصف «رحلة تكيفية» بـ«نموذج واحد ودولة واحدة (لا سريع/متقدم)» و«الحياة اليومية أولًا والمتقدم مطوي» — لا نص يفرض شاشة واحدة. التنفيذ الحالي: مسودة (وصف واحد إلزامي، زبون اختياري، معرف تلقائي) → تكلفة (الناقص يُحفظ صراحة) → اتفاق (اليومي أولًا، المتقدم مطوي) — يلبي كل متطلب مستعاد. تفسير «الرحلة الواحدة» موثق في التقرير السابق (قرار تطبيقي #2) |
| Impact | لا عيب مثبت؛ إعادة البناء كشاشة واحدة كانت ستستدعي العمل الهيكلي الممنوع في هذه الجولة |
| Minimum safe remediation | لا شيء — الحفاظ مع التوثيق |
| Tests | تغطية قائمة خضراء: `OrdJourneys.dom.test.tsx` (لافتة الإنشاء وظهورها الشرطي)، ومسار المسودة المحفوظ مغطى باختبارات المسودات القائمة |
| Acceptance criteria | لا إعادة تصميم صامتة ✔؛ الدليل موثق ✔؛ اللبس مسجل للمالك ✔ (§9 و§13) |
| Rollback boundary | لا ينطبق (لا تغيير) |

## 7. الملفات المتغيرة ولماذا

| الملف | نوع التغيير | لماذا |
|---|---|---|
| `apps/prototype-web/client/src/U11.dom.test.tsx` | تعديل مضاعف | SET-003-A: مضاعف preferences يدوي ناقص ⇒ الخدمة الحقيقية + الخدمات الأربع عبر مخزن اختبار |
| `apps/prototype-web/client/src/Settings.lockGate.dom.test.tsx` | تعديل مضاعفين | SET-003-A نفسه في موقعي الت doble (beforeEach المجموعة ٦ وmountSettings المجموعة ٥) |
| `apps/prototype-web/client/src/application/preferences/preferenceService.ts` | +3 أسطر (كاتب × سطر) + تعليقات | SET-003-B: نقل `disabledCapabilities` في الكاتب الثلاثة |
| `apps/prototype-web/client/src/application/preferences/preferenceService.test.ts` | +3 اختبارات | انحدار ب/ج: كل كاتب غير مرتبط + سجل قديم + تجميد عقد الخدمة |
| `apps/prototype-web/client/src/Set003SettingsSection.dom.test.tsx` | ملف جديد (اختبار واحد) | انحدار أ: سطح الفشل نفسه (صفحة الإعدادات + قسم القدرات) عبر خدمات حقيقية |
| `apps/prototype-web/client/src/pages/OrderDetail.tsx` | تعديل مسار التحرير | ORD-003-C: تعبئة مسبقة + حقلا حصص + حفظ الحصص + عرضهما + مفتاح لكل فتح لوحة |
| `apps/prototype-web/client/src/OrdJourneys.dom.test.tsx` | +3 اختبارات | انحدار ج: التعبئة/البقاء، التعديل الثاني حدث موثق، الخروج من المشتركة |
| `CHANGELOG.md` | إدخال واحد | DOC: توثيق إصلاحات المتابعة للمستخدم |
| `docs/operations/current-state.md` | سطر حالة + ملاحظة | DOC: الحالة الحية تطابق main (عقد الإغلاق §9) |
| `reports/agent-report/README.md` | صف فهرس | عرف تقارير الوكلاء (append-only) |
| هذا الملف و`test-results/gates.txt` | جديد | عقد التقرير المطلوب لهذه الجولة |

**إجمالي كود الإصلاح:** 7 ملفات (5 اختبارات/مضاعفات، 2 إنتاج: `preferenceService.ts`‏+9 أسطر و`OrderDetail.tsx`‏+83/−3). لا ملف حُذف، لا اختبار حُذف، لا إعادة تسمية، لا نقل.

## 8. الاختبارات وCI: الأوامر الفعلية والرموز والعدّ وروابط التشغيل

**ملاحظة بيئة:** بـ`pnpm` عبر Corepack بالإصدار المثبت في المستودع 9.15.9 (مطابق لـCI). الصيغة الفعلية المستخدمة للأوامر المركزة هي صيغة المستودع `pnpm --filter @micro/prototype-web test -- <path>`.

| الأمر (كما نُفذ فعليًا) | الخروج | العدّ | ملاحظات |
|---|---|---|---|
| `pnpm --filter @micro/prototype-web test -- client/src/Set003Capabilities.dom.test.tsx` | 0 | 1 ملف / 4 اختبارات | سلوك SET-003 الأصلي محفوظ |
| `pnpm --filter @micro/prototype-web test -- client/src/Settings.lockGate.dom.test.tsx client/src/U11.dom.test.tsx` | 0 | 2 ملف / 15 اختبارًا | **صفر رفض غير معالج** (كان 15+1) |
| `pnpm --filter @micro/prototype-web test -- client/src/OrdJourneys.dom.test.tsx` | 0 | 1 ملف / 9 اختبارات | 6 قائمة + 3 انحدار جديدة |
| `pnpm --filter @micro/prototype-web test -- client/src/Set003SettingsSection.dom.test.tsx` | 0 | 1 ملف / 1 اختبار | انحدار السطح الفاشل |
| `pnpm --filter @micro/prototype-web test -- client/src/application/preferences/preferenceService.test.ts` | 0 | 1 ملف / 6 اختبارات | 3 قائمة + 3 انحدار |
| `pnpm test -- tests/domain/craft-order-delivery-terms.test.ts` | 0 | 1 ملف / 13 اختبارًا | عقد الدومين غير ممسوس |
| `pnpm check` (المحاولة 1) | 1 | — | فشل prettier وحده في `OrdJourneys.dom.test.tsx` (تنسيق) — صُحح بـ`prettier --write` والمرحلة `68de2c1` |
| `pnpm check` (المحاولة 2 — النهائية) | **0** | جذر: 36 ملفًا / 404 اختبارات؛ ويب: 190 ملفًا / 1341 اختبارًا | صفر رفض غير معالج؛ lint ‏0 خطأ / 36 تحذيرًا (≤37)؛ كثافة كلها ضمن السقوف (OrderDetail ‏179/179 بلا رفع)؛ بناء 646,888/650,000 خام و154,359/155,000 gzip — PASS |

روابط تشغيل CI على GitHub تُدرج في §11/§12 بعد فتح الـPR وتشغيله (تُحدَّث في المرحلة التوثيقية الأخيرة قبل الدمج). المعيار المقبول: خروج 0، لا «Vitest caught … unhandled errors»، لا `.only`/`.skip` (فحص `check-test-focus` ضمن الأبواب)، وتسجيل الأمر والرمز ورابط التشغيل.

## 9. نتيجة البوابة الهيكلية لـORD-001

**النتيجة: البوابة غير مُفعَّلة في هذه الجولة — لا عمل هيكلي مطلوب ولا منفذ.**

- سؤال الجولة: هل يفرض التكليف المستعاد «تجربة طلب من شاشة واحدة»؟ **الجواب من الأدلة المستعادة: لا.** لا في ملخص الجلسة، ولا في تقرير التنفيذ، ولا في current-state §1-ب أي نص يفرض شاشة واحدة؛ الموصوف «نموذج واحد ودولة واحدة (لا سريع/متقدم)» و«المتقدم مطوي» — وهو منفذ.
- لذلك لم يُبدأ المسح الهيكلي (الذي نُفّذ وأُغلق أصلًا في المجموعة ٧ ووافق المالك على خطة المعالجة الرباعية — AGENTS.md §11)، ولم يُخطط أي إعادة هيكلة، ولم يُخف أي عمل هيكلي داخل هذا الـPR: الـdiff كله أسطر محدودة في سبعة ملفات داخل النطاق.
- **اللبس المتبقي (قرار مالك، `DEFERRED`):** إن كان قصد المالك الأصلي حرفيًا «شاشة واحدة للطلب البسيط»، فهذا قرار منفصل يستدعي — بموجب §11 — موجة معتمدة من خطة المعالجة الهيكلية وقرار مالك صريح؛ يُرحَّب بتوجيه المالك ويُفتح حينها عمل مستقل. التفسير الحالي (رحلة ثلاثية الخطوات بنموذج واحد) موثق ومختبر وقائم على main.

## 10. الأثر المالي والدلالي والمخطط والتصدير والتخزين

| البُعد | الأثر | الدليل |
|---|---|---|
| قواعد المال | **لا تغيير.** الإيراد/التكلفة/الكاش/القبض/النتيجة/سقوف القبض/العربون كلها كما هي؛ الحصص المشتركة بقيت معلومة سياقية لا تدخل أي معادلة (كما في محرر الاتفاق والدومين) | اختبارات الدومين الـ13 + `OrdJourneys` خضراء؛ لا سطر واحد في `src/domain` تغيّر |
| دلالة الشروط | الحفظ والتحرير فقط؛ تصفير الحصص خارج «مشتركة» هو العقد القائم نفسه (buildDeliveryTerms يرفض الحصص خارجها أصلًا) | policies.ts س448 (قائم، لم يُمس) |
| المخطط/التصدير | **لا تغيير** — `localSchemaVersion`/`localExportVersion` ‏35/27 كما هي؛ لا كيان دائم جديد (حقل `disabledCapabilities` مسجل في حزمة سابقة وحارس touchpoints أخضر) | `storage/local/types.ts` لم يتغير؛ `check-entity-touchpoints` ضمن الأبواب |
| التخزين | كتابة التفضيلات نفسها (سجل واحد) — الإصلاح يضمن نقل الحقل بدل إسقاطه؛ توافق رجعي كامل (سجل بلا الحقل = كل القدرات مفعلة) | اختبار السجل القديم |
| حتمية العمليات | محسّنة صراحة: مفتاح عملية جديد لكل فتح لوحة شروط (إعادة المحاولة داخل المحاولة الواحدة تبقى حتمية) — نفس نمط لوحة التراجع القائم | اختبار «التعديل الثاني حدث موثق» |
| نشر الإنتاج | خارج نطاق هذه الجولة؛ يتبع سير عمل Cloudflare بعد الدمج — لا ادعاء نشر من جهتنا | — |

## 11. بيان الدمج (Merge manifest)

- **الفرع:** `fix/product-journey-verification-20260916` (قاعدة `db003f4fa0b1f237d4499a7a3633b290ec28f4fa`).
- **المراحل:** `ac8176a` (SET-003 أ/ب: المضاعفات + بقاء الحقل + الانحدار) → `d36fc5f` (ORD-003-ج: تحرير الشروط المشتركة) → `68de2c1` (تنسيق prettier لاختبارات الانحدار) → مرحلة التوثيق (هذا التقرير + CHANGELOG + current-state + فهرس التقارير) — تُدرج SHA النهائية للفرع هنا عند الدفع: `8f14abdcb226f094f17afb3da83b77de242b5370 (تُحدَّث إن أُضيفت مرحلة توثيق بعدها)`.
- **الـPR:** يُفتح إلى `main` بعد الدفع؛ رقمه ورابط تشغيل CI النهائي عليه يُدوَّنان هنا قبل الدمج: `PR #160 — https://github.com/Qays7753/Micro/pull/160` و`رابط تشغيل CI النهائي على رأس الفرع يُسجل لحظة اكتماله (الـRun المرتبط بآخر دفعة قبل الدمج)`.
- **طريقة الدمج:** merge commit (عرف المستودع: `Merge pull request #NNN …`)، بتفويض المالك الصريح لهذه الجولة بعد استيفاء كل البوابات.
- **شروط الدمج (كلها يجب أن تتحقق):** استعادة التكليف ومطابقته ✔؛ لا مانع متبقٍ في أ/ب/ج ✔ (كلها منفذة ومختبرة)؛ لا عمل هيكلي مخفي ✔؛ الـdiff محصور في النطاق المعتمد ✔؛ فحوص CI المطلوبة خضراء بلا رفض غير معالج (تُتحقق لحظة الدمج)؛ التقرير والـPR يصفان التغييرات بدقة ✔؛ لا كتابة مباشرة على main ✔.

## 12. تحقق ما بعد الدمج (قراءة فقط)

الإجراء الملتزم به بعد الدمج (كل نتائجه تُعلن في التقرير الختامي داخل المحادثة — لا كتابة إضافية على main بعد الدمج لأن التفويض لا يشملها):
1. التحقق من SHA دمج الـPR على main عبر GitHub API وتسجيله.
2. التحقق من أن ملفات الدمج هي ملفات الـPR حصرها (لا ملفات غير متوقعة) عبر مقارنة `git diff db003f4..main`.
3. التحقق من تشغيل CI المرتبط بالدمج على main وخروجه 0 بلا «Vitest caught … unhandled errors».
4. إعادة تشغيل `pnpm check` محليًا على main المدموج (قراءة فقط) للتأكد من الخروج 0.
5. التأكد من عدم تغيير أي ملف خارج نطاق الـPR ومن عدم إجراء أي نشر إنتاج (النشر يتبع سير عمل Cloudflare المستقل ولا يُدَّعى من جهتنا؛ ‏HTTP 200 وحده ليس دليل نشر).
**النتائج: `<FILL_POSTMERGE>`** (تُملأ في التقرير الختامي بالمحادثة).

## 13. المحددات والقرارات المؤجلة

1. **النص الحرفي للتكليف الأصلي غير متاح** (`INFERRED`): استُعيد جوهره من ثلاثة مصادر مستقلة (ملخص الجلسة + التقرير السابق + current-state). أي بند كان سيعتمد على نص حرفي غائب صُنّف PRESERVE/DEFERRED لا FIX_NOW — ما عدا أ/ب/ج وهي مثبتة بأدلة كود حية مستقلة عن النص (CI + مصدر + إعادة إنتاج).
2. **ORD-001 «شاشة واحدة؟»** (`DEFERRED` لقرار المالك): راجع §9.
3. **`branches: ain]` في `.github/workflows/ci.yml`** (`VERIFIED` كعيب إملائي قديم، `PRESERVE` في هذه الجولة): موجود منذ ما قبل هذه الحزمة (≥ 9fb1b15) وخارج نطاق الـ13 issue، وتشغيلات push على main تحدث فعليًا رغمه (الدليل: الـRun 35046087986 نفسه push على main). **لم يُصلح عمدًا** (خارج النطاق المعتمد) ويُرفع للمالك كقرار مستقل من سطر واحد.
4. **اختبار جهاز فعلي (MOB-001)**: لا عتاد لمس حقيقي في بيئة التنفيذ (`NOT_EXECUTED` — قيد بيئة مسجل أصلًا في التقرير السابق)؛ الخطوة اليدوية اللاحقة قائمة كما هي.
5. **موثوقية ادعاء «404+1334 أخضر» في التقرير السابق**: أُعيد التحقق من الصفر هذه الجولة (`VERIFIED` الآن: 404 + 1341). سبب فشل الادعاء السابق (`INFERRED`): المرجّح أن عدّ الملفات الناجحة قُرئ دون رمز خروج التشغيل الكامل؛ لا يمكن إثبات ما جرى في جلسة سابقة.
6. **CI بعد الدمج على main**: يتحقق في §12؛ لو تعطل تشغيل push على main لأي سبب بنيوي، فتشغيل الـPR نفسه هو البوابة الموثقة.

## 14. تعليمات التراجع

- **حد التراجع الكلي:** `db003f4fa0b1f237d4499a7a3633b290ec28f4fa` (آخر حالة معتمدة قبل هذه الجولة). التراجع الكامل: `git revert -m 1 <merge-commit>` على main (يرجع كل أ/ب/ج معًا) — لا حاجة لأي ترحيل بيانات: كل الحقول اختيارية متوافقة رجعيًا.
- **تراجع انتقائي:** أ/ب معًا = `git revert ac8176a`؛ ج = `git revert d36fc5f`. المرحلتان مستقلتان بنيويًا (لا تشاركان ملفًا واحدًا).
- **أثر الرجوع:** رجوع أ/ب يعيد فشل CI (الرفضات الـ16) ويعيد إسقاط حقل القدرات في الكاتب الثلاثة — يجب عدم رجوعهما إلا مع بديل؛ رجوع ج يعيد تصفير الحصص ومسح القيم بالتحرير (فقدان بيانات تشغيلي محتمل للسجلات الجديدة بعد الدمج) — الحصص المسجلة قبل الدمج لا يمسها شيء لأن الدومين لم يتغير.
- **لا تراجع يحتاج إجراء بيانات**: لا مخطط ولا تصدير تغيّرا.

## 15. الكتابات المنفذة وغير المنفذة صراحةً

**نُفذ (على فرع واحد + فتح PR + دمج مشروط):**
- فرع `fix/product-journey-verification-20260916` من `db003f4` وثلاث مراحل كود (`ac8176a`، `d36fc5f`، `68de2c1`) + مرحلة توثيق (هذا التقرير و`test-results/gates.txt` و`CHANGELOG.md` و`docs/operations/current-state.md` و`reports/agent-report/README.md`).
- دفع الفرع إلى `Qays7753/Micro`، وفتح PR واحد إلى main، وانتظار فحوص CI، والدمج بطريقة merge commit بعد استيفاء §11 حصرها.

**لم يُنفذ صراحةً:**
- **لا كتابة مباشرة على `main`** (كل شيء عبر الـPR؛ لا push ولا commit على main من هذه الجولة).
- **لا كتابة في `Qays7753/Documents`** ولا في أي مستودع آخر.
- **لا تعديل PR #141 أو أي PR/فرع آخر**، ولا حذف/إعادة تعيين/دفع قسري لأي فرع أو تقرير تاريخي.
- **لا تغيير مخطط/تصدير/عملة/تقريب/اعتراف إيراد/تكلفة/كاش/دلالة مسؤولية التسليم** — ولا رفع أي سقف رقمي محروس (الكثافة بقيت 179/179 لـOrderDetail دون رفع؛ lint ‏36≤37؛ ميزانية الحزمة داخل الحد).
- **لا عمل هيكلي/نقل ملفات/توسيع نطاق** خارج أ/ب/ج + التوثيق.
- **لا إصلاح لـ`ci.yml`** (خارج النطاق — مُبلَّغ في §13-3).
- **لا نشر إنتاج ولا ادعاء نشر**؛ ولا استخدام لأي سر في الكود/التقارير (توكن الدفع عبر ملف مؤقت خارج المستودع يُحذف بعد كل استخدام).
