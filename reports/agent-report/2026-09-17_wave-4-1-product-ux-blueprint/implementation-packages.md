# خطة حزم تنفيذ الموجة الرابعة (4.2 / 4.3 / 4.4) — Wave 4.1

**BASE_SHA:** `f8b37062c14aecf85abf5f64e11a191b00eb3c0d` · **التاريخ:** 17/09/2026
**حالة هذه الخطة:** تصميم — **لا تنفَّذ قبل بوابة اعتماد المالك** (الهيكل النهائي، أماكن الخصائص، الأسماء النهائية، Wireframes الأساسية، ترتيب الحزم).
**المرجعية:** قرارات §4 المعتمدة في تكليف Wave 4.1 + جدول القرارات الموحد `W4-DEC-F01..F14` (independent-review.md §5.2) + انتقالات `T1..T10` (target-information-architecture.md §4) + سجل `GAP-4.3-01..17` (state-matrix.md) + فحوص القبول (usability-test-plan.md ب-1/ب-2/ب-3).

---

## 0. القواعد الحاكمة للتنفيذ كله

1. **حزمة واحدة قابلة للمراجعة في الوقت نفسه** — لا PR ضخمًا للواجهة كلها؛ كل حزمة أدناه PR مستقل بترتيب الدمج المحدد.
2. **لا تُعاد.design الشاشة أكثر من مرة:** كل شاشة تُلمس مرة واحدة بنيويًا في 4.2 (مكان/اسم/مدخل)، ومرة واحدة بصريًا في 4.3 (Wireframe/مكونات)، ثم 4.4 فحوص فقط بلا إعادة تصميم — ما عدا إصلاحات معطلة موثقة من اختبار المستخدم.
3. **كل تغيير نص ظاهر يُحدَّث مع اختباره الحارس في نفس الالتزام** (نمط EXE-016/REV-010).
4. **العقود تُحدَّث مع الكود لا بعده:** أي نقل مسار/بديل كنوني/منتج معامل جديد = التزام واحد يشمل العقد (26/33/40) + الحارس + الكود.
5. **الحدود الرقمية محفوظة:** Schema/Export 35/27، سقف lint، ميزانية الحزمة — أي رفع قرار مالك ظاهر في diff مزدوج.
6. **الحد الأدنى لكل حزمة:** `pnpm check` خروج 0 (خط الأساس 1849 اختبارًا عند BASE_SHA + الجديد) + فحوص قبول الحزمة + مراجعة Diff مستقلة للنطاق البصري.
7. **كل GAP-4.3 رهن بوابته:** GAP-4.3-01/10/11 (المربوطة بF06/F09/F07) لا تُنفذ قبل حسم المالك لتلك البوابات.

---

## 1. Wave 4.2 — الهيكل والتنقل والتسمية (5 حزم)

### P-4.2-1 — تثبيت App Shell والتنقل والتسمية الموحدة

| الحقل | القيمة |
|---|---|
| Screen IDs | القشرة (MicroAppShell/AppHeader/BottomNav — تظهر مع كل الأسطح) + SCR-057 + SCR-049 + SCR-053 + SCR-037 + SCR-038 |
| Routes | `/`، `/orders`، `/finance`، `/tools`، `/market` + الترويسة (كل الأسطح) |
| الملفات المتوقعة | `app/navigation.ts` (تسمية «منتجاتي وخدماتي» بدل «الكتالوج»)، نصوص أزرار الرجوع في `pages/Parties.tsx` (99)، `Suppliers.tsx` (71)، `CashWallets.tsx` (94)، `OwnerEntitlement.tsx` (613)، `FinancialEventEditor.tsx` (463/469/722/1007)، `G5DeclarationEditor.tsx` (114)، `OwnerWithdrawalEditor.tsx` (194)، `Home.tsx` (229/450) + اختباراتها الحارسة (جدول REV-010 كاملًا في usability-test-plan.md ب-1-3) |
| التبعيات | لا شيء (أول حزمة) — تسبق كل شيء |
| المخاطر | فشل اختبارات نصية حرفية عند التوحيد (مضبوط: التحديث في نفس الالتزام)؛ نسيان سطح نصي (مضبوط: جدول REV-010) |
| اختبارات القبول | ب-1-1 (1) + ب-1-3 (جدول مطابقة نصوص الرجوع) + ب-1-4 (بوشم «سجّل» F12/GAP-4.3-13 + «سلامة الحسابات» N-22 حيث يظهر) + `pnpm check` |
| الصور المطلوبة | الترويسة والشريط قبل/بعد على 390×844 (SCR-057) |
| Rollback Boundary | الالتزام الأخير قبل الحزمة — رجوع نظيف بلا أثر بيانات (نصوص فقط) |
| ترتيب الدمج | **الأول** |

### P-4.2-2 — تنظيف «أدواتي» (T4 + T5 + T6 + F03)

| الحقل | القيمة |
|---|---|
| Screen IDs | SCR-037 (+ إزالة مراجع SCR-041/SCR-020/SCR-048/SCR-036/SCR-011 من صف الحالة) |
| Routes | `/tools` |
| الملفات المتوقعة | `pages/Tools.tsx` (حذف بطاقة النسخ T4؛ حذف استدعاءي `partyLedger.read()` :125 و`supplierPurchases.readSummary()` :130 وشاراتهما T5؛ حذف صف «السوق والتوصيل» الميت :134 T6؛ حذف قسم حالة الوحدات كاملًا بقرار F03-أ أو إبقاء الصفوف غير المالية الثلاثة F03-ب) + تقليص قائمة التوافق في `ownershipBoundaries.exe017.test.ts` بالالتزام نفسه (عقد 40 §6-4) |
| التبعيات | P-4.2-1 (التسمية) — يمكن التوازي |
| المخاطر | كسر الحارس المعماري إن حُذفت الاستدعاءات دون تقليص القائمة (مضبوط: نفس الالتزام)؛ فقدان مدخل حالة الوحدات (مضبوط: F03 بيتها الموحد في الإعدادات) |
| اختبارات القبول | ب-1-1 (4): لا استدعاء سجل مالي في Tools.tsx؛ الحارس المعماري أخضر؛ WF-009 مطابقة بنيوية |
| الصور المطلوبة | أدواتي قبل/بعد (SCR-037 — الدليل الأهم للتغيير) |
| Rollback Boundary | الالتزام الأخير — رجوع يعيد الصفوف والشارات بلا أثر بيانات (قراءات فقط كانت) |
| ترتيب الدمج | الثاني |

### P-4.2-3 — «البيانات والنسخ الاحتياطي» قسمًا مسمى في الإعدادات (T2)

| الحقل | القيمة |
|---|---|
| Screen IDs | SCR-055 |
| Routes | `/settings` (+ `?focus=guided-import` قائم) |
| الملفات المتوقعة | `pages/Settings.tsx` + `components/settings/*` (تجميع التصدير/الاستعادة/الافتتاحي الموجه/التذكير/تشخيص النسخة/التصفير في قسم مسمى واحد بترتيب «الأقل تدميرًا أولًا» وفق WF-010/WF-025؛ لا خدمة تُنقل — الخدمات مقيمة في وحدتها الصحيحة أصلًا بعقد 40) |
| التبعيات | P-4.2-2 (إزالة بطاقة أدواتي أولًا حتى لا يتكرر المدخل) |
| المخاطر | ازدواج مدخل النسخ مؤقتًا لو دُمجت قبل P-4.2-2 (مضبوط بالترتيب) |
| اختبارات القبول | ب-1-1 (2) جزء الإعدادات؛ مدخل واحد لكل قدرة؛ خدمات النقل ما تزال محصورة في وحدة الإعدادات وجذر التركيب (حارس EXE-017) |
| الصور المطلوبة | الإعدادات قبل/بعد (SCR-055) |
| Rollback Boundary | الالتزام الأخير — لا أثر بيانات (إعادة تنظيم سطح فقط) |
| ترتيب الدمج | الثالث |

### P-4.2-4 — سطح «المالية ← المزيد» + نقل فحص السلامة (T1 + F01 + F05 + REV-003)

| الحقل | القيمة |
|---|---|
| Screen IDs | سطح جديد `/finance/more` (SCR افتراضي جديد يُسجل في الجرد عند التنفيذ) + SCR-041 + SCR-049 |
| Routes | `/finance/more` (جديد — بقرار F01) + `/finance` + `/tools/integrity` (يبقى بقرار F05) |
| الملفات المتوقعة | صفحة السطح الجديد + `pages/Finance.tsx` (مدخل «المزيد») + مدخل سلامة الحسابات من السطح الجديد + **خطة REV-003 إلزامية**: نقل منتجي `?event=`/`?layer=` الثمانية+ (QuickExpenseForm.tsx:155، FinancialEventEditor.tsx:805، walletLedgerService.ts:53/58/71، activityService.ts:119/281، Statement.tsx:361، FinancePeriodResultSection.tsx:109) أو توافقهما من الموقعين + تحديث عقد 26 §2.2/§3 + الاختبارين الحارسين (U001.dom:159، G5Activity.dom:100) **في التزام واحد** + زر رجوع فحص السلامة ديناميكي بالمصدر (GAP-4.3-03) |
| التبعيات | P-4.2-1 (تسمية «سلامة الحسابات» N-22)؛ بوابة F01 محسومة |
| المخاطر | **مخاطرة الانحدار الأكبر في الموجة** (REV-003): 8+ منتجي وصلات عميقة محروسين — لذلك الالتزام الواحد الجامع؛ كسر وصلات الاستعادة القديمة إن حُذف المسار (مضبوط: F05 يبقيه) |
| اختبارات القبول | ب-1-1 (2)(3)؛ ب-1-2 (جدول الروابط القديمة كاملًا)؛ وصلات `?event=` تعمل من الموقع الجديد والقديم أو منتجوها محدثون؛ زر الرجوع الديناميكي |
| الصور المطلوبة | المالية المستوى الأول بعد إضافة «المزيد» + السطح الجديد + فحص السلامة بموضعه الجديد (3 صور) |
| Rollback Boundary | الالتزام الأخير — رجوع يعيد المدخل إلى أدواتي ويحذف السطح الجديد؛ لا بيانات (السطح قارئ) |
| ترتيب الدمج | الرابع |

### P-4.2-5 — سياسات التوزيع للمالية + فصل الكتالوج + منتج destinationWalletId (T3 + T7 + F02 + F04)

| الحقل | القيمة |
|---|---|
| Screen IDs | SCR-036 + سطح السياسات (موضعه بقرار F02: تبويب «شو صار خلال الفترة» الموصى به أو قسم في «المزيد») + SCR-022 |
| Routes | `/catalog` + `/finance` (أو `/finance/more` حسب F02) + `/cash/distribute?destinationWalletId=` + `/cash/wallet/:id` |
| الملفات المتوقعة | نقل قسم `CatalogPoliciesSection` من واجهة الكتالوج إلى سطح المالية المالك (الخدمة `application/finance/recurringWorkService.ts` لا تتحرك — مقيمة في بيتها بعقد 40؛ ينقل السطح فقط) + تحديث بديل الكتالوج الكنوني إلى `/` بقرار F04 (`navigationContract.ts:187` + عقد 26 §2.2 + الاختبار) + الفعل السياقي «وزّع على هذه المحفظة» في `WalletLedger.tsx` (يظهر عند وجود كاش غير موزع؛ يفتح `/cash/distribute?destinationWalletId=<id>&returnTo=/cash/wallet/<id>`) + **تحديث قائمة منتجي المعاملات في عقد 26 §3** (المنتج القانوني الأول لـ`destinationWalletId`) |
| التبعيات | P-4.2-4 (سطح «المزيد» إن اختير موضع السياسات فيه)؛ بوابتا F02/F04 محسومتان |
| المخاطر | إغلاق سطح التوافق بعقد 40 §4 (مضبوط: تحديث العقد بالالتزام)؛ كسر روابط الكتالوج القديمة (مضبوط: تحديث البديل الكنوني مع الاختبار) |
| اختبارات القبول | ب-1-1 (2) جزء السياسات؛ الكتالوج بلا Writer سياسات (حارس EXE-017 يبقى أخضر — النقل سطحي)؛ زر «وزّع على هذه المحفظة» يختار المحفظة من البداية الباردة (اختبار EXE-016 القائم يغطي المستهلك)؛ WF-007/WF-008 مطابقة بنيوية |
| الصور المطلوبة | الكتالوج بعد الفصل + موضع السياسات الجديد + دفتر المحفظة بالفعل السياقي (3 صور) |
| Rollback Boundary | الالتزام الأخير — رجوع يعيد القسم إلى الكتالوج ويحذف الفعل السياقي؛ لا بيانات |
| ترتيب الدمج | الخامس (الأخير في 4.2) |

---

## 2. Wave 4.3 — الشاشات والمكونات (6 حزم — بعد اعتماد Wireframes الأساسية)

> **قاعدة الحزمة:** كل شاشة تُلمس مرة واحدة. الترتيب من المشترك إلى الخاص حتى تُبنى المكونات قبل مستهلكيها.

### P-4.3-1 — المكونات المشتركة والقوالب الخمسة

| الحقل | القيمة |
|---|---|
| Screen IDs | كل مستهلكي TPL-A..E (الجرد كاملًا) |
| Routes | لا مسار — طبقة مكونات |
| الملفات المتوقعة | مكونات القوالب وفق design-system.md (البطاقات الثلاث، نظام الأزرار الثماني مع CTA اللاصق للمحررات، الحقول وEnglishNumberInput، الإفصاح `<details>`، FeedbackNote) — **بلا تغيير قيم الرموز** (المواصفة تحفظ القائم؛ الإضافات قواعد توظيف فقط) |
| التبعيات | اكتمال 4.2 كلها |
| المخاطر | إغراء «التحسين» أثناء البناء (مضبوط: المواصفة تحفظ القائم وتوسم المقترح) |
| اختبارات القبول | حراس الرموز والتباين (82 زوجًا) وكثافة النص أخضر بعد أي مكون جديد؛ مطابقة TPL-A..E بنيويًا |
| الصور المطلوبة | عينة مكون واحد لكل قالب (5) |
| Rollback Boundary | الالتزام الأخير |
| ترتيب الدمج | الأول في 4.3 |

### P-4.3-2 — الرئيسية وورقة التسجيل السريع (WF-004 + WF-011)

SCR-057 + الورقة (قرار NAV-001) · Routes: `/` · الملفات: `pages/Home.tsx` + `QuickActionSheet.tsx` + `QuickSaleForm/QuickExpenseForm` (تسمية حقل السعر المصححة GAP-4.3-07؛ حقل التاريخ GAP-4.3-06؛ Enter يسجل GAP-4.3-05/F13) · المخاطر: نصوص الإيصالات المثبتة بالاختبارات (تُحدَّث معًا) · القبول: WF-004/WF-011 مطابقة + حالات «غير مسجل» قائمة + ب-2 فحوص الورقة · الصور: 2 (قبل/بعد) · Rollback: آخر التزام · الدمج: الثاني.

### P-4.3-3 — المالية المستوى الأول (WF-006 + F09 + GAP-4.3-10)

SCR-049 · Routes: `/finance` · الملفات: `pages/Finance.tsx` (إعادة ترتيب «الوضع الآن» أولًا — W4-NEW-09؛ بطاقات الموقف روابط لمصادرها + سطح «مين عليّ؟» الموحد بقرار F09 — أهم قرار للفئة المستهدفة؛ إغلاق W4-NEW-02/03) · التبعيات: F09 محسومة + P-4.3-1 · المخاطر: أكبر تغيير إدراكي في الموجة (يُختبر بمستخدمين في 4.4) · القبول: WF-006 مطابقة + الروابط تفتح المصدر الصحيح بمساري التسديد · الصور: 2 · الدمج: الثالث.

### P-4.3-4 — سلسلة العمل والمشاركة (WF-005/012/014/015/016/027 + F06)

SCR-053/003..010 + SCR-030/SCR-052 · Routes: `/orders*`, `/direct-sales/*`, `/collect`, `/share/preview` · الملفات: `OrderDetail.tsx`, `DeliveryReview.tsx`, `Collect.tsx`, `SharePreview.tsx` (زر «نزّل النص كملف» GAP-4.3-11/F07 أولوية منخفضة؛ زر «شارك إشعار القبض» في نتيجة التحصيل GAP-4.3-01/F06 **مع تحديث عقد 33 §3/§7.6 في نفس الالتزام — REV-009**) · المخاطر: مسّ عقد المشاركة (مضبوط: العقد يُحدَّث مع الكود والأزرار تلتزم «حدث محفوظ قائم فقط») · القبول: ب-2 فحوص السلسلة + مصفوفة الحالات م5/م14 + عقدي 26/33 · الصور: 3 · الدمج: الرابع.

### P-4.3-5 — الكاش والمحافظ (WF-017..019)

SCR-018..029 · Routes: `/suppliers*`, `/cash*` · الملفات: `CashWallets/CashWalletEditor/WalletLedger/CashTransferEditor/CashDistribution/CashCount/CashAdjustmentEditor/CashReversalEditor`, `Suppliers/SupplierPurchaseEditor` · القبول: WF-017/018/019 + مصفوفة م6/م7 + معجم F08 الموحد («وزّع/غطِّ») · الصور: 2 · الدمج: الخامس.

### P-4.3-6 — المخزون ومال المالك والقروض والأصول + الإعدادات عمقًا (WF-020..025)

SCR-031..035, SCR-013..016, SCR-042..047, SCR-055 · الملفات: صفحاتها (إصلاحا W4-NEW-01 GAP-4.3-08 وW4-NEW-08 GAP-4.3-09 هنا؛ WF-025 عمق النسخ والاستعادة) · القبول: WF-020..025 + مصفوفات م8..م13 · الصور: 3 · الدمج: السادس (الأخير).

---

## 3. Wave 4.4 — الجودة الشاملة وQA (5 حزم)

### P-4.4-1 — التاريخ والمبالغ والRTL (F10 + F11 + rtl-and-accessibility.md أ/ب/ج)

نقطة واحدة `formatLocalDateTime` (12 ساعة «03:30 م» + الفاصلة العربية + تغطية `HH:MM` المستقل — F10/GAP-4.3-14) + توثيق قرار عمّان للعرض (F11) + فحوص عزل الأرقام والمبالغ عبر الأسطح والتصدير (اتساق الدقة ملزم).

### P-4.4-2 — الوصول والكيبورد وEscape (rtl-and-accessibility.md د/هـ + GAP-4.3-04/15/16)

Escape + إعادة تركيز للوحتي «قريبًا»؛ `aria-invalid`+`aria-describedby` للأسطح الناقصة؛ ترحيل FeedbackNote الصريح؛ inputMode وfocus order و44px عبر القوالب.

### P-4.4-3 — المقاسات الثلاثة (360×800 / 390×844 / 430×932)

فحص كل قالب (TPL-A..E) ومستهلكيه: لا Scroll أفقي، CTA مع الكيبورد، Safe Areas، التفاف النصوص، محاذاة الأرقام — محاكي؛ **الجهاز الفعلي عند توفره فقط ويُعلن** (لا ادعاء لمس بلا جهاز).

### P-4.4-4 — الانحدار الكامل (ب-3 + مصفوفة الحالات كقائمة فحص)

الرحلات الـ14 كاملة بعد آخر حزمة + مصفوفة الحالات (140 صفًا) + العقود 26/33/40 + `pnpm check` كاملًا + REV-010 جدول مطابقة نصوص الرجوع النهائي.

### P-4.4-5 — اختبار المستخدمين ثم الإصلاحات المثبتة

بروتوكول usability-test-plan.md أ (6 مشاركين، 8 مهام، العتبات الكمية أ-5) — **لا يُنفذ على أشخاص حقيقيين إلا بموافقة وترتيبات المالك المنفصلة**؛ إصلاحات موثقة بمصدر (مهمة/مستخدم/مؤشر) فقط — لا تفضيل بصري.

---

## 4. جدول التبعيات وترتيب الدمج الإجمالي

```
P-4.2-1 ──┬──> P-4.2-2 ──> P-4.2-3 ──> P-4.2-4 ──> P-4.2-5
           └──(توازٍ محدود: 2 مع 1 فقط)
4.2 كاملة ──> P-4.3-1 ──> P-4.3-2 ──> P-4.3-3 ──> P-4.3-4 ──> P-4.3-5 ──> P-4.3-6
4.3 كاملة ──> P-4.4-1 ──> P-4.4-2 ──> P-4.4-3 ──> P-4.4-4 ──> P-4.4-5
```

**بوابات المالك:** (أ) قبل 4.2: اعتماد الهيكل والأسماء وWireframes الأساسية وترتيب الحزم + جدول F01..F14. (ب) بعد كل موجة: تقرير مراجعة قبل التالية. (ج) F06/F09/F07 لا تنفذ غيابًا قبل حسمها.

## 5. القيود

- كل أرقام الأسطر من الكود عند BASE_SHA — قد تنزاح بحلول تنفيذ 4.2؛ تُحدَّث عند فتح كل حزمة.
- هذه خطة تنفيذ مستقبلية — لا شيء منها نُفذ أو جُرب في Wave 4.1؛ التقديرات الجهدية تُشتق عند فتح كل حزمة.
- خطة REV-003 (توافق `?event=`/`?layer=`) شرط تنفيذ إلزامي داخل P-4.2-4 وليست خيارًا.
