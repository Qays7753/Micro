# جرد الشاشات الكامل — Wave 4.1 / الوكيل 1 (W41-2-a)

**BASE_SHA:** `f8b37062c14aecf85abf5f64e11a191b00eb3c0d` · **التاريخ:** 2026-09-17 · **الوكيل:** 1 — معمارية المنتج والهيكل المعلوماتي.
**القاعدة:** قراءة وتحليل فقط — لم يُعدَّل أي ملف داخل المستودع. كل حقيقة أدناه مستخرجة من الكود عند BASE_SHA (المسارات مثبتة)، وما ليس كذلك موسوم **INFERRED** أو **UNVERIFIED**.

## 0. التحقق العددي (أولًا)

- **58 نمط طريق** في `MicroRouter.tsx` = **55 هدف عرض** (53 مكوّن صفحة فريدًا + تحويلان inline) **+ 3 أنماط** تتشارك المكوّن نفسه (DirectSaleEditor ×2، SupplierPurchaseEditor ×2، MaterialEditor ×2). 55 + 3 = **58** ✓.
- **بلا شاشة مستقلة (4 أنماط):**
  1. `/orders/new` (SCR-003): مكوّن `NewDraft` تحويل صرف إلى `/orders/draft/new?intent=…` يحفظ `returnTo`/`from` — قرار F-003 (لا شاشة اختيار منافسة لزر التسجيل السريع).
  2. `/finance/new/owner_withdrawal_cash` (SCR-013): تحويل inline إلى `/finance/withdraw` يحفظ الاستعلام كاملًا — EXE-009 (إحالة المسار الموازي القديم للمدخل الموحد).
  3. `/review` (SCR-054): تحويل inline إلى `/finance` — المراجعة اندمجت نبضة أعلى المالية (F-003) والمسار القديم يقود إليها لا إلى 404.
  4. `*` (SCR-058): `NotFound` **له شاشة فعلية** (404 بلغة صادقة) — يُدرج صفًا كاملًا لا «بلا شاشة»؛ المذكور هنا لتغطية التفسير العددي فقط.
- عدد الصفوف في الجداول أدناه = **58** ✓ (SCR-001 → SCR-058 بترتيب جدول السياق المشترك §3).

**مصطلحات:** «القسم الحالي» = المنتجون الفعليون للطريق (تتبّع `navigate`/`withReturnTo` بالgrep داخل `pages/` و`components/`). «العودة» = بديل `canonicalReturnFor` من `navigationContract.ts` ما لم يُذكر خلافه.

---

## أ. التأسيس والاستخدام الأول

### SCR-001 — Setup (`/setup`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «ما اسم مشروعك؟» ← «وين تحط فلوسك؟» ← «شو وضع الدرج هلق؟» (3 خطوات؛ h1 متغير بالخطوة) |
| Route | `/setup` (kind: setup — الكروم العام مخفي) |
| القسم الحالي | يفتح تلقائيًا من `StartupGate` عند غياب البروفايل (`navigate("/setup", {replace:true}`)؛ وليس له زر في أي سطح |
| المالك التقني | لا يطبق (تأسيس أولي) — كتاباته موزعة: `profiles.save` (إعدادات/بيانات) + `cashContinuity.openWallet` (مالية) |
| الهدف الحالي | معالج 3 خطوات: اسم المشروع → اسم محفظة الدرج الافتراضية (قابل للتخطي) → الموقف الافتتاحي (معروف/مجهول/صفر)، ثم يحفظ ويقود إلى `/` بلافتة `?setup=1` |
| الهدف المقترح | تأسيس المشروع ومحفظته الأولى في أقل من دقيقة بلا مصطلحات محاسبية |
| المستخدم/المهمة | مالك جديد في أول إقلاع؛ يريد البدء دون جدار إعداد |
| الإجراء الأساسي | «احفظ وابدأ في مشروعي الآن» (خطوة 3) |
| الإجراءات الثانوية | «تخطَّ المحفظة الآن — أسجلها لاحقًا من مالي» (خطوة 2)؛ «خطوة سابقة»؛ مسودة تُحفظ محليًا أثناء الكتابة (`micro.setup-draft.v1`) |
| البيانات المقروءة | `profiles.load` (وجود البروفايل)، مسودة الإعداد المحلية |
| الكتابة | `profiles.save` (Setup.tsx:150) + `cashContinuity.openWallet` عبر نموذج الخطوة |
| الحالات | Default ✓ (الخطوات) / Loading ✓ («جارٍ الحفظ…» disabled) / Error ✓ (رسالة «حط اسم لمشروعك أولًا.») / Success ✓ (لافتة `?setup=1` في الرئيسية) / Empty — لا يطبق (أول استخدام) / Locked — لا / Disabled ✓ (زر الحفظ أثناء الحفظ) |
| العودة | ينتقل بعد الحفظ إلى `/` (ليس «رجوعًا») |
| القرار | **إبقاء** |
| سبب القرار | بوابة أولى رقيقة مثبتة (S3-17/F-002/Set003 اختبارات `Setup.ui.test.tsx`)؛ لا يعارض أي عقد |

### SCR-002 — Foundation (`/foundation`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «شو عندك هلق؟» (overline: «خطوة التأسيس · اختيارية بالكامل») |
| Route | `/foundation` |
| القسم الحالي | الرئيسية وحدها: زر «صفحة الأساس» داخل قسم «مالي» في Home (القرار ٧ — باب أمامي دائم الوصول) |
| المالك التقني | لا يطبق (سطح تأسيس موزع): أزراره تكتب عبر المالية (`owner_investment_cash`) والمحافظ والمخزون والموردين |
| الهدف الحالي | طبقات إفصاح (الكاش/رأس مالك/ديون قائمة/مواد متوفرة) تشرح «لماذا/ما يتأثر/الأثر المالي/أين تظهر النتيجة» ثم تحيل لمحررات الإدخال الافتتاحي؛ + بطاقة «عندي ملف موقف جاهز» تحيل للإعدادات `?focus=guided-import` |
| الهدف المقترح | إدخال الموقف الافتتاحي الموجود فعلًا (كاش/رأس مال/ديون/مواد) بلا افتراض صفر |
| المستخدم/المهمة | مالك بدأ مشروعه قبل التطبيق؛ يريد ترحيل ما يعرفه فقط |
| الإجراء الأساسي | «محفظة ورصيد بداية» (قسم الكاش، `action="create"`) |
| الإجراءات الثانوية | «سجل استثمارًا نقديًا»، «رصيد سابق لحق المالك»، «سجل التزامًا لمورد»، «شراء مواد قائم»، «مادة ورصيد بداية»، «فتح الاستيراد» (ملف موقف)، «تخطَّ وأكمل لاحقًا» |
| البيانات المقروءة | ملخص المحافظ/المالك/الموردين/المواد المعروض في عناوين الطبقات |
| الكتابة | لا يكتب بنفسه — كل أزراره تحيل لمحررات المالكة |
| الحالات | Default ✓ / Empty ✓ (كل طبقة تعرض «لم تسجل… بعد») / Error ✓ («تعذر قراءة الموقف» + إعادة) / Loading ✓ / Success — عبر المحررات المُحال إليها / Locked — لا / Disabled — لا |
| العودة | `canonicalReturnFor` = `/` (وليس مسجلة في الخريطة؛ السطح يُفتح من الرئيسية بـ`withReturnTo`) |
| القرار | **إبقاء** |
| سبب القرار | قرار مالك معتمد (القرار ٧: دائم الوصول لا يغلق بعد اليوم الأول)؛ عقد 26 يذكره صراحة |

---

## ب. العمل والطلبات (المبيعات المباشرة والمسودات)

### SCR-003 — NewDraft (`/orders/new`) — **تحويل، بلا شاشة مستقلة**

| الحقل | القيمة |
|---|---|
| الاسم الحالي | لا يملك عنوانًا — المكوّن `<Redirect>` صرف |
| Route | `/orders/new` → `/orders/draft/new?intent=customer_order\|planned_design&returnTo=…` |
| القسم الحالي | لا منتج داخلي حي — الروابط القديمة/الخارجية فقط (قرار F-003) |
| المالك التقني | لا يطبق (توافق مسارات) |
| الهدف الحالي | يحفظ النية (`intent`) ووجهة الرجوع (يرقّي `from` القديم إلى `returnTo`) ويحيل لمحرر المسودة |
| الهدف المقترح | الحفاظ على توافق الروابط القديمة بلا شاشة اختيار منافسة |
| المستخدم/المهمة | صاحب رابط قديم أو عمق يدوي |
| الإجراء الأساسي | — (تحويل فوري) |
| الإجراءات الثانوية | — |
| البيانات المقروءة | `useSearch` فقط |
| الكتابة | لا شيء |
| الحالات | — (تحويل) |
| العودة | يمرر `returnTo` كما هي |
| القرار | **إبقاء كتحويل** |
| سبب القرار | عقد 26 §2.2 قاعدة 8: «`/orders/new` يمرّر وجهة الرجوع كما هي (وترقّي from القديم)» |

### SCR-004 — DirectSaleEditor — إنشاء (`/direct-sales/new`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «تسجيل بيع مباشر» (h1: `editing ? "تصحيح بيع مباشر" : "تسجيل بيع مباشر"`) |
| Route | `/direct-sales/new` (يدعم `?product=<catalog-item-id>&returnTo=/catalog`) |
| القسم الحالي | أزرار «تسجيل بيع مباشر» في Orders و«سجّل أول بيع» (فراغ العمل) و«سجّل بيع هذا المنتج/هذه الخدمة» في بطاقة عنصر الكتالوج؛ وشاشة النجاح «سُجّل البيع» |
| المالك التقني | العمل (محرر مبيعات؛ كتاباته مالية عبر خدمة المبيعات: `directSales.record` يكتب حدثًا ماليًا) |
| الهدف الحالي | تسجيل بيع نقدي/آجل كامل: صنف، كمية، إيراد، وجهة كاش أو «غير موزع»، ملاحظة؛ وبعد الحفظ شاشة «سُجّل البيع» بوصل ووصل مشاركة وأفعال تصحيح |
| الهدف المقترح | تسجيل بيع تمّ الآن بمبالغه ووجهة كاشه في نموذج واحد |
| المستخدم/المهمة | صاحب مشروع يبيع مباشرة بلا طلب تنفيذ |
| الإجراء الأساسي | «حفظ البيع المباشر» |
| الإجراءات الثانوية | «شارك وصل البيع» (من شاشة النجاح)؛ تصحيح لاحق من `/direct-sales/:id`؛ إلغاء موثق بسبب |
| البيانات المقروءة | `directSales`، `catalog` (اقتراح المرجع `?product`)، `projectFinance`، `cashContinuity` (المحافظ)، `saleCollectionReversal`، `formDrafts` |
| الكتابة | `directSales.record` / `directSales.update` / `directSales.cancel` (DirectSaleEditor.tsx:359/374/447) + `saleCollectionReversal` للتراجع |
| الحالات | Default ✓ / Empty ✓ (فراغ بلا محافظ يفسَّر) / Loading ✓ / Error ✓ (رسائل الخدمة `micro-field-error`) / Success ✓ («سُجّل البيع» + إيصال) / Locked — لا / Disabled ✓ (زر الحفظ أثناء الحفظ) + `UnsavedChangesGuard` |
| العودة | `/orders` |
| القرار | **إبقاء** |
| سبب القرار | القلب اليومي للعمل؛ عقد 26 §2 قاعدة 4 يوثق عودته للمصدر بعد الحفظ |

### SCR-005 — DirectSaleEditor — تصحيح (`/direct-sales/:id`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «تصحيح بيع مباشر» (وضع التحرير) |
| Route | `/direct-sales/:id` |
| القسم الحالي | صفوف «مبيعاتي» في Orders؛ ووصل ورقة الإضافة بعد بيع سريع («افتح السجل») |
| المالك التقني | العمل (تصحيح سجل مبيعات) |
| الهدف الحالي | نفس المكوّن بوضع تحرير: قراءة البيع، تصحيحه، تسجيل دفعة/تحصيل لاحق، وتراجع موثق عن قبضة (`SaleCollectionReversalSection`) |
| الهدف المقترح | مراجعة بيع مسجل وتصحيحه بتراجع موثق لا حذف صامت |
| المستخدم/المهمة | مالك يراجع بيعًا سابقًا أو يصححه |
| الإجراء الأساسي | «حفظ تصحيح البيع» |
| الإجراءات الثانوية | تراجع موثق عن قبضة بمعاينة؛ إلغاء البيع بسبب؛ مشاركة |
| البيانات المقروءة | نفس SCR-004 + البيع المحدد |
| الكتابة | `directSales.update` / `directSales.cancel` + `saleCollectionReversal.reverse` |
| الحالات | Default ✓ / Error ✓ («بيع غير موجود»…) / Success ✓ / Empty — لا يطبق (معرّف محدد) / Loading ✓ / Disabled ✓ |
| العودة | `/orders` |
| القرار | **إبقاء** (صف مستقل عن SCR-004 لأن النمطين يغطيان نيتين مختلفتين — إنشاء/تصحيح — بمكوّن واحد) |
| سبب القرار | النمطان يخدمان رحلتين؛ الفرق موثق في h1 الشرطي والسلوك |

### SCR-006 — AgreementEditor (`/orders/draft/:id/agreement`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «سجّل ما اتفقت عليه» |
| Route | `/orders/draft/:id/agreement` (`?created=1` لافتة نجاح) |
| القسم الحالي | زر من DraftEditor («التالي» في رحلة المسودة→الاتفاق)؛ ومن Orders عبر المحررات |
| المالك التقني | العمل |
| الهدف الحالي | تسجيل الاتفاق: السعر، تاريخ التسليم، العربون (مبلغ + وجهة كاش صريحة أو «غير موزع» — FC-04)، ثم إنشاء الطلب من المسودة (`agreements.createFromDraft`) |
| الهدف المقترح | تحويل المسودة إلى اتفاق ملزم بعربونه وتاريخه |
| المستخدم/المهمة | مالك اتفق مع عميل على سعر وموعد |
| الإجراء الأساسي | حفظ الاتفاق (زر save) |
| الإجراءات الثانوية | وجهة كاش العربون «غير موزع»؛ الرجوع للمسودة |
| البيانات المقروءة | `drafts`، `costs` (يحرس «احفظ نسخة تكلفة صالحة قبل تسجيل الاتفاق»)، `agreements`، `cashContinuity`، `projectFinance`، `partyLedger` |
| الكتابة | `drafts.save` + `agreements.createFromDraft` (AgreementEditor.tsx:258/271) |
| الحالات | Default ✓ / Error ✓ («التكلفة المطلوبة غير متاحة») / Success ✓ (`?created=1`) / Empty ✓ (فراغ مسودة) / Loading ✓ / Disabled ✓ |
| العودة | `/orders` |
| القرار | **إبقاء** |
| سبب القرار | تسلسل المسودة-الاتفاق-التكلفة الداخلي ثابت بقرار عقد 26 §7 («لا ping-pong») |

### SCR-007 — CostEditor (`/orders/draft/:id/cost`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «{اسم القطعة}» (h1: اسم المسودة؛ overline: نسخة التكلفة) |
| Route | `/orders/draft/:id/cost` |
| القسم الحالي | زر «احسب التكلفة» من DraftEditor |
| المالك التقني | العمل (تكلفة التنفيذ؛ تغذي المالية قراءة الهامش) |
| الهدف الحالي | بناء نسخة تكلفة (مواد + وقت + بنود) مع تمييز الناقص (`incomplete`)؛ «حفظ نسخة التكلفة» أو «حفظ مسودة تكلفة ناقصة» |
| الهدف المقترح | توثيق تكلفة التنفيذ المخططة بكل مكوناتها أو إعلان نقصها |
| المستخدم/المهمة | مالك يحسب كلفة طلب قبل الاتفاق |
| الإجراء الأساسي | «حفظ نسخة التكلفة» |
| الإجراءات الثانوية | «حفظ مسودة تكلفة ناقصة»؛ مقترحات مواد المخزون؛ بنود إضافية |
| البيانات المقروءة | `drafts`، `costs`، `costEstimates`، `inventory` (مقترحات المواد) |
| الكتابة | `costs.saveSnapshot` (CostEditor.tsx:300) |
| الحالات | Default ✓ / Error ✓ («تعذر فتح التكلفة») / Success ✓ / Empty ✓ (تكلفة فارغة) / Loading ✓ / Disabled ✓ |
| العودة | `/orders` |
| القرار | **إبقاء** |
| سبب القرار | قاعدة AGENTS §6: التكلفة الناقصة لا تساوي صفرًا — السطح ينفذها بدقة |

### SCR-008 — DraftEditor (`/orders/draft/:id` — يشمل `id=new`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «طلب من عميل» أو «تصميم مخطط» (حسب `intent`) |
| Route | `/orders/draft/:id` (`?intent=…&estimate=<id>`) — النمط يغطي الإنشاء (`new`) والتحرير |
| القسم الحالي | أزرار الرئيسية السريعة («طلب من عميل»/«مسودة تصميم»)؛ صفوف «مسودات قيد الإكمال» في Orders؛ جسر التقدير→المسودة من Tools وEstimateDetail |
| المالك التقني | العمل |
| الهدف الحالي | محرر النية الفارغ (و٥ §٥-١): المسودة تُنشأ عند أول إدخال؛ يصف القطعة والكمية ثم يقود للاتفاق/التكلفة |
| الهدف المقترح | بدء طلب أو تصميم بأقل ما يعرفه المالك الآن |
| المستخدم/المهمة | مالك بدأ عملًا جديدًا مع عميل أو مخططًا له |
| الإجراء الأساسي | «احفظ المسودة» ثم «التالي» للاتفاق |
| الإجراءات الثانوية | «احسب التكلفة»؛ حذف المسودة؛ نسخ قيم التقدير المصدر (مقترحات معلنة) |
| البيانات المقروءة | `drafts`، `catalog`، `costEstimates` (المصدر) |
| الكتابة | `drafts.save` (DraftEditor.tsx:264) |
| الحالات | Default ✓ / Error ✓ («تعذر فتح المسودة») / Empty ✓ («مسودة تحتاج وصفًا») / Success ✓ («تم حفظ المسودة على هذا الجهاز.») / Loading ✓ / Locked ✓ («هذه المسودة أصبحت طلبًا محليًا») / Disabled ✓ |
| العودة | `/orders` |
| القرار | **إبقاء** |
| سبب القرار | رحلة العمل الأساسية؛ حذف `/orders/draft/new` الحرفي موثق في MicroRouter (تعارض Switch) |

### SCR-009 — DeliveryReview (`/orders/:id/deliver`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «مراجعة التسليم» |
| Route | `/orders/:id/deliver` |
| القسم الحالي | زر التسليم من OrderDetail (OrderDetail.tsx:673) |
| المالك التقني | العمل (التسليم) — كتابته مالية عميقة (إيراد/دين/مخزون/قبض) |
| الهدف الحالي | سطح كامل قبل الالتزام (عقد D5): مراجعة المال (سعر/مقبوض/عربون/متبقٍ)، معاينة استهلاك المواد مع خيارات النقص، تعديل السعر بسبب، قبض عند التسليم بوجهة كاش؛ ثم `commitDelivery` + إيصال نجاح |
| الهدف المقترح | تسليم الطلب بمراجعة واحدة واعية لكل الأثر المالي والمخزني |
| المستخدم/المهمة | مالك سلّم العمل ويريد تسجيل التسليم بصدق |
| الإجراء الأساسي | «أكّد التسليم» |
| الإجراءات الثانوية | تعديل السعر بسبب؛ خيارات نقص المواد؛ قبض الآن بوجهة؛ الرجوع لتفاصيل الطلب |
| البيانات المقروءة | `deliveryReview`، `cashContinuity` (المحافظ) |
| الكتابة | `deliveryReview.commitDelivery` (DeliveryReview.tsx:163) |
| الحالات | Default ✓ / Error ✓ / Success ✓ (إيصال ORD-002: رقم الطلب، المقبوض، المتبقي، وجهة الكاش، أثر المخزون) / Loading ✓ («جارٍ تحضير المراجعة…») / Disabled ✓ (submitting) / Empty — لا يطبق |
| العودة | `/orders/:id` (زر الرجوع لتفاصيل الطلب)؛ البديل الكنوني `/orders` |
| القرار | **إبقاء** |
| سبب القرار | عقد D5 — سطح قبل الالتزام؛ الحد الكنوني للتصحيح `reverseDelivery` يعتمد عليه |

### SCR-010 — OrderDetail (`/orders/:id`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | اسم الطلب (h1: `order.orderName \|\| itemName`) |
| Route | `/orders/:id` |
| القسم الحالي | صفوف «طلباتي» في Orders؛ بنود «اليوم» في الرئيسية؛ أحداث المالية (`onOpenOrder`) |
| المالك التقني | العمل |
| الهدف الحالي | مركز حياة الطلب: الحالة والخطوة التالية، عربونات إضافية، تحصيل الدين، تصنيف العربون المحتفظ، تعيين اسم العميل، مراجعة التسليم، تراجع موثق عن التسليم/قبضة، تعديل السعر بعد الاتفاق، إلغاء بسبب، الربح التقديري (خلف إفصاح)، النقل والتوصيل (خلف إفصاح)، المصدر: تقدير، و«شارك مع العميل» عبر `/share/preview` |
| الهدف المقترح | متابعة الطلب وتنفيذه وتحصيله من مكان واحد |
| المستخدم/المهمة | مالك يدير طلبًا حتى الإغلاق |
| الإجراء الأساسي | الفعل التالي بحسب الحالة (تسليم/تحصيل/متابعة) — `DecisionPanel` علوي |
| الإجراءات الثانوية | عربون إضافي؛ تحصيل؛ تراجع موثق (تسليم/قبضة)؛ تعديل سعر؛ إلغاء؛ تصنيف عربون محتفظ؛ مشاركة؛ تفاصيل إضافية (الربح/النقل/المصدر) |
| البيانات المقروءة | 15 خدمة (OrderDetail.tsx:87-103): `actualTime`، `agreements`، `agreementContext`، `fulfillment`، `deliveryReview`، `inventory`، `drafts`، `costEstimates`، `collectionReversal`، `retainedDeposits`، `cashContinuity`، `projectFinance`، `partyLedger`… |
| الكتابة | `fulfillment.applyDeliveryTerms/assignCustomerName/collectDeposit`، `retainedDeposits.classify/reclassify`، `deliveryReview.reverseDelivery`، `collectionReversal.reverse`، `projectFinance.distributeUnallocated` |
| الحالات | Default ✓ / Error ✓ («تعذر قراءة الطلب» + إعادة محاولة R1) / Empty — لا يطبق (معرّف) / Success ✓ (وصلات كل فعل) / Loading ✓ / Locked ✓ (طلب غير متاح محليًا) / Disabled ✓ |
| العودة | `/orders` |
| القرار | **إبقاء** |
| سبب القرار | أكبر محاور المنتج (1771 سطرًا) لكنه مبرر تشغيليًا: كل أفعال الطلب هنا (عقد U-005 سطح يبقي التنقل) |

---

## ج. الجدول والمواعيد

### SCR-011 — ScheduleEditor (`/schedule/:id`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «وقت ومدة الموعد» |
| Route | `/schedule/:id` |
| القسم الحالي | صفوف المواعيد في Schedule وOrders وبتات اليوم في الرئيسية |
| المالك التقني | العمل |
| الهدف الحالي | تعديل توقيت موعد مرتبط بطلب (وقت/مدة) بلا أثر مالي (`schedules.updateTiming`) |
| الهدف المقترح | ضبط موعد تسليم أو متابعة |
| المستخدم/المهمة | مالك يعدّل موعده |
| الإجراء الأساسي | «حفظ الموعد» |
| الإجراءات الثانوية | الرجوع للجدول |
| البيانات المقروءة | `schedules` |
| الكتابة | `schedules.updateTiming` (ScheduleEditor.tsx:116) |
| الحالات | Default ✓ / Error ✓ («الموعد غير متاح محليًا») / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/schedule` |
| القرار | **إبقاء** |
| سبب القرار | «أجّله من محرره» وعد ظاهر في Orders |

### SCR-012 — Schedule (`/schedule`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «المواعيد» |
| Route | `/schedule` (`?focus=capacity\|recurrence`) |
| القسم الحالي | «افتح جدول المواعيد» في Orders؛ صف «المواعيد والمتابعات» في حالة وحدات Tools؛ بند السعة في الرئيسية يفتح `/schedule?focus=capacity` |
| المالك التقني | العمل (قراءة تشغيلية بلا أثر مالي) |
| الهدف الحالي | سطح قراءة: قرار اليوم (السعة)، أقسام متأخر/اليوم/قادم، طبقة «عرض الأسبوع والتقويم» (شهر + خطة 7 أيام)، طبقة «التكرار والسعة» (`?focus`)، إنشاء مواعيد متابعة وخدمة متابعة يومية (`service.create/cancel`) |
| الهدف المقترح | رؤية ضغط الأسبوع والمواعيد واتخاذ قرار السعة |
| المستخدم/المهمة | مالك ينظم وقته التشغيلي |
| الإجراء الأساسي | فتح الموعد ذي الأولوية (صف القرار) |
| الإجراءات الثانوية | عرض الأسبوع/التقويم؛ التكرار والسعة؛ إنشاء موعد متابعة؛ إلغاء موعد |
| البيانات المقروءة | `schedules.overview/monthOverview`، `recurrences.list` |
| الكتابة | `service.create` / `service.cancel` (Schedule.tsx:463/494 — مواعيد المتابعة) |
| الحالات | Default ✓ / Empty ✓ («لا توجد طلبات تحتاج موعدًا الآن» + «بدء طلب») / Error ✓ + إعادة / Success ✓ / Loading ✓ / Disabled — لا |
| العودة | `/` (البديل الكنوني للسطح) |
| القرار | **إبقاء** |
| سبب القرار | عقد 26 §3.1 يوثق قيمتَي `focus` هنا؛ F-070 مقطع دائم في العمل |

---

## د. المالية — الأحداث والمالك والمتوقعات

### SCR-013 — تحويل `owner_withdrawal_cash` (`/finance/new/owner_withdrawal_cash`) — **بلا شاشة مستقلة**

| الحقل | القيمة |
|---|---|
| الاسم الحالي | — (تحويل inline في MicroRouter) |
| Route | `/finance/new/owner_withdrawal_cash` → `/finance/withdraw?<الاستعلام كاملًا>` |
| القسم الحالي | لا منتج حي — توافق الروابط القديمة (EXE-009) |
| المالك التقني | المالية |
| الهدف الحالي | إحالة المسار الموازي القديم لسحب المالك إلى المدخل الموحد `/finance/withdraw` الذي يوجه للسياسة الصحيحة — لا كاتب موازٍ |
| الهدف المقترح | حماية الروابط القديمة من كاتب مالي موازٍ |
| المستخدم/المهمة | رابط قديم/عمق خارجي |
| الإجراء الأساسي | — (تحويل) |
| الإجراءات الثانوية | — |
| البيانات المقروءة | `useLocation` (الاستعلام) |
| الكتابة | لا شيء |
| الحالات | — |
| العودة | يحفظ `?from` كاملًا فيعاد المصدر الأصلي بعد الحفظ |
| القرار | **إبقاء كتحويل** |
| سبب القرار | عقد 26 §2.2 قاعدة 8 يوثقه حرفيًا؛ `OwnerWithdrawalLegacyRedirect` في MicroRouter |

### SCR-014 — FinancialEventEditor (`/finance/new/:type`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | حسب النوع (h1: `content.title`): استثمار نقدي، مصروف مدفوع، التزام مورد، تسديد التزام، أمانة قُبضت، أمانة سُلّمت، هالك بلا نقد… |
| Route | `/finance/new/:type` — الأنواع السبعة: `owner_investment_cash`, `operating_expense_cash`, `operating_expense_payable`, `payable_settlement_cash`, `amanah_held_cash`, `amanah_released_cash`, `loss_non_cash` |
| القسم الحالي | تفصيل «تسجيل حركة أو فتح مصدر» في Finance (بعضها مشروط بالمبالغ القائمة)؛ بطاقات Foundation؛ دفتر المالك (الاستثمار)؛ ورقة المصروف السريع تستعمل النموذج المشترك (EXE-007) |
| المالك التقني | المالية (`projectFinance.record` — الكاتب الوحيد) |
| الهدف الحالي | محرر موجه لكل نوع حدث مالي عام: مبلغ، تاريخ، مصدر، ملاحظة، قواعد النوع (مثل ملاحظة إلزامية للمصروف) ومعاينة أثر |
| الهدف المقترح | تسجيل حدث مالي عام واحد بنوعه وسببه |
| المستخدم/المهمة | مالك يسجل حادثة مالية ليست بيعًا ولا طلبًا |
| الإجراء الأساسي | حفظ الحدث |
| الإجراءات الثانوية | مراجعة دفتر المالك (من فرع المالك)؛ مسودة نموذج محلية |
| البيانات المقروءة | `projectFinance`، `cashContinuity`، `partyLedger`، `ownerEntitlement` (حسب النوع) |
| الكتابة | `projectFinance.record` (FinancialEventEditor.tsx:626) |
| الحالات | Default ✓ / Error ✓ («نوع الحدث غير متاح» + رسائل الخدمة) / Empty ✓ / Success ✓ («تم حفظ الحدث المالي محليًا.») / Loading ✓ / Disabled ✓ + حارس مدخلات |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | الكاتب المالي العام الموحد؛ EXE-007 وحّد نموذج المصروف مع الورقة السريعة |

### SCR-015 — OwnerWithdrawalEditor (`/finance/withdraw`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «سحب من المشروع لنفسك؟» |
| Route | `/finance/withdraw` |
| القسم الحالي | لا يظهر له زر مباشر في Finance (البطاقة تحيل للدفتر الموحد — S2-07)؛ المدخل الفعلي: دفتر مال المالك («اسحب لنفسك») |
| المالك التقني | المالية (`ownerEntitlement.recordMovement` / `projectFinance.record`) |
| الهدف الحالي | المدخل الموحد (X-05): يسأل سؤالًا واحدًا ويوجه للسياسة الصحيحة — سحب مقابل حق مسجل أو سحب حر معلن؛ حارس تكرار تقاطعي |
| الهدف المقترح | سحب مالك واحد موجه بالسياسة بلا مسارين |
| المستخدم/المهمة | مالك يأخذ مالًا لنفسه |
| الإجراء الأساسي | «سجّل السحب» |
| الإجراءات الثانوية | «راجع الدفتر الموحد»؛ خيار السحب الحر مقابل الحق |
| البيانات المقروءة | `ownerEntitlement`، `projectFinance` |
| الكتابة | `ownerEntitlement.recordMovement` أو `projectFinance.record` (OwnerWithdrawalEditor.tsx:127/138) |
| الحالات | Default ✓ / Error ✓ («تعذر قراءة دفتر المالك») / Success ✓ / Loading ✓ / Disabled ✓ + تكرار تقاطعي |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | EXE-009 — إغلاق المسار الموازي؛ المدخل الواحد قرار مالك |

### SCR-016 — OwnerEntitlement (`/finance/owner-entitlement`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «مال المالك» |
| Route | `/finance/owner-entitlement` |
| القسم الحالي | بطاقة «مال المالك» في Finance (وضع الآن) وزر «مال المالك» في تفصيل الإجراءات؛ ورابط الدفتر من الرئيسية (بطاقة الحقيقة) |
| المالك التقني | المالية (عقد 40: `ownerEntitlementService`) |
| الهدف الحالي | الدفتر الموحد لمالك واحد: الرصيد وحالته، «أدخل مالًا للمشروع» و«اسحب لنفسك»، طبقات: حركات المالك، سياسات حق المالك (إنشاء/خلف/إيقاف)، الحقوق والأرصدة والحركات مع تراجعات موثقة لكل نوع |
| الهدف المقترح | معرفة حقك الفعلي في المشروع وإدارته بدفتر واحد |
| المستخدم/المهمة | مالك يفصل ماله عن مال المشروع |
| الإجراء الأساسي | «أدخل مالًا للمشروع» (`action="create"`) |
| الإجراءات الثانوية | «اسحب لنفسك»؛ سياسات الحق (طبقة)؛ سجل الحركات (طبقة)؛ تراجع موثق عن حركة/حق/افتتاح |
| البيانات المقروءة | `ownerEntitlement` (النظرة الشاملة) |
| الكتابة | `createPolicy`/`createPolicySuccessor`/`recordEntitlement`/`recordMovement`/`reverseMovement`/`reverseEntitlement`/`reverseOpeningBalance` (OwnerEntitlement.tsx:273-588) |
| الحالات | Default ✓ / Error ✓ / Empty ✓ (رصيد مسوّى/بلا سياسات) / Success ✓ (إشعارات) / Loading ✓ / Disabled ✓ + تحذير التكرار التقاطعي |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | عقد 40 يسنده؛ S2-07 مدخل مالك واحد من المالية |

### SCR-017 — G5DeclarationEditor (`/finance/g5/declaration`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «تحصيل أو التزام قريب» |
| Route | `/finance/g5/declaration` |
| القسم الحالي | بطاقة قرار الكاش في Finance («أعلن تحصيلًا أو التزامًا قريبًا») ولوحة G5 في قراءة الفترة |
| المالك التقني | المالية (`g5.createDeclaration`) |
| الهدف الحالي | تسجيل متوقع: قبض/دفع متوقع بمبلغ وتاريخ ومصدر ومعرفة (معروف/تقديري) + تراجع موثق |
| الهدف المقترح | إعلان متوقع مالي قريب قبل حدوثه |
| المستخدم/المهمة | مالك يعرف قبضًا أو دفعًا قادمًا |
| الإجراء الأساسي | «حفظ المتوقع» |
| الإجراءات الثانوية | التراجع عن متوقع (من سجل المتوقعات) |
| البيانات المقروءة | `g5` |
| الكتابة | `g5.createDeclaration` (G5DeclarationEditor.tsx:85) |
| الحالات | Default ✓ / Error ✓ (تحقق الإدخال) / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/finance` |
| القرار | **إبقاء — مع إعادة تسمية مقترحة للطريق فقط** |
| سبب القرار | السطح سليم؛ البادئة `g5` داخلية تقنية في المسار لا تظهر للمستخدم — لا تغيير مطلوب الآن (إعادة التسمية تكلفة رابط بلا فائدة مستخدم). **INFERRED**: أي إعادة تسمية طريق مستقبلية يجب أن تحفظ تحويلًا |

---

## هـ. الموردون والمشتريات

### SCR-018 — SupplierPurchaseEditor — دفعة (`/suppliers/purchase/:id/payment`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «دفعة إلى {اسم المورد}» (overline: دفعة مورد) |
| Route | `/suppliers/purchase/:id/payment` |
| القسم الحالي | زر «سجل دفعة» في صف الشراء المفتوح (Suppliers) |
| المالك التقني | المالية (ذمم الموردين) — الجسر التشغيلي للاستلام يخص العمل |
| الهدف الحالي | تسجيل دفعة على شراء قائم: المبلغ ووجهة الكاش ومعاينة المتبقي؛ «الدفع يخفض ما بقي ولا يسجل مصروفًا مرة ثانية» |
| الهدف المقترح | سداد جزء من ذمة مورد بوجهة كاش موثقة |
| المستخدم/المهمة | مالك دفع لمورده |
| الإجراء الأساسي | حفظ الدفعة |
| الإجراءات الثانوية | تراجع موثق عن دفعة |
| البيانات المقروءة | `supplierPurchases` (الشراء)، `cashContinuity` |
| الكتابة | `supplierPurchases.recordPayment` / `reversePayment` (SupplierPurchaseEditor.tsx:368/451) |
| الحالات | Default ✓ / Error ✓ («شراء المواد غير موجود») / Success ✓ / Empty — لا يطبق / Loading ✓ / Disabled ✓ |
| العودة | `/suppliers` |
| القرار | **إبقاء** (نمط مستقل عن SCR-019 لأنه نية مختلفة: دفعة لا تحرير شراء) |
| سبب القرار | عقد 28/TR-07 يفصل رحلة الدفع عن رحلة الشراء |

### SCR-019 — SupplierPurchaseEditor — شراء (`/suppliers/purchase/:id` — يشمل `new`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «سجل شراء مواد» (جديد) / «شراء من {المورد}» (تحرير) — overline: شراء مواد/تفاصيل شراء وتصحيحه |
| Route | `/suppliers/purchase/:id` (`?purchase=<id>` لوضع الاستلام) |
| القسم الحالي | زر «سجل شراء مواد» في Suppliers؛ «شراء مواد قائم» في Foundation؛ جسر الاستلام `?purchase=` من صفحة الشراء |
| المالك التقني | المالية |
| الهدف الحالي | تسجيل شراء مواد بالأجل/نقدًا: المورد، البنود، الإجمالي، الدفعات، الاستحقاق؛ التصحيح بتراجع موثق؛ بطاقة «استلم هذا الشراء» تقود لمحرر الاستلام (`/inventory/movement/receipt?purchase=…`) |
| الهدف المقترح | توثيق شراء مواد وذمته ودفعاته في سجل واحد |
| المستخدم/المهمة | مالك اشترى مواد لمشروعه |
| الإجراء الأساسي | حفظ الشراء («أكّد تعديل الشراء» في التحرير) |
| الإجراءات الثانوية | «استلم هذا الشراء» (جسر EXE-011)؛ تراجع موثق؛ دفعات لاحقة |
| البيانات المقروءة | `supplierPurchases`، `inventory` (حالة الاستلام)، `cashContinuity` |
| الكتابة | `supplierPurchases.recordPurchase` (SupplierPurchaseEditor.tsx:296) |
| الحالات | Default ✓ / Error ✓ / Empty ✓ / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/suppliers` |
| القرار | **إبقاء** |
| سبب القرار | مبدأ AGENTS §6: شراء المخزون لا يساوي تكلفة البيع — السطح يطبقه |

### SCR-020 — Suppliers (`/suppliers`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «الموردون والمشتريات» |
| Route | `/suppliers` |
| القسم الحالي | روابط Finance: بطاقة «عليّ للموردين»، سطر «ما نعرفه الآن»، زر تفصيل «تسجيل حركة»؛ وصف حالة وحدات Tools |
| المالك التقني | المالية |
| الهدف الحالي | «ما عليك للموردين» + قائمة المشتريات المفتوحة (المتبقي/المدفوع/الاستحقاق) مع «سجل دفعة» و«عدّل/تراجع»؛ وقسم «آخر المشتريات المسددة» |
| الهدف المقترح | معرفة ما عليك للموردين ومتابعة سداده |
| المستخدم/المهمة | مالك يدير ذمم الشراء |
| الإجراء الأساسي | «سجل شراء مواد» (`action="create"`) |
| الإجراءات الثانوية | «سجل دفعة» لكل صف؛ «عدّل/تراجع»؛ «السجل والتصحيح» للمسددة |
| البيانات المقروءة | `supplierPurchases.readSummary` |
| الكتابة | لا يكتب (يحيل للمحررات) |
| الحالات | Default ✓ / Empty ✓ («لا تسجل شراء مواد كمصروف تشغيل…») / Error ✓ + إعادة / Success — / Loading ✓ / Disabled — |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | عقد 26 §1 مستوى 2؛ بيت ذمم الموردين |

---

## و. الكاش والمحافظ

### SCR-021 — CashWalletEditor (`/cash/wallet/new`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «أين يوجد الكاش الآن؟» |
| Route | `/cash/wallet/new` |
| القسم الحالي | «محفظة ورصيد بداية» في CashWallets وFinance (طريق «سجّل محفظة ورصيد بداية») وFoundation والخطوة 2 من Setup |
| المالك التقني | المالية (`cashContinuity.openWallet`) |
| الهدف الحالي | إنشاء محفظة (درج/بنكي/رقمية) برصيد افتتاحي معروف أو مجهول معلن أو صفر موثق، بتاريخ بداية |
| الهدف المقترح | إعلان مكان كاش ورصيد بدايته |
| المستخدم/المهمة | مالك يضيف مكان كاش جديدًا |
| الإجراء الأساسي | «حفظ محفظة ورصيد البداية» |
| الإجراءات الثانوية | خيار «لا أعرف الرصيد الآن» (D-004 يفتح طريق لاحق)؛ تاريخ سابق |
| البيانات المقروءة | — (نموذج صرف) |
| الكتابة | `cashContinuity.openWallet` (CashWalletEditor.tsx:40) |
| الحالات | Default ✓ / Error ✓ / Success ✓ / Empty — لا يطبق / Loading ✓ / Disabled ✓ |
| العودة | `/cash` |
| القرار | **إبقاء** |
| سبب القرار | رحلة CASH-001/EXE-008 لحوكمة الافتتاح |

### SCR-022 — WalletLedger (`/cash/wallet/:id`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | اسم المحفظة (h1) — overline: «دفتر محفظة · المبالغ (د.أ)» |
| Route | `/cash/wallet/:id` (`?entry=<id>` تركيز حركة) |
| القسم الحالي | زر «السجل» في بطاقة كل محفظة (CashWallets)؛ ومصادر حركات المحفظة |
| المالك التقني | المالية |
| الهدف الحالي | قارئ سطحي: رصيد المحفظة الآن، ملاحظة التراجعات (RestatementNote)، حركات بالتسلسل مع رابط مصدر لكل حركة و«تراجع» للأثر غير المعكوس، وحالات الافتتاح المجهول |
| الهدف المقترح | مراجعة كل ما حدث في محفظة واحدة بالتسلسل |
| المستخدم/المهمة | مالك يدقق حركات درجه/حسابه |
| الإجراء الأساسي | فتح مصدر حركة (زر المصدر في الصف) |
| الإجراءات الثانوية | تراجع عن أثر؛ ضبط بسبب (من بطاقة المحفظة الأم) |
| البيانات المقروءة | `cashContinuity` (نظرة المحفظة) |
| الكتابة | لا يكتب |
| الحالات | Default ✓ / Empty ✓ («لا توجد حركات على هذه المحفظة بعد.») / Error ✓ («دفتر محفظة غير متاح») / Success — / Loading ✓ / Locked ✓ (رصيد افتتاح مجهول معلن) / Disabled — |
| العودة | `/cash` |
| القرار | **إبقاء — وهو المقترح بيت المنتج المرئي لـ`destinationWalletId`** (انظر ملف الهيكل المستهدف §5) |
| سبب القرار | عقد 26 §1 (قارئ سطح)؛ هنا تتوضح نية «وزّع على هذه المحفظة» طبيعيًا |

### SCR-023 — CashOpeningLaterEditor (`/cash/wallet/:id/opening-later`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «سجّل رصيد «{اسم المحفظة}» الموثق لاحقًا» |
| Route | `/cash/wallet/:id/opening-later` |
| القسم الحالي | زر «سجّل رصيدًا موثقًا لاحقًا» في بطاقة المحفظة ذات الافتتاح المجهول (CashWallets) |
| المالك التقني | المالية |
| الهدف الحالي | إكمال رصيد الافتتاح المجهول كحدث موثق إضافي (لا إعادة كتابة)؛ يرفض إن كان الرصيد معروفًا أصلًا |
| الهدف المقترح | إغلاق جهل رصيد افتتاحي بمستند لاحق |
| المستخدم/المهمة | مالك عرف رصيد درجه لاحقًا |
| الإجراء الأساسي | «سجّل الرصيد الموثق» |
| الإجراءات الثانوية | — |
| البيانات المقروءة | `cashContinuity` (المحفظة) |
| الكتابة | `cashContinuity.recordOpeningBalanceLater` (CashOpeningLaterEditor.tsx:75) |
| الحالات | Default ✓ / Error ✓ / Success ✓ / Locked ✓ («رصيد افتتاحي هذه المحفظة معروف أصلًا») / Loading ✓ / Disabled ✓ |
| العودة | `/cash` |
| القرار | **إبقاء** |
| سبب القرار | D-004 قرار موثق (حدث إضافي لا إعادة كتابة) |

### SCR-024 — CashTransferEditor (`/cash/transfer`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «حوّل كاشًا بين محافظك» |
| Route | `/cash/transfer` |
| القسم الحالي | زر «تحويل بين المحافظ» في CashWallets (يظهر عند محفظتين+) |
| المالك التقني | المالية |
| الهدف الحالي | نقل كاش بين محفظتين («التحويل نقل كاش لا مصروف ولا ربح»)؛ يرفض بمحفظة واحدة |
| الهدف المقترح | تحويل مال بين مكانَي كاش |
| المستخدم/المهمة | مالك نقل مالًا من الدرج للحساب |
| الإجراء الأساسي | «حفظ التحويل» |
| الإجراءات الثانوية | — |
| البيانات المقروءة | `cashContinuity.overview` |
| الكتابة | `cashContinuity.transfer` (CashTransferEditor.tsx:57) |
| الحالات | Default ✓ / Error ✓ / Empty ✓ («تحتاج محافظتين للتحويل») / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/cash` |
| القرار | **إبقاء** |
| سبب القرار | رحلة قائمة ومحوترة |

### SCR-025 — CashDistribution (`/cash/distribute`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «وزّع الكاش غير الموزع» |
| Route | `/cash/distribute` (`?mode=cover` + `?destinationWalletId=<id>` + `returnTo`) |
| القسم الحالي | شريط «كاش غير موزع» في Finance («وزّع على محفظة»)، زر «وزّع غير الموزع» في CashWallets، قرار تغطية الدفعة في Finance (`mode=cover`)، وOrderDetail (توزيع بعد القبض)؛ **لا منتج داخلي يمرر `destinationWalletId`** (مدخل وصلة عميقة يدوي/خارجي موثق في عقد 26 §3) |
| المالك التقني | المالية |
| الهدف الحالي | توزيع صريح للكاش غير الموزع على محفظة (أو تغطية صرف من محفظة) بمبلغ وملاحظة؛ حارس المحفظة الصالحة مع إشعار صادق للمعرّف غير الصالح |
| الهدف المقترح | إعطاء الكاش العالق وجهة موثقة |
| المستخدم/المهمة | مالك قبض كاشًا لم يُنسب بعد |
| الإجراء الأساسي | «سجّل التوزيع» |
| الإجراءات الثانوية | اتجاه «تغطية صرف»؛ ملاحظة |
| البيانات المقروءة | `projectFinance` (الموقف)، `cashContinuity.overview` |
| الكتابة | `projectFinance.distributeUnallocated` (CashDistribution.tsx:128) |
| الحالات | Default ✓ / Error ✓ («لا توجد محافظ بعد…» + السالب مفسَّر) / Empty ✓ (الخيار النائب «اختر محفظة…») / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/cash` |
| القرار | **إبقاء + إضافة منتج مرئي لـ`destinationWalletId` من دفتر المحفظة** (W4-DEC — التفصيل في ملف الهيكل §5) |
| سبب القرار | عقد 26 EXE-016 يوثق المعامل ويقول «لا منتج داخلي له اليوم» — الفجوة التي تحددها هذه الجولة |

### SCR-026 — CashCount (`/cash/count`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «عدّ اللي في الدرج فعلًا» (النجاح: «انسجّلت التسوية») |
| Route | `/cash/count` |
| القسم الحالي | زر «عدّ الصندوق» في CashWallets (شرط محفظة واحدة+) وسطر «ما نعرفه الآن» في Finance |
| المالك التقني | المالية |
| الهدف الحالي | مقارنة المعدود بالمسجل وعرض الفرق ثم تسجيل تسوية موثقة (ضبط بسبب) |
| الهدف المقترح | مطابقة الدرج مع السجل دوريًا |
| المستخدم/المهمة | مالك يغلق يومه/أسبوعه |
| الإجراء الأساسي | «سجّل التسوية» |
| الإجراءات الثانوية | اختيار المحفظة؛ تعديل المعدود |
| البيانات المقروءة | `cashContinuity.overview` |
| الكتابة | `cashContinuity.adjust` (CashCount.tsx:101) |
| الحالات | Default ✓ / Error ✓ / Empty ✓ (اختر محفظة) / Success ✓ («انسجّلت التسوية») / Loading ✓ / Disabled ✓ |
| العودة | `/cash` |
| القرار | **إبقاء** |
| سبب القرار | «الانحراف التراكمي يُعالج هنا» — وعد ظاهر في الكود |

### SCR-027 — CashAdjustmentEditor (`/cash/wallet/:id/adjust`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «اضبط كاش {اسم المحفظة}» |
| Route | `/cash/wallet/:id/adjust` |
| القسم الحالي | زر «ضبط بسبب» في بطاقة المحفظة (CashWallets) |
| المالك التقني | المالية |
| الهدف الحالي | ضبط فرق كاش بسبب إلزامي وبيان («الضبط قرار موثق لا حذف») |
| الهدف المقترح | تصحيح رصيد محفظة بسبب معلن |
| المستخدم/المهمة | مالك وجد فرقًا له سبب |
| الإجراء الأساسي | حفظ الضبط |
| الإجراءات الثانوية | — |
| البيانات المقروءة | `cashContinuity` (المحفظة) |
| الكتابة | `cashContinuity.adjust` (CashAdjustmentEditor.tsx:53) |
| الحالات | Default ✓ / Error ✓ («لم نجد محفظة الكاش» + تحقق الإدخال) / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/cash` |
| القرار | **إبقاء** |
| سبب القرار | نفس كاتب CashCount (adjust) برحلة سبب صريح |

### SCR-028 — CashReversalEditor (`/cash/entry/:id/reverse`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «تراجع عن هذا الأثر» |
| Route | `/cash/entry/:id/reverse` |
| القسم الحالي | زر «تراجع» في صف الأثر بسجل CashWallets (وليس دفتر المحفظة — الدفتر يحيل لمصدره) |
| المالك التقني | المالية |
| الهدف الحالي | تراجع موثق عن أثر كاش بسبب إلزامي؛ «التراجع محفوظ» |
| الهدف المقترح | إلغاء أثر كاش خاطئ بتوثيق |
| المستخدم/المهمة | مالك سجل كاشًا بالخطأ |
| الإجراء الأساسي | «تراجع عن هذا الأثر» |
| الإجراءات الثانوية | — |
| البيانات المقروءة | `cashContinuity` (الأثر) |
| الكتابة | `cashContinuity.reverse` (CashReversalEditor.tsx:84) |
| الحالات | Default ✓ / Error ✓ («لم نجد أثر الكاش») / Success ✓ («تراجع محفوظ») / Loading ✓ / Disabled ✓ |
| العودة | `/cash` |
| القرار | **إبقاء** |
| سبب القرار | «سجل لا يحذف بصمت» — مبدأ حاكم |

### SCR-029 — CashWallets (`/cash`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «محافظ الكاش» |
| Route | `/cash` |
| القسم الحالي | زر «محافظ الكاش» في تفصيل إجراءات Finance؛ وبطاقة حقيقة الكاش في الرئيسية تفتح مصدرها الدقيق (دفتر المحفظة) |
| المالك التقني | المالية |
| الهدف الحالي | كاش المحافظ المعلن + الأمانات + غير الموزع + الكلي؛ أزرار: محفظة جديدة، عدّ الصندوق (أو «لاحقًا»)، وزّع غير الموزع (شرط ≠0)، تحويل (شرط محفظتين)؛ قائمة المحافظ (سجل/ضبط/رصيد لاحق)؛ «سجل لا يحذف بصمت» (آخر 8 آثار + تراجع) |
| الهدف المقترح | إدارة أماكن الكاش وكل أفعاله من بيت واحد |
| المستخدم/المهمة | مالك يدير نقوده |
| الإجراء الأساسي | «محفظة ورصيد بداية» |
| الإجراءات الثانوية | عدّ الصندوق؛ وزّع غير الموزع؛ تحويل؛ السجل/ضبط/رصيد لاحق لكل محفظة؛ تراجع أثر |
| البيانات المقروءة | `cashContinuity.overview/entries`, `projectFinance` (الموقف) |
| الكتابة | لا يكتب (يحيل) |
| الحالات | Default ✓ / Empty ✓ («لا تجعل الكاش المسجل بداية غامضة…») / Error ✓ + إعادة / Success — / Loading ✓ / Disabled ✓ (أفعال «لاحقًا» معلنة بشرطها لا معطلة) |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | عقد 26 §1 مستوى 2؛ الأفعال المشروطة بنمط «لاحقًا» نموذج يجب تعميمه |

### SCR-030 — Collect (`/collect`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «حصّل من مين عليه إلَي» (النجاح: «قبضت من {اسم}») |
| Route | `/collect` (`?source=order:<id>\|sale:<id>`) |
| القسم الحالي | زر «عربون أو تحصيل» في الرئيسية؛ «حصّل…» من OrderDetail؛ اختصار «حصّل من {اسم}» من Parties؛ وبند دين اليوم في الرئيسية |
| المالك التقني | المالية (تحصيل الذمم) — واعٍ بمصدر العمل |
| الهدف الحالي | ورقة تحصيل واعية بالسياق: اختيار الذمة أو قراءة `?source`، مبلغ (كامل المتبقي/نصف/مخصص)، وجهة كاش أو «غير موزع»، معاينة الأثر؛ ثم نتيجة القبض بمصير الكاش و«افتح السجل» |
| الهدف المقترح | تسجيل قبض واحد على دين قائم بوجهته |
| المستخدم/المهمة | مالك قبض من عميل مدين |
| الإجراء الأساسي | «سجّل القبض» (حفظ) |
| الإجراءات الثانوية | تعبئة سريعة (كامل/نصف)؛ وجهة «غير موزع»؛ «افتح السجل» بعد النجاح |
| البيانات المقروءة | `collections.listReceivableSources`، `cashContinuity.overview` |
| الكتابة | `collections.collect` (Collect.tsx:155) |
| الحالات | Default ✓ / Empty ✓ («لا توجد ديون قابلة للتحصيل الآن.») / Error ✓ / Success ✓ (شاشة نتيجة كاملة) / Loading ✓ / Disabled ✓ + منع تجاوز المتبقي |
| العودة | `/` |
| القرار | **إبقاء** |
| سبب القرار | عقد 26 §2.3 (دفتر الناس يفتحها بذمة محددة)؛ سلوك D-003 |

---

## ز. المخزون والمواد

### SCR-031 — MaterialEditor — جديد (`/inventory/material/new`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «أي مادة تسجّل؟» |
| Route | `/inventory/material/new` |
| القسم الحالي | «مادة جديدة» في InventoryMaterials؛ «مادة ورصيد بداية» في Foundation |
| المالك التقني | العمل (مخزون تشغيلي؛ رصيده يغذي التكلفة) |
| الهدف الحالي | إعلان مادة متتبعة: الاسم، الوحدة، الرصيد الافتتاحي بحالته (معروف/مجهول/صفر) وقيمته |
| الهدف المقترح | إضافة مادة للمخزون برصيدها المعلن |
| المستخدم/المهمة | مالك يبدأ متابعة مادة |
| الإجراء الأساسي | حفظ المادة |
| الإجراءات الثانوية | رصيد مجهول معلن (يفتح طريق تأكيد لاحق) |
| البيانات المقروءة | `inventory` |
| الكتابة | `inventory.openMaterial` (MaterialEditor.tsx:145) |
| الحالات | Default ✓ / Error ✓ / Empty ✓ / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/inventory` |
| القرار | **إبقاء** |
| سبب القرار | بوابة تفعيل المخزون (القرار ٩: لقطة رصيد تكفي) |

### SCR-032 — MaterialEditor — تأكيد (`/inventory/material/:id/confirm`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «أكّد رصيد {اسم المادة}» (h1: `أكّد رصيد …`) |
| Route | `/inventory/material/:id/confirm` |
| القسم الحالي | زر تأكيد الرصيد في صف المادة غير المؤكدة (InventoryMaterials) |
| المالك التقني | العمل |
| الهدف الحالي | تأكيد رصيد مادة قائمة بوضع التأكيد (نفس المكوّن) — «أكّد الرصيد» — حدث توثيقي إضافي |
| الهدف المقترح | إغلاق جهل رصيد مادة قائمة |
| المستخدم/المهمة | مالك عرف رصيده الفعلي لاحقًا |
| الإجراء الأساسي | «أكّد الرصيد» |
| الإجراءات الثانوية | — |
| البيانات المقروءة | `inventory` (المادة) |
| الكتابة | `inventory.confirmMaterialOpening` (MaterialEditor.tsx:112) |
| الحالات | Default ✓ / Error ✓ («المادة غير موجودة») / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/inventory` |
| القرار | **إبقاء** (نمط مستقل — عقد 28: «تأكيد رصيد مادة قائمة، نفس مكوّن الرحلة بوضع التأكيد») |
| سبب القرار | موثق في MicroRouter وrouteClassifier |

### SCR-033 — InventoryReversalEditor (`/inventory/movement/:id/reverse`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «تراجع عن حركة المادة» |
| Route | `/inventory/movement/:id/reverse` |
| القسم الحالي | زر «تراجع» في سجل الحركات (InventoryMaterials) |
| المالك التقني | العمل |
| الهدف الحالي | تراجع موثق عن حركة مخزون بسبب |
| الهدف المقترح | إلغاء حركة مخزون خاطئة بتوثيق |
| المستخدم/المهمة | مالك سجل حركة بالخطأ |
| الإجراء الأساسي | «تراجع عن حركة المادة» |
| الإجراءات الثانوية | — |
| البيانات المقروءة | `inventory` (الحركة) |
| الكتابة | `inventory.reverse` (InventoryReversalEditor.tsx:48) |
| الحالات | Default ✓ / Error ✓ («لم نجد حركة المادة») / Success ✓ («تراجع محفوظ») / Loading ✓ / Disabled ✓ |
| العودة | `/inventory` |
| القرار | **إبقاء** |
| سبب القرار | مبدأ عدم الحذف الصامت |

### SCR-034 — InventoryMovementEditor (`/inventory/movement/:type`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | حسب النوع: «استلم شراء مواد» / «استهلك مادة» / «سجل هدر مادة» / «اضبط كمية مادة» |
| Route | `/inventory/movement/:type` — الأنواع: `receipt`, `consume`, `waste`, `adjust` (`?purchase=`, `?order=`, `?sale=`, `?material=`) |
| القسم الحالي | أزرار InventoryMaterials (استلام شراء/استهلاك أو استلام نقص/هدر/ضبط جرد)؛ جسر الاستلام من صفحة الشراء (`?purchase=`)؛ الاستهلاك من OrderDetail/DirectSaleEditor (`?order=`/`?sale=`) |
| المالك التقني | العمل (حركة مخزون؛ الهدر يكتب حدث خسارة غير نقدية — أثر مالي عبر الخدمة) |
| الهدف الحالي | محرر حركة واحد لكل نوع: مادة، كمية، قيمة (للاستلام)، ربط مرجع، معاينات أثر صادقة (رصيد بعدي، منع سالب، خيارات النقص)، وسؤال مالك الهدر (يؤثر الربح؟) |
| الهدف المقترح | تسجيل حركة مخزون واحدة بنوعها وربطها |
| المستخدم/المهمة | مالك يستلم/يستهلك/يهدر/يضبط مواد |
| الإجراء الأساسي | حفظ الحركة |
| الإجراءات الثانوية | خيارات النقص (recordShortage/consumeWithShortage)؛ ملاحظة؛ ربط مرجع |
| البيانات المقروءة | `inventory`، `supplierPurchases` (جسر)، `catalog` (قوالب) |
| الكتابة | `inventory.receivePurchase/consume/waste/adjust/recordShortage/consumeWithShortage` (InventoryMovementEditor.tsx:316-391) |
| الحالات | Default ✓ / Error ✓ («حركة غير متاحة») / Empty ✓ («لا مواد متتبَّعة بعد» + «مادة جديدة») / Success ✓ / Loading ✓ / Disabled ✓ + منع الرصيد السالب |
| العودة | `/inventory` |
| القرار | **إبقاء** |
| سبب القرار | عقد 28 (لا افتراض مادة صامتة)؛ EXE-012 فصل الهدر عن الضبط |

### SCR-035 — InventoryMaterials (`/inventory`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «المواد والمخزون» |
| Route | `/inventory` |
| القسم الحالي | زر هادئ «المواد والمخزون» في Orders؛ زر «المواد والمخزون» في تفصيل إجراءات Finance؛ صف حالة الوحدات في Tools |
| المالك التقني | العمل |
| الهدف الحالي | حالة التفعيل (أو بوابته الصادقة «تفعيل بتاريخ اليوم»)، أزرار الحركات الأربعة (المشروطة بوجود مواد)، قائمة المواد المتتبعة (الرصيد/الحالة/النواقص/تأكيد الرصيد)، وسجل الحركات مع التراجع |
| الهدف المقترح | متابعة موادك وكمياتها وحركاتها |
| المستخدم/المهمة | مالك يدير مخزونه |
| الإجراء الأساسي | «مادة جديدة» |
| الإجراءات الثانوية | استلام شراء؛ استهلاك أو استلام نقص؛ هدر مادة؛ ضبط جرد؛ تأكيد رصيد؛ تراجع حركة؛ «عرض الحركات غير المكتملة» |
| البيانات المقروءة | `inventory.overview` (+ حركات/نواقص) |
| الكتابة | `inventory.activate` (التفعيل) |
| الحالات | Default ✓ / Empty ✓ (لا مواد) / Error ✓ + إعادة / Success ✓ (لافتات) / Loading ✓ / Locked ✓ («المخزون غير مفعّل») / Disabled ✓ (أفعال «لاحقًا» مشروطة) |
| العودة | `/inventory` فالبديل الأعلى — السطح مفتوح من العمل والمالية مع `returnTo` |
| القرار | **إبقاء** |
| سبب القرار | القرار ٩ (تفعيل صريح)؛ عقد 26 §1 مستوى 2 |

---

## ح. منتجاتي وخدماتي (الكتالوج)

### SCR-036 — Catalog (`/catalog`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «منتجاتي وخدماتي» (overline: «مرجع اختياري») |
| Route | `/catalog` |
| القسم الحالي | قسم «منتجاتي وخدماتي» في الرئيسية؛ زر هادئ في Orders؛ صف حالة الوحدات في Tools |
| المالك التقني | الكتالوج-مستهلك (عقد 40 §2: بيانات مرجعية + قراءة View Model من الخدمة المالية؛ **لا يملك سياسة مالية**) |
| الهدف الحالي | 5 أقسام: (1) العناصر «ابدأ بالاسم فقط/أعمال متكررة» بأسعار وتكاليف افتراضية وزر «سجّل بيع هذا المنتج»؛ (2) الوحدات والتحويلات؛ (3) القوالب «ماذا أجهز عادةً؟»؛ (4) **فترة القراءة والسياسة «اقرأ قبل أن تقرر» — سطح كتابة سياسة توزيع** (`recurringWork.createPolicy*`)؛ (5) «أعمال متكررة وقراءة القرار» مع إيقاف سياسة |
| الهدف المقترح | مرجع ما تكرره من منتجات وخدمات وقوالب تكلفته — لا سياسة ربح |
| المستخدم/المهمة | مالك يبيع أشياء متشابهة ويريد سرعة الإدخال |
| الإجراء الأساسي | إنشاء عنصر («ابدأ بالاسم فقط») |
| الإجراءات الثانوية | تعديل الافتراضات؛ تعطيل؛ الوحدات/التحويلات؛ القوالب ومراجعاتها؛ **السياسات (تنقل إلى المالية في الهيكل المستهدف)**؛ القراءات |
| البيانات المقروءة | `catalog` (عناصر/وحدات/تحويلات/قوالب)، `inventory` (مواد القوالب)، `recurringWork.readRecurringWork` (View Model مالي) |
| الكتابة | `catalog.create/updateDefaults/deactivate/createUnit/deactivateUnit/…` + **`recurringWork.createPolicy/createPolicySuccessor/deactivatePolicy` (مالية — ستنتقل)** |
| الحالات | Default ✓ / Empty ✓ / Error ✓ / Success ✓ (FeedbackNote) / Loading ✓ («جارٍ قراءة الفهرس المحلي…») / Disabled ✓ + `UnsavedChangesGuard` |
| العودة | `/tools` (البديل الكنوني الحالي) |
| القرار | **إبقاء + نقل قسم «فترة القراءة والسياسة» إلى سطح مالي** (قرار معتمد) + مراجعة البديل الكنوني (W4-DEC-IA-2) |
| سبب القرار | عقد 40 §4 يسجل القسم كسطح توافق مؤقت: «الكتالوج لا يملك السياسة»؛ قرار المالك في السياق المشترك §5 |

---

## ط. أدواتي

### SCR-037 — Tools (`/tools`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «احسب قبل أن تلتزم» (h1) — overline: «أدواتي» |
| Route | `/tools` |
| القسم الحالي | مقعد الشريط السفلي الرابع |
| المالك التقني | أدوات مستقلة (عقد 40 §3 عقد الأهلية) |
| الهدف الحالي | (1) بطاقة قاعدة الأداة («تقديري دومًا»)؛ (2) صف الحاسبة؛ (3) «تقديراتي المحفوظة» (فتح/ابدأ مسودة/حذف بتأكيد)؛ (4) «حالة الوحدات» — 9 صفوف: حاسبة (مفعّل)، **النسخ الاحتياطي والبيانات (رابط إلى /settings)**، **فحص سلامة مالي (/tools/integrity)**، المخزون، الكتالوج والقوالب، المواعيد، **الموردون والمشتريات (شارة مشتقة من readSummary)**، **دفتر الناس (شارة مشتقة من partyLedger.read)**، السوق والتوصيل (غير متاح — رابط إلى /tools نفسه) |
| الهدف المقترح | بيت أدوات التفكير المستقلة فقط (الحاسبة والتقديرات) — بلا شارات مالية وبلا بطاقة نسخ |
| المستخدم/المهمة | مالك يحسب قبل أن يلتزم |
| الإجراء الأساسي | «افتح الحاسبة» |
| الإجراءات الثانوية | تقديرات محفوظة (فتح/مسودة/حذف)؛ صفوف حالة الوحدات (التي ستنظف) |
| البيانات المقروءة | `costEstimates.list` + للشارات: `inventory.readActivation`, `catalog.listUnits/list`, `schedules.overview`, **`supplierPurchases.readSummary`, `partyLedger.read`** (قائمة التوافق المحصورة في عقد 40 §4) |
| الكتابة | `costEstimates.remove` فقط (الأداة المستقلة) |
| الحالات | Default ✓ / Empty ✓ («لا توجد تقديرات محفوظة بعد.») / Error ✓ (UX-02: إعادة محاولة) / Success ✓ / Loading ✓ / Disabled ✓ (صف «غير متاح») |
| العودة | — (مقعد دائم) |
| القرار | **إبقاء + تنظيف معتمد**: إزالة بطاقة «النسخ الاحتياطي والبيانات»؛ إزالة شارات الموردين/دفتر الناس المشتقة من السجل المالي؛ معالجة صف «السوق والتوصيل» (W4-NEW-IA-02) |
| سبب القرار | قرارات مالك معتمدة (السياق المشترك §5 + عقد 40 §6 بنود 3-4) |

### SCR-038 — Market (`/market`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «السوق» |
| Route | `/market` |
| القسم الحالي | المقعد الخامس في الشريط السفلي فقط |
| المالك التقني | مستقبلي (عقد 40: وحدة مستقلة مستقبلية — لا شيء يُقرأ/يُكتب) |
| الهدف الحالي | إعلان «قريبًا» صادق لسوق موردين مستقبلي + توجيه للتسجيل الحالي من المالية/العمل |
| الهدف المقترح | إعلان توسعة بلا وظيفة وهمية |
| المستخدم/المهمة | مالك يتساءل عن السوق |
| الإجراء الأساسي | «رجوع لمشروعي» |
| الإجراءات الثانوية | — |
| البيانات المقروءة | لا شيء |
| الكتابة | لا شيء (يحرسه الاختبار المعماري) |
| الحالات | Default ✓ (إعلان) / البقية لا تطبق عمدًا |
| العودة | `returnPath` (EXE-005: بوصلة معلنة لا history.back) |
| القرار | **إبقاء** |
| سبب القرار | قرار مالك NAV-001 — مقعد معلن «قريبًا» |

### SCR-039 — CostCalculator (`/tools/calculator`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «حاسبة التكلفة والسعر» |
| Route | `/tools/calculator` (`?estimate=<id>` وضع تعديل) |
| القسم الحالي | «افتح الحاسبة» في Tools؛ «عدّل التقدير» من EstimateDetail |
| المالك التقني | أدوات مستقلة (`application/cost-estimates/`) |
| الهدف الحالي | حاسبة عميقة: مواد + وقت + بنود اختيارية → سعر حماية حي؛ حفظ/تحديث تقدير؛ «احسب قبل أن تلتزم — النتيجة تقديرية دومًا» |
| الهدف المقترح | تجربة تكلفة وسعر قبل أي التزام |
| المستخدم/المهمة | مالك يسعّر عملًا جديدًا |
| الإجراء الأساسي | «احسب تكلفة وسعر حماية» ثم «احفظ التقدير لمراجعته لاحقًا» |
| الإجراءات الثانوية | حفظ التعديلات على تقدير قائم؛ بدء مسودة من التقدير المحفوظ |
| البيانات المقروءة | `costEstimates`، مقترحات مواد المخزون (قراءة مرجعية موثقة) |
| الكتابة | `costEstimates.save/update` (مخزن الأداة وحده) |
| الحالات | Default ✓ / Empty ✓ / Error ✓ / Success ✓ (يفتح صفحة التقدير) / Loading ✓ / Disabled ✓ + حارس مدخلات |
| العودة | `/tools` |
| القرار | **إبقاء** |
| سبب القرار | عقد 40 §2 (أدوات مستقلة — صفر أثر مالي) |

### SCR-040 — EstimateDetail (`/tools/estimate/:id`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | عنوان التقدير (h1) — overline: «تقدير محفوظ» |
| Route | `/tools/estimate/:id` |
| القسم الحالي | صفوف «تقديراتي المحفوظة» في Tools؛ وشاشة نجاح الحاسبة |
| المالك التقني | أدوات مستقلة |
| الهدف الحالي | قراءة التقدير: سعر الحماية (أو «غير متوفر بعد»)، المدخلات والافتراضات، أفعال: «ابدأ مسودة من هذا التقدير»، «عدّل التقدير»، «احذف التقدير نهائيًا» بتأكيد مسمّى |
| الهدف المقترح | مراجعة تقدير محفوظ واتخاذ قرار الالتزام |
| المستخدم/المهمة | مالك عاد لتقدير قديم |
| الإجراء الأساسي | «ابدأ مسودة من هذا التقدير» |
| الإجراءات الثانوية | «عدّل التقدير» (الحاسبة بوضع التعديل)؛ الحذف بتأكيد |
| البيانات المقروءة | `costEstimates.get` |
| الكتابة | `costEstimates.remove` (EstimateDetail.tsx:65) |
| الحالات | Default ✓ / Empty — لا يطبق (معرّف) / Error ✓ («لم نجد هذا التقدير») / Success — / Loading ✓ / Disabled ✓ |
| العودة | `/tools` |
| القرار | **إبقاء** |
| سبب القرار | عقد 26 §2.2 يوثق عودتها ونفسها للجسر |

### SCR-041 — ToolsIntegrity (`/tools/integrity`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «فحص سلامة مالي» |
| Route | `/tools/integrity` |
| القسم الحالي | سطر «فحص سلامة مالي — اطمن على أرقامك» في Finance؛ صف في Tools؛ رابط من نتيجة الاستعادة في Settings |
| المالك التقني | **المالية** (عقد 40 §2: `application/finance/integrityCheckService` — MIC-1..16 قراءة فقط) — السطح مؤقت في أدواتي |
| الهدف الحالي | وعد «يقرأ أرقامك ولا يغيّر شيئًا»؛ «افحص الآن»؛ خلاصة + قائمة الفحوص مع الحالات والمجاميع والفوارق والسجلات المتأثرة وروابط تصحيح سياقية |
| الهدف المقترح | فحص اتصاب أرقامك دوريًا وبعد الاستعادة |
| المستخدم/المهمة | مالك يشك برقم أو يتحقق بعد استيراد |
| الإجراء الأساسي | «افحص الآن» |
| الإجراءات الثانوية | فتح السجلات المتأثرة (روابط تصحيح) |
| البيانات المقروءة | السجل المالي كاملًا (قراءة فقط، لقطة قبل/بعد يحرسها اختبار) |
| الكتابة | لا شيء (وعد معماري) |
| الحالات | Default ✓ («لم تُشغَّل بعد») / Empty — لا يطبق / Error ✓ / Success ✓ (PASS/WARN/UNAVAILABLE/FAIL + مجاميع) / Loading ✓ («جارٍ الفحص…») / Disabled ✓ |
| العودة | `/tools` (اليوم) |
| القرار | **نقل إلى: المالية ← المزيد ← سلامة الحسابات** (قرار مالك معتمد) |
| سبب القرار | عقد 40 §4 يسجله سطحًا مؤقتًا؛ السياق المشترك §5 قرر وجهته |

---

## ي. الأصول والقروض

### SCR-042 — AssetEditor (`/assets/new`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «شراء للاستخدام الطويل» |
| Route | `/assets/new` |
| القسم الحالي | «سجّل أصلًا» في Assets |
| المالك التقني | المالية (عقد 29 — دفتري مشتق من الأحداث) |
| الهدف الحالي | تسجيل أصل: الاسم، الفئة، قيمة وطريقة الاقتناء (نقد/ذمم)، التاريخ، العمر الإنتاجي (قد يكون مجهولًا) |
| الهدف المقترح | تسجيل شراء طويل الاستخدام كأصل لا مصروف |
| المستخدم/المهمة | مالك اشترى آلة/جهازًا |
| الإجراء الأساسي | «احفظ الأصل» |
| الإجراءات الثانوية | عمر مجهول معلن (بلا إهلاك حتى تحديد) |
| البيانات المقروءة | `assets` |
| الكتابة | `assets.create` (AssetEditor.tsx:142) |
| الحالات | Default ✓ / Error ✓ / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/assets` |
| القرار | **إبقاء** |
| سبب القرار | عقد 29 |

### SCR-043 — AssetDetail (`/assets/:id`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | اسم الأصل (h1) |
| Route | `/assets/:id` |
| القسم الحالي | بطاقات قائمة Assets |
| المالك التقني | المالية |
| الهدف الحالي | تفصيل الأصل: الدفتري والإهلاك المسجل والمستحق، تسجيل إهلاك موثق، أرشفة/إخراج بتوثيق، تراجع موثق |
| الهدف المقترح | متابعة أصل واحد وإهلاكه وأرشفته |
| المستخدم/المهمة | مالك يدير أصوله |
| الإجراء الأساسي | تسجيل إهلاك الفترة (من التفصيل) |
| الإجراءات الثانوية | «تراجع موثق»؛ أرشفة |
| البيانات المقروءة | `assets` |
| الكتابة | خدمات الأصول (إهلاك/أرشفة/تصحيح — عبر `assets`) |
| الحالات | Default ✓ / Error ✓ / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/assets` |
| القرار | **إبقاء** |
| سبب القرار | عقد 29 + تصحيح مراجعة 4-c (العودة للقائمة) |

### SCR-044 — Assets (`/assets`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «الأصول» |
| Route | `/assets` |
| القسم الحالي | طبقة «الأصول» في Finance (وضع الآن) |
| المالك التقني | المالية |
| الهدف الحالي | خلاصة (الدفتري الكلي، إهلاك مستحق غير مسجل، مجهولي العمر) + بطاقات الأصول + «سجّل أصلًا» |
| الهدف المقترح | رؤية أصولك بقيمها الدفترية |
| المستخدم/المهمة | مالك يراجع ما يملكه المشروع من أدوات طويلة الأمد |
| الإجراء الأساسي | «سجّل أصلًا» |
| الإجراءات الثانوية | فتح بطاقة أصل |
| البيانات المقروءة | `assets.overview` |
| الكتابة | لا يكتب |
| الحالات | Default ✓ / Empty ✓ («لا أصول مسجلة بعد.») / Error ✓ / Loading ✓ / Disabled — |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | عقد 29 — طبقتان مستقلتان بمدخلين |

### SCR-045 — LoanEditor (`/loans/new`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «أعطيت مالًا يُعاد» |
| Route | `/loans/new` |
| القسم الحالي | «سجّل قرضًا» في Loans |
| المالك التقني | المالية |
| الهدف الحالي | تسجيل قرض صادر: المستفيد، المبلغ، التاريخ، وجهة الكاش |
| الهدف المقترح | توثيق مالٍ عند غيرك يُعاد |
| المستخدم/المهمة | مالك أقرض مالًا |
| الإجراء الأساسي | «احفظ القرض» |
| الإجراءات الثانوية | — |
| البيانات المقروءة | `cashContinuity` (المحافظ) |
| الكتابة | `loans.create` (LoanEditor.tsx:100) |
| الحالات | Default ✓ / Error ✓ / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/loans` |
| القرار | **إبقاء** |
| سبب القرار | عقد 29 |

### SCR-046 — LoanDetail (`/loans/:id`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | اسم المستفيد (h1) |
| Route | `/loans/:id` |
| القسم الحالي | بطاقات قائمة Loans |
| المالك التقني | المالية |
| الهدف الحالي | تفصيل القرض: الأصل، المسدد، المتبقي؛ تسديد/تصحيح (`loans.correctLoan`)؛ تراجع موثق |
| الهدف المقترح | متابعة قرض حتى استرداده |
| المستخدم/المهمة | مالك يتابع قرضًا أعطاه |
| الإجراء الأساسي | تسجيل تسديد/دفعة |
| الإجراءات الثانوية | «تراجع موثق»؛ تصحيح |
| البيانات المقروءة | `loans` |
| الكتابة | `loans.correctLoan` (LoanDetail.tsx:111) |
| الحالات | Default ✓ / Error ✓ / Success ✓ / Loading ✓ / Disabled ✓ |
| العودة | `/loans` |
| القرار | **إبقاء** |
| سبب القرار | عقد 29 |

### SCR-047 — Loans (`/loans`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «القروض» |
| Route | `/loans` |
| القسم الحالي | طبقة «القروض» في Finance (وضع الآن — تظهر أيضًا العربونات المحتفظة المعلقة) |
| المالك التقني | المالية |
| الهدف الحالي | خلاصة «قائم عند الناس» + بطاقات القروض + «سجّل قرضًا» + ورقة تسديد سريعة (RepaymentSheet) |
| الهدف المقترح | رؤية مالك عند غيرك ومتابعة استرداده |
| المستخدم/المهمة | مالك أقرض ودائنون لديه |
| الإجراء الأساسي | «سجّل قرضًا» |
| الإجراءات الثانوية | فتح بطاقة؛ تسديد سريع من القائمة |
| البيانات المقروءة | `loans.overview` |
| الكتابة | عبر `RepaymentSheet` (خدمات القروض) |
| الحالات | Default ✓ / Empty ✓ («لا قروض صادرة بعد.») / Error ✓ + إعادة / Loading ✓ / Disabled — |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | عقد 29 |

---

## ك. دفتر الناس والقراءات المالية

### SCR-048 — Parties (`/parties`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «مين عليه إلَي، وعليّ لمين؟» (overline: «دفتر الناس») |
| Route | `/parties` |
| القسم الحالي | سطر «افتح دفتر الناس» في Finance؛ بطاقة «أثناء غيابك» في الرئيسية (دين فات موعده) |
| المالك التقني | المالية (قراءة مجمعة للذمم من الطلبات والمشتريات والالتزامات — لا كيانات إضافية) |
| الهدف الحالي | خلاصتا «لك عند الناس»/«عليك للناس»؛ بحث بالاسم؛ صف لكل اسم (متكرر/لك/عليك/عدد الحركات) بأحداثه وروابط مصادرها؛ واختصار «حصّل من {اسم}» يفتح ورقة التحصيل بالذمة |
| الهدف المقترح | معرفة من عليه لك وممن عليك بالاسم |
| المستخدم/المهمة | مالك يتذكر ديون الأشخاص |
| الإجراء الأساسي | فتح تفاصيل اسم (الإفصاح) ثم «حصّل من …» |
| الإجراءات الثانوية | البحث؛ فتح كل حركة بمصدرها |
| البيانات المقروءة | `partyLedger.read` |
| الكتابة | لا شيء («هذا الدفتر قراءة مجمّعة… ما يُسجَّل منه شيء جديد») |
| الحالات | Default ✓ / Empty ✓ («لا يوجد ديون على أحد بعد.» + فراغ البحث) / Error ✓ + إعادة / Success — / Loading ✓ / Disabled — |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | D-003: الدفتر نموذج قراءة والتحصيل من ورقته |

### SCR-049 — Finance (`/finance`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «مالي» |
| Route | `/finance` (`?view=period`, `?layer=corrections\|events`, `?event=<id>`) |
| القسم الحالي | مقعد الشريط السفلي الثالث |
| المالك التقني | المالية |
| الهدف الحالي | السطح الرئيسي بوضعين: «الوضع الآن» (نبضة المراجعة المدمجة F-003، قرار الكاش، مال المالك، بطاقات الموقف، شريط غير الموزع، «ما نعرفه الآن» بروابط المصادر، عربونات معلقة، طبقتا الأصول والقروض) و«شو صار خلال الفترة» (نتيجة الفترة بشهورها، التغطية والتعادل + سجل المتوقعات، كشف الفترة). وخلف الإفصاح: «تسجيل حركة أو فتح مصدر» (محافظ/موردون/مواد/مصروف مدفوع/التزام/تسديد/أمانة قُبضت/أمانة سُلّمت/هالك/مال المالك) + «آخر ما حدث — القارئ الكامل» + طبقتا سجل الأحداث وسجل التصحيحات |
| الهدف المقترح | فهم وضعك المالي ونتيجة فترتك واتخاذ القرار المالي التالي |
| المستخدم/المهمة | المالك المالي للمشروع |
| الإجراء الأساسي | قرار الكاش الأعلى («أعلن تحصيلًا أو التزامًا قريبًا» / «وزّع على محفظة» / فتح الدفتر الموحد) |
| الإجراءات الثانوية | كل ما خلف `<details>`: الأصول، القروض، التغطية والتعادل + سجل المتوقعات، تسجيل حركة أو فتح مصدر (10 أفعال)، القارئ الكامل، سجل الأحداث، سجل التصحيحات |
| البيانات المقروءة | `financialPulse`، `projectFinance` (موقف/فترة/رؤى)، `ownerEntitlement`، `g5`، الأحداث، الطلبات، الأصول، القروض، العربونات |
| الكتابة | لا يكتب مباشرة (يحيل) |
| الحالات | Default ✓ / Empty ✓ («لا محفظة معلنة بعد — سجّل…» طُرق) / Error ✓ («تعذر قراءة الوضع المالي») / Success ✓ / Loading ✓ / Disabled ✓ (أمانة سُلّمت مشروطة بوجود أمانات) |
| العودة | — (مقعد دائم؛ زر رجوع يظهر عند `returnTo`) |
| القرار | **إبقاء + استقبال المنقولات المعتمدة**: فحص السلامة (خلف «المزيد»/الإفصاح) وسياسات التوزيع (سطح مالي) |
| سبب القرار | عقد 40 §4 يسجل الوجهة؛ بطاقات الموقف وتدرّج الوضع/الفترة قرار NAV-002 |

### SCR-050 — Statement (`/finance/statement`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «كشف الفترة» |
| Route | `/finance/statement` |
| القسم الحالي | بطاقة «قصة الأسبوع ببساطة» في قراءة الفترة بFinance؛ سطر «كشف الفترة» في «ما نعرفه الآن» |
| المالك التقني | المالية |
| الهدف الحالي | كشف أسطر عربية مفصولة (ما دخل/ما خرج/تصحيحات/أمانات/ذمم/مال المالك) بفترة قابلة للتغيير؛ «ولّد ونزّل التقرير» (تنزيل دائم + مشاركة نص اختيارية — عقد 32)؛ كل سطر بمصدره |
| الهدف المقترح | قراءة قصة فترتك المالية ببساطة ومشاركتها يدويًا |
| المستخدم/المهمة | مالك يريد ملخصًا يفهمه أو يشاركه |
| الإجراء الأساسي | «ولّد ونزّل التقرير» |
| الإجراءات الثانوية | تغيير الفترة؛ فتح مصادر الأسطر؛ مشاركة النص |
| البيانات المقروءة | `statement.read(from, to)` |
| الكتابة | لا شيء (تقرير قراءة) |
| الحالات | Default ✓ / Empty ✓ / Error ✓ («تعذر قراءة الكشف») / Success ✓ (نتيجة التوليد) / Loading ✓ / Disabled ✓ |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | عقد 32 (تقرير محلي) + عقد 26 §2.2 |

### SCR-051 — FinanceActivity (`/finance/activity`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «آخر ما حدث» |
| Route | `/finance/activity` |
| القسم الحالي | «افتح السجل الكامل» في الرئيسية؛ و«آخر ما حدث — القارئ الكامل» في Finance |
| المالك التقني | المالية (عقد 30 — القارئ الموحد لكل العائلات) |
| الهدف الحالي | قارئ كل النشاط بفلاتر (عائلات/فترة/حد)؛ كل صف بكلمة أثر ومصدر قابل للفتح؛ الصفوف الأحدث أولًا |
| الهدف المقترح | تتبع كل ما حدث في مشروعك من مصدر واحد |
| المستخدم/المهمة | مالك يبحث عن «شو صار» |
| الإجراء الأساسي | فتح مصدر صف |
| الإجراءات الثانوية | الفلاتر (عائلات/فترة/عدد) |
| البيانات المقروءة | `activity.read` |
| الكتابة | لا شيء |
| الحالات | Default ✓ / Empty ✓ / Error ✓ + إعادة / Loading ✓ / Disabled — |
| العودة | `/finance` |
| القرار | **إبقاء** |
| سبب القرار | عقد 30 — قارئ موحد أغلق القصّات المنفصلة |

### SCR-052 — SharePreview (`/share/preview`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | عنوان المسودة (h1: `draft.title`) — overline: معاينة المشاركة |
| Route | `/share/preview` (الحالة عبر `history.state` لا الاستعلام — «لا استعلام يكشف المحتوى») |
| القسم الحالي | **منتجه الوحيد في الكود: OrderDetail** (زر «شارك مع العميل» — customerShareDraft بنقطة قرار واحدة) |
| المالك التقني | لا يطبق بالعقد 40 — عقد 33 يملك سلوك المشاركة؛ السطح عمق نصي فوق العمل |
| الهدف الحالي | عرض النص كاملًا، تعديله يدويًا بحرية، «شارك/أرسل النص» أو نسخ؛ بلا إرسال تلقائي ولا قراءة جهات اتصال |
| الهدف المقترح | مراجعة نص المشاركة وتعديله قبل الإرسال بيدك |
| المستخدم/المهمة | مالك يريد إخبار عميله بحالة طلبه/قبضته |
| الإجراء الأساسي | «شارك» (أو «أرسل النص») |
| الإجراءات الثانوية | نسخ للحافظة؛ تعديل النص |
| البيانات المقروءة | `history.state.draft` (ShareDraft من `shareMessageService`) |
| الكتابة | لا شيء (نص فقط) |
| الحالات | Default ✓ / Empty ✓/Locked ✓ («لا نص للمشاركة» — بلا مسودة) / Success ✓ (نتيجة الإرسال) / Error ✓ / Disabled ✓ |
| العودة | `/finance` (البديل الكنوني = مصدر الاستدعاء) |
| القرار | **إبقاء** |
| سبب القرار | عقد 33 — معاينة قبل أي إرسال؛ الوصول من تفاصيل العملية فقط مطابق للعقد |

---

## ل. العمل (السطح الرئيسي) والإعدادات والملف والرئيسية

### SCR-053 — Orders (`/orders`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «العمل» (overline: «وجهة العمل») |
| Route | `/orders` |
| القسم الحالي | مقعد الشريط السفلي الثاني |
| المالك التقني | العمل |
| الهدف الحالي | «الأولوية الآن» (دائمًا)، طلباتي، مبيعاتي (مباشرة)، مسودات قيد الإكمال، المواعيد (مقطع دائم + «افتح جدول المواعيد»)، «تسجيل بيع مباشر»، أزرار هادئة «منتجاتي وخدماتي»/«المواد والمخزون» (تحترم SET-003)، «إنشاء مسودة أخرى»، وحالة فراغ «يومك مفتوح — سجّل أول بيع» |
| الهدف المقترح | مركز متابعة كل عملك: ماذا يُنفَّذ وماذا يُسلَّم وماذا يُحصَّل |
| المستخدم/المهمة | المالك المنفذ |
| الإجراء الأساسي | «تسجيل بيع مباشر» (أو فتح صف الأولوية) |
| الإجراءات الثانوية | منتجاتي وخدماتي؛ المواد والمخزون؛ إنشاء مسودة؛ فتح الجدول |
| البيانات المقروءة | `dailyFollowUp.read`, `directSales.list`, `schedules.overview`, `preferences.readDisabledCapabilities` |
| الكتابة | لا شيء (يحيل) |
| الحالات | Default ✓ / Empty ✓ (EmptyState كامل) / Error ✓ + إعادة / Success — / Loading ✓ / Disabled ✓ (إخفاء احترامي للقدرات SET-003) |
| العودة | — (مقعد دائم) |
| القرار | **إبقاء** |
| سبب القرار | تصميم المجموعة ١ §8 (الأولوية قبل القوائم) — مثبت باختبارات Orders.ui |

### SCR-054 — Redirect `/review` — **بلا شاشة مستقلة**

| الحقل | القيمة |
|---|---|
| الاسم الحالي | — (تحويل inline إلى `/finance`) |
| Route | `/review` → `/finance` |
| القسم الحالي | لا منتج — توافق الروابط القديمة |
| المالك التقني | المالية (وجهته) |
| الهدف الحالي | المسار القديم يقود إلى المالية حيث اندمجت المراجعة نبضة أعلاها (F-003) «لا إلى 404» |
| الهدف المقترح | حفظ الروابط القديمة |
| المستخدم/المهمة | رابط قديم |
| الإجراء الأساسي | — |
| الإجراءات الثانوية | — |
| البيانات المقروءة | لا شيء |
| الكتابة | لا شيء |
| الحالات | — |
| العودة | — |
| القرار | **إبقاء كتحويل** |
| سبب القرار | عقد 26 §7: «`/review` يقود إلى `/finance`» |

### SCR-055 — Settings (`/settings`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «الإعدادات» (overline: «التحكم المحلي») |
| Route | `/settings` (`?focus=guided-import`) |
| القسم الحالي | زر الترس في الترويسة (كل الأكثر السطحية)؛ «انسخ الآن»/«افتح الإعدادات» من الرئيسية؛ صف النسخ في Tools؛ «فتح الاستيراد» من Foundation |
| المالك التقني | الإعدادات-البيانات (عقد 40: `localTransferService` + `guidedOpeningImportService` — منشئ المظروف ومستهلكه) |
| الهدف الحالي | سطر «الحقيقة المحلية»؛ مدخل «ملفك وملف مشروعك»؛ أقسام مطوية: «احمِ بياناتك» (تصدير متحقق، استيراد بمعاينة وبوابة رمز + نسخة ما قبل الاستبدال + فحص سلامة بعدها EXE-014، تصفير محمي، تقرير تشخيص، تذكير نسخ)؛ «كيف تنجز عملك غالبًا؟» (نمط تشغيل)؛ «قدرات مشروعك» (SET-003)؛ «المظهر»؛ «إدخال موقف افتتاحي» (`?focus`) + قفل التطبيق (LockSettingsCard) |
| الهدف المقترح | تهيئة التطبيق وحماية بياناته ونسخه — لا عمليات يومية |
| المستخدم/المهمة | مالك يضبط ويحمي |
| الإجراء الأساسي | تصدير نسخة متحققة (داخل «احمِ بياناتك») |
| الإجراءات الثانوية | الاستيراد؛ التصفير؛ التشخيص؛ القدرات؛ المظهر؛ نمط العمل؛ الإدخال الافتتاحي؛ رمز القفل |
| البيانات المقروءة | `transfers`، `guidedOpeningImport`، `actualTime`، `preferences`، `integrityCheck` (بعد الاستعادة)، ملخص البيانات |
| الكتابة | `transfers.createVerifiedExport/confirmImport`، `guidedOpeningImport.confirm`، `actualTime.saveOperatingMode`، `preferences.saveDisabledCapabilities`، والتصفير |
| الحالات | Default ✓ / Empty — لا يطبق / Error ✓ (إشعارات تخزين مصنفة) / Success ✓ («completion») / Loading ✓ (isWorking) / Locked ✓ (بوابات رمز القفل `DataActionPinGate`) / Disabled ✓ |
| العودة | `returnTo` (من الترس غالبًا السطح الأصلي) |
| القرار | **إبقاء + توطيد «البيانات والنسخ الاحتياطي» كقسم مسمى داخله** (قرار مالك معتمد) |
| سبب القرار | عقد 40 §2 (مالك النسخ والاستعادة)؛ EXE-014 |

### SCR-056 — Profile (`/profile`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «ملفك وملف مشروعك» |
| Route | `/profile` |
| القسم الحالي | «ملف المالك» في ترويسة الرئيسية؛ «افتح الملف» في Settings |
| المالك التقني | الإعدادات-البيانات (هوية محلية) |
| الهدف الحالي | عرض/تعديل هوية المالك (اسم/بريد اختياريان) وملف المشروع (الاسم، العملة واللغة، المحافظ، آخر نسخة متحققة)؛ «تسجيل الدخول والمزامنة لاحقًا» |
| الهدف المقترح | معلوماتك ومعلومات مشروعك في مكان واحد |
| المستخدم/المهمة | مالك يعرّف عن نفسه ومشروعه |
| الإجراء الأساسي | «عدّل ملفك» (ثم «احفظ ملفك») |
| الإجراءات الثانوية | إلغاء التعديل؛ عرض المعرّف المحلي |
| البيانات المقروءة | `ownerProfile`, `profiles` |
| الكتابة | `ownerProfile.save` + `profiles.save` (Profile.tsx:109/118) |
| الحالات | Default ✓ / Empty ✓ («غير مكتوب بعد — اختياري») / Error ✓ («تعذر فتح ملفك») / Success ✓ («حُفظ») / Loading ✓ / Disabled ✓ |
| العودة | `/` |
| القرار | **إبقاء** |
| سبب القرار | عقد 26 §7: سطح بلا مقعد سادس يفتح من الترويسة/الإعدادات |

### SCR-057 — Home (`/`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | اسم النشاط (h1) — overline: «مشروعي الآن» |
| Route | `/` (`?setup=1` لافتة) |
| القسم الحالي | المقعد الأول في الشريط السفلي |
| المالك التقني | لا يطبق (مركز قراءة متعدد المالكين — يقرأ الكل ولا يكتب شيئًا) |
| الهدف الحالي | مركز التحكم: ترويسة (اسم+تاريخ+ملف المالك)؛ «سجّل بسرعة» (بيع/مصروف بالورقة + طلب عميل/مسودة تصميم/عربون أو تحصيل كمسارات)؛ «أثناء غيابك» (7 أيام)؛ «الأهم الآن»؛ «اليوم»؛ «ما هو مسجل حتى الآن؟» (بطاقات حقائق: مصدر/طريق)؛ قسم «مالي» (صفحة الأساس + زر مالي)؛ قسم «منتجاتي وخدماتي»؛ «مسارات مرتبطة فقط» (المواعيد/نتيجة الفترة)؛ «آخر ما حدث» + «افتح السجل الكامل»؛ سطر المكان |
| الهدف المقترح | نقطة البداية اليومية: شو مهم الآن وسجّل بسرعة |
| المستخدم/المهمة | المالك يوميًا |
| الإجراء الأساسي | أزرار «سجّل بسرعة» (البيع/المصروف ورقةً فوق الشاشة) |
| الإجراءات الثانوية | ملف المالك؛ صفحة الأساس؛ المواعيد؛ نتيجة الفترة؛ السجل الكامل؛ الإعدادات (من سطر الحقيقة/الغياب) |
| البيانات المقروءة | `homeControlCenter.read` (بطاقات+اليوم+الغياب+المسارات+آخر ما حدث)، `preferences.readDisabledCapabilities` |
| الكتابة | لا شيء (ورقة البيع/المصروف تكتب عبر خدماتها عند الفتح) |
| الحالات | Default ✓ / Empty ✓ («يومك مفتوح») / Error ✓ («تعذر تحميل مشروعك» + إعادة S5-08) / Success ✓ (`?setup=1`) / Loading ✓ (بلا وميض عند التحديث الخلفي) / Locked ✓ (بطاقات «غير مسجل — سجّله») / Disabled ✓ (إخفاء أزرار الطلب عند إيقاف القدرة) |
| العودة | — (المقعد الأول) |
| القرار | **إبقاء** |
| سبب القرار | NAV-001: أفعال التسجيل السريع بيتها هنا بعد إزالة FAB |

### SCR-058 — NotFound (`*`)

| الحقل | القيمة |
|---|---|
| الاسم الحالي | «هذه الصفحة ليست جزءًا من هذا الإصدار» |
| Route | `*` |
| القسم الحالي | أي مسار غير معروف |
| المالك التقني | لا يطبق |
| الهدف الحالي | 404 بلغة صادقة + مخرج واحد «العودة إلى مشروعي الآن» |
| الهدف المقترح | إغلاق مهذب للمسارات غير الموجودة |
| المستخدم/المهمة | مستخدم ضغط رابطًا قديمًا/خاطئًا |
| الإجراء الأساسي | «العودة إلى مشروعي الآن» |
| الإجراءات الثانوية | — |
| البيانات المقروءة | لا شيء |
| الكتابة | لا شيء |
| الحالات | Default ✓ (حالة واحدة) |
| العودة | `/` |
| القرار | **إبقاء** |
| سبب القرار | مبدأ Micro: كل وجهة لها مخرج واضح |

---

## م. ملخص مكتشفات IA الجديدة (مصنفة وفق قواعد الجودة)

| المعرف | الوصف | التصنيف | الدليل |
|---|---|---|---|
| **W4-NEW-IA-01** | قائمة ورقة «سجّل» الخماسية (طلب عميل/مسودة تصميم/عربون أو تحصيل + بيع/مصروف) لا تُفتح كمدخل مستقل بعد إزالة FAB (NAV-001) — تُوصل فقط من داخل نموذجَي البيع/المصروف بزر «رجوع إلى القائمة»؛ أي أن 3 من 5 أفعال تسجيل مسنودة خلف مسار غير مكتشف | NEW_UX_FINDING (DEEPENS NAV-001) | `quickRecording.tsx` (لا استدعاء `openQuickForm("menu")` في أي صفحة) + `QuickSaleForm.tsx:296` و`QuickExpenseForm.tsx:270` («رجوع إلى القائمة») |
| **W4-NEW-IA-02** | صف «السوق والتوصيل» في «حالة الوحدات» بأدواتي: حالة «غير متاح» ورابطه `/tools` (نفس الصفحة) — صف ميت ذاتي المرجع يكرر مقعد السوق المستقل | NEW_UX_FINDING | `Tools.tsx:134` (`{ label: "السوق والتوصيل", state: "not_available", href: "/tools" }`) |
| **W4-NEW-IA-03** | البديل الكنوني للكتالوج هو `/tools` بينما منتجوه الفعليون اليوم: الرئيسية والعمل (وأدواتي)؛ بعد التنظيف المعتمد لأدواتي (إزالة بطاقة النسخ والشارات المالية) يبقى صف الكتالوج في حالة الوحدات رابطًا جانبيًا ضعيفًا — البديل الكنوني سيرث واجهة بيت خاطئ | NEW_UX_FINDING (DEEPENS قرار تنظيف أدواتي) | `navigationContract.ts:187` (`"/catalog": "/tools"`) + المنتجون (Home.tsx:480, Orders.tsx:323, Tools.tsx:117) |
| **W4-NEW-IA-04** | ازدواج بيت حالة القدرات: قسم «قدرات مشروعك» في الإعدادات (SET-003: تعطيل + إحصاء قائم) يكرر وظيفيًا صفوف «حالة الوحدات» في أدواتي (تفعيل/تهيئة/مفعّل) — قارئان لحالة الوحدات بمفردات مختلفة في سطحين | NEW_UX_FINDING | `SettingsCapabilitiesSection.tsx` (قسم القدرات) مقابل `Tools.tsx:85-135` (حالة الوحدات) |
| — | ازدواج بنية الإدخال السريع (أزرار الرئيسية مقابل قائمة الورقة) جزء من W4-NEW-IA-01 نفسه (سبب تصميمي واحد) | مدموج أعلاه | — |
| — | شارات الموردين/دفتر الناس المشتقة من السجل المالي في أدواتي | DUPLICATE_OF قرار معتمد (عقد 40 §6 بند 3-4) — لا يُعاد تسجيله كمكتشف جديد | عقد 40 §4 |

**تخصيص نطاق المعرفات (لتفادي تصادم الوكلاء عند الدمج):** الوكيل 2 استعمل `W4-NEW-01..09` (ملف UX المالي) والوكيل 4 استعمل `W4-NEW-001..005` (القاموس) — لذلك تُرقَّم مكتشفات هذا الجرد `W4-NEW-IA-01..04` وبواباته `W4-DEC-IA-1/2`. على الوكيل 5 (الدمج) اعتماد هذا التخصيص أو إعادة ترقيم شاملة مرة واحدة.

**ملاحظات INFERRED/UNVERIFIED في هذا الجرد:**
- «القسم الحالي» لـSCR-015 (السحب): استنتاج أن المدخل الفعلي هو دفتر المالك فقط — **INFERRED** من غياب زر مباشر في Finance.tsx (البطاقة تحيل للدفتر — S2-07) وتعليق الكود.
- عدم وجود منتجات داخلية أخرى لـ`destinationWalletId` غير المدخل اليدوي: مثبت بالبحث في كل `pages/` و`components/` (المطابق الوحيد: CashDistribution.tsx:38 يقرؤه) — لكن البحث شمل الواجهة فقط لا مستقبلات مستقبلية؛ **UNVERIFIED** خارج نطاق الكود الحالي.
- أسعار/ملصقات النسخة المنشورة (micro-prototype.pages.dev) غير مُتحقق منها حيًّا — المرجع هو الكود عند BASE_SHA (وفق `DEPLOYMENT_COMMIT_NOT_VERIFIED` في السياق المشترك).

**إحصاء الجرد:** 58 صفًا (SCR-001→SCR-058) · 4 أنماط بلا شاشة مستقلة موثقة السبب (SCR-003/013/054 تحويلات؛ SCR-058 NotFound له شاشة 404 فعلية وموثقة كصف) · 3 مكوّنات تخدم نمطين+ موثقة في صفوف مستقلة مع الفرق (SCR-004/005، SCR-018/019، SCR-031/032).
