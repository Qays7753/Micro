# تقرير تنفيذ — Stage 2 / الموجة 4: OPS-007 + OPS-009 (إعلان سبب المراجعة في مقارنة المادة المنفذة + دليل حتمية لقطة التكلفة)

**التاريخ:** 2026-09-21 · **PR:** #200 · **الفرع:** `stage-2/completion-remaining` · **خط الأساس:** `da9588933a4b23b559a98823d84f24da8762b183` (رأس main المتحقق — دمج Phase 0 عبر PR #199 بCI أخضر على SHA نفسه)
**Workstream:** WS-167 · **Items:** OPS-007, OPS-009 · **الالتزامات:** Claim `39ec52b` ← كود `33a505b` ← توثيق (هذا الالتزام)

## 1. خط الأساس والهوية
رأس main المتحقق نفسه (`da958893` — MERGE SHA لـPhase 0 وCI أخضر على SHA نفسه). Phase 0 (إغلاق OPS-004/WS-166 VERIFIED) دُمج عبر PR #199 وتحقق. فريق التحليل المشترك: **6 عمال قراءة-فقط** (عقود؛ سلامة مالية/مجال؛ تطبيق/تخزين؛ واجهة/وصول؛ اختبارات/CI؛ فريق أحمر) على النطاق نفسه؛ بطاقة القرار الموحدة سُجلت قبل الكود. **الاكتشاف الحاسم:** مقارنة عقد ١٣ منفذة أصلًا على main (`readOrderActualMaterialComparison` + `ActualMaterialPanel`) — لا إعادة تنفيذ (AGENTS §14)؛ الموجة تغلق فجوات الصدق المتبقية فقط.

## 2. الملفات المتغيرة (كاملة)
معدّل: `application/inventory/inventoryMaterialService.ts` (نموذج القراءة: `reviewReasons` + اشتقاقها من مصدرها الصريح)؛ `components/order/ActualMaterialPanel.tsx` (سطر الأسباب المرئي + إزالة قسر `?? 0` الكامن)؛ `application/inventory/inventoryMaterialService.test.ts` (+6 اختبارات)؛ `scripts/text-density-count.py` (سقف OrderDetail 181→183 موثق — تسميتان مفروضتان). جديد: `components/order/ActualMaterialPanel.dom.test.tsx` (6 اختبارات DOM). توثيق: التراكر + current-state §51 + هذا التقرير + سجل QA + فهرس التقارير + Views/Excel/meta.

## 3. السلوك قبل/بعد
- **قبل:** needs_review يعرض الفرق بنبرة تحذير وعنوان «يحتاج مراجعة» لكن **بلا سبب مصرَّح** (مخالف عقد ١٣ سطر ٣٦: «فرق المادة مع سبب نقص المعرفة»)؛ وقسر `?? 0` الكامن في العرض يحوّل null الدفاعي إلى 0.00؛ ولا يوجد دليل طرف-إلى-طرف أن تغيّر السعر الحالي (استلام جديد) لا يمس لقطة الطلب التاريخية (أقرب دليل كان على مستوى الدومين فقط).
- **بعد:** نموذج القراءة يحمل `reviewReasons` (`snapshot_knowledge` \| `actual_cost_unknown`) مشتقة من مصدرها الصريح (knowledgeState للقطة / costKnowledge للاستهلاك)؛ البطاقة تعرض السبب نصًا مرئيًا («سبب المراجعة: لقطة التكلفة ليست معروفة…» / «التكلفة غير معروفة لبعض الاستهلاك المسجل — القيمة المنفذة المسجلة أدنى من الحقيقة.»)؛ الفرق يبقى ظاهرًا (إخفاؤه ممنوع) لكنه مؤهَّل بسببه فلا يُقرأ 0.00 واثقة ولا نتيجة نهائية؛ null الدفاعي يظهر «—» لا 0.00؛ والاختبار الجديد يثبت: استلام جديد بسعر مختلف (والدليل السعري يتحرك فعلًا إلى السعر الجديد) → لقطة الطلب التاريخية deep-equal بلا أي تغيير والمقارنة تقرأ المخطط من اللقطة المجمدة.

## 4. العقود والـinvariants
عقد ١٣ (نموذج القراءة سطر ١٧-٢١ كما هو؛ سطر ٣٦: الفرق مع السبب؛ إخفاء الفرق ممنوع؛ سطر ٤٠-٤٦: لا تغيير Snapshot/سعر/نتيجة؛ لا schemaVersion/exportVersion جديدان)؛ عقد ٢٨ §٥ (التكلفة غير المعروفة لا تظهر 0.00 واثقة في أي سطح ومنها مقارنة الطلب)؛ عقد ٠٣ (اللقطة تُجمّد بالقيمة؛ لا تعدّل في مكانها)؛ عقد ٠٥ §4.4 (تغيير سعر مادة لاحقًا يؤثر في Snapshot الجديد فقط)؛ عقد ١١ (الاستهلاك رابط تكلفة فعلية لا يغيّر Snapshot سابقًا). **المخطط/التصدير:** `NO_SCHEMA_OR_EXPORT_CHANGE` (35/27). **المعنى المالي:** `NO_FINANCIAL_SEMANTIC_CHANGE` — حساب الفرق نفسه (actual−planned) لم يتغير؛ لا Writer ولا حركة ولا COGS.

## 5. قرار التنفيذ ولماذا هو أدنى
إغلاق فجوة صدق فوق قراءة قائمة: سبب واحد مصرَّح لكل مصدر نقص (لا مصفوفة تسميات معرفة جديدة — حالة النتيجة النوعية ظاهرة أصلًا في بطاقة النتيجة بالصفحة نفسها)؛ لا صف لكل مادة (خارج البند)؛ لا سطح قوالب (لا ربط قالب↔استهلاك في النموذج — عامل الفريق الأحمر R2)؛ لا حالة نطاق جديدة `UNAVAILABLE` في الدومين (enum مغلق عقديًا — قرار مالك لو أُريد)؛ رفع سقف كثافة موثق +2 فقط (تسميتان مفروضتان) بسابقة FinanceMore 35→37.

## 6. الاختبارات والتحقق
| الأمر | النتيجة |
|---|---|
| `corepack pnpm exec vitest run client/src/application/inventory/inventoryMaterialService.test.ts` | 33/33 PASS (27 قائمة + 6 جديدة) |
| `corepack pnpm exec vitest run client/src/components/order/ActualMaterialPanel.dom.test.tsx` | 6/6 PASS |
| `pnpm check` | **EXIT 0** — 36+239 ملفًا؛ 404+1674 اختبارًا؛ lint 37/37؛ كثافة «All surfaces within caps» (OrderDetail 182/183)؛ budget **615,929/146,588 ≤ 650,000/155,000** |
| `python3 scripts/operations-control/validate.py` | valid |
| `node scripts/check-secrets.mjs` / `check-test-focus.mjs` | PASS (1620 ملفًا / 275 ملف اختبار) |
| `git diff --check` | نظيف |

ملاحظة جودة: أثناء جولة `pnpm check` وسيطة ظهر **خطأ غير معالج وحيد عابر** في `QuickActionSheet.category.test.tsx` (مؤقت radix focus-scope بعد teardown في jsdom) بلا أي فشل اختبار (1674/1674 ناجحة) — غير متصل بهذه الموجة (لم تلمس ملفاته)؛ ثلاث إعادات تشغيل معزولة وجولتان كاملتان بعدها: نظيفة بلا الخطأ (سابق على main؛ يسجل كملاحظة لا كإصلاح نطاق).

**مسارات الفشل المغطاة:** لقطة تقديرية → سبب snapshot_knowledge مع بقاء الفرق؛ استهلاك مجهول التكلفة → سبب actual_cost_unknown مع تصريح «أدنى من الحقيقة»؛ السببان معًا؛ recorded بلا أسباب؛ قبل/بعد التسليم وإعادة قراءة متطابقة بلا كتابة (مطابقة اللقطة الكاملة للمخزن)؛ **تغيّر السعر الحالي لا يمس اللقطة التاريخية مع تحرك الدليل السعري**؛ not_recorded بلا صفر مضلل؛ القيم المفقودة «—»؛ لا كلمة «نهائي» في أي مسار.

## 7. QA الحي والجهاز
`NOT_EXECUTED` — لا متصفح حي في البيئة؛ سطر الأسباب بفئة عرض قائمة (`micro-local-truth`) داخل بطاقة InfoCard قائمة. سجل QA: `docs/quality/stage-2-ops-007-009-qa-2026-09-21.md`.

## 8. التراكر والعروض
OPS-007/OPS-009: CLAIMED → IN_PROGRESS → IN_REVIEW (pr:200). WS-167: CLAIMED → IN_PROGRESS → IN_REVIEW (pr:200). Views/Excel/meta مُجددة وunzip -t سليم وvalidator ناجح.

## 9. القيود والأدلة التاريخية
قيد الـ6 بنود في `materialSuggestionsFrom` (مسار دليل أسعار القوالب) لا يمس مقارنة الطلب (تقرأ اللقطة والحركات لا المقترحات) — يسجل كقيود معروفة للعمل المستقبلي على مستوى القوالب؛ عامل الفريق الأحمر R9. صف لكل مادة وربط القالب↔الاستهلاك خارج البند. أدلة Documents الخارجية HISTORICAL_EVIDENCE لم تُفتح. QA الحي NOT_EXECUTED.

## 10. حدود الاستعادة
حد آمن أخير: `da958893`. الرجوع = عدم دمج PR #200 — لا هجرة ولا كتابة بيانات. آخر التزام آمن على الفرع = `33a505b` حيث `pnpm check` أخضر EXIT 0.

## 11. الكتابات المنفذة وغير المنفذة
**نُفذت:** فرع `stage-2/completion-remaining` + دفع؛ 3 التزامات (Claim/كود/توثيق)؛ PR #200؛ تحديثات تراكر المصدر مع Views/Excel/meta. **لم تُنفذ:** لا دفع مباشر لmain؛ لا دمج قبل CI + manifest؛ لا حذف/إعادة تسمية فروع؛ لا وصول لDocuments؛ لا نشر/إعدادات/Cloudflare؛ `NO_TOKEN_STORED`؛ لا تنظيف فروع.
