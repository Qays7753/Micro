/** Style: Micro decision path — opening a material records known stock, not a retroactive expense. */
/* مبدأ Micro: رصيد المادة أثر مخزون معلن، ويظهر تاريخه دون تحويله إلى كاش أو مصروف. */
import { ArrowRight, Boxes, Save } from "lucide-react";
import { useRef, useState } from "react";
import { useLocation } from "wouter";
import { usePrototypeServices } from "@/app/PrototypeServicesContext";
import { EnglishNumberInput } from "@/components/forms/EnglishNumberInput";
import { EnglishQuantityInput } from "@/components/forms/EnglishQuantityInput";
import { LocalDateField } from "@/components/forms/LocalDateField";
import type { MaterialUnit } from "@micro-domain/inventory-material/index.js";
import { localDateInAmman } from "@/presentation/formatters";
const ammanDate = () => localDateInAmman();
export default function MaterialEditor() { const [, navigate] = useLocation(); const { inventory, notifyDataChanged } = usePrototypeServices(); const [name, setName] = useState(""); const [unit, setUnit] = useState<MaterialUnit>("piece"); const [quantityMilli, setQuantityMilli] = useState(0); const [valueMinor, setValueMinor] = useState(0); const [quantityValid, setQuantityValid] = useState(true); const [valueValid, setValueValid] = useState(true); const [date, setDate] = useState(ammanDate); const [note, setNote] = useState("رصيد مادة معلن"); const [message, setMessage] = useState<string | null>(null); const [saving, setSaving] = useState(false); const operationKey = useRef(`material-ui-${globalThis.crypto?.randomUUID?.() ?? Date.now()}`); async function save() { if (!name.trim() || !note.trim() || !quantityValid || !valueValid || quantityMilli < 0 || valueMinor < 0 || (quantityMilli === 0) !== (valueMinor === 0)) { setMessage("أدخل اسم المادة، أو كمية وقيمة افتتاحيتين موجبتين معًا، بالأرقام 0–9."); return; } setSaving(true); const result = await inventory.openMaterial({ name, unit, openingQuantityMilli: quantityMilli, openingValueMinor: valueMinor, occurredOn: date, note, operationKey: operationKey.current }); setSaving(false); if (!result.ok) { setMessage(result.message); return; } notifyDataChanged(); navigate("/inventory"); } return <section className="micro-page micro-finance-page"><button className="micro-back-button" type="button" onClick={() => navigate("/inventory")}><ArrowRight aria-hidden="true" /> المواد والمخزون</button><div className="micro-page-heading"><span className="micro-overline">بداية معلنة</span><h1>ما المادة المتوفرة الآن؟</h1><p>سجّل وحدة ثابتة وكمية وقيمة تعرفهما. هذا مخزون متاح، وليس مصروف بيع.</p></div><section className="micro-decision-card"><Boxes aria-hidden="true" /><div><span>حد الحقيقة</span><strong>رصيد المادة لا يغير الكاش أو نتيجة الفترة.</strong><p>يمكن ترك الكمية والقيمة صفرًا لتعريف المادة قبل استلام شراء لاحق.</p></div></section><section className="micro-form-card"><label className="micro-field"><span>اسم المادة</span><input value={name} onChange={(event) => setName(event.target.value)} placeholder="مثال: خشب زان" /></label><label className="micro-field"><span>الوحدة الثابتة للمادة</span><select value={unit} onChange={(event) => setUnit(event.target.value as MaterialUnit)}><option value="piece">قطعة</option><option value="meter">متر</option><option value="kilogram">كيلوغرام</option><option value="liter">لتر</option><option value="other">وحدة أخرى</option></select></label><label className="micro-field"><span>الكمية الافتتاحية</span><EnglishQuantityInput valueMilli={quantityMilli} onMilliChange={setQuantityMilli} onTextValidityChange={setQuantityValid} aria-label="الكمية الافتتاحية" /><small>مثال بالأرقام 0–9: 1.250. تحفظ كأجزاء ألفية.</small></label><label className="micro-field"><span>قيمة الرصيد الافتتاحي بالدينار الأردني</span><EnglishNumberInput value={valueMinor} kind="money" onNumericChange={setValueMinor} onTextValidityChange={setValueValid} aria-label="قيمة الرصيد الافتتاحي" /></label><LocalDateField label="تاريخ البداية" value={date} onChange={(event) => setDate(event.target.value)} /><label className="micro-field"><span>بيان مختصر</span><textarea value={note} onChange={(event) => setNote(event.target.value)} placeholder="مثال: عدّ مخزون بداية التسجيل" /></label>{message ? <p className="micro-field-error" role="status">{message}</p> : null}<button className="micro-button micro-button-primary micro-save-cost" type="button" disabled={saving} onClick={save}><Save aria-hidden="true" />{saving ? "جارٍ الحفظ…" : "حفظ المادة ورصيد البداية"}</button></section></section>; }
