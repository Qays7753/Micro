/** Slice 2 cost screen: one decision surface for known/estimated/incomplete cost and a non-binding protection price. */
import { ArrowRight, CircleAlert, Plus, Save, Trash2 } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { useLocation, useParams } from "wouter";
import { usePrototypeServices } from "@/app/PrototypeServicesContext";
import type { CostEditorInput } from "@/application/cost/costService";
import type { DraftCostMaterial, OrderDraft } from "@/storage/local/types";

const minor = (value: string) => Math.round((Number(value) || 0) * 100);
const major = (value: number) => (value / 100).toFixed(2);
const money = new Intl.NumberFormat("ar-JO", { style: "currency", currency: "JOD", minimumFractionDigits: 2 });
const defaultInput = (quantity: number): CostEditorInput => ({ materialItems: [], time: null, packagingMinor: 0, deliveryMinor: 0, wasteMinor: 0, safetyBufferMinor: 0, quantity });
const knowledgeCopy = { known: ["تكلفة معروفة", "كل بنود الوقت والتكلفة المدخلة معروفة."], estimated: ["تكلفة تقديرية", "هناك بند أدخلته كتقدير، فراجع افتراضه قبل تثبيت السعر."], incomplete: ["تكلفة ناقصة", "وقت العمل أو بنود مؤثرة غير مسجلة؛ لا توجد نتيجة نهائية هنا."], partial: ["تكلفة جزئية", "بعض البنود متاحة فقط؛ لا توجد نتيجة نهائية هنا."], stale: ["تكلفة تحتاج مراجعة", "بعض أسعار المواد لم تعد ضمن مدة الحداثة."], variable: ["تكلفة متغيرة", "هناك بند تقديري يحتاج مراجعة قبل تثبيت السعر."] } as const;

function toInput(draft: OrderDraft, activeId: string | null): CostEditorInput {
  const record = draft.costSnapshots.find(snapshot => snapshot.id === activeId);
  if (!record) return defaultInput(draft.quantity);
  return { materialItems: record.materialItems.map(item => ({ ...item })), time: record.time ? { ...record.time } : null, packagingMinor: record.packagingMinor, deliveryMinor: record.deliveryMinor, wasteMinor: record.wasteMinor, safetyBufferMinor: record.safetyBufferMinor, quantity: draft.quantity };
}

export default function CostEditor() {
  const params = useParams<{ id: string }>(); const [, navigate] = useLocation(); const { drafts, costs, dataVersion, notifyDataChanged } = usePrototypeServices();
  const [draft, setDraft] = useState<OrderDraft | null>(null); const [form, setForm] = useState<CostEditorInput | null>(null); const [state, setState] = useState<"loading" | "ready" | "error">("loading"); const [message, setMessage] = useState<string | null>(null); const [isSaving, setIsSaving] = useState(false);
  useEffect(() => { let active = true; drafts.get(params.id).then(result => { if (!active) return; if (!result.ok || !result.value) { setState("error"); return; } setDraft(result.value); setForm(toInput(result.value, result.value.activeCostSnapshotId)); setState("ready"); }); return () => { active = false; }; }, [dataVersion, drafts, params.id]);
  const preview = useMemo(() => form ? costs.preview(form) : null, [costs, form]);
  if (state === "loading" || !form) return <div className="micro-route-loading" role="status">جارٍ فتح تكلفة المسودة…</div>;
  if (state === "error" || !draft) return <section className="micro-page micro-not-found"><h1>تعذر فتح التكلفة</h1><p>ارجع للمسودة ثم أعد المحاولة.</p><button className="micro-button micro-button-primary" type="button" onClick={() => navigate("/orders")}>الطلبات</button></section>;
  const status = preview?.ok ? knowledgeCopy[preview.snapshot.knowledgeState] : null;
  function setMaterial(index: number, patch: Partial<DraftCostMaterial>) { setForm(current => current ? { ...current, materialItems: current.materialItems.map((item, itemIndex) => itemIndex === index ? { ...item, ...patch } : item) } : current); }
  function addMaterial() { setForm(current => current ? { ...current, materialItems: [...current.materialItems, { name: "", quantity: 1, unit: "قطعة", unitPriceMinor: 0, confidence: "known" }] } : current); }
  async function saveSnapshot() { if (!draft || !form) return; setMessage(null); setIsSaving(true); const result = await costs.saveSnapshot(draft, form); setIsSaving(false); if (!result.ok) { setMessage(result.message); return; } setDraft(result.draft!); notifyDataChanged(); setMessage(`تم حفظ نسخة التكلفة ${result.draft!.costSnapshots.length} على هذا الجهاز.`); }
  return <section className="micro-page micro-cost-page"><button className="micro-back-button" type="button" onClick={() => navigate(`/orders/draft/${draft.id}`)}><ArrowRight aria-hidden="true" /> العودة للمسودة</button><div className="micro-page-heading"><span className="micro-overline">Snapshot تكلفة</span><h1>{draft.itemName || "وصف القطعة"}</h1><p>أدخل ما تعرفه. الصفر ليس بديلًا عن وقت أو تكلفة لا تعرفها.</p></div>{preview?.ok ? <section className="micro-cost-result" data-knowledge={preview.snapshot.knowledgeState}><span>سعر الحماية لكل قطعة</span><strong>{money.format(preview.snapshot.priceFloorMinor / 100)}</strong><small>تكلفة القطعة {money.format(preview.snapshot.unitCostMinor / 100)} + هامش الحماية الذي أدخلته.</small><div><b>{status?.[0]}</b><p>{status?.[1]}</p></div></section> : <p className="micro-field-error" role="alert">{preview?.message ?? "راجع المدخلات."}</p>}<section className="micro-form-card"><div className="micro-cost-section-heading"><div><h2>المواد</h2><p>المادة الموجودة ليست مجانية؛ أدخل تكلفتها أو اتركها كتقدير واضح.</p></div><button className="micro-icon-button" type="button" aria-label="إضافة مادة" onClick={addMaterial}><Plus aria-hidden="true" /></button></div>{form.materialItems.length === 0 ? <p className="micro-empty-inline">لم تضف مادة بعد. قد تبقى التكلفة ناقصة إن لم تدخل وقت العمل وبنودًا مؤثرة.</p> : form.materialItems.map((item, index) => <div className="micro-material-row" key={`${index}-${item.name}`}><label className="micro-field"><span>المادة</span><input value={item.name} onChange={event => setMaterial(index, { name: event.target.value })} placeholder="مثال: خشب" /></label><label className="micro-field"><span>الكمية</span><input type="number" min="0.01" step="0.01" value={item.quantity} onChange={event => setMaterial(index, { quantity: Number(event.target.value) })} /></label><label className="micro-field"><span>الوحدة</span><input value={item.unit} onChange={event => setMaterial(index, { unit: event.target.value })} placeholder="لوح" /></label><label className="micro-field"><span>تكلفة الوحدة (د.أ)</span><input type="number" min="0" step="0.01" inputMode="decimal" value={major(item.unitPriceMinor)} onChange={event => setMaterial(index, { unitPriceMinor: minor(event.target.value) })} /></label><label className="micro-field"><span>درجة المعرفة</span><select value={item.confidence} onChange={event => setMaterial(index, { confidence: event.target.value as DraftCostMaterial["confidence"] })}><option value="known">معروف</option><option value="estimated">تقديري</option></select></label><button className="micro-delete-row" type="button" aria-label="حذف المادة" onClick={() => setForm(current => current ? { ...current, materialItems: current.materialItems.filter((_, itemIndex) => itemIndex !== index) } : current)}><Trash2 aria-hidden="true" /></button></div>)}</section><section className="micro-form-card"><div className="micro-cost-section-heading"><div><h2>وقت العمل</h2><p>إذا لا تعرفه الآن، اتركه ناقصًا بدل أن تسجله صفرًا.</p></div><button className="micro-text-action" type="button" onClick={() => setForm(current => current ? { ...current, time: current.time ? null : { minutes: 60, hourlyRateMinor: 0, confidence: "estimated" } } : current)}>{form.time ? "لا أعرف الوقت الآن" : "إدخال الوقت"}</button></div>{form.time ? <div className="micro-field-grid"><label className="micro-field"><span>الدقائق</span><input type="number" min="0" inputMode="numeric" value={form.time.minutes} onChange={event => setForm(current => current?.time ? { ...current, time: { ...current.time, minutes: Number(event.target.value) || 0 } } : current)} /></label><label className="micro-field"><span>سعر الساعة (د.أ)</span><input type="number" min="0" step="0.01" inputMode="decimal" value={major(form.time.hourlyRateMinor)} onChange={event => setForm(current => current?.time ? { ...current, time: { ...current.time, hourlyRateMinor: minor(event.target.value) } } : current)} /></label><label className="micro-field"><span>درجة المعرفة</span><select value={form.time.confidence} onChange={event => setForm(current => current?.time ? { ...current, time: { ...current.time, confidence: event.target.value as DraftCostMaterial["confidence"] } } : current)}><option value="known">معروف</option><option value="estimated">تقديري</option></select></label></div> : <p className="micro-missing-time"><CircleAlert aria-hidden="true" /> وقت العمل غير معروف؛ ستبقى النتيجة ناقصة حتى تسجله.</p>}</section><section className="micro-form-card"><div className="micro-cost-section-heading"><div><h2>بنود أخرى وحماية السعر</h2><p>تدخل فقط ما ينطبق على هذه القطعة، وتظل المصاريف العامة خارج التحميل التلقائي.</p></div></div><div className="micro-field-grid">{([ ["تغليف", "packagingMinor"], ["توصيل", "deliveryMinor"], ["هدر", "wasteMinor"], ["هامش الحماية لكل قطعة", "safetyBufferMinor"] ] as const).map(([label, field]) => <label className="micro-field" key={field}><span>{label} (د.أ)</span><input type="number" min="0" step="0.01" inputMode="decimal" value={major(form[field])} onChange={event => setForm(current => current ? { ...current, [field]: minor(event.target.value) } : current)} /></label>)}</div></section>{message ? <p className="micro-save-note" role="status">{message}</p> : null}<button className="micro-button micro-button-primary micro-save-cost" type="button" disabled={isSaving || !preview?.ok} onClick={saveSnapshot}><Save aria-hidden="true" />{isSaving ? "جارٍ حفظ النسخة…" : "حفظ نسخة التكلفة"}</button><p className="micro-cost-disclaimer">هذا **سعر حماية** من التكلفة المدخلة، لا سعر سوق ولا قرار بقبول الطلب. الاتفاق والعربون لا يُسجلان هنا.</p></section>;
}
