/**
 * التحصين الكامل (المجموعة ٣ — عقد §31 من المجموعة ٢): نصوص رحلة الاسترجاع
 * بعد تعارض قِدم التخزين `storage_stale` وملاحظة قفل المراجعة الموثقة (D-031).
 * مشتركة بين أكثر من سطح (محرر المشتريات، محرر الموعد، تفاصيل الطلب) — لذلك
 * تسكن في app/ مع بقية العقود المشتركة (كما تفعل نصوص الاسترداد في StartupGate)
 * ولا تُحتسب نصوص شاشة واحدة. هذه نصوص حالة لحظية تظهر فقط أثناء التعارض
 * أو القفل، لا ديكامنيا دائمًا: تُعرض عبر حالات مكوّنة، وتفسيرها واحد في كل
 * الأسطح التي تستهلكها.
 */

/** سبب التعارض ومخرجه — نفس عقد §31: إعادة قراءة ثم قرار واعٍ، لا تكرار أعمى. */
export const STALE_CONFLICT_NOTE =
  "تغيّر هذا السجل من مسار آخر بعد فتحك له (نافذة أو تبويب آخر)، ورفضنا الكتابة فوق التغيير الأحدث. اضغط «أعد قراءة السجل الحالي» لرؤية الحالة الجديدة، ثم راجع قيمك وأعد الحفظ بقرار واعٍ — لن نعيد إرسال القيم القديمة تلقائيًا.";

/** فعل الاسترجاع: إعادة قراءة السجل الحالي عبر مسار القراءة المعتمد نفسه. */
export const STALE_RELOAD_ACTION_LABEL = "أعد قراءة السجل الحالي";

/** بعد إعادة القراءة: القيم غير المحفوظة باقية في الحقول — القرار للمستخدم. */
export const STALE_RELOADED_NOTE =
  "أُعيدت قراءة السجل الحالي — قيمك غير المحفوظة بقيت في الحقول كما هي، والأسفل يعرض الحقيقة المسجلة الآن. راجعها ثم احفظ من جديد بقرار واعٍ.";

/** D-031: ملاحظة قفل المراجعة على السجل المسلّم غير المتراجَع عنه. */
export const DELIVERED_REVIEW_LOCK_NOTE =
  "هذا الطلب المسلّم مقفل في «يحتاج مراجعة»: لا عمليات مالية عامة عليه الآن. المخرج الموثق الوحيد هو «تراجع موثق عن التسليم» أدناه، وبعده تستأنف التنفيذ أو تلغي بقرار صريح — القراءة والتاريخ يبقيان متاحين.";

/* ─── Z2.3/Z2.4 (§3.4 — قواعد المخرجات المشتركة للبيع والمصروف) ───
 * نصوص نتيجة العملية الصادقة: مشتركة بين أكثر من سطح (البيع السريع،
 * المصروف السريع، شاشة نتيجة محرر البيع المباشر) — لذلك تسكن هنا مع
 * بقية العقود المشتركة، ولا تُحتسب نصوص شاشة واحدة (كإخواتها أعلاه).
 * هذه نصوص نتيجة لحظة العمل (reused / نجاح جزئي) تُعرض عبر الوصل أو
 * شاشة النتيجة فقط عند وقوعها، وتفسيرها واحد في كل الأسطح. */

/** Reused (§3.4): الحدث موجود سابقًا بنفس المفتاح — لا سجل جديد أُنشئ.
 *  عنوان وصل محايد ناجح الحماية: ليس «سُجّل…» (نجاح فرِش) وليس خطأً. */
export const REUSED_RECORD_RECEIPT_TITLE = "سجل موجود سابقًا";

/** Reused (§3.4): تفصيل الوصل — الحماية منعت نسخة ثانية بلا كذب ولا تجاهل. */
export const REUSED_RECORD_PROTECTION_NOTE =
  "حماية من التكرار: هذا الحدث مسجل سابقًا بنفس المفتاح — لم يُنشأ سجل جديد ولم يتكرر أثره المالي.";

/** Partial Success (§3.4) على شاشة نتيجة البيع المباشر: العملية الكاملة
 *  (البيع وقبضه) سُجلت محليًا، لكن الخطوة التابعة (نسبة القبض للمحفظة)
 *  لم تتم — المنجز والناقص مفصولان، والقبض محفوظ في غير الموزع لا فاقد؛
 *  الفعل التالي فتح السجل وتوزيع القبض بقرار، لا إعادة تسجيل البيع. */
export function saleDonePartialAttributionNote(reason: string): string {
  return `سُجل البيع محليًا وقُبضه محفوظ، لكن نسبته للمحفظة لم تتم: ${reason} — القبض باقٍ في الكاش غير الموزع حتى توزّعه بنفسك.`;
}

/** OPS-003 (عقد ٤١ §٨) — تحذير المصروف اليدوي المتزامن مع تذكير متكرر غير
 *  معالج: ظاهر غير حاجب. الصياغة نفسها التي يعرضها المحرر الموجه إلى حرفها
 *  (التعريف واحد للمدخلين) — المصروف اليدوي يبقى مشروعًا بقرار صريح، ولا
 *  يُستنتج أنه يعالج التكرار. */
export function recurringOccurrenceWarningNote(seriesTitle: string): string {
  return `لديك تذكير مصروف متكرر غير معالج لهذه الفترة: «${seriesTitle}» — سجّله من تفصيل التذكير إن شئت، أو تابع هنا بقرارك الصريح.`;
}
